ACK_PACKET_RECEIVED AUD-jini-infra-tovu-pg-2026-08-12-R2 -- I received the packet and will work on it.

I’m using the `understand-diff` skill because this is a commit-range compliance inspection. I’ll reconcile every ledger item against code, then inspect the new diff and its minimum behavioral context.

The diff-analysis skill’s graph helpers are not available in this session, so I’m checking each repository’s local knowledge-graph artifact and the exact commit file lists. If the artifacts are absent or stale, I’ll fall back to commit-scoped Git inspection and direct source reads, as the audit still requires an evidence-backed disposition.

The Tovu graph artifact is absent and Jini’s artifact predates the infra package, so neither can support this review; the skill therefore falls back to direct Git/source inspection. I also found unrelated dirty-worktree changes and am excluding them from the audit rather than treating current working-tree noise as part of the packet.

Initial source inspection confirms the migration order is plan → preflight → snapshot → journal → one transaction → verification, with ALTER and index DROP/CREATE inside that transaction. I’m now testing the generator against Drizzle’s actual runtime metadata—not just reading its comments—because the parity and completeness guards are only trustworthy if their assumptions match the objects Drizzle really exposes.

The L9 dispute has a concrete counterexample in the new post-commit path: an in-memory declaration can commit, then fail verification, return `DDL_FAILED`, and keep the schema change because there is neither rollback nor recovery journal. I’m reproducing that branch directly in `:memory:` before assigning its final disposition.

The route review is consistent across plan/confirm/execute for both ceremonies, and the default seeded owner completes both end-to-end; `authorizeForHooks` is a read-only decision helper, so exporting it does not expose mutation execution. A separate issue emerged in the journal fix: a caught post-commit failure can leave a recoverable marker while the server continues accepting writes, so the next boot’s whole-file restore can erase those later writes. I’m validating the non-filesystem test surfaces now.

{
  "ack": "ACK_PACKET_RECEIVED AUD-jini-infra-tovu-pg-2026-08-12-R2 -- I received the packet and will work on it.",
  "threat_model_accepted": true,
  "rejection_reason": null,
  "auditor_scope_check": "Audited TM-001 against the R2 commit surfaces and minimum behavioral context. Read Tovu data-module.ts, migration-journal.ts, migration-recovery.ts, snapshot.ts, restore.ts and reconciliation/recovery tests; generate-postgres-schema.ts, schema.ts, schema.postgres.ts and both PostgreSQL tests; gated-mutations gateway.ts/composition.ts, database/recovery hooks, both route triples, route tests, server/deps.ts and owner-disable wiring. Read Jini infra package.json, README, db/core and db/sqlite barrels/ports, driver-isolation guard, identity seed/authorize/grant code. The Tovu knowledge graph was absent and Jini's predates infra, so commit-scoped Git/source inspection was used. Unrelated working-tree changes were excluded. Mismatches: the preamble says ten commits while R2 lists twelve; R2 prevailed. The source contains 7 CHECKs, but only 6 are sealed-shape checks plus posts_body_format_shape, contrary to the packet and generator comment claiming seven sealed checks (/Users/la/Programming/Tovu/development/scripts/generate-postgres-schema.ts:323). The route suites could not bind a socket under the inspector sandbox; 34 non-network parity/gateway tests passed, both inspected projects typechecked, and direct runtime probes were used for generator and in-memory behavior.",
  "findings": [
    {
      "id": "F1",
      "title": "Post-commit recovery can erase writes accepted after the failed declaration",
      "domain": "D1",
      "severity": "high",
      "blocking": true,
      "checked": "Tracked a fault after transaction commit through failure return, production boot composition, and next-boot recovery. The transaction flag becomes true before verification; the catch leaves the journal non-terminal; the production readiness promise logs and swallows the error; recovery later restores the whole pre-DDL snapshot (/Users/la/Programming/Tovu/src/features/plugins/data-module.ts:863, /Users/la/Programming/Tovu/src/server/deps.ts:368, /Users/la/Programming/Tovu/src/features/plugins/migration-recovery.ts:40).",
      "expected": "After a committed DDL attempt becomes unverified, the process must either atomically terminalize the journal with the DDL or stop/quarantine all writes until recovery completes.",
      "observed": "Commit 62426db correctly avoids the false ROLLED_BACK label, but returns DDL_FAILED while leaving VERIFYING. Production catches that startup failure and resolves the optional readiness chain, allowing the site and later initialization to continue. On the next boot, recovery replaces content.db with the old snapshot, deleting every legitimate content write made after that snapshot.",
      "why_it_matters": "A rare verification or post-commit journal failure becomes delayed whole-database data loss rather than a contained migration failure.",
      "recommended_fix": "Perform post-DDL verification inside the DDL transaction and atomically update the journal to COMMITTED in that same transaction. If any ambiguity can remain after commit, transition the whole site to a durable blocked state and prevent serving/writes until immediate restart recovery. Add a regression test that injects the post-commit failure, attempts a later content write, and proves the write is either refused or survives recovery.",
      "confidence": "high",
      "file": "/Users/la/Programming/Tovu/src/features/plugins/data-module.ts:863"
    },
    {
      "id": "F2",
      "title": "L9 is valid: in-memory DDL bypasses the frozen snapshot gate and can fail after committing",
      "domain": "D6",
      "severity": "high",
      "blocking": true,
      "checked": "Adjudicated the coordinator's transaction-rollback argument against the actual post-commit branch. Non-no-op :memory: declarations receive no snapshot or journal, while verification runs after the transaction commits (/Users/la/Programming/Tovu/src/features/plugins/data-module.ts:819, /Users/la/Programming/Tovu/src/features/plugins/data-module.ts:828, /Users/la/Programming/Tovu/src/features/plugins/data-module.ts:854). A direct runtime reproduction returned DDL_FAILED with snapshotPath null and no journal while the allegedly failed sku column remained live.",
      "expected": "Under frozen invariant 2, only a fully reconciled no-op may skip snapshot-before-DDL. At minimum, every failure reported for an allowed ephemeral path must leave the transaction unapplied.",
      "observed": "The coordinator is wrong that transaction rollback covers every failure an in-memory database can suffer. A post-commit verification failure occurs while the same process remains alive: rollback is no longer possible, the function reports failure, and callers can observe the committed schema change.",
      "why_it_matters": "This is both a literal bypass of a mandatory gate and a concrete result/state divergence in supported memory-mode execution.",
      "recommended_fix": "With TM-001 frozen, reject non-no-op DDL when no recoverable snapshot can be created. If memory dev mode must remain, first move every fallible verification step inside the transaction and obtain an explicit threat-model exception rather than silently weakening invariant 2.",
      "confidence": "high",
      "file": "/Users/la/Programming/Tovu/src/features/plugins/data-module.ts:828",
      "patch": "diff --git a/src/features/plugins/data-module.ts b/src/features/plugins/data-module.ts\n--- a/src/features/plugins/data-module.ts\n+++ b/src/features/plugins/data-module.ts\n@@ -189,7 +189,7 @@ import type { JournalPhase } from \"./migration-journal\";\n import { checkNamespaceAdoption } from \"./plugin-identity\";\n import type { PluginProvenance } from \"./plugin-identity\";\n-import { discardCommittedSnapshot, snapshotDb } from \"./snapshot\";\n+import { discardCommittedSnapshot, isInMemoryDbPath, snapshotDb } from \"./snapshot\";\n@@ -819,6 +819,16 @@ export async function declareDataModule(\n     return { ok: true, created: [], altered: [], snapshotPath: null };\n   }\n+\n+  if (isInMemoryDbPath(dbPath)) {\n+    return {\n+      ok: false,\n+      created: [],\n+      altered: [],\n+      snapshotPath: null,\n+      error: { code: \"SNAPSHOT_REQUIRED_FOR_DDL\", message: \"non-no-op dataModule DDL requires a recoverable pre-DDL snapshot\" },\n+    };\n+  }\n \n   const preflight = runPreflightChecks(db, dbPath, decl);"
    },
    {
      "id": "F3",
      "title": "CHECK chunk walker silently de-parameterizes string values",
      "domain": "D2",
      "severity": "medium",
      "blocking": true,
      "checked": "Inspected every current CHECK chunk and probed another valid Drizzle CHECK shape. Current expressions reconstruct identically, but a parameterized sql template such as column = ${'doc'} exposes the bound string as a primitive string chunk. checkSql returns every primitive string verbatim (/Users/la/Programming/Tovu/development/scripts/generate-postgres-schema.ts:335). The probe rendered a = doc instead of a quoted value or a fail-closed error.",
      "expected": "Only explicitly understood literal-fragment and source-column chunks should render; parameters and every other chunk shape must fail closed.",
      "observed": "Caused by 56fe89b: typeof chunk === 'string' is treated as trusted SQL. The parity test only counts check( occurrences, so it cannot detect the semantic mutation (/Users/la/Programming/Tovu/src/db/__tests__/schema-postgres-parity.test.ts:59).",
      "why_it_matters": "A routine, valid way of authoring a future CHECK can generate invalid or semantically different PostgreSQL DDL while drift and parity tests pass.",
      "recommended_fix": "Accept only StringChunk-like objects whose value is an array of strings and column objects belonging to the current table; reject primitive strings, parameters, nested SQL, and arbitrary name-bearing objects. Add a negative generator fixture for a bound string parameter.",
      "confidence": "high",
      "file": "/Users/la/Programming/Tovu/development/scripts/generate-postgres-schema.ts:335",
      "patch": "diff --git a/development/scripts/generate-postgres-schema.ts b/development/scripts/generate-postgres-schema.ts\n--- a/development/scripts/generate-postgres-schema.ts\n+++ b/development/scripts/generate-postgres-schema.ts\n@@ -332,22 +332,23 @@ const EXPORT_NAME_BY_TABLE: Map<object, string> = new Map(\n  * are all lowercase snake_case, so they need no quoting; a name requiring quotes would be a new\n  * situation this function should be taught about rather than silently mangling.\n  */\n-function checkSql(value: unknown): string {\n+function checkSql(value: unknown, sourceColumns: ReadonlySet<SQLiteColumn>): string {\n   const chunks = (value as { queryChunks?: unknown[] })?.queryChunks;\n   if (!Array.isArray(chunks)) {\n     throw new Error('a CHECK constraint exposed no queryChunks — Drizzle internals changed; revisit checkSql()');\n   }\n   return chunks\n     .map((chunk) => {\n-      if (typeof chunk === 'string') return chunk;\n       const literal = (chunk as { value?: unknown }).value;\n-      if (literal !== undefined) return Array.isArray(literal) ? literal.join('') : String(literal);\n-      const columnName = (chunk as { name?: string }).name;\n-      if (typeof columnName === 'string') {\n+      if (Array.isArray(literal) && literal.every((part) => typeof part === 'string')) return literal.join('');\n+      if (sourceColumns.has(chunk as SQLiteColumn)) {\n+        const columnName = (chunk as SQLiteColumn).name;\n         if (!/^[a-z_][a-z0-9_]*$/.test(columnName)) {\n           throw new Error(`CHECK references column \"${columnName}\", which needs quoting — teach checkSql() first`);\n         }\n         return columnName;\n       }\n       throw new Error('unrecognised CHECK query chunk — revisit checkSql()');\n@@ -374,9 +375,10 @@ function renderExtras(\n   // (IS NULL / AND / OR / comparisons / string literals), and checkSql throws rather than guessing\n   // if it ever meets a chunk shape it does not recognise.\n+  const sourceColumns = new Set<SQLiteColumn>(cfg.columns);\n   const checks = cfg.checks.map((ch) => {\n     const c = ch as unknown as { name: string; value: unknown };\n-    return `    check(${JSON.stringify(c.name)}, sql\\`${checkSql(c.value)}\\`),`;\n+    return `    check(${JSON.stringify(c.name)}, sql\\`${checkSql(c.value, sourceColumns)}\\`),`;\n   });"
    },
    {
      "id": "F4",
      "title": "Completeness guard does not cover index configuration, so partial-index predicates are dropped",
      "domain": "D2",
      "severity": "medium",
      "blocking": true,
      "checked": "Inspected renderExtras and Drizzle's current runtime index config. Every index config already has own keys name, columns, unique, where and table; a valid .where(sql...) probe populated where. The renderer emits only name, uniqueness and columns (/Users/la/Programming/Tovu/development/scripts/generate-postgres-schema.ts:363).",
      "expected": "Every supported structural modifier must be translated, and every unsupported populated modifier must stop generation.",
      "observed": "Caused by 90ca299 and not closed by b1ffc90: the Object.keys completeness guard is applied only to table config and columns, not index configs. A partial SQLite index becomes an unfiltered PostgreSQL index, while index-count parity and exact generator drift both pass.",
      "why_it_matters": "Dropping a partial predicate changes uniqueness and query semantics and is precisely the silent structural-divergence class the generator guards claim to prevent.",
      "recommended_fix": "Apply shape/value guards recursively to index configs. Until predicate translation exists, reject any populated where value and any index expression not identical to a source column. Add negative fixtures for partial and expression indexes. The broad Object.keys guard can indeed false-fail when a Drizzle upgrade adds an inert enumerable property, but that is an acceptable fail-closed upgrade review; its defect is incomplete coverage, not strictness.",
      "confidence": "high",
      "file": "/Users/la/Programming/Tovu/development/scripts/generate-postgres-schema.ts:363",
      "patch": "diff --git a/development/scripts/generate-postgres-schema.ts b/development/scripts/generate-postgres-schema.ts\n--- a/development/scripts/generate-postgres-schema.ts\n+++ b/development/scripts/generate-postgres-schema.ts\n@@ -280,6 +280,8 @@ interface TableExtras {\n   foreignKeys: string[];\n }\n \n+const KNOWN_INDEX_CONFIG_KEYS = new Set([\"name\", \"columns\", \"unique\", \"where\", \"table\"]);\n+\n@@ -360,8 +362,21 @@ function renderExtras(\n   tsNames: Map<SQLiteColumn, string>\n ): TableExtras {\n   const ref = (c: unknown): string => `t.${tsNames.get(c as SQLiteColumn) ?? (c as SQLiteColumn).name}`;\n   const indexes = cfg.indexes.map((idx) => {\n-    const cols = idx.config.columns.map(ref).join(\", \");\n+    assertKnownShape(idx.config, KNOWN_INDEX_CONFIG_KEYS, () => `index \"${idx.config.name}\"'s config`);\n+    if (idx.config.where !== undefined) {\n+      throw new Error(\n+        `index \"${idx.config.name}\" declares a partial-index WHERE predicate, which this generator does not translate`\n+      );\n+    }\n+    const cols = idx.config.columns\n+      .map((column) => {\n+        const tsName = tsNames.get(column as SQLiteColumn);\n+        if (!tsName) {\n+          throw new Error(`index \"${idx.config.name}\" contains an expression or ordering modifier that is not translated`);\n+        }\n+        return `t.${tsName}`;\n+      })\n+      .join(\", \");\n     const builder = idx.config.unique ? \"uniqueIndex\" : \"index\";\n     return `    ${builder}(${JSON.stringify(idx.config.name)}).on(${cols}),`;\n   });"
    }
  ],
  "out_of_scope_fatal_warnings": [],
  "score": 7.7,
  "score_rationale": "The ledger fixes are mostly real and current PostgreSQL output matches the current source surface, but one delayed data-loss path, the unresolved snapshot-gate bypass, and two validated silent generator-divergence paths keep the packet below the 8.5 floor.",
  "top_issues_reducing_score": [
    "A caught post-commit failure can cause next-boot recovery to erase intervening writes.",
    "In-memory non-no-op DDL violates the frozen snapshot invariant and can report failure after committing.",
    "CHECK parameters can be silently rendered as raw SQL.",
    "Partial-index predicates are silently dropped because completeness checks do not recurse into index configs."
  ],
  "path_to_10": [
    "Make DDL, verification, and the terminal journal marker one atomic transaction, or durably block all writes before returning an ambiguous post-commit failure.",
    "Resolve L9 under the frozen invariant by rejecting unsnapshotable DDL; any memory-mode exception requires an explicit TM revision.",
    "Add fail-closed fixtures for parameterized CHECKs, partial/expression indexes, custom FK/PK names, and every nested Drizzle config object.",
    "Run the generated schema against an ephemeral real PostgreSQL server and compare catalog tables, column types/defaults, identity properties, FK targets/actions, CHECK definitions, uniqueness, PKs, and indexes. Typecheck-only evidence proves TypeScript validity, not executable DDL correctness.",
    "Add the 63-byte guard to generated column identifiers before a PostgreSQL plugin-DDL path is implemented.",
    "Add explicit operator diagnostics for missing/corrupt recovery snapshots; the current thrown restore error fails closed but can wedge every boot without a guided resolution path."
  ],
  "blocking_gate": "FAIL",
  "ledger_reconciliation": [
    {
      "id": "L1",
      "claimed": "fixed",
      "verified": true,
      "evidence": "/Users/la/Programming/Tovu/src/features/plugins/data-module.ts:487 — existing columns are checked symmetrically for NOT NULL and PK drift before planning DDL; focused tests cover both directions at /Users/la/Programming/Tovu/src/features/plugins/__tests__/data-module-column-reconciliation.test.ts:280."
    },
    {
      "id": "L2",
      "claimed": "fixed",
      "verified": true,
      "evidence": "/Users/la/Programming/Tovu/development/scripts/generate-postgres-schema.ts:374 — runtime introspection found exactly 9 FKs and 7 CHECKs; all 9 targets/actions match generated entries beginning at /Users/la/Programming/Tovu/src/db/schema.postgres.ts:34, and all 7 current CHECK expressions reconstruct textually and semantically identically beginning at line 36. F3 is a new unsupported-chunk defect, not recurrence of dropping the current constraints."
    },
    {
      "id": "L3",
      "claimed": "fixed",
      "verified": true,
      "evidence": "/Users/la/Programming/Tovu/development/scripts/generate-postgres-schema.ts:248 — isUnique is translated, and the single source workspaces.slug unique modifier at /Users/la/Programming/Tovu/src/db/schema.ts:29 appears in generated output at /Users/la/Programming/Tovu/src/db/schema.postgres.ts:922."
    },
    {
      "id": "L4",
      "claimed": "fixed",
      "verified": true,
      "evidence": "/Users/la/Programming/Jini/packages/infra/README.md:3 — README now documents openSqliteConnection/SqliteDbOpsAdapter, the ORM-free surface, and better-sqlite3 as the sole optional driver peer."
    },
    {
      "id": "L5",
      "claimed": "fixed",
      "verified": true,
      "evidence": "/Users/la/Programming/Jini/packages/infra/package.json:1 — directory inspection confirmed packages/infra/package-lock.json is absent; the package is represented only by its workspace manifest."
    },
    {
      "id": "L6",
      "claimed": "fixed",
      "verified": true,
      "evidence": "/Users/la/Programming/Tovu/src/features/plugins/data-module.ts:849 — the flag remains false for transaction failures and is set immediately after transaction return; recordFailurePhase at line 794 writes ROLLED_BACK only while false. The original false-label/skip defect is fixed, but F1 records the new delayed-restore data-loss consequence."
    },
    {
      "id": "L7",
      "claimed": "fixed",
      "verified": true,
      "evidence": "/Users/la/Programming/Tovu/src/features/plugins/data-module.ts:553 — origin='c' correctly selects explicitly-created indexes while excluding SQLite UNIQUE/PK autoindexes; missing and mismatched declared indexes are planned at line 587 and DROP/CREATE executes inside the transaction at line 748."
    },
    {
      "id": "L8",
      "claimed": "fixed",
      "verified": true,
      "evidence": "/Users/la/Programming/Tovu/src/features/recovery/gated-hooks.ts:90 and /Users/la/Programming/Tovu/src/features/database/gated-hooks.ts:95 — both real hooks are instance-scoped; confirm and pre-lock execute helpers also specify instance scope, while authorizeForHooks consistently fails closed at /Users/la/Programming/Tovu/src/core/gated-mutations/gateway.ts:147. Exporting that pure authorization helper exposes no mutation. The seeded owner succeeds end-to-end and is wired in both composition roots; non-owner workspace grants are denied."
    },
    {
      "id": "L9",
      "claimed": "disputed",
      "verified": false,
      "evidence": "/Users/la/Programming/Tovu/src/features/plugins/data-module.ts:863 — direct :memory: reproduction reached post-commit verification failure, returned DDL_FAILED with no snapshot/journal, yet retained the newly-added live column. Transaction rollback therefore does not cover every in-memory failure, and frozen invariant 2 is violated."
    }
  ]
}