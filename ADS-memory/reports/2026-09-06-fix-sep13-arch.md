# Programmer(Sep1-3 Arch) — incremental report

Dispatched from `ADS-memory/reports/2026-09-01-to-03-review-architecture.md` (`ad20fc76`), branch
`restructure/apps-website-phased`.

## Item 1 — BYOK never applied `withReadOnlyToolConstraint` — FIXED, commit `08ace3ea`

Confirmed the finding: `tool-executor-stack.ts`'s `createAssistantToolExecutor` (the extraction from
`1bb6fa67`) was called only by `agent-daemon-server.ts`. `byok-tool-surface.ts` hand-composed its own
inline stack (`createToolExecutor` -> conditional `withToolAttemptAudit` -> `withToolFailureRecovery`)
that never wrapped `withReadOnlyToolConstraint` at all.

**RED** (`apps/website/src/assistant/__tests__/byok-tool-surface.test.ts`, two new tests under
"READ-ONLY PARITY"): registered a fake write tool directly on the BYOK surface's registry, dispatched
it through `executeMetaTool`'s `execute_delegated_tool` under a `constrainPrincipalToReadOnlyTools`
principal. Before the fix: the call completed and the fake handler ran (`writeCalls === 1`,
`isError` undefined) — proving the gap live against the real BYOK dispatch path, not an inferred risk.

**Fix**: `tool-executor-stack.ts`'s `AssistantToolExecutorDeps` collapsed `auditSink`/`workspaceId`
into an optional `toolAttemptAudit: {sink, workspaceId}` (matching `createByokToolSurface`'s own
option shape exactly), so a caller with no durable sink still gets a real executor with the audit
decorator simply skipped — needed because BYOK's own tests deliberately compose a bare (unaudited)
surface and that had to keep working. `byok-tool-surface.ts` now calls
`createAssistantToolExecutor({registry, surfaceExchanges, toolAttemptAudit?})` instead of its own
inline composition. Updated the daemon's own call site (`agent-daemon-server.ts:484`) and the two
fixtures in `read-only-tool-constraint.composition.test.ts` to the new shape.

**GREEN**: both new tests pass. Full regression:
- `byok-tool-surface.test.ts`: 27/28 pass. The 1 failure (`INCIDENT FIX: a search_tools call...`,
  asserting `detail` carries a raw `query` field) is PRE-EXISTING and unrelated — `git status` on
  `tool-catalog-audit.ts`/`tool-catalog-query.ts` shows clean (already committed), and it failed
  identically before this change (confirmed in the RED run). `searchToolsAuditDetail` now emits
  `queryLength` instead of `query` (a redaction) and this test was never updated. **Not fixed here —
  out of scope, flagged for whoever owns that area.**
- `read-only-tool-constraint.composition.test.ts` (the daemon-side stack): 9/9 pass, unaffected.
- `npx tsc -p tsconfig.json --noEmit`: exit 0, clean.

**Behavior change** (disclosed per NO SILENT BEHAVIOUR CHANGES): BYOK's executor now refuses a
read-only-constrained principal's write-tool dispatch (denied, not run). Inert today — nothing sets a
read-only wire flag for BYOK yet (verified: `assistant-byok.ts`'s `resolveTurnInputsOrRespond` always
builds a plain `{id: authed.id}` principal). Unconstrained callers (100% of production traffic today)
are byte-identical — proven by a paired "UNCONSTRAINED" test in the same new block.

Files changed: `apps/website/src/assistant/tool-executor-stack.ts`,
`apps/website/src/assistant/byok-tool-surface.ts`,
`apps/website/src/assistant/__tests__/byok-tool-surface.test.ts`,
`apps/website/src/assistant/__tests__/read-only-tool-constraint.composition.test.ts`,
`apps/website/src/server/inbound/assistant/agent-daemon-server.ts`.

## Item 2 — SSRF egress policy triplication — FIXED, commits `7b2a2007`, `916eb8b0`

Read all three literals before touching anything:
- `deps.ts:1115` `mailHttpClientPolicy`, `deps.ts:1137` `customCredentialsHttpClientPolicy`,
  `app.ts:~792` inline literal.

All three were byte-for-byte identical (`allowedSchemes: ["https"], denyPrivateAddresses: true,
devHostAllowlist: [], maxRedirects: 0, connectTimeoutMs: 10_000, maxResponseBytes: 1_000_000,
maxDecompressedBytes: 1_000_000`) — no deliberate variation to preserve, so this is a pure
extraction, not a unification decision.

1. `apps/website/src/platform/http/egress-policies.ts` (new) — `SINGLE_HOP_HTTPS_EGRESS_POLICY`.
   Exported directly, not through `index.ts` (deliberately types-only per its own header), matching
   how `createDefaultHttpClient` itself is imported directly. Commit `7b2a2007`, bundled with...
2. ...wiring `app.ts` (free file) to it, replacing the inline literal.
3. `deps.ts`: checked `git status --porcelain` immediately before touching it — clean both times
   (once before starting item 2, once again right before this edit) — never blocked by
   `fix-comp-root`. Wired both locals onto the shared constant and removed the now-moot "Identical
   policy shape" comment. Commit `916eb8b0`.

Also fixed `routes/types.ts`'s `customCredentialsHttpClient` doc, which hand-spelled the same 7
field values AND named the now-renamed `mailHttpClientPolicy` local — the same drift shape this fix
exists to close, so it seemed worth closing rather than leaving a second stale copy of the claim.
Bundled into commit `916eb8b0` since it documents the field `deps.ts` builds.

Did NOT touch the `deps.ts` comment at ~439-441 (module doc claims outbox/event-bus are in-memory)
vs. line 934's real `new SqliteOutboxAdapter(db)` — confirmed both still present, reporting only per
dispatch instructions, not in scope for either item.

**Verification**: `npx tsc -p tsconfig.json --noEmit` clean after each of the 3 file changes.
`platform/http/__tests__/client.test.ts` + `import-boundary.test.ts`: 34/34 pass, confirming the new
file doesn't cross the `transport.fetch` import boundary and the guarded-client behavior (SSRF
denial, redirect stripping, timeout/size caps) is unaffected. No RED-first test added — this is a
behavior-preserving refactor (byte-identical values verified before editing), not a bug fix.

## Everything requested is done.

3 commits total: `08ace3ea` (item 1), `7b2a2007` + `916eb8b0` (item 2). Nothing left outstanding on
either item. Two things flagged for other owners, not fixed here:
- `byok-tool-surface.test.ts`'s pre-existing `query`/`queryLength` assertion drift (unrelated to
  either item, confirmed present before my changes).
- `deps.ts`'s stale in-memory-outbox comment (~439-441) vs. the real `SqliteOutboxAdapter` at line
  934 — not mine per the dispatch.
