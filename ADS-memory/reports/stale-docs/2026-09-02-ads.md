# Stale-docs audit — AI-Dev-Shop + ADS-memory (2026-09-02)

Scope: `AI-Dev-Shop/` framework/skills/harness-engineering/integrations docs,
`ADS-memory/governance|knowledge|meta`, root `AGENTS.md`/`CLAUDE.md`, and
`START-HERE.md` (root, not `AI-Dev-Shop/` — the dispatch's stated path was wrong;
the file lives at repo root). `development/todos.md` was in original scope but
handed off mid-task to `stale-docs-tovu`, who was found already live-editing it
with matching corrections — see "Handoff" below.

### `/Users/la/Programming/Tovu/START-HERE.md`
CLAIMED:  "Status: greenfield... there is no code yet" (line 13-14); "No git in
          this repo yet (deliberate — initialize when you start)" (Housekeeping);
          "the 14 accepted decisions" (ADR-INDEX reference, line 25); "Tailor
          AGENTS.md / CLAUDE.md to Tovu (the copies still speak in Runner terms)"
          (Housekeeping).
ACTUAL:   Repo has `apps/website`, `apps/admin`, `apps/site-chat`, `packages/sdk`,
          2,388+ commits, an active feature branch. `ADS-memory/reports/architecture/ADR-INDEX.md`
          has 57 entries through ADR-062. Root `AGENTS.md`/`CLAUDE.md` are already
          Tovu-specific (no Runner-terms leftover found).
EVIDENCE: `git log --oneline | wc -l` → 2388; `ls apps/ packages/`; `grep -oE
          "^\| \[?0*[0-9]+" ADS-memory/reports/architecture/ADR-INDEX.md | wc -l`
          → 57 (highest ID ADR-062); `grep -n "treat \`tovu/\` as the active root"
          AGENTS.md CLAUDE.md` → no match. This was already flagged in prior
          session memory (`reference_start_here_md_claims_greenfield.md`,
          2026-08-30) but never actually fixed in the file until now.
ACTION:   fixed — rewrote the status line, added a "Housekeeping — done since
          this was written" section, updated the ADR count, and added a framing
          note that the v1-slice/build-order content below is design intent, not
          a from-scratch entry point.

### `AI-Dev-Shop/AGENTS.md` — path placeholder drift (lead #3)
CLAIMED:  N/A — checked whether `<ADS_PROJECT_KNOWLEDGE_ROOT>`/`ADS-project-knowledge/`
          references across 29 files (mostly `harness-engineering/agent-evals/**`
          eval-design fixtures and `harness-engineering/validators/*.py`) would
          fail if followed literally today.
ACTUAL:   Not stale. `AGENTS.md:99-102` already carries an explicit "Legacy note"
          declaring `<ADS_PROJECT_KNOWLEDGE_ROOT>` equivalent to `<ADS_MEMORY_ROOT>`,
          and states the `ADS_PROJECT_KNOWLEDGE_ROOT` env var is "still honored as
          a fallback." Confirmed true in code, not just asserted.
EVIDENCE: `grep -rln "ADS_PROJECT_KNOWLEDGE_ROOT" AI-Dev-Shop/harness-engineering/`
          → hits in 4 `validators/*.py` files and
          `validators/check_codebase_memory_capability.sh`, i.e. the fallback is
          actually implemented, not just documented.
ACTION:   left (no fix needed — doc and code agree).

### `AI-Dev-Shop/AGENTS.md` and root `CLAUDE.md` — cited scripts (lead #4)
CLAIMED:  `harness-engineering/validators/resolve_subagent_mode.sh`,
          `harness-engineering/hooks/session-record.sh`,
          `framework/operations/scripts/ads-initialization.sh`,
          `framework/operations/scripts/install-slash-commands.sh` with flags
          `--check`/`--install`/`--overwrite`/`--include-project`; SessionStart
          hook wiring in `.claude/settings.json`.
ACTUAL:   All confirmed present, executable, and flags match exactly.
EVIDENCE: `ls -la` each path (all exist, `-rwxr-xr-x`); `grep -n -- "--check\|
          --install\|--overwrite\|--include-project"
          install-slash-commands.sh` matches CLAUDE.md's documented usage
          verbatim; `grep -n "ads-initialization.sh" .claude/settings.json` shows
          the SessionStart wiring.
ACTION:   left (accurate).

### `AI-Dev-Shop/integrations/backends.manifest.json` (lead #5)
CLAIMED:  6 backends (`codegraph`, `code-review-graph`, `understand-anything`,
          `graphify`, `codebase-memory-mcp`, `serena`); `graphify` and
          `codebase-memory-mcp` are tier `"blessed"` with named validators.
ACTUAL:   All 3 validator scripts named in the manifest
          (`check_codegraph_capability.sh`, `check_graphify_capability.sh`,
          `check_codebase_memory_capability.sh`) exist and are executable.
          `codebase-memory-mcp` (the blessed backend) is in fact live and
          connected as an MCP server in this very session
          (`mcp__codebase-memory-mcp__*` tools available) — strong corroboration
          the "blessed" designation reflects real, working status, not aspiration.
          `understand-anything`/`serena` (candidate tier, no validator) correctly
          have no `integrations/<tool>/upstream/` dir on disk yet — consistent
          with the manifest's own "nothing installed automatically" policy, not
          a staleness bug.
EVIDENCE: `ls -la` on all 3 validator paths; tool list in this session's system
          reminders; `ls AI-Dev-Shop/integrations/<tool>/`.
ACTION:   left (accurate).

### `ADS-memory/governance`, `ADS-memory/knowledge`, `ADS-memory/meta`
CLAIMED:  N/A — read all files (`constitution.md`, `learnings.md`,
          `memory-store.md`, `project_notes.md`, `project_memory.md`,
          `task-tracker.md`, `governance/README.md`, `governance/adrs/README.md`,
          `governance/adrs/ADR-INDEX.md`, `governance/contracts/*`).
ACTUAL:   No false claims found. `governance/adrs/ADR-INDEX.md` (GOV-ADR-001..004)
          is a genuinely separate, small, accurate registry from the 57-entry
          architecture `ADR-INDEX.md` START-HERE.md cites — both are internally
          consistent, no drift between them. Referenced files
          (`framework/templates/governance-adr-template.md`,
          `skills/adr-governance/SKILL.md`) exist. `constitution.md` is
          self-consistently a "faithful codification... owner ratification still
          recommended" doc — no stale status claim.
EVIDENCE: Direct reads of all files listed above; existence checks on every
          script/template path they cite.
ACTION:   left (accurate). See "Needs your decision" for `task-tracker.md`.

## Handoff — `development/todos.md`

Originally in my scope. Mid-audit, an `Edit` attempt failed with "file modified
since read" — `git diff` showed `stale-docs-tovu` was already live-correcting
the exact same stale entries I had independently verified (§1 architecture
boundary enforcement, §15 DevEx eslint/lint/CI claims, §24 dependency-cruiser —
all previously false, now correctly fixed with "CORRECTED 2026-09-02" notes and
real evidence). Sent them a coordination message ceding the file and flagging
three more stale lines in case their sweep doesn't reach them yet:
- line ~845: "ADR-013 names AG-UI as the protocol; no implementation yet" —
  false, ADR-059 (Accepted 2026-08-18) implements a real AG-UI canary transport.
- line ~848: "no MCP code exists in `src/`" — false (and doubly stale: `src/`
  itself was renamed to `apps/website/src/` at commit `708e81b2`).
  `apps/website/src/assistant/` alone has 50+ MCP-related files including a
  `mcp-federation/` subsystem.
- line ~727-728: "the Agentic UI/AI layer... still almost entirely unbuilt — no
  CopilotKit/AG-UI/MCP code exists anywhere in `src/` yet, only a stub FAB" —
  false. AssistantDock, the daemon, BYOK, MCP federation, and a tool registry
  all ship (confirmed via `find apps -iname AssistantDock*`, matches this
  session's own memory context).
No reply received before this report was written; not verified whether they
acted on the three lines above.

## Needs your decision

- **`ADS-memory/meta/task-tracker.md`** — self-dated "Last updated: 2026-07-30",
  now ~5 weeks behind current work (today is 2026-09-02). It's a journal-style
  living checklist, not a claims-of-fact doc — every entry is dated and it says
  plainly it's a snapshot — so nothing in it is technically false, but treating
  a 5-week-old open-questions list (e.g. "STILL OPEN... Supabase as an actual
  database backend") as current risks re-litigating something that may already
  be resolved. I did not attempt to reconcile a month of work against it — that
  is a project-management refresh, not a doc-correctness fix, and out of
  proportion for this pass. Recommend either archiving it to a dated file and
  starting a fresh tracker, or doing a dedicated reconciliation pass.
- **`development/todos.md`** — handed to `stale-docs-tovu`; confirm with them
  directly whether the 3 flagged lines above got folded in.
- I did not attempt a line-by-line audit of the ~300 files under
  `harness-engineering/agent-evals/**` (eval-design docs/fixtures/benchmark
  suites). These are self-contained eval scenarios, not descriptions of Tovu's
  live state, so they carry much lower "an agent will act on this and be
  misled" risk than START-HERE.md/todos.md/AGENTS.md — flagging as an
  explicit scope decision, not an oversight, given the volume involved.
