# Stale-docs register — Jini (2026-09-02)

Agent: Docs. Repo: `/Users/la/Programming/Jini`. Scope: `packages/*/src/**` comments, every
`packages/*/source-map.md`, package-level `README.md`s. `packages/integrations/src/media-providers/`
is report-only (another agent is actively writing code there) — no edits made in that subtree.

Work was fanned out across parallel forked subagents (source-map.md files, README audit, two
src-comment sweeps) plus direct work by the coordinating agent on `media-providers` (report-only,
including the specific `vendor-adapter.ts` claim flagged in the dispatch). All entries below were
independently verified by whichever agent reports them — see each EVIDENCE line.

---

## Report-only — media-providers (do not edit)

### packages/integrations/src/media-providers/dispatch/vendor-adapter.ts:15-26
CLAIMED:  "several real vendors need a *second* network call inside their 'response parsing' step
          (SenseAudio's SSRF-guarded asset download, AIHubMix's Gemini-native redirect)"
ACTUAL:   AIHubMix's Gemini-vs-OpenAI routing is decided entirely in `buildRequest` (aihubmix.ts:72-123),
          *before* any fetch — a pre-flight routing decision, not a second call inside `parseResponse`.
          AIHubMix *does* have a genuine second-call-in-parseResponse case, but it's the unrelated
          `entry.url` image-download fallback in the OpenAI-shape branch (aihubmix.ts:164-167, via
          `assertAndFetchExternalAsset`) — same SSRF-guarded-download pattern as SenseAudio, just
          mislabeled in this comment as "Gemini-native redirect." SenseAudio's own citation is accurate
          (senseaudio.ts:180-201 does call `assertAndFetchExternalAsset` inside `parseResponse`).
EVIDENCE: packages/integrations/src/media-providers/dispatch/providers/aihubmix.ts:72-123 (buildRequest,
          the Gemini-vs-OpenAI branch, no fetch), :125-176 (parseResponse), :164-167 (the real second
          call); packages/integrations/src/media-providers/dispatch/providers/senseaudio.ts:180,201
ACTION:   report-only (media-providers) — proposed replacement text:

```
 * `parseResponse` is intentionally still a full function, not a further
 * declarative shape — several real vendors need a *second* network call
 * inside their "response parsing" step (SenseAudio's SSRF-guarded asset
 * download, AIHubMix's SSRF-guarded `entry.url` image-download fallback —
 * see `aihubmix.ts`'s `parseResponse`, the non-Gemini branch), which a
 * purely declarative parser DSL can't express without becoming its own
 * bespoke escape hatch anyway. (AIHubMix's Gemini-vs-OpenAI routing is a
 * *separate* decision made entirely in `buildRequest`, before any fetch —
 * see `aihubmix.ts`'s doc comment — not an example of a second
 * `parseResponse` call.) The genericity this module buys is in the
 * *harness* (uniform credential-check -> build -> fetch -> parse flow,
 * pluggable via the registry), not in eliminating vendor-specific wire
 * logic that doesn't actually repeat across vendors. Where response-parsing
 * genuinely IS identical across vendors (MiniMax's and SenseAudio's TTS
 * envelope), see `response-parsers.ts`'s `createHexEnvelopeAudioParser` for
 * the shared, reusable piece.
```

### media-providers — other claims checked, found accurate (no action, listed for the record)
- `source-map.md`'s "Async-polling vendors" / "Simple synchronous REST vendors" bucket (lines ~96-118):
  cross-checked every named `render*` function (Volcengine/Grok/OpenRouter/AIHubMix video, Fal
  image/video, Leonardo image) against `dispatch/providers/*.ts` — none of the "not yet ported" ones
  exist as real exports today; the list is still current. EVIDENCE: `grep -rn "^export.*function
  render(AIHubMixVideo|VolcengineVideo|GrokVideo|OpenRouterVideo|FalImage|FalVideo|LeonardoImage|
  MinimaxImage)" packages/integrations/src/media-providers` → no matches.
- `README.md`'s "Per-vendor renderers" list (lines 45-50): all 18 named renderers exist as real
  exports, and no unlisted renderer exists. EVIDENCE: `grep -rhn "^export (async )?function render"
  packages/integrations/src/media-providers/dispatch/providers/*.ts` → exactly the 18 named.
- `README.md`'s "sideEffects" claim (line 23-25) and `better-sqlite3` dynamic-import claim (line 17-21):
  match `packages/integrations/package.json`'s `sideEffects` field and
  `sqlite-task-store.ts`'s `await import('better-sqlite3')`.

---

## Findings — source-map.md files (fixed unless noted)

### packages/http-kit/source-map.md:339-1825 (20 occurrences)
CLAIMED:  Refers throughout to `@jini/node-host` / `packages/node-host/` as the consumer package of
          `@jini/http` via `createLocalNodeDaemon`.
ACTUAL:   `packages/node-host` doesn't exist — renamed to `packages/server` (`@jini-ai/server`);
          `create-local-node-daemon.ts` lives there.
EVIDENCE: `ls packages/node-host` → not found; `grep -rl createLocalNodeDaemon packages` →
          `packages/server/src/create-local-node-daemon.ts`; `packages/server/package.json:2`
ACTION:   fixed (bulk `node-host`→`server` across all 20 hits)

### packages/http-kit/source-map.md:298-301
CLAIMED:  "`packages/cli/source-map.md` ... does not exist in this repository ... no CLI port has
          happened yet"
ACTUAL:   `packages/cli/source-map.md` exists (622 lines); `packages/cli/src/index.ts` is a real barrel.
          True when originally written, false now.
EVIDENCE: `packages/cli/src/index.ts:1-9`
ACTION:   fixed (rewritten as historical note + current state)

### packages/cli/source-map.md:81
CLAIMED:  "`@jini/http` is a stub (`packages/http/src/index.ts` is a one-line placeholder) with no
          `/status`/`/shutdown` route to call"
ACTUAL:   `@jini/http` (http-kit) has 38 source files including `daemon-status.ts`, registering
          `/api/daemon/status` and `/api/daemon/shutdown`, wired via `builtin-features.ts:754`.
EVIDENCE: `ls packages/http-kit/src/`; `packages/server/src/builtin-features.ts:754`
ACTION:   needs-decision (file falls outside the batch that owned it; flagged for follow-up)

### packages/server/source-map.md:1
CLAIMED:  "# `@jini/node-host` — provenance" (title asserts this as the package's current identity)
ACTUAL:   Package is `@jini-ai/server`, directory `packages/server`; `packages/node-host` doesn't exist.
EVIDENCE: `packages/server/package.json` `"name": "@jini-ai/server"`; `find packages -iname
          "*node-host*"` → no match
ACTION:   fixed (title + explanatory note; historical body left as dated history)

### packages/core/source-map.md:20
CLAIMED:  "Not yet implemented: kernel service tokens (`ToolRegistry`/`ToolExecutor`,
          `Principal`/`Authorizer` ...)"
ACTUAL:   Both are implemented and exported from `@jini/core`'s public barrel; the same file's own
          "Part 3" section (lines 46-60) documents them landing 2026-07-18 — the top summary was never
          updated after that.
EVIDENCE: `packages/core/src/index.ts:14,28`
ACTION:   fixed

### packages/diagnostics/source-map.md:69
CLAIMED:  "`redaction.test.ts`, `zip.test.ts`, `agent-logs.test.ts` ported near-verbatim (all 22 tests)"
ACTUAL:   Those three files now hold 24 tests (coverage extended after the original port).
EVIDENCE: `git log --oneline -- packages/diagnostics/` (two later commits extend coverage); direct
          `it(`/`test(` count = 24
ACTION:   fixed (reworded to "22 tests at the time of this port" + note they now hold 24)

### packages/registry/source-map.md:536
CLAIMED:  `github-client.test.ts` has 37 tests
ACTUAL:   41 tests
EVIDENCE: `npx vitest run src/__tests__/github-client.test.ts` → "41 tests"
ACTION:   fixed

### packages/cli/src/index.ts:7-9
CLAIMED:  "no pack has registered against `CommandRegistry` yet because no HTTP-client-mode pack
          exists in this repo to call"
ACTUAL:   `src/main.ts` (the real `jini` bin) registers `run`/`daemon`/`version` commands against a
          `CommandRegistry` instance.
EVIDENCE: `packages/cli/src/main.ts:235-238`
ACTION:   fixed

### packages/sqlite/source-map.md:285
CLAIMED:  "`src/db/index.ts` re-exports only the four ported concerns' public surface"
ACTUAL:   Also re-exports two undocumented modules: `tool-catalog/` and `chat-history/`, neither has a
          file-map row or provenance entry anywhere in the doc.
EVIDENCE: `packages/sqlite/src/db/index.ts:36-44`
ACTION:   fixed (row annotated with the gap, not fabricated provenance)

### packages/mcp/source-map.md (current-state framing, most recent dated section)
CLAIMED:  "full suite 332/332 passing (18 files)" as current state
ACTUAL:   407 tests, 22 files pass today; two whole tool modules (`tool-catalog-tools.ts`,
          `component-catalog-tools.ts`) ship with zero provenance entry in the doc.
EVIDENCE: `npx vitest run` → "22 passed / 407 passed"; `git log --oneline -- .../component-catalog-tools.ts`
ACTION:   needs-decision (count trivially fixable; the two undocumented tool files need real
          provenance narrative this sweep wasn't equipped to reconstruct)

### packages/memory/source-map.md:373
CLAIMED:  `note-store.ts:170-171` is `assertSafeSubdir`'s final guard
ACTUAL:   That check now lives at `note-store.ts:188-190` as its own exported function
          (`isMultiSegmentOrAbsoluteWin32Path`); line numbers drifted after later helpers were added above.
EVIDENCE: packages/memory/src/note-store.ts:159-171, 188-190
ACTION:   fixed

### packages/memory/source-map.md:406
CLAIMED:  "registers `@jini/http`'s memory routes at the composition root"
ACTUAL:   `registerMemoryRoutes` is exported from `@jini-ai/http-kit`, not a package named `@jini/http`.
EVIDENCE: examples/reference-web/src/daemon.ts:16-21
ACTION:   fixed

### packages/agentic/source-map.md (File map section)
CLAIMED:  Every file lives directly under `src/` (`src/gen-ui/*`, `src/capability.ts`, `src/dom/*`, etc.)
ACTUAL:   A 2026-08-08 restructure (commit `06612923`) moved everything except `src/index.ts` into
          `src/core/`; doc never updated.
EVIDENCE: `find src -type f -name "*.ts"`; package.json exports map (`./core`, `./dom`→`dist/core/dom`);
          `git show --stat 06612923`
ACTION:   fixed (correction note added after the title rather than rewriting every path)

### packages/ui/src/renderers/source-map.md:1 / packages/chat/src/core/source-map.md:1 /
### packages/chat/src/react/source-map.md:1
CLAIMED:  `@jini/renderers-react`, `@jini/chat-core`, `@jini/chat-react` are standalone packages.
ACTUAL:   All three were folded into their parent packages as subpaths (`@jini-ai/ui`'s `./renderers`,
          `@jini-ai/chat`'s `./core` and `./react`) — 2026-08-09 and 2026-08-03 respectively.
EVIDENCE: `.changeset/fold-renderers-react-into-ui.md`; `packages/ui/package.json` exports."./renderers";
          `git log --oneline -- packages/chat/src/{core,react}/source-map.md` (commit `9abc8713`)
ACTION:   fixed (dated fold-notice header added to each; historical body left untouched)

### packages/chat/src/core/source-map.md (2026-07-29 entry) vs packages/chat/src/react/source-map.md
### (2026-07-30 entry)
CLAIMED:  chat-core's entry: "packages/chat-react 610/610 passing" (2026-07-29); chat-react's own entry
          one day later states its pre-addition baseline as "578/43" then "603/603."
ACTUAL:   Unresolved — apparent same-package test-count drop 610→578 in one day with no documented
          deletion event.
EVIDENCE: both files' dated sections, as cited
ACTION:   needs-decision (would require checking out historical commits/running tests; out of
          proportion for a dated historical entry that isn't a current-state claim)

### packages/ui/source-map.md:5691-5695
CLAIMED:  "`packages/chat-react/src/agent-bridge/dom-page-driver.ts`'s doc comment mentions the old
          path and was not edited" (left for a dispatching agent to fix)
ACTUAL:   `packages/chat-react` no longer exists; the file now lives at
          `packages/agentic/src/core/dom/dom-page-driver.ts`. The stale `rich-text-input/` path
          reference the note describes is still present there, at line 620.
EVIDENCE: `find packages -iname "dom-page-driver*"`; `packages/agentic/src/core/dom/dom-page-driver.ts:620`
ACTION:   fixed (appended dated update note in source-map.md; did NOT touch the src file itself — src
          comments were another batch's scope)

### packages/ui/source-map.md:3717
CLAIMED:  "the sandboxed-iframe/postMessage-bridge core (`@jini/renderers-react`, currently a stub)
          does not exist yet"
ACTUAL:   A parallel task landed the real sandboxed-iframe/postMessage core the same day
          (`sandbox-bridge.ts`/`sandboxed-document.ts` are real, non-stub); later folded into
          `@jini-ai/ui` as `src/renderers/`.
EVIDENCE: packages/ui/src/renderers/source-map.md:343; packages/ui/src/renderers/sandbox-bridge.ts
ACTION:   fixed (appended dated update note)

### Pervasive `packages/deploy/` stale-path pattern (out of scope for the batches that found it)
CLAIMED:  Several source-map.md files still cite `packages/deploy/source-map.md` /
          `packages/deploy/src/...` as live paths.
ACTUAL:   Package renamed `deploy`→`devops` (`@jini-ai/devops`); `packages/deploy/` doesn't exist
          outside stale worktrees.
EVIDENCE: `packages/devops/package.json:2`
ACTION:   fixed only where in-scope (`packages/devops/source-map.md`'s own references, and
          `packages/agent-runtime/src/*.ts` doc-header citations — see src findings below). needs-decision
          (not fixed, flagged): `packages/mcp/source-map.md:22,70`, `packages/http-kit/source-map.md:
          271,679,1032`, `packages/memory/source-map.md:279`, `packages/desktop-host/source-map.md:95`,
          `packages/agent-runtime/source-map.md:912,1008` all still cite the dead path.

### Verified accurate, no action (a representative sample; full list is large)
`packages/daemon/source-map.md` — 24-agent-def registry count, per-format def assignments, antigravity
id-guard removal, `authorizeToolInvocation`/`internal.ts` structure (large file, dated/append-only log;
not every one of ~1300 remaining lines individually re-verified). `packages/agent-runtime/source-map.md`
— file maps for `runtimes/`, `agent-protocol/`, `providers/` sections. `packages/protocol/source-map.md`
— `ApiErrorCode` union, `ResolvedRegistryEntrySchema` fields. `packages/artifacts/source-map.md` — file
map. `packages/platform/source-map.md` — file existence, dependency list, coverage thresholds.
`packages/capability-providers/source-map.md` — "wired by @jini/http's connectors.ts" claim, still true.
`packages/desktop-host/source-map.md` — windows-registry.ts naming-vs-behavior claim.
`packages/registry/source-map.md`'s inline `file.ts:NNN-MMM` line citations have drifted in several
places as the file grew (described functions/behavior still accurate, only line pointers stale) — not
itemized, out of proportion to re-audit every citation across ~580 lines.

Pervasive, deliberately NOT flagged: every source-map.md still refers to its own package under the old
`@jini/` npm scope instead of current `@jini-ai/`. Repo history shows the scope-rename sweep
(`fb6d4ae8`, 378 files) explicitly did not touch source-map.md files, and a separate commit
(`291e7d1c`) fixed the same stale scope specifically in `AGENTS.md` because it's "actively read every
session" — implying source-map.md is treated as historical record that intentionally keeps old names.
Only genuine structural folds (package no longer exists at all) were corrected, not the scope prefix.

---

## Findings — README.md files (fixed)

### packages/README.md:71,75,94-97,111-113
CLAIMED:  `@jini-ai/media` exists as a standalone package (dependency table, dispatch-provider
          attribution, peer-dependency table, `sideEffects` example).
ACTUAL:   Collapsed into `@jini-ai/integrations`'s `./media-providers` subpath 2026-08-09 (commit
          `dc6efe5b`). `@jini-ai/chat-react`/`@jini-ai/renderers-react` also don't exist as packages
          (subpaths of `@jini-ai/chat`/`@jini-ai/ui`); both declare `react`/`react-dom` as *optional*
          peers, not required.
EVIDENCE: `git log --oneline --all -- packages/media`; `packages/integrations/package.json` sideEffects
          field (`./dist/media-providers/dispatch/...`); `packages/ui/package.json` and
          `packages/chat/package.json` `peerDependenciesMeta`
ACTION:   fixed (4 clusters)

### packages/chat/README.md:74
CLAIMED:  usage example imports `from '@jini-ai/chat-core'`
ACTUAL:   `@jini-ai/chat-core` retired 2026-08-03 (the same file says so two paragraphs earlier);
          package is `@jini-ai/chat`, subpath `./core`.
EVIDENCE: `packages/chat/package.json` name field; no `chat-core` package.json anywhere
ACTION:   fixed

### packages/ui/README.md:27-30,49
CLAIMED:  chat/artifact UI lives in "`@jini-ai/chat-core`, `@jini-ai/chat-react`, and
          `@jini-ai/renderers-react` (separate packages, kept separate deliberately)"
ACTUAL:   `chat-core`/`chat-react` are subpaths of `@jini-ai/chat`; the renderer registry is
          `@jini-ai/ui`'s own `./renderers` subpath, not a separate package at all.
EVIDENCE: `packages/ui/package.json` exports."./renderers"; `packages/ui/src/renderers/index.ts`
ACTION:   fixed

### packages/admin/README.md:21
CLAIMED:  `@jini-ai/admin/react` *(not built yet)*
ACTUAL:   Built: real `./react` export, 9 source files + 9 test files (`Sidebar`, `DataTable`,
          `RowMenu`, `ConfirmButton`, `ConfirmDialog`, `InteractiveHtmlEditor`, hooks). Only
          `<AdminShell>` and panels are missing.
EVIDENCE: `packages/admin/src/react/index.ts`; `find packages/admin/src/react -type f`
ACTION:   fixed

### packages/admin/README.md:136-138
CLAIMED:  "Slice 1: `/core` and `/browser`, 76 tests ... `/react` is not built yet, and of the twelve
          planned ports only `AdminIdentityPort` is specified"
ACTUAL:   `/core` has 75 tests, `/browser` has 0 test files, `/react` has 147 tests. `src/core/ports/`
          specifies 16 ports, each with real exported interfaces/types.
EVIDENCE: `grep -rhoE "^\s*(it|test)\(" packages/admin/src/{core,browser,react}`; `ls
          packages/admin/src/core/ports/`
ACTION:   fixed

---

## Findings — src/** comments (fixed)

### packages/agent-runtime/src/{auth,capabilities,claude-stream,env,executables,invocation,
### json-event-stream,launch,mcp,metadata,mmd-routes,models,opencode-log,paths,prompt-augmenter,
### prompt-budget,prompt-file,qoder-stream,registry,resolution,terminal-launch,types}.ts (21 files)
CLAIMED:  "Ported verbatim from OD's `apps/daemon/src/runtimes/core/<file>`" (a `core/` subdirectory)
ACTUAL:   No `core/` subdirectory exists in OD's `apps/daemon/src/runtimes/` — every cited file sits
          flat directly in `apps/daemon/src/runtimes/`. Content diffs confirm the ports themselves are
          genuinely verbatim; only the cited path was wrong.
EVIDENCE: `find /Users/la/Programming/OSS-Repos/open-design/apps/daemon/src/runtimes -iname paths.ts`
          → flat path, no `core/`; confirmed via directory listing + diff for all 21 files
ACTION:   fixed (all 21 files, path-only correction)

### packages/devops/src/deploy/{reachability,github-pages,types,vercel,cloudflare-pages}.ts,
### packages/agent-runtime/src/providers/anthropic-messages.ts:8
CLAIMED:  Doc comments cite `packages/deploy/source-map.md` / `packages/deploy/src/netlify.ts`.
ACTUAL:   Package renamed `deploy`→`devops` (`@jini-ai/devops`); `packages/deploy/` doesn't exist in
          the working tree (only in stale worktrees).
EVIDENCE: `packages/devops/package.json:2`; `ls packages/deploy` → not found
ACTION:   fixed (6 files, path-only correction)

No false comments found (after triaging ~145 verified hits in group A packages and ~815 comment-anchored
hits triaged, ~30+ deep-verified, in group B) beyond the two clusters above. Both sweep agents report
several "ported from/verbatim" comments referencing the external OD origin repo that could not be
independently re-verified from Jini alone in their assigned packages (group B); group A's agent had OD
repo access and cross-checked ~15 such claims directly, finding them accurate.

---

## Not executed / out of scope

- Full line-by-line coverage of every source-map.md and README.md was infeasible at this scope
  (~19,300 lines of source-map.md + ~3,235 lines of README.md across 25 + 28 files); work was
  prioritized per the dispatch's own ranking (provenance/wiring/count/status claims first, prose
  ignored). Several files note "spot-checked, not exhaustive" explicitly above.
- `packages/daemon/source-map.md`'s ~1300 lines of addenda beyond the four load-bearing claims checked
  (routines/scheduler, terminal-session.ts, MCP-injection mechanisms, retry-classifier reconciliation,
  byte-journal) were not individually re-verified.
- `better-sqlite3`-dependent test counts in `packages/registry/source-map.md` (183/183, 104/104) could
  not be run in this sandbox (no compiled native binding) — environment gap, not a doc defect finding.

## Commits

No git commands were run by any subagent (per directive, the coordinating agent commits centrally).
The coordinating agent will commit all edited Jini files in one commit after this register is written.
