# ADR Index

Decisions are numbered and immutable once accepted; supersede rather than edit.
Context for ADR-002…009: `tovu-v1-design.md` §8 (weaknesses review) and the
2026-07-01 design session.

**Deliberately absent:** ADR-056, 057 exist on disk (`reports/architecture/ADR-0{56,57}-*.md`) but are withheld by their own header — each says explicitly "**Do not add to `ADR-INDEX.md` until a human accepts it**" and both remain DRAFT. This is unlike a PROPOSED entry (048/058/060/062/063/064), which the index carries pre-acceptance; these two asked not to be. Checked 2026-09-06 — still DRAFT, still absent on purpose. Re-check this note if either is accepted.

**ADR-055 accepted 2026-09-08** (owner sign-off — Leona) and moved out of the list above into the table below. Its own dependency on ADR-053 (then still DRAFT, see the list above) was left open rather than resolved at the time — see ADR-055's own "Open item" note. **Follow-up, same day:** ADR-053 has since also been accepted (owner sign-off — Leona) and moved into the table below too, resolving that open item; ADR-055's own "Open item" note is left as-written (a dated record of what was true when it was written), not edited to match.

**ADR-053 accepted 2026-09-08** (owner sign-off — Leona) and moved out of the list above into the table below. Its own 2026-09-08 "Status note" addendum records what has and hasn't shipped since its 2026-08-03 authoring: Decisions 1/2/4/6 shipped (browser-as-host, the `@jini-ai/ui` confirmation builder, the daemon-side fork point via the `kind:'ext'` channel, and the stopgap exclusion of `content_post_delete` lifted now that it works); Decision 3 was superseded outright by ADR-055's single held-open call rather than built as decided; Decision 5's underlying `okResult()` risk is unfixed in the pinned dependency but unreached by any shipped tool today.

| ADR | Decision | Status |
|---|---|---|
| [001](ADR-001-agentic-commerce-platform.md) | Agent-native commerce platform: modular monolith, OperationRegistry, plan/execute | Accepted |
| [002](ADR-002-blessed-rendering-target.md) | React is the blessed v1 rendering target; contracts stay renderer-agnostic; Vue shell → contract test | Accepted |
| [003](ADR-003-plugins-never-get-ddl.md) | Plugins never run DDL; extension fields in namespaced JSON with indexing/promotion policy | Accepted |
| [004](ADR-004-plugin-artifact-format.md) | Plugin artifact: prebuilt ESM + signed manifest, versioned side-by-side in the install dir | Accepted |
| [005](ADR-005-sdk-compatibility-promise.md) | SDK public-API surface, semver, deprecation ladder, API snapshot tests. **Amended 2026-07-08 (ADR-024):** transport-agnostic ABI (§6) — hooks async-only, payloads serializable-only, no live core objects, caps by handle; frozen now so Tier-2 out-of-process isolation is a later runtime swap, not a semver break. | Accepted (amended 2026-07-08) |
| [006](ADR-006-ports-rule-of-two.md) | A port requires two plausible adapters, one being built now | Accepted |
| [007](ADR-007-structural-workspace-scoping.md) | `workspaceId` required in every event, repo port, job, cache key (implemented) | Accepted |
| [008](ADR-008-change-sets.md) | Change-set vocabulary (actorId/changeSetId/tables) in v1; full propose/preview/apply/revert later | Accepted |
| [009](ADR-009-decoupling-strategy.md) | Hybrid decoupling: typed calls (sync), outbox events (async), hooks (extension), compensation (multi-step) | Accepted |
| [010](ADR-010-declarative-themes-by-default.md) | Themes are declarative (no code) by default for the third-party library; code themes = trusted mode | Accepted (amends 002) |
| [011](ADR-011-deployment-topologies-open-design-host.md) | Two topologies: standalone single binary (primary) + open-design Electron desktop as multi-site AI host; arrow points open-design → Tovu only | Accepted |
| [012](ADR-012-site-template-and-instantiation.md) | New sites instantiate a versioned template into the install-dir; runtime is shared, not copied per site; full copy only at binary export (implements 011's fork/export) | Accepted |
| [013](ADR-013-assistant-copilotkit-agui-tool-surface.md) | Assistant = one CopilotKit client + one AG-UI daemon agent; `tools.ts` registry with an execution `surface` (frontend/data) is the seam; agent detection ported from OD; composer rebuilt headless, not ported | Accepted |
| [014](ADR-014-assistant-profiles-layered-capability-manifest.md) | One assistant engine + N declarative Profiles (consumer/admin/operator); registry entry adds `contexts`/`scope`/`auth`/`effect` axes; manifest layered across Tovu (site tools) + Tovu-Runner (operator tools); filter enforced server-side | Accepted (extends 013) |
| [015](ADR-015-drizzle-sql-data-layer-behind-ports.md) | Drizzle ORM is the SQL data layer (adopted 2026-07-06), strictly behind the repo ports; generated migrations committed; one shared schema satisfies rule-of-two (SQLite now, Postgres next); swap cost bounded to the infra adapter layer | Accepted (relates to 006) |
| [016](ADR-016-agentic-document-editing.md) | Agentic document editing: agent makes bulk edits via CopilotKit frontend actions (not MCP — editor is browser-side); every edit set is a propose→review→accept/reject→revert change-set; we build the review/diff layer ourselves (no paid Tiptap Content AI); edit tools RBAC-gated | Accepted (extends 013, applies 008) |
| [017](ADR-017-three-pane-content-template-preview-editor.md) | Three-pane editor (Content/Template/Preview) = the data→template→output layers; template pane is ADR-010 trusted-mode code, preview sandboxed; React blessed, Vue/Angular as contract-tested targets; assistant edits across panes via the ADR-016 review layer | Proposed (blocked on theme system) |
| [018](../pipeline/001-admin-command-gateway/adr.md) | Admin command gateway = the single admin mutation write path (Decorator over feature calls); revert executor + `(entityType,operation)` inverse-applier registry; `ChangeSetRepoPort` rule-of-two (in-memory now, Drizzle/SQLite next); concurrency via compare-and-set status flip; actor/permission seam reserved in the gateway signature (Art. VI dev-only exception). Full evaluation in the SPEC-001 pipeline ADR. | Accepted (implements 008; relates 006/007/009/015) |
| [019](ADR-019-theme-bundles-plugin-dependencies.md) | Theme bundles: a declarative theme may declare plugin dependencies (`requires`) resolved + installed in one consented step; behavior stays in the plugin plane (no JS-capable theme class); referenced deps primary, vendored allowed; catalog labels "pure theme" vs "theme + plugins (requests permissions)" | Accepted 2026-07-07 (extends 010; relates 003/004; SPEC-004 OQ-06, SPEC-005) |
| [020](ADR-020-theme-capability-tiers.md) | Theme capability tiers: **Declarative** (ADR-010 default, data-only) / **Templated** (NEW — **LiquidJS**, sandboxed logic, no JS) / **Code** (ADR-010 §2 trusted mode, JS). `theme.json.tier` drives onboarding + install consent. Liquid renders over the *same* component registry; TipTap content injects via `{{ content }}`. Tier-2 guardrails (pin ≥10.26.0, isolated render, allowlisted tags/filters, lint-before-publish) extend C6/REQ-06. "Dev wants JS" = Tier 3, not a new untrusted-JS class (ADR-019 rejection stands). Tier-3 isolation deferred. **Amended 2026-07-07 (swarm debate):** §6 correction — same-origin theme JS can steal the admin session, so Tier-3 client JS needs a *separate cookie-less origin + CSP `connect-src 'none'`*, else themes ship no code (motion via signed plugin components); render-IR is canonical (JSON + Liquid both serialize to it); prefer Astro/raw-JSX over Next. | Accepted 2026-07-07 (extends 010; relates 002/019/004/003/017; SPEC-004/005) |
| [022](ADR-022-content-model-entries-registry-expression-indexes.md) | Content model: generic `entries` + **content-types-as-data registry** (post/page seeded, not hardcoded); universal typed columns + namespaced JSON `fields.ext.{owner}.*` (validated on write); **query surface = core-provisioned typed/partial/composite expression indexes** on the JSON path (`CREATE INDEX`/`DROP INDEX` only, engine-maintained → zero drift/no rebuild tooling, ports to PG jsonb) — **refines ADR-003's "generated column" mechanism**, verified on SQLite 3.49.2; never-brick foundations as v1 acceptance criteria (**single write chokepoint + CI canary**, complete append-only revisions w/ actor+monotonic seq, ULIDs) so full event-sourcing is adoptable later behind the unchanged API; generic `taxonomies+terms(parentId)+entry_terms`. Defers the replay engine. **Open:** plugin own-tables ceiling inherited from ADR-003 (owner not sold → TODO §6). **Amended 2026-07-08 (ADR-024):** `pluginId` write-attribution at the chokepoint; expression language must be total/bounded-cost (Tier-1 trust primitive). | Accepted 2026-07-08 (extends 003; relates 021/015/010/020/001/008/009; from 3-round swarm debate; amended 2026-07-08 by 024) |
| [021](ADR-021-identity-and-authorization.md) | Identity & authorization: one `principals` table (user/agent/api_key/system); humans get RBAC (roles→policies→permissions, Directus-shaped), machines get scoped grants, plugin capabilities stay a separate axis (SPEC-005). `authorize()` is ordinary core code called by the gateway — **no PolicyPort** (rule-of-two; one evaluator). Flat permission strings + code-side catalog (`tovu permissions list`); ADR-014 ranks re-expressed as strings (one language). **Composite `(workspace_id,id)` FKs** on every join (relational workspace isolation); **disable-only principals** (audit durability). Agents = delegated principals, live `grant ∩ delegator` intersection. `api_keys` in v1 (Tovu-Runner headless auth). Per-site `content.db` storage; argon2id + revocable sessions; ABAC seam nullable + fail-closed. | Accepted 2026-07-07 (fills SPEC-001 seam; relates 006/007/008/011/013/014/016/015/020; from 2-round swarm debate) |
| [023](ADR-023-core-mediated-plugin-data-modules.md) | Core-mediated plugin data modules — **lifts ADR-003's no-DDL ceiling** (resolves ADR-022 §Open / TODO §6): plugins may own real relational tables, but only by **declaring schema-as-data that core alone executes**; never a raw DB handle; **namespaced `p_{pluginId}__*`**; **capability-gated** (`dataModule`, default-deny); **snapshot-before-every-DDL** (SQLite backup API) → fail = restore + leave uninstalled; **typed-only writes** (preserve ADR-022 chokepoint + attribution) + **`sqlite3_set_authorizer`-sandboxed raw reads**; **retain-data-on-uninstall**; never-brick = *recoverable to a working state*, not zero-loss; transform DSL + resumable core-run backfill; **v1 = seams only** (namespace + `dataModule` key + stable IDs + whole-file snapshot/restore + journaled timeline + SDK seam), engine built v-next against a real demand plugin. **SPLIT-finalized:** recoverability half holds unconditionally now; access-control half (gate/typed-writes/sandboxed-reads) is **advisory until ADR-024 §4 Tier-2 isolation ships** (a Tier-3 in-process plugin can bypass it). | Accepted 2026-07-11 (extends 003/022; relates 024/015/021/008/011; resolves TODO §6; from 2-round swarm debate + 3-round `/audit-work` `TM-adr023-dataModule-001`, round-3 unanimous PASS Codex 9.0/agy 10.0) |
| [024](ADR-024-plugin-execution-and-trust-model.md) | Plugin execution & trust model: **tiered "install from anyone"** (generalizes ADR-020 to plugins) — **Tier-1 declarative** (zero-code manifest: types/fields/taxonomies/expression-indexes/settings/declarative admin; install-from-anyone-safe *now*) / **Tier-2 sandboxed code** (ships *with* the marketplace; first rung = **per-site Electron `utilityProcess` isolation**, machine→site blast radius) / **Tier-3 trusted code** (today's in-process reality; local/first-party only; labeled "full machine access"). **Marketplace forcing function:** "from anyone" = Tier-1 now / Tier-2 later / **never Tier-3**; Tier-3 never listable ⇒ shipping the marketplace *is* shipping the sandbox; installability ≠ enablement. **Amends ADR-005:** freeze a transport-agnostic ABI **now** (async + serializable-only, no live objects, caps by handle) — irreversible under the semver promise. Tier-1 safe only if ADR-022's expression language is **total/bounded** (amendment). Capability manifest **namespace shape** frozen now, contents iterate; hooks = explicit priority + no cross-plugin deps in Phase-0; client/admin-JS isolation = separate ADR (generalizes ADR-020, unblocks OQ-07). Spine = two ladders (trust + recovery) + control/extension planes. Motivating fact: multi-site Electron host → one in-process plugin owns every site's DB/secrets. | Accepted 2026-07-08 (extends 020/004/005; relates 003/023/025/021/022/011/019; SPEC-005; **ACCEPTED gate cleared** — plugin-catalog demand audit `reports/audits/20260708-plugin-catalog-demand-audit.md`: Tier-1 covers ~60–73% of demand, vs ~50% bar; condition = core-mediated primitives on the near-term roadmap; from 2-round swarm planning debate) |
| [025](ADR-025-plugin-client-js-isolation.md) | Plugin client / admin JS isolation — **generalizes ADR-020 §6 from themes to plugins**: plugin-contributed admin/settings/editor/client JS runs in a **sandboxed cross-origin, cookie-less iframe**, never same-origin with the operator session (else same-origin JS `fetch()`es core admin APIs with the operator's cookie = silent session theft). Talks to core **only via `postMessage` RPC** (origin-checked, serializable-only, **aligned with ADR-024 §3 ABI** — no live objects); **strict CSP** (locked `connect-src`, network only as a core-mediated capability); **capability-gated** (`ui.adminPanel`-style, default-deny, ADR-021 axis). Decide-now boundary, mechanism built with the panel work. **Unblocks OQ-07** (admin-surface/extension-panel registry). One dissent: agy would defer to Tier-2 UI work; majority (Codex/Fable/Primary) decide-now (cheap + blocks OQ-07). | Accepted 2026-07-16 (owner sign-off; extends 020; relates 024/021/013/014/022; unblocks OQ-07; from 2-round swarm planning debate, 3-1 majority, agy dissented) |
| [026](ADR-026-core-mediated-atomic-multi-write.md) | Core-mediated **atomic multi-write primitive** for plugin data — **amends ADR-024 §3 / ADR-023 §7**. Origin: a 4-way `/cowork` ABI-ergonomics spike (Opus/Fable/Codex/Gemini) found the one *correctness*-shaped cost of the frozen async ABI is **no shared transaction across `await`** — multi-row/multi-table invariants force hand-rolled OCC + a compensating undo that is *itself racy*. **Redesigned via a 2-round formal `/debate` (2026-07-11, full 3-way convergence):** public author surface is **named, manifest-registered, schema-checked commands** (not raw table tuples), compiling to an internal **guarded, transactional IR** — a frozen (not deferred) bounded-predicate + relative-mutation (`increment`/`decrement`) grammar reusing ADR-024 §5's total/bounded-cost discipline; explicit **tagged scalars, exactly one representation per field** (ISO-8601 dates; `money-int` XOR `money-decimal`, non-float relative-mutation math); a frozen `scope: "plugin"\|"coordinated"` discriminant (only `plugin` executes in v1, namespace derived from `pluginId`); explicit **reads-are-advisory** correctness statement + read-only guards for unmutated rows. Evidence: `reports/spikes/20260709-plugin-abi-ergonomics-finding.md` + debate report `reports/swarm-consensus/runs/20260711T160337Z-adr026-atomic-write-consensus-report.md`. Cleared 3 `/audit-work` rounds under `TM-adr026-atomic-write-001` (round 1 FAIL → 5 fixes folded; round 2 FAIL on 1 new finding in a self-found fix → fixed; round 3 unanimous PASS, agy 10.0/Codex 10.0, zero findings) — **then reopened**: an independent Fable-model verification pass (explicitly requested to check the audit, not assumed unnecessary) found 2 further gaps neither external auditor caught across 3 rounds — unsafe/unspecified comparison-guard evaluation for `money-decimal` (F1, hard blocker) and an unscoped/contradictory guard-target namespace rule (F2) — both fixed. **Round-4 re-audit then found the same gap shape recurring a 4th time** for ISO-8601 date ordered-comparisons (fixed) — **and round 5 found it a 5th time** for generic-string collation. Rather than patch a 6th instance, §2 now states a **closed-vocabulary rule**: every scalar/operator combination must have specified evaluation+storage semantics or is disallowed (full enumeration incl. generic `number` bounded to `MAX_SAFE_INTEGER`, pre-emptively closed before any round reached it). **Round-6 (first full-pass, not diff-only, per user feedback on audit scope) found 3 bugs in the round-5 fix itself**: baseline `set`/`in`/`isNull` accidentally banned for every type, `number` bound missing `MIN_SAFE_INTEGER`, and no `NaN`/`Infinity` exclusion — all fixed. **Round-7 (full-pass) found 2 more gaps in the round-6 numeric-bounding fix**: the "both checkpoints" bound language couldn't actually apply to a relative-mutation *result* (live row value unknown until execution — needed a third, execution-time in-transaction bound check) and generic `number` still admitted `-0` (silently collides with `0` on storage/round-trip) — both fixed. **Pre-round-8 Coordinator self-review (no external tokens) found 2 more instances of the same defect shape**: `money-int`'s wire representation was never specified at all (unfounded "BigInt-safe" claim); `money-decimal`'s "canonical" form was asserted but never defined (same `-0`-collision-shaped gap, for decimals) — both fixed. Broad/full-pass `/audit-work` scope is now a **hard standing rule** for this project, not a preference. **Round-8 full-pass re-audit: agy 10.0 PASS / Codex 8.0 FAIL (1 blocker)** — execution-time checkpoint generalized from generic `number` to all relative-mutation-capable scalar kinds. **Round-9 added a 3rd independent auditor (Fable subagent): agy 10.0 PASS / Codex 7.0 FAIL (2 blockers) / Fable 8.2 FAIL (1 blocker + 2 low)** — generic `number` relative mutation now requires `Number.isSafeInteger`; intra-batch guard-evaluation ordering made normative; date canonical form now requires calendar-valid ranges (was admitting `T24:00` aliasing next-day `T00:00`); `money-decimal` scale must be >= 1; `money-int` wording clarified. All 5 fixed. Round-10 (agreed exit condition: clean/advisory-only across all 3 = ACCEPTED): Codex 9.6 PASS (1 advisory) / agy 10.0 PASS (zero findings) / Fable 8.6 FAIL — 1 real blocker: `p_{pluginId}__{table}` namespace encoding wasn't injective (plugin `shop` + table `eu__orders` collided with plugin `shop__eu` + table `orders`), a genuine cross-plugin isolation bypass — plus 3 low notes (coordinated-scope rejection checkpoint, `expectedVersion` operand kind, column affinity for date/number/boolean). All 5 fixed. Round-11 (same exit condition, not met): Codex 6.8 FAIL / agy 9.8 PASS (advisory-only) / Fable 7.0 FAIL — 2 independent auditors found 2 different residual collisions in the "fixed" namespace encoding (case-folding: `Shop`/`shop` collide under SQLite's case-insensitive identifiers; boundary-underscore merge: `acme_`+`inventory` and `acme`+`_inventory` both collide) — closed structurally this time via a restricted lowercase-alphanumeric-plus-hyphen grammar (no underscore permitted) replacing the delimiter-ban patch, plus string/`null` column affinity and `expectedVersion` counter-source/nonexistent-row clarifications. All 5 fixed. **Round-12 (exit condition MET): Codex 9.1 PASS / agy 10.0 PASS / Fable 9.4 PASS — zero blockers from any auditor for the first time since round 3; both Codex and Fable independently ran from-scratch falsification attempts against the round-11 namespace grammar (9 distinct attack families incl. case-folding, boundary merges, Unicode normalization, SQLite quoting/DQS, identifier truncation, prefix-subsumption) and found no collision. 5 corroborated advisories folded (mandatory SQLite identifier quoting, ADR-023 charset-alignment note, identifier length bound, `null`-guard lowering via `isNull`, generic-`number` affinity pinned to `REAL`).** ADR-026 marked **ACCEPTED**. | **Accepted** 2026-07-11 (amends 024/023; relates 005/022; SPEC-005; redesigned from `/cowork` spike via formal `/debate`; cleared 12 full-pass, 3-auditor `/audit-work` rounds under `TM-adr026-atomic-write-001`, agreed exit condition met round 12) |
| [028](ADR-028-settings-layered-ledger.md) | Settings — **Layered Settings Ledger** (dedicated tables, NOT settings-as-entries; killed Option A on the authority-surface argument — `content.write` reaches agents). Own tables reuse **ADR-022** discipline (single write chokepoint + append-only `setting_revisions` + bounded/total validation + ULIDs + CI canary): `setting_definitions` (schemas-as-data registry) + per-scope `setting_values_{global,workspace,user}` + `setting_revisions`. Resolver `user ?? workspace ?? global ?? default` (I1–I5 + alias-transparency, depth ≤1). **Round-1 audit `TM-settings-001` FAIL → all fixes folded here:** B1 values key on stable `setting_id` only (no ns/key on value tables → rename can't orphan) · B2 CHECKs bar site-owned defs from the global scope + from platform namespaces (no cross-tenant leak) · B3/F1 **marker-row rename** (fresh-ULID v1 alias marker; active def keeps setting_id/version → the alias CHECK never bricks a retyped setting) · B4 FK `RESTRICT` + ledgered **purge service** (no CASCADE bypass of the chokepoint; GDPR redacts, never deletes the ledger) · normative **secret gate** (reject `secret:true` until secret-store ADR) · **F3 full `settings.*` catalog** (definition-lifecycle + reset + read perms + `site.*` namespace fencing → no shadowing) · totality (reject NULL default), `op='coerce'` repair (pure read-path, ledgered write-back), PRAGMA/sentinel/def-cache workspace-qualification. **Core-only subset ships now** (retire `PresentationSettingsRepoPort`→`core.presentation.activeThemeId`, theme presets→`theme.{id}`); **plugin path gated on capability-taxonomy-v1** (ADR-024 §6); **secret path gated on the Integrations ADR**. | Accepted 2026-07-11 (reuses 022; relates 021/007/003/023/024/020/025/015/009; core-only greenlit to spec; from 3-round swarm debate + 3-round `/audit-work` `TM-settings-001`, round-3 PASS Codex 9.1/agy 9.8) |
| [027](ADR-027-media-assets-subsystem.md) | Media / Assets subsystem (Tier-2 `media` lib). **Hybrid model:** a seeded `media` **entry** (ADR-022: editorial fields + revisions + taxonomy + `entry_refs`) + two **core-owned single-writer sidecars** `asset_blobs` + `asset_renditions` (narrows INV-3, stamps attribution); no third table — the entry is identity, source binding = **write-once** `bodyJson.$.source.sha256`. **Storage:** `BlobStorePort` content-addressed, per-workspace dedup PK `(workspace_id, sha256)`, **workspace-prefixed keys + per-generation storage epochs** (the GC race-killer). **`ImageTransformPort`** out-of-process node-worker + test-double (rule-of-two); scan/video = seams, not ports. **Transforms:** named declarative only (append-only `transform_registry` that outlives its declaring theme; serve-if-exists), no arbitrary `?w=&h=` (deferred). **Frozen immutable URL** `/m/{assetId}/{transformName}.v{version}/{slug}.{ext}` (version-in-path — immutable via write-once assetId; source-replace mints a NEW entry + ADR-026 batched ref-rewrite; slug/ext cosmetic; `Cache-Control: immutable`); `bodyJson` stores refs not URLs. **GC (audited):** "unreferenced" defined over non-purged entries + refs + snapshots; `BEGIN IMMEDIATE` serialization; bytes-before-row / unlink-after-commit; journaled 2-phase; **grace = max(30d default, oldest retained snapshot)** (retention owned by the pending Storage/Backups primitive, not ADR-023). **Security:** one `MediaIngressPolicy` (SSRF resolve-then-connect, pixel-bomb, magic-byte); re-encode renditions (EXIF strip); **originals never on admin origin** — mint-only signed URL on **cookie-less media origin** (generalizes ADR-025 to uploaded bytes) + attachment/nosniff; MIME allowlist keyed by pipeline (pdf/av/archives private-only); **SVG IN** behind `media.upload_svg`; no live scanner v1 (quarantined seam + launch-gate); `blob_kind` remote = metadata-only v1. **Surfaces:** one registry, flat `media.*`, admin + AI share handlers (no back door). **v1:** ~22 items IN; deferred TUS/arbitrary-transforms/live-scanner/transcoding/S3-adapter, each with a named seam. | Accepted 2026-07-09 (extends 022/025; relates 012/006/007/021/009/023/026/024/020; from 2-round swarm debate + round-1 audit TM-media-001 FAIL → blockers folded → round-2 re-audit PASS (Fable 8.7; Codex 8.3 clause-gaps; Gemini degraded) → 6 clause-gap amendments folded) |
| [029](ADR-029-menus-navigation.md) | Menus / navigation (admin section). Consumes ADR-039 routing `urlFor`/`isActive`; `RouteTarget` union is routing-owned; term-target schema (`entry_refs`/`term_refs`) owed; perms `admin.menus.manage`. Overnight-sweep draft; round-2 cross-cutting fold + Round-3/4 audit folds applied; backend built (`src/navigation`). Wave 1. | Accepted 2026-07-10 (sweep 029–036; relates 039/022; cleared `/audit-work` `TM-admin-sweep-001`, 3 rounds, unanimous PASS) |
| [030](ADR-030-members.md) | Members (admin section). Imports ADR-037 mail; **publishes `AudienceDirectoryPort`** (`subscriberId=memberId`, anon signup=minimal member); ships ADR-021 `kind='member'`. **Owns the D1c consent record** — `member_consents` **Tier-2-pinned always-present**, purpose-keyed, evidence-payloaded, `members.consent.request/confirm/revoke`; magic-link rate-limit = HARD precondition; magic-link URLs via ADR-040. Perms `admin.members.*`. Backend built (`src/members`). Wave 1. | Accepted 2026-07-10 (sweep; relates 037/021/022/040; D1c LOCKED 4–0; cleared `/audit-work` `TM-admin-sweep-001`, 3 rounds, unanimous PASS) |
| [031](ADR-031-comments.md) | Comments (admin section). Depends on ADR-023 grammar + ADR-026 (+025 UI); anonymous-ingress `system` actor = seeded principal ULID; implements the `principal.erasure.requested` GDPR handler; perms `feature.comments.post`/`admin.comments.moderate`. Backend built (`src/comments/*`, SPEC-033/035/036) + admin moderation frontend shipped. Wave 2. | Accepted 2026-07-10 (sweep; relates 023/026/025; cleared `/audit-work` `TM-admin-sweep-001`, 3 rounds, unanimous PASS) |
| [032](ADR-032-seo.md) | SEO (admin section). Restate ADR-028 as PROPOSED; consumes ADR-039 `canonicalUrl` composed from ADR-040 origin (host-header-injection-proof), `seo.base_url` retired (Round-3 fold); `page.head` seam owed to theme owner; perms `admin.seo.manage`. **Wave 1 contingent on ADR-028 ACCEPTED** + 039/040 frozen (both now accepted). | Accepted 2026-07-10 (sweep; relates 028/039/040; cleared `/audit-work` `TM-admin-sweep-001`, 3 rounds, unanimous PASS) |
| [033](ADR-033-redirects.md) | Redirects (admin section). Gated on ADR-039 (forward chain + D2b slot, now accepted); the `SlugChangeCapture` **implementer is core Tier-2** (ADR-026 tx-handle fix); fail-closed `LINK_PRESERVATION_UNAVAILABLE`; open-redirect oracle covers the fully-interpolated target on both read AND write paths (Round-3 fold — closed a real SSRF gap); perms `admin.redirects.manage`. Wave 2. | Accepted 2026-07-10 (sweep; relates 039/026/040; cleared `/audit-work` `TM-admin-sweep-001`, 3 rounds, unanimous PASS) |
| [034](ADR-034-newsletter.md) | Newsletter (admin section). Imports ADR-037 mail + ADR-038 http; consumes Members' `AudienceDirectoryPort`; **owns confirmation mechanics only** (D1c writes into Members via `members.consent.*`); unsubscribe token = `KeyringPort.derive()` + `consent_revision_id`; links via ADR-040; must NOT ship to real recipients before Members live. `SubscriberDirectoryPort` corrected to a single-evaluator dependency, not a port (Round-3/4 fold). Perms `admin.newsletter.*`. Wave 2. | Accepted 2026-07-10 (sweep; relates 037/038/030/040; cleared `/audit-work` `TM-admin-sweep-001`, 3 rounds, unanimous PASS) |
| [035](ADR-035-analytics.md) | Analytics (admin section). **Split (D4):** `analytics-ingest` (beacon + PII-death-at-`AnalyticsSinkPort` + `KeyringPort.derive()`-based salt custody, Round-3/4 fold corrected the PII claim + erasure scope) stays Tier-2 core; storage+dashboards+export = bundled Tier-3 plugin with a **binding re-home clause**; imports ADR-038 `ForwardingSink`; perms `admin.analytics.view`. Ingest backend built (`src/analytics`). Wave 2. | Accepted 2026-07-10 (sweep; relates 038/023/022; cleared `/audit-work` `TM-admin-sweep-001`, 3 rounds, unanimous PASS) |
| [036](ADR-036-integrations-api.md) | Integrations / API webhooks (admin section). **Imports `HttpClientPort`+`EgressPolicy` from ADR-038**; allowlists from ADR-040; fixed crypto drift; `KeyringPort` gains a generic `derive()` method (Round-4 fold — homes the crosscutting derivation primitive Newsletter/Analytics also need); `webhooks.beforeDispatch` failure is fail-closed by behavior, not just label (Round-3 fold); perms `admin.integrations.manage`. Backend built (`src/integrations`). Wave 1. | Accepted 2026-07-10 (sweep; relates 038/040/025; cleared `/audit-work` `TM-admin-sweep-001`, 3 rounds, unanimous PASS) |
| [037](ADR-037-core-mail-primitive.md) | `core/mail` primitive — the single Tier-2 `MailerPort` (rich shape, `capabilities()`, `MailerSendResult` union), resolving the double MailerPort collision (030/034). `SmtpMailerAdapter`+`HttpApiMailerAdapter` are the real rule-of-two pair (Console/in-memory don't count, Round-3/4 fold clarified doctrine scope). Round-2: `sourceContext`/feedback-correlation, required `idempotencyKey`, webhook-signature verify, non-optional `sendBatch`. Implemented (`src/mail`), all 3 consumers refactored to import it. | Accepted 2026-07-10 (from sweep-crosscutting-001/002; extends 006/009/024; supersedes 030/034 local ports; cleared `/audit-work` `TM-admin-sweep-001`, 3 rounds, unanimous PASS) |
| [038](ADR-038-core-http-egress-primitive.md) | `core/http` primitive — `HttpClientPort` + structural **`EgressPolicy`** (only-constructor-returns-guarded-decorator; no consumer gets an unguarded client; reserved `rateLimit` slot added, Round-3 fold). Round-2: address-family-complete SSRF deny (IPv6/IPv4-mapped), module-private transport + import CI-canary, `requestPinned` transport, strip-auth-on-redirect, decompressed cap; allowlists from ADR-040. Implemented (`src/http`), Integrations refactored to import it. | Accepted 2026-07-10 (from sweep-crosscutting-001/002; extends 006/024/025; supersedes 036 local port; cleared `/audit-work` `TM-admin-sweep-001`, 3 rounds, unanimous PASS) |
| [039](ADR-039-routing-contract-v0.md) | `routing` contract v0 — forward resolver chain (Redirects) + inverse `urlFor`/`isActive` (Menus/SEO) + routing-owned `RouteTarget` + the D2b in-tx slug-capture slot (capture-failure aborts rename); acknowledges Menus as a real second in-tx participant (Round-3 fold). Round-2: **`SlugChangeCapture` implementer is Tier-2 (ADR-026 collision fix)**, global lock order + DB-only + no-outbox-in-tx, fail-closed no-op, `RouteResolveContext` workspace scope; `canonicalUrl` from ADR-040. Implemented (`src/routing`, entryRef/url/route resolution; termRef + Redirects-side rules deferred as named seams). | Accepted 2026-07-10 (from sweep-crosscutting-001/002; extends 009/022; gates 033; cleared `/audit-work` `TM-admin-sweep-001`, 3 rounds, unanimous PASS) |
| [040](ADR-040-core-origin-registry-v0.md) | `core/origin` registry v0 — trusted canonical-origin source (`OriginRegistryPort {canonicalOrigin→VerifiedOrigin; isAllowedRedirectTarget; isAllowedEgressTarget}`) for SEO canonical, magic-links, newsletter links, redirect read-path, egress allowlist. Verified workspace setting, never raw request host (host-header-injection-proof); `scheme` widened to allow `http` for dev-capability same-origin only, https-only unconditionally on the cross-origin allowlist path (Round-3/4 fold fixed a real bug here). Implemented (`src/origin`), open-redirect oracle probe-verified against real Node `URL` parser behavior (IDNA/punycode, trailing-dot, backslash bypass). | Accepted 2026-07-10 (from sweep-crosscutting-002; extends 006/007/024; relates 032/021/034/033/038/039; cleared `/audit-work` `TM-admin-sweep-001`, 3 rounds, unanimous PASS) |
| [042](ADR-042-structural-debt-remediation.md) | Structural debt remediation (graph-audit): shared SQLite repo lookup base (`findOneBy`, ~35 sites/11 adapters), settings register-definitions dispatch split into write-service dedupe + closed-union route dispatch, `content-db.ts→server/seed.ts` boundary fix, `MailPort` shape freeze + `ATTACHMENTS_UNSUPPORTED` fail-closed pin, dedupe `bootAuthenticated` test helper (14 files). Pages/Posts merge considered and **rejected** (owner decision, reaffirmed 3-vs-1 in debate with a falsifiable revisit tripwire). Cleared `/audit-work` (`TM-ADR042-STRUCTURAL-006`, 1 blocker found+fixed, 2 re-audit rounds clean) and a 2-round `/debate` (7-of-9 points unanimous; 2 points — MailPort freeze timing, Pages/Posts timing — majority-decided, dissent recorded, owner accepted majority as final). | Accepted 2026-07-15 |
| [043](ADR-043-collections.md) | Collections (operator-defined content types): first real build of ADR-022's deferred `entries`/`content_types` engine — a content-type registry (data, not DDL) + validated JSON field bag + core-provisioned expression indexes. Rejects Directus-style per-collection tables (ADR-023 `dataModule` is for plugin code, not zero-code operators) and EAV. **Storage shape took 4 debate rounds to converge** (oscillated new-table → additive-`posts` → identity-anchor across R1-R3, each participant landing on a different answer) — resolved unanimously in R4 by an explicit Coordinator optionality/reversibility argument: new `entries` table, `posts` left fully untouched, migration deferred. From a 4-round swarm `/debate` (2026-07-14); cleared 6 rounds of `/audit-work` under `TM-ADR-CONTENT-ADMIN-005`. | Accepted 2026-07-14 |
| [046](ADR-046-production-readiness-and-composition-hardening.md) | Production-readiness and composition hardening: classify every capability by runtime mode; make externally visible state durable; await critical boot and expose readiness/module status; move composition to feature-owned typed server modules; enforce import/route/documentation fitness in CI. Retains the modular monolith and rejects a DI container or microservices. **Debate fold-in (2026-07-16):** outbox durability is demand-paged (trigger: first production SMTP adapter, not webhooks/inventory-first), BR-04 transaction-seam needs its own pre-Phase-1 design decision, Phase-0 enforcement is a purpose-scoped `MailerPort` seam gate (not constructor-level — verified members/forms share an identical `purpose: "transactional"` tag), Phase-1 durability scheduling is pull-based per capability, webhook `payload_json` NULL-then-update race folded as a named Phase-1 pre-req, `dependency-cruiser` baselines alongside Phase 0, worktree isolation mandated during Phase 3. | Accepted 2026-07-16 (owner sign-off; 2-round swarm debate + 2 Fable adversarial-review additions, unanimous on Candidate A; relates 006/007/009/015/027/030/035/036/038/040/042) |
| [044](ADR-044-categories-and-tags.md) | Categories & Tags: one shared taxonomy system (`taxonomies`/`terms`/`entry_terms`), categories=hierarchical/tags=flat via one boolean, real relational joins (never JSON arrays). `entry_terms` uses a soft polymorphic `(workspaceId, contentType, contentId)` reference (precedented by ADR-041's sidecar actor-identity pattern) so it ships now against `posts`, independent of ADR-043 Collections. Converged unanimously across all 3 debate rounds. Cleared 6 rounds of `/audit-work` under `TM-ADR-CONTENT-ADMIN-005` (highest finding density of the batch, all fixed). | Accepted 2026-07-14 |
| [045](ADR-045-backups-recovery-screen.md) | Backups/Recovery admin screen: extends ADR-041 by designing the Recovery screen ADR-041 left as a "sibling face, not yet specified." Two separate screens (Storage=Timeline, Recovery=action), never tabs; Recovery's IA centers on a blocking, itemized discarded-write-window disclosure shown before any restore confirmation; `costClass`-aware degraded states; supersedes the pre-ADR-041 `/admin/backups`+`/admin/database` sitemap entries. Converged unanimously across all 3 debate rounds. Cleared `/audit-work` under `TM-ADR-CONTENT-ADMIN-005`, clean since round 3. **Naming correction 2026-07-20:** "Storage" renamed to "Database" throughout (see ADR-041's naming-correction note); Recovery unchanged. | Accepted 2026-07-14 |
| [047](ADR-047-widgets-region-and-embed-placement.md) | Widgets: entry-native instances (reuse ADR-022, same substrate as ADR-029 menus) with **dual placement** — site-wide region binding via a seeded `widget_area` entry + derived/reconciled binding index (mirrors `nav_location_bindings` exactly — debate-amended from raw binding rows) + inline TipTap `widgetEmbed` node (page-scoped, resolved into the render IR before the `{{content\|render_rich_text}}` seam, chokepoint-enforced guardrails). New explicit resolver contract (batch-first, cost-bounded, failure-isolated). Registry split into data (registration) vs. code (behavior, ADR-024 tier-gated). `entry_refs` extended for widget refs + config-internal refs — stated as a real v1 build dependency (verified not yet running code), not a free reuse. Contact Form ships in v1 (thin adapter over the already-built `src/forms/`+`MailerPort`). Competitor survey: WordPress/Drupal are the real prior art; Ghost/Strapi/Directus/Payload don't have this pattern at all. | **Debate cleared** 2026-07-21 (2 rounds, full 4/4 convergence — Primary/agy/Codex/Fable; see `reports/swarm-consensus/runs/20260721-widgets-adr047-consensus-report.md`); owes `/audit-work` before ACCEPTED. **Discrepancy flagged 2026-09-06:** the implementation has since shipped and is live (`apps/website/src/features/widgets/`, 19 files, commits through 2026-09-05) while the one `/audit-work` run against it (`20260721T171409Z-adr047-widgets`) is still **FAIL** (agy 6.0/10, below the 8.5 floor, blocking gate FAIL on 2 authorization/`entry_refs` findings) and has never been re-run. Status stays NOT Accepted deliberately — shipped code is not itself an audit pass — but the gap between "live in production" and "owes its audit gate" is an owner decision to close (re-run `/audit-work` now that later widgets commits may have addressed the findings, or explicitly accept the residual risk). |
| [041](ADR-041-storage-timeline.md) | Storage admin surface: read-first **Timeline** over the never-brick ledger (migrations/snapshots/index-provisions/template-upgrades, drift banner); **one write op** — forward-migrate, snapshot-anchored, no rollback verb (raw row edit/SQL console/DB-first mode all rejected). `db-ops` port (SQLite online-backup / Postgres `pg_dump`+blue-green); **operational ledger moves to a sidecar `ops/` journal in the install-dir** (survives `content.db` restore — extends ADR-012's layout); `plan→confirm→execute` gateway with `authorize()` at **both** confirm and execute (ADR-021 §2/§6); **composite `(workspace_id,id)` actor identity** on every ledger row (ADR-021 §4, soft-referenced across the sidecar/`content.db` boundary); **amends ADR-023 §4** with an explicit `index.provision`/`index.drop` no-restore-point carve-out (+ mandatory Postgres `CREATE INDEX CONCURRENTLY`); **global `storage_write_watermark`** (not a per-entry ADR-022 §4b sequence) drives an honest cross-table discarded-window disclosure (entries+sessions+identity/authz+plugin tables); mandatory sensitive-column redaction (`users`/`api_keys`/`sessions`) in the optional Tier-3 read-only browser; Postgres blue/green gets an explicit `CUTOVER` state-machine phase; quiesce-residual honesty corrected to **ADR-024 §4 Rung 2** (capability sandbox), not Rung 1. **Amends SPEC-003**: `SERVE_SITE` auto-migrate is cost-gated (cheap → same snapshot-anchored path under a no-token boot policy; expensive/unavailable → refuse + surface pending). Emerged from a 3-round swarm `/debate` (2026-07-09, unanimous endorsement, not-ADR-ready pending a 3-blocker/6-must-fix punch-list) + this fold-in pass; cleared a 3-round `/audit-work` pass under `TM-ADR-STORAGE-CONTENT-004`, unanimous PASS. | Accepted 2026-07-14 |
| [048](ADR-048-api-key-issuance-and-create-principal-plumbing.md) | API-key issuance plumbing (SPEC-006 REQ-08 gap-fix): closes a Programmer escalation that found zero `ApiKeyRecord`/`ApiKeysRepoPort`/schema/adapter plumbing anywhere despite `APIKEY_ISSUE`/`APIKEY_REVOKE` being spec'd since v0.5.0. Adds `api_keys` (schema design done here, small/mechanical transcription of state.spec §2 — Database Agent dispatched only to verify indexes + generate the migration). **Key-hashing primitive: SHA-256 over a high-entropy random secret, ordinary code (not a port)** — argon2id rejected as a threat-model mismatch for a machine-generated, high-entropy credential (promoted to Governance ADR GOV-ADR-004). **`CREATE_PRINCIPAL`'s HTTP surface is `[NEEDS CLARIFICATION]`, routed to Spec Agent, not decided here** — it is fully specified at the state layer (state.spec §3) but has zero HTTP/CLI surface in the approved api.spec.md, an unreachable-transition gap distinct from an implementation gap. Two `critical-internal-constraints.md` units designated (`ESCALATE_SECURITY`/`ESCALATE_IRREVERSIBLE`): the AC-26 wildcard-source-policy check must run independently of, not inferred from, the INV-07 clamp; `revokeApiKey`'s frozen-policy retirement must assert exactly-one-row before deleting, since `ApiKeyRecord` carries no stored pointer to its frozen policy. | **PROPOSED** — requires human architecture sign-off; `CREATE_PRINCIPAL` additionally blocked on Spec Agent ratification; no fresh Red-Team/`/audit-work` pass has run over this plumbing (relates 006/021/046) |
| [050](ADR-050-settings-change-propagation.md) | Settings change propagation: **removes ADR-028 §8's per-layer VALUE cache outright** (keeps the definition cache) rather than repairing it — its stated precondition, "exactly one repo instance for the process lifetime," became permanently false when ADR-049's agent daemon shipped as a second OS process sharing the same SQLite file, and with no TTL a stale entry survived until restart. Reproduced only when the server happened to hold the key cached, so the same tool call worked or not depending on what had been read moments earlier. Rejected TTL / DB generation counter / cross-process invalidation messages; `assistant/custom-instructions.ts` had already hit this and concluded an uncached read is "cheap: one row," so this generalizes its per-call-site workaround. Open tabs then re-read via two independent triggers onto one bus carrying **namespace names only, never values** — assistant-run completion (deliberately not keyed on tool names: coupling that rots vs. a few sub-ms reads) and an **SSE feed that polls `setting_revisions.seq`**, which is cross-process by construction because the ledger is shared state and an in-process emitter never was; `Last-Event-ID` doubles as the cursor so reconnects resume rather than skip. Disclosure is an allowlist (another principal's user-layer change is withheld — a bare namespace name would reveal they exist and are active). A refresh **refuses** to overwrite uncommitted edits and re-checks after its own await; surfaced and fixed a latent bug where `timer.current` was never nulled on debounce fire. Verified live: agent write and a raw `sqlite3` write both flipped the UI in place, no restart, no reload. | Accepted 2026-07-31 (implemented + verified; amends 028 §8; relates 007/021/049, SPEC-007) |
| [049](ADR-049-assistant-adopts-jini-kit-supersedes-copilotkit-agui.md) | Assistant engine adopts the published `@jini-ai` kit (`core`+`daemon`+`node-host`+`http`+`agent-runtime`+`agentic`+`chat-core`+`chat-react`) as the tool-execution/run/chat substrate — **supersedes ADR-013 §1–4,8's CopilotKit + AG-UI client/protocol choice**, never implemented (`@copilotkit/*`/`@ag-ui/*` were never installed). ADR-014's Profile model (consumer/admin/operator; `contexts`/`scope`/`auth`/`effect`) is retained in full, re-expressed as the `ToolPolicy` fed into Jini's `ToolExecutor` — Tovu keeps owning *what* is exposed to *whom*, Jini owns *how* a call runs/streams/audits. Existing `features/*/agent-tools.ts` domain catalogs become the registered manifest content, not thrown away. Multi-agent support (Jini's 24-adapter `AGENT_DEFS` registry) replaces an uncommitted, rejected Claude-only CLI-spawn build that had no tool-registry/profile gate at all. In-page DOM agent control (`@jini-ai/agentic`'s `page.*` verbs) explicitly deferred to a follow-up ADR. | Accepted 2026-07-28 (owner sign-off in conversation, matching ADR-025/046 precedent; not yet through `/debate`/`/audit-work`; relates 011/013/014/021) |
| [051](ADR-051-admin-server-state-layer.md) | Admin server-state layer: adopts TanStack Query but confines it to `apps/admin/src/lib/fetch-query` behind a 4-export, library-free contract, with an **ESLint `no-restricted-imports` rule at `error`** as the load-bearing guarantee (an abstraction protected only by reviewer diligence acquires a second importer and the rip-out claim silently becomes false). Context: the admin had NO caching — 40 non-test files/153 `api.*` call sites each re-fetching on mount via a hand-written `useState` triple — which cost a real ~2.5s 24-process CLI `--version` sweep every time the Execution tab remounted, and left writes refreshing only whoever remembered to call `load()`. The tipping signal was ADR-050 shipping `settings-refresh-bus`+`change-feed`+SSE: a hand-rolled cache-*invalidation* system for an app with no cache. **Explicitly NOT adopted in Jini** — `@jini-ai/ui` is port-injected (23 feature files take `Port`/`dependencies`; its `fetch()` calls live in swappable `dependencies.ts` defaults), so a query library there would force a `QueryClientProvider` on every host and invert the contract. Consequence, named rather than left implicit: a **two-tier model** — React-level `fetch-query` for Tovu-owned sections, port-implementation memos for Jini-driven surfaces whose requests never cross a Tovu React boundary (`execution-settings.ts`'s `cachedDetection` is correct where it is, not a cleanup target). **Prefix invalidation promoted to a contract term** (`['redirects']` invalidates `['redirects',id,'hits']`) and test-pinned, since a `Map`-backed replacement doing exact-key equality would compile, pass review, and silently stop refreshing derived data. Answers ADR-050's "do not reintroduce a cache": that failure needed two processes with no cross-process invalidation and no TTL — none holds for a single browser tab — but its discipline is kept via `staleTime: 0` and `retry: false`. One pilot screen (`Redirects.tsx`) migrated and verified live; the other 39 files are deliberately uncommitted pending review of the pilot. **Two external adversarial review rounds (gpt-5.6-terra xhigh) folded in:** round 1 found the lint boundary was claimed-but-unenforced (ESLint ran nowhere in CI — `npm run lint` is Biome-only and non-blocking), a disabled query leaking a cached error against its own contract, a stale write-error banner blaming a write that succeeded, and inventory counts inflated by a grep that could not match multi-line `api\n  .method()`; round 2 found the widened boundary still bypassable via dynamic `import()`, `.mts`, and an over-broad `adapter.*` exemption, plus a stale list-refresh error masquerading as a write failure. All fixed and re-probed; `npm run complexity` is now a blocking CI step. | Accepted 2026-08-01 (infra + pilot implemented and verified; relates 028 §8/049/050, SPEC-007) |
| [052](ADR-052-tovu-runner-is-its-own-desktop-product.md) | Tovu-Runner is its own desktop product assembled on `@jini-ai/desktop-host/electron`; open-design untouched. **Supersedes ADR-011 §2** (open-design's Electron app as the multi-site host) and **reverses its `desktop/`-cancellation consequence**; ADR-011 §1 (standalone single binary) unaffected, and its HTTP-boundary/no-source-import discipline restated — only *which* app sits across the boundary changes. Resolves a tension nothing reconciled: ADR-011 assigned the host role to open-design, ADR-014/SPEC-006 treated Runner as its own product (SPEC-006 shipped `api_keys` in v1 *because Runner needs headless auth*), and open-design's current source has **zero** Tovu-spawning code — one grep hit, in a doc. **Electron not Tauri** on assembly-completeness, not preference: Electron implements all 6 ports across 8 files each with a test; Tauri's `NotImplementedError` appears in 5 files (no JS-reachable `printToPDF`/custom-scheme equivalent) vs 1 anywhere under `electron/`. **Assembly + one new component:** the recon's verification pass found "70-80% exists" *understated* — `allocatePort({…, reserved: Set<number>})` already supports allocating N non-colliding ports as a call shape, and `resolveDaemonRegistryPath(dataDir, fileName)` is parameterized by both, so N instances is N calls, crash-safe liveness included. The genuinely missing layer is the **fleet supervisor** (owns the project list, allocates ports as a coordinated set, starts/stops/health-checks children, surfaces them in a UI). **Topology re-verified in source:** one project = one OS process = one port = one workspace for its lifetime (`resolve-workspace.ts` resolves one at boot, baked into `deps`, 35+ routes reject mismatches, `app.ts:796` `// single-workspace v1`); `content.db`'s multi-row support is orthogonal ADR-007 tenancy so a *grown* install still boots, **not** a concurrency mechanism. **Scope calls:** `@jini-ai/admin` NOT adopted (Runner's nav is app-level and spans projects; `@jini-ai/ui`'s `./chat` + `@jini-ai/agentic/a2ui` still in scope); **per-instance owner credentials NOT provisioned in v1** — owner decision, analysed and declined, not missed: `@jini-ai/cms` requires `ownerPassword` with no default precisely so no host ships a shared credential, but Tovu supplies `TOVU_ADMIN_PASSWORD ?? "tovu-dev"` via `seedIdentity` on the **serve** path, so every spawned site boots `admin`/`tovu-dev` absent an override; accepted as a deliberate local-desktop posture with a named tripwire (first non-loopback bind or first publish) and a cheap deferred mitigation. **Corrects the recon body** (lines 133/146/190/203): "no way to log into `/admin` after `tovu init`" is wrong — the owner *is* seeded, on serve not init; the search looked for `firstBoot`/`seedOwner`, the symbol is `seedIdentity` in identity wiring. Method lesson: an empty symbol search is evidence about names, not capability. **Repo:** orphan branch `rebuild/v1` on the existing Tovu-Runner repo (Tovu's literal pre-split ancestor, frozen mid-rename, 389 never-committed changes, no reusable code); `main` intact at `fdaf214`, abandoned tree moved out (1.9GB → 2.7MB after dropping 5 stale `node_modules`, a 621MB turbopack cache, and a graph DB; ~850KB was real source). **IA:** `admin-sitemap.md` §2's carved-out **(A) App rail** ("spans projects… out of scope for this sitemap except that the project admin mounts inside it") *is* Runner — surface (B) nests inside it. Collapsible rail → chat panel → main; chat is an operator agent driving ADR-014's `create_site`/`generate_video`/`queue_task` composed one-way above Tovu's site tools. Twelve sections grouped Fleet / Work / Operations / Access. **Carried forward:** SPEC-006 OQ-05 (cross-site federation, owned, resolve-by 2026-11-30), shared-vs-per-instance BYOK secrets (no evidence either way), and — **resolved same-day** — Runner's backend is **Jini-native** (`@jini-ai/daemon` run lifecycle + `ToolExecutor` + agent executor, `http-kit` routes, `sqlite` store; supervisor registers as a pack on that daemon), on the ADR-049 precedent, so `runner.*` verbs inherit the deny-by-default gate rather than re-deriving one — they start and stop OS processes, a more dangerous surface than the content tools the gate was built for. Hybrid rejected: the supervisor is the part most needing an audited boundary. | Accepted 2026-08-03 (owner sign-off in conversation, matching ADR-025/046/049 precedent; not yet through `/debate`/`/audit-work`; supersedes 011 §2; relates 007/012/014/021/049, SPEC-003/006) |
| [054](ADR-054-public-visitor-assistant.md) | The public visitor assistant is a **separate product** from the admin assistant (ADR-049), sharing only UI components. Triggered by "put the chat on the frontend, period" — measuring the public site showed that is not a component mount: pages are **fully server-rendered with zero executable JavaScript** (only `application/ld+json` on `GET /welcome`), rendered via `src/server/http/site/render.ts` across five themes, and the only JS bundle Tovu serves anywhere is `apps/admin/dist` at `/admin` (`app.ts:760-762`). **Backend decision: the direct-provider proxy, NOT the agent-CLI daemon.** Mounts `@jini-ai/http-kit`'s `registerModelProxyRoutes` (`POST /api/proxy/{provider}/stream`), which was built and tested (http-kit 83/83) but mounted by **nothing** in Tovu — `grep -rn "api/proxy" src apps/admin/src` returned 0 hits, while Jini's own server mounts it at `builtin-features.ts:389`. Decisive property is not preference but failure mode: the daemon **spawns an OS process per run** with filesystem + write-capable MCP access (entries, forms, identity, widgets), so N anonymous visitors spawn N processes and any visitor could rewrite the site; the proxy has **no tool surface by default**, making the allowlist *additive* rather than a subtraction from a write-capable agent — restrictions that must be enumerated can be forgotten, a default-empty surface fails closed. **Tool allowlist:** `search_published_entries`, `get_published_entry`, `list_categories`; everything else denied, and **published-only is contract, not detail** (an assistant that reads drafts leaks unreleased content). Provider key stays server-side — the whole point of the relay. **Injection at the renderer, not per theme**, since five templates means five things to desync and silently omits every custom theme, contradicting "on the frontend, period." Costs named: a **second build target** that must not import admin code (an accidental import ships admin internals to anonymous visitors); an anonymous endpoint against a paid API is a **cost-attack surface needing rate limiting before public exposure**; and **Gemini is unusable with tools today** — measured 2026-08-03, `google-messages.ts` sends no `thought_signature` and every currently-served Gemini model rejects the tool continuation with HTTP 400 (Anthropic/OpenAI unaffected). | Accepted 2026-08-03 (decision recorded; implementation not started; relates 049, SPEC-007) — **Open:** rate-limit design, whether logged-in admins see the visitor assistant or the admin one, and prompt-injection via published content |
| [058](ADR-058-site-assistant-credential-store.md) | The site's public-assistant provider key gets a **dedicated, encrypted-at-rest credential store** — not the settings ledger (ADR-028 §6 blocks `secret:true` and a non-secret registration would put a live key in the append-only, exportable `setting_revisions` log) and not a second admin BYOK. **First real implementation of `SecretSealerPort`**, the seam ADR-036 §8 named and deferred ("the recoverable secret-store ADR") — two other subsystems (lipay merchant credentials, deploy-plugin tokens) already hit this same gap and sidestepped it with install-wide env vars because neither needed a UI-rotatable key; this feature can't take that shortcut. AES-256-GCM via `node:crypto`, no new dependency; key derived through the existing `KeyringPort.derive()` (`EnvOrFileKeyring`, already wired for webhook signing/newsletter tokens) reusing `TOVU_INTEGRATIONS_ROOT_KEY` rather than minting a second root secret, purpose-namespaced (`"secret-sealer.v1"`) for domain separation. A **dedicated fail-closed instance** (`allowFileFallback:false`) means a missing root key throws `SECRET_STORE_UNCONFIGURED` rather than silently generating a key file, deliberately asymmetric with the shared webhook-signing instance's file fallback (justified: a regenerated signing-secret file is low-blast-radius, a paid provider key encrypted under an operator-unaware key is not). **Write-only contract**: PUT never echoes the key, GET returns `isSet`+a pre-computed `masked` column (matches `@jini-ai/ui`'s existing `••••`+last-4 convention) with zero decrypt on the read path. New core migration `site_assistant_credentials` (one row per workspace, CHECK enforcing the sealed-blob columns are all-null or all-set). Runtime consumer (`site-assistant.ts`) prefers the stored credential and falls through to the existing `env.GEMINI_API_KEY` on any absence or decrypt failure — additive, existing deployments and the E2E suite are unaffected. **Flags a real upstream gap** rather than forking around it: `@jini-ai/ui`'s `ByokProviderForm`/`ByokConfig` has no write-only/masked mode (`apiKey` is a plain required string, `onConfigChange` wants plaintext back), so Phase 3 needs a thin Tovu wrapper for the key field specifically while still reusing `ExecutionTab`/`SettingsDialogShell` chrome for everything else. Honest limitation stated plainly: defends a leaked DB backup, not a compromised live host (env var + DB together = full compromise). | PROPOSED — Phase 1 (architecture) checkpoint; owner sign-off required before Phase 2 server work begins; relates 028 §6/036 §8/049/054 |
| [059](ADR-059-assistant-transport-ag-ui-canary.md) | Reopens ADR-049's transport conclusion only: AG-UI (`@ag-ui/core`/`@ag-ui/client`/`@ag-ui/encoder` — confirmed MIT-licensed, neutral `ag-ui-protocol`-org packages, not CopilotKit's commercial layer) ships as a third, flagged, deletable `ChatTransport` behind `chat-core`'s existing reduced `AgentEvent` seam, additive to the existing Local CLI and BYOK paths. **Verified live**, not asserted: canary proved out with 10 real Tovu tool calls through a real browser. ADR-049 Decisions 2–7 (tool execution via `@jini-ai/core`/`daemon`, the Profile model, `agent-tools.ts` catalogs, multi-agent support, in-page DOM control deferral, reuse boundary) are unaffected and remain in force; `mcp-ui`/`a2ui` (unrelated to AG-UI despite the name) stay out of scope. | Accepted 2026-08-18 (owner sign-off in conversation; Decision 3 amended same-day — corrected the CopilotKit-adjacency reasoning, not the outcome; supersedes 049 Decision 1 only; relates 049/013/014) |
| [060](ADR-060-runner-unified-operator-chat.md) | Runner's unified operator chat — one chat that can drive both the fleet and individual sites, without blurring the LEFT (`runner.*` fleet operator) / RIGHT (Tovu site admin) chat boundary `sections.ts` exists to enforce. **D1:** every verb the unified chat calls still begins with `runner.`; Tovu tool definitions are never registered into Runner's allowlist — site reach is delivered by Runner-owned verbs calling Tovu's HTTP API, so `runner.*` leaking into Tovu's public visitor widget (the failure mode ADR-054 designed against) becomes structurally impossible rather than a discipline to maintain. **D2:** the OpenAPI converter's output (`tovu-openapi-mcp.ts`) becomes a catalog for schema validation/discovery, never registered as tools — measured over `Tovu/openapi/*.yaml`: naive registration is ~400 callable tools across 3 sites, unfiltered (`x-status: planned` filters zero operations today), including `delete_user`/`delete_role`. Two-tier surface instead: a small set of curated fan-out verbs (media-import, credential-propagation) owning partial-failure reporting, plus one generic `{projectId, operationId, args}` escape hatch validated against the catalog — flat tool count regardless of N×M, cost named honestly (the allowlist gate moves from per-operation to per-verb, requiring an explicit destructive-operation deny-list). **D3:** the stale auth-gap comment in the converter is corrected — Tovu's api-key auth (ADR-048) is verified shipped, not proposed; bootstrap must go through a real admin session since API keys cannot self-issue (`rejectApiKeyCredential`); new projects get a persisted-in-vault random owner password (`TOVU_ADMIN_PASSWORD` only seeds a *new* DB — `seedIdentity` never re-seeds an existing owner, so a per-spawn random password would lock Runner out after first boot); every existing project stays on `admin`/`tovu-dev` until adopted via login + key issuance + optional `reset_user_password`; Runner's key authority is capped by INV-07 to never exceed its bootstrapping owner. **D4:** a Tovu flag to suppress the admin assistant inside Runner's embedded webview is a named tripwire gated on Tier 1/Tier 2 shipping first, not decided here. Runner has zero test coverage today, so a fan-out verb acting destructively across N sites ships onto an untested codebase; only 8 of 29 declared `runner.*` verbs are implemented. | PROPOSED — requires owner sign-off before implementation; relates 052/048/021/049/054/014, SPEC-006 |
| [061](ADR-061-runner-tools-for-tovu-site-assistant.md) | A narrow `runner.*` surface for Tovu's site assistant — **supersedes ADR-014 §3's capability clause only** ("Tovu never imports Runner tools"), as applied to what the admin-context assistant may *invoke*; ADR-014's Profile model, axes, layered manifest and server-side-enforcement rule are unchanged and are what this leans on. Mirror image of ADR-060, deliberately far more bounded. **The ADR-011 one-way arrow is NOT violated:** that rule governs *code* dependencies, and Tovu's external-MCP store is a generic extension point — the row Runner writes holds a launcher path and opaque tool-name strings, so zero Runner knowledge ships into Tovu (verified: no `apps/website/src` file imports from Tovu-Runner). What changes is the **capability/trust boundary**: `sections.ts:5-16`'s "the right chat cannot stop a process" is now enforced by an allowlist rather than by the `runner.` prefix. **D1:** a second, separate allowlist (`Tovu-Runner/src/contracts/site-assistant-tools.ts`), first and only entry `runner.create_site` — two files, not one list with a boolean, so adding a fleet verb cannot widen the site surface as a side effect. **D2:** `runner.project.delete`/`.stop`/`.restart`/`.navigate` are excluded **at compile time** (`as const satisfies readonly Exclude<RunnerToolName, FleetOnlyVerb>[]`; adding one is a TS2820 and `npm run typecheck` is a gate) — required widening `RUNNER_SECTIONS` from an annotation to `as const satisfies` so the literals survive. **D3:** audience is a property of the bearer TOKEN (discriminated union, per-project credential), never of a request body. **D4:** three redundant gates — advertisement, invocation, and a `ToolPolicy` denying the `tovu-site-assistant` role anything outside the narrow list. **Threat model:** the site assistant reads site content, which is user- and (once published) attacker-supplied, so the question is not what is convenient but what is safe for an attacker who can write text into a site to trigger; `create_site` is purely additive (worst case = a junk site), whereas delete has no undo and stop/restart take a *sibling* site off the air. Revisiting the exclusions requires all three of: a Runner-rendered confirmation outside the injected context, a reversal path, and own-project-only scoping. **D5:** no credential enters the site's db — the row stores a path to a per-boot, mode-0700 launcher, so a stable path carries per-boot secrets and the row never goes stale (also sidesteps ADR-058 sealing and its 503). **D6:** records the new data direction the media work introduces (site → Runner's shared library), which touches ADR-027 and does **not** inherit this ADR's safety argument — it writes attacker-influenceable bytes into a store other sites read. **Verified end-to-end 2026-08-29**, not asserted: federation admitted exactly `mcp__runner__runner_create_site`; one site-assistant turn created "Dummy created website" (port 3002) in Runner's fleet; a direct MCP probe got `isError` for delete/stop/navigate/project.list; Runner's own left chat still reaches `runner.project.list`. **Costs named:** Runner has zero test coverage so nothing regression-tests the three runtime gates; registration authenticates as the seeded `admin`/`tovu-dev` (ADR-060 D3 is the exit); a registered row makes `TOVU_ADMIN_ASSISTANT=off` sites start an agent daemon anyway (read from source, `off` branch not exercised); `sections.ts`'s header sentence is now false as written and must be corrected. | ACCEPTED — signed off 2026-08-29; supersedes 014 (capability clause only); relates 011/060/058/052/054/027/049 |
| [062](ADR-062-runner-shared-media-library.md) | Runner's shared media library — the design ADR-061 D6 deferred. **Runner has no media library at all today** (`runner.generate_video`/`generate_image` are declared in `sections.ts:127` and unimplemented), so a blob store is a precondition for the generation verbs, not only for site contributions. **Store (D1–D6):** blobs at `<userData>/media/blobs/<ab>/<cd>/<sha256>` + an index in Runner's existing sqlite — explicitly NOT inside any project's `uploads/`, because `deleteProject` recursively removes the install dir (`project-provisioner.ts:550-551`) and would erase the library along with whichever site contributed first. **Copy, never reference** (a reference dangles on project deletion, and silently follows a superseded asset after an ADR-027 write-once source-replace); the blob outlives its contributor. **Dedup on sha256 alone** — not Tovu's `(workspaceId, sha256)`, since a *shared* library is one namespace by definition — with ADR-027's `bodyJson.$.source.sha256` accepted as a *claim* and re-hashed after ingest, because it arrives through a prompt-influenced path. **Attribution from the credential, never the arguments** (the bridge's existing rule, per ADR-061 D3), modelled as a many-to-many contribution ledger rather than a `contributed_by` column, since dedup makes multiple genuine contributors normal. **Schema** `{sourceUrl, sha256, filename?, title?, alt?}` — no projectId (from the token), no inline bytes (the bridge caps bodies at 256 KiB); **SSRF closed structurally**, not filtered: the caller's own port is resolved from its credential and is the only constructible origin. **Duplicate contribution returns `{deduped: true}`** — not an error, and explicitly not a silent success; zero-contribution blobs become GC candidates with a grace period, per ADR-027. **D7 — the ingress decision, and it reverses the framing:** a media contribution does NOT inherit ADR-061's safety argument for `create_site` (purely additive, worst case a junk site). Six failure modes it introduces that `create_site` lacks — content-type confusion, decompression bombs detonating during the mandatory EXIF-strip re-encode, malicious SVG (ADR-027 gates it on `media.upload_svg`; **Runner has no equivalent operator-consent mechanism**), storage exhaustion (unbounded and invisible, unlike a growing fleet), **cross-site content injection** (site A's injected assistant plants an asset that site B's operator later trusts *because it is their own library*), and **second-order injection into the LEFT chat** via attacker-supplied `title`/`alt` reaching the operator agent's context — re-opening exactly the boundary ADR-061 exists to protect, from a direction ADR-061 did not consider. **Recommendation: do not build a site-facing push verb at all.** Build the mirror — `runner.media.import_from_site {project, assetIds}`, a LEFT-chat operator-invoked pull — which delivers the same capability over the same store and collapses the whole surface, because a human chooses what enters. Not a sidestep: **ADR-060 D2 Tier 1 already names a media-import fan-out verb**, so this completes a planned verb rather than competing with it. Six named conditions (out-of-process bounded re-encode, SVG excluded outright, cookie-less media origin, per-project quota, untrusted-metadata handling, quarantine-until-promoted) are recorded as the conjunction required to *reconsider* push — with the honest note that even if all six were met, pull still delivers the same thing with less risk. **Cost named:** ADR-061's compile-time guard does NOT enforce this conclusion — a `runner.media.*` verb is *eligible* and would compile onto the site-assistant allowlist; adding it to `FLEET_ONLY_VERBS` is the cheap way to make D7 structural rather than advisory. Also: the library is the first durable, non-reconstructible state Runner has ever held, and there is no backup story. Build order: store → `import_from_site` → never a push verb. | PROPOSED — requires owner sign-off; relates 061/060/027/052/012 |
| [063](ADR-063-admin-tab-deep-linking.md) | Admin tab deep-linking: path segments name sub-resources (e.g. a named form's own Submissions tab, agent-navigable), query params (`?tab=`) stay view modifiers — ratifies a convention already *partially* live in code (Deployment/Database/Security/Source Control/Settings/Media all ship `?tab=`) but never written down, which is why Forms and Themes independently drifted to a different no-URL shape with nobody choosing that on purpose. Direct `<<SUBAGENT_DISPATCH>>` report — no upstream spec, no Coordinator preflight token, no Constitution pass (N/A for this dispatch shape, not skipped); pattern evaluation, tradeoffs, migration plan and re-evaluation triggers included. No implementation yet. | PROPOSED 2026-08-31 (report-only dispatch; no existing tab-URL ADR superseded — this is the first) |
| [064](ADR-064-agent-guided-deployment.md) | Agent-guided deployment: a non-technical owner types "deploy my site" in the admin chat and is guided to a working deployment, including credential collection, under one hard invariant — **a credential must never pass through the agent's message stream** (a token typed into chat lands in the transcript, run events, `ai_chat_messages`, and the provider's own logs). The new credential tool reuses ADR-053/055's MCP-UI confirmation/return-path exchange mechanism verbatim as the out-of-band collection surface, and extends (does not revoke) the ADR-041/045 human-only-lever discipline the existing `deployment_set_dockerfile` tool already observes for `docker build`/`docker push`. Builds directly on `development/docs/deployment/deployment-constraints.md` §9/§6 (load-bearing, not re-derived). No implementation yet. | PROPOSED 2026-08-31 (requires owner sign-off; relates deployment-constraints.md/053/055/058/041/045) |
| [053](ADR-053-mcp-ui-confirmation-transport.md) | MCP-UI confirmation transport: **the browser (`ChatPane`/`AssistantDock`) becomes the real MCP-UI host, not the spawned headless CLI** — it is the only actor in the pipeline with a human present; `@jini-ai/ui/mcp-ui`'s `McpUiHost`/`useMcpUiHost` is adopted as that host rather than building one fresh. `delete-confirmation-ui.ts` is replaced (not patched) by `@jini-ai/ui`'s confirmation-surface builder — the two speak protocol-incompatible postMessage shapes. **The fork point is inside the daemon, at the moment the tool handler executes, before its return value ever crosses back toward the CLI:** the handler emits two separate outputs — the ordinary, minimal, model-safe acknowledgment as its actual tool-call return value, and the UI resource directly onto the run's own event log via the existing `kind:'ext'` escape hatch (`registerExtEventRenderer`), which the CLI never sees. No `AgentEvent` type change, no `ChatPane` source change. Names `content_post_delete`'s destructive path as a **stopgap-excluded** tool until this ships, mirroring how `database_execute_migrate_forward` and its three siblings are already excluded — a confirmation the model can self-approve is worse than no confirmation. | Accepted 2026-09-08 (owner sign-off — Leona; Decision 3 superseded same-day by 055's single held-open call; Decision 6 stopgap since lifted — see this ADR's own 2026-09-08 status note for the full shipped/not-shipped breakdown; relates 049/021/055/064) |
| [055](ADR-055-mcp-ui-return-path.md) | MCP-UI return path — one path, two behaviors, selected by whether the handler is permitted to act. **Non-destructive surfaces** (forms, pickers) return the human's answer straight to the agent as the tool call's ordinary result, no gate, no token. **Gated/destructive surfaces** (delete, anything with side effects) **block**: the handler awaits the human's out-of-band answer, **acts on it server-side**, and returns the outcome — the agent has the data but was never the actor, closing the prompt-injection gap a "route approval back as context, let the agent call delete" design would reopen. **Supersedes ADR-053 Decision 3** (the confirmation-token redemption endpoint) outright — a blocking single call needs no secret to mint/redeem/leak/replay. Decision 7 adds `ToolExecutionContext.emitSurface` (a parked handler cannot show the surface it is waiting on without one); Decision 9 generalizes the park into a full exchange (`open/send/receive/close`, inbound-buffered, multi-turn) with `askOnce` as the one-shot helper; Decision 10 keeps the correlation handle in MCP-UI's tool-call params only because that channel has no other way to carry it back. This is the mechanism every `*_trash_*`/`*_tombstone`/`*_delete_*` confirmation dialog in the codebase runs on (`content_post_delete` first, then the narrow-path delete-confirmation build's six tools, 2026-09-08). | Accepted 2026-09-08 (owner sign-off — Leona; supersedes 053 Decision 3; relates 053, whose own Decisions 1–4 are assumed but which itself remains DRAFT — open item, see ADR-055's own note; relates 064) |
| [065](ADR-065-content-template-naming-convention.md) | Static-theme content-templates (the `theme.json` `templates` array entries a Post or Page selects via `templateChoice`) are named for the content kind they were designed for: `posts-*.html` / `pages-*.html`. **Descriptive, not enforced** — nothing in `validateTemplateDeclarations`/the render path checks a template's filename against the selecting row's `kind`, and 6 live `kind:"page"` rows on `sites/tovu-com/content.db` already select Post-prefixed templates today, evidence enforcement would have to reconcile. Standalone theme route pages (`index.html`, `about.html`, etc.) are out of scope. `pages-default.html` (renamed from `page-shell.html`) is the one entry that is NOT a free choice — it's the Tovu-owned closed-vocabulary filename `STATIC_TIER_PAGE_SHELL_ID`/`STATIC_TIER_PAGE_SHELL_TEMPLATE` look up directly, bypassing `templates` entirely. Rename (`blog-post.html`→`posts-default.html`, `blog-sidebar-template.html`→`posts-sidebar.html`, `page-shell.html`→`pages-default.html`) lands only on `sites/tovu-com/themes/static/basic/` for now; the shipped seed `basic` plus `basic-2`/`tailark-*`/`__original-themes__` keep legacy names, which must keep resolving. No governing ADR found on this narrower topic (ADR-010/017/020 checked, none cover template-file naming). | Accepted 2026-09-03 (relates 010/012/020) |
