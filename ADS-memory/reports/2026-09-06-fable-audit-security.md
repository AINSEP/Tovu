# Fable audit — SECURITY lens — window 4b89cd09..efc6847e

- Auditor: Fable 5.1 (subagent `fable-security`), dispatched 2026-09-07 00:01 local
- Audit HEAD (frozen): `efc6847ed4490d0f57cd94d16b8cf46a6489a88a`
- Window: `4b89cd09..efc6847e` — 243 commits, all 2026-09-06
- Skills loaded: `AI-Dev-Shop/agents/security/skills.md`
- Mode: read-only. No tests, builds, typechecks, servers. Commit cadence: every ~5 min.
- Already fixed, NOT re-reported: J01, MI-01, MI-02, chat-run death path (`f682eff2`).
- Off limits: `apps/admin/src/features/menus/MenuEditor.*`.

Status legend: **CONFIRMED** = traced reachable path from untrusted input to sink, caller named. **PLAUSIBLE** = inferred, path not fully traced. **REFUTED** = codex claim did not survive reading the code.

## 0. Progress log (append-only, newest last)

- 00:01 — report created, first commit.
- 02:05 — INV-05 shape sweep beyond the window (§3a), Runner launcher answer (§3b), seed DB (§3c), desktop session hygiene + SEC-08 (§3d), threat assessment, open questions. Audit complete pending owner questions.
- 01:45 — assistant proxy header stamping, NUL-escape diffs (--text), admissions route, localStorage backup, secret sweep, window sink sweeps done; ledger for all 243 commits appended (§6).
- 01:10 — escapeHtml copies closed, sites tool, dev proxy, orphan reaper, IPC stubs, pages permission grant verified. §1i.
- 00:58 — media-import + http client, executor parity, sites route, adopt, duplicateSite, scripts, autosave read. §1f–1h; SEC-05/06.
- 00:44 — chat attachment trio + refusal-notice read; §1e. Next: BYOK executor stack, sites binding, adopt, duplicateSite, export href, escapeHtml copies, scripts, autosave route, media-import + http egress at HEAD.
- 00:33 — daemon-auth, INV-05 route, external-mcp guard/put/trust read. §1b–1d.
- 00:22 — boot token (`15548bef`) read end to end: `boot-session-token.ts`, `dev-auth.ts:391-427`, `serve.ts:259-283`, `desktop-auth.cjs`, `tovu-server.cjs`. Verdict below (§3a). INV-05 fix commit read; route file next.
- 00:12 — desktop read at frozen SHA: `project-delete-guard.cjs`, `project-ipc.cjs`, `main.cjs`, `desktop-auth.cjs`, `tovu-server.cjs`, `keyed-serializer.cjs`, `preload.mts`, `site-registry.cjs` (partial). D-04/D-05 verdicts below.

## 1. Codex claim verification (priority 1)

### D-04 — stale created-project row authorizes deletion of a replacement directory
**CONFIRMED (code read at efc6847e). Severity: High** (irreversible data loss; the guard's own doc claims it "fails CLOSED" on anything it cannot prove, and it does not prove this).

- `apps/desktop/src/project-ipc.cjs:151-167` `handleDelete(id)`: `row = readTrackedProjects().find(siteDir === id)`; if `mayEraseProjectDirectory(row, {repoRoot})` → `fsp.rm(id, {recursive:true, force:true})`.
- `apps/desktop/src/project-delete-guard.cjs:104-108` `mayEraseProjectDirectory`: tests ONLY (a) `row.origin === "created"` and (b) real-path containment outside `repoRoot`. Nothing consults the live directory: no marker check (`content.db`/`config.json`), no inode/dev identity, no workspaceId compare against the row. The row is the sole authority and the row is a path string in `desktop-projects.json`.
- Attack/loss path (operator-driven, no external attacker needed): create site A at `/x/a` (row: created) → move A elsewhere → place site B (or any files) at `/x/a` → the card for `/x/a` still shows (`readSiteName` reads B's `config.json`, so the card is even titled with B's name and `deleteErasesFiles: true`) → confirm delete → B is erased. B was never created by this app.
- Same shape as INV-05: the authorization is made against a stored record that no longer denotes the thing it names.
- Not mitigated by `rescanProjects` — discovery only adds rows; it never re-validates or downgrades an existing `created` row (see `adoptDiscoveredProjects`, to be confirmed in §1 sweep).

### D-05 — Delete erases a project while Start is opening it
**CONFIRMED (code read). Severity: Medium** (needs the operator to click Start then Delete inside the ≤60 s boot window; effect is a `tovu serve` child left running over an rm'd directory plus an `openSites` entry with no tracked row).

- `handleStart` (`project-ipc.cjs:205-211`) enters `deps.serializer.run(id, …)`; `handleDelete` (`:151-167`) never touches `deps.serializer` — `serializer` appears exactly once in the file, in `handleStart`.
- `main.cjs:569-576` `openSiteServer`: `openSites.set(siteDir, …)` only AFTER `await startSiteBackend()` (spawn + boot line wait, up to `DEFAULT_READY_TIMEOUT_MS = 60_000`, + boot-token redeem). During that window `openSites.get(id)` is `undefined`, so `handleDelete` skips `server.stop()`, untracks the row, and `rm -rf`s the directory the child is booting in. Startup then completes and publishes `{server}` into `openSites` and a crash-safety row into `open-sites.json` (`recordSiteOpened`, `main.cjs:472`).
- Reverse ordering also holds: `handleStart` validates the row OUTSIDE the serialized fn, so a delete landing between validation and `openSiteServer` starts a server for an untracked (and, for a `created` row, erased) directory.
- The `handleDelete` doc comment ("The server is stopped … BEFORE the directory is removed, so `content.db` is never unlinked out from under a live handle") is false for this interleaving.

### D-05 — Delete erases a project while Start is opening it
_(pending)_

### Sweep — authz against stale / attacker-influenceable record (INV-05 shape)
_(pending)_

## 1a. Loopback boot token (`15548bef`) + desktop auto-auth (`2aa317ab`) — what holding the token grants

Read: `apps/website/src/features/identity/boot-session-token.ts` (whole), `apps/website/src/server/inbound/admin-http/dev-auth.ts:258-427`, `apps/website/src/cli/commands/serve.ts:259-283`, `apps/desktop/src/desktop-auth.cjs` (whole), `apps/desktop/src/tovu-server.cjs:23-60,295-334,426-522`, `apps/desktop/main.cjs:407-488`.

**What the token grants:** one redemption → `mintSessionForPrincipal(deps, await deps.ownerPrincipalId)` (`dev-auth.ts:403-410`) → an ordinary revocable `sessions` row for the **seeded owner principal** of `deps.workspaceId`, with the library's default lifetime (the desktop-auth header says 30 days). It is a full-owner admin session, not a scoped one. That is the intended design; no bypass flag exists downstream.

**Scoping, verified in code (all hold):**
- Minted only when `--emit-boot-token` is on argv (`serve.ts:280`), inside the `listening` callback, so never for a failed boot; every other `tovu serve` has the route present but permanently closed (`redeem` on an unarmed store → `false` for every input, `boot-session-token.ts:95`).
- Single-use, digest+`timingSafeEqual`, failed redeem does not burn (`:94-106`).
- Route checks `req.socket.remoteAddress ∈ {127.0.0.1, ::1, ::ffff:127.0.0.1}` BEFORE looking at the token (`dev-auth.ts:275-278, 393`); `X-Forwarded-For`/`req.ip` not consulted.
- Token travels on the child's stdout pipe only (`stdio: ["ignore","pipe","pipe"]`, `tovu-server.cjs:450`); parent redacts it from mirrored output and from every `describeBootFailure` error string (`:38-51, 463, 485, 516`). Never in env, never on disk.
- Parent redeems (`authenticateSiteSession`, `main.cjs:407-432`) inside `startSiteBackend` BEFORE any `BrowserWindow`/`<webview>` exists, so an XSS in the site's admin (the `<webview>` guest is loopback and could POST to the route) finds the token already spent. If the parent's redeem fails on a transport error the token stays armed for the child's lifetime, but nothing else holds it.
- Reuse guard (`hasActiveSessionCookie`, `desktop-auth.cjs:192-195`) skips the mint when a `tovu_session` cookie already sits in the partition → `emitBootToken: false` → nothing minted.

**Observations (not defects in the token itself):**
- **SEC-03 (Low, PLAUSIBLE — pre-existing, not introduced in window, but the fleet-UI default makes it the default desktop experience):** `serve.ts:259` `app.listen(port)` binds ALL interfaces. Every desktop-spawned site admin (login form + `/api/assistant` daemon proxy) is reachable from the LAN. The boot-session route is loopback-gated, so the token is safe; the exposure is the ordinary admin login surface (rate-limited `LOGIN_STRICT`) on a machine whose operator never asked to serve a network. Commit `15548bef`'s own message acknowledges the bind. Recommend `app.listen(port, "127.0.0.1")` in the desktop-spawned path (the shell already hardcodes `http://127.0.0.1:${port}` as origin, `tovu-server.cjs:491`).
- **SEC-04 (Low, CONFIRMED):** `TOVU_AGENT_DAEMON_TOKEN` is minted per launch into the child's ENVIRONMENT (`tovu-server.cjs:299-301`), and `desktop-auth.cjs:22-24` itself states the reason env is unsuitable for a secret ("readable from the process list by anything running as this user"). The same reasoning was applied to the boot token and not to the daemon token. What the daemon token grants is examined in §1b (daemon-auth). Not written to disk by anything in this repo — the "launcher at 0700 baking a bearer" generation path is Tovu-Runner's, not found here: no `writeFileSync`/`chmod 0o700` of a token exists under `apps/desktop`, `development/scripts`, or `apps/website/src` (grep for `0o700|chmodSync|launcher` — only `agent-plugins/install.ts` dirs, `webhooks/keyring.env.ts` key file at 0600, `atomic-write.ts`).
- No rate limiter on `/auth/boot-session` (login has `LOGIN_STRICT`). Irrelevant at 256-bit entropy and loopback-only; noting for completeness, not a finding.

## 1b. Daemon token — what holding `TOVU_AGENT_DAEMON_TOKEN` grants
Read `apps/website/src/assistant/daemon-auth.ts` (whole), `agent-daemon-server.ts:27-57, 891-935, 1245`. The daemon binds `127.0.0.1` (`:1245`) and gates every route with `requireAgentDaemonToken` (no loopback exemption) except `POST /api/delegated-tool-calls`, which is exempt but requires a live `runId` (`resolvePrincipal`, `:891`). A holder of the token can start runs and execute tools against `content.db` — as the principal the proxy stamps into the run (`:27`, `:670` — decoded from the forwarded request). So the daemon token ≈ "any local process can drive the assistant as whichever principal id it stamps". Same-user threat model only; consistent with SEC-04 being Low.

## 1c. INV-05 fix (`8e973578`) — verified
`routes/workspace/delete.ts:64-98`: `:workspaceId !== deps.workspaceId` → 404; `authorize(workspace.manage)`; then identity guard `targetWorkspaceId === deps.workspaceId` → 409 `BOUND_WORKSPACE`. Because the first check narrows the target to the bound id, `deleteWorkspace` (`:92`) is unreachable over HTTP. Guard is on identity not count — matches the fix's claim. No sibling arm exists (workspace_delete tool is unwired per Jini's `UNWIRED_WORKSPACE_TOOL_IDS`; not re-verified in Jini, taken as a CLAIM). **Fix holds.**

## 1d. External MCP write grants (`0d9e41d5`, `ae13e739`, `37ac1943`) — in progress
- `routes/external-mcp/guard.ts:32-59`: one gate for list/put/delete — workspace narrow + `admin.integrations.manage` (site-owner level; a stored `command` is spawned as a child at daemon boot, so this permission == code execution as the Tovu process, stated in the file). Consistent across the three arms.
- `routes/external-mcp/put.ts:86`: `writeAllowedToolNames` is `asStringField` — a non-string collapses to `""` → clears every write grant (fail-closed; noted in-file as known).
- `mcp-federation/trust.ts:403-450`: both lists consulted; `writeAllowedButNotAllowlisted` is surfaced, never admitted. Admission is computed once at connect (R5) — config read at daemon start, confirmed by `put.ts:105-108` `restartRequired: true`.
- `refusal-notice.ts` (what the MODEL is told) — pending read for secret leakage.

- `refusal-notice.ts` (whole): every remote name re-checked against `SAFE_REMOTE_NAME` before it reaches the prompt (`:92-111`); explanations are fixed literals; connection id is operator-authored + pattern-validated upstream; list capped at 10 (`:100`). No command/env/URL of the connection is rendered. **No leak.**

## 1e. Chat attachments (`0b298d86`, `f281d3a2`, `feb8a777`, `72e1e529`, `3b196ffb`) — verified, one accepted widening
- `features/media/read-chat-attachment.ts` (whole): ref allowlisted (`:106`), sidecar dir parent-equality (`:282`), batch dir re-derived from canonical root not from the sidecar (`:193-201`), owner compared BEFORE the file is touched and an ownerless record matches nobody (`:296-303`), `isUnchangedAttachment` dev/ino/size + realpath (`:305-307`). HTTP shell (`routes/assistant/get-chat-attachment.ts`) collapses every refusal to one 404 body and sniffs content type from bytes, forcing download for html/svg/xhtml. **Sound.**
- `promote-chat-attachment.ts:38-52`: promotion is scoped by RUN claim (`resolveForRun(ref, ctx.run.id)`), not by owner; the file states the first-claimer-wins model for an unclaimed attachment. Cross-principal promotion therefore needs the victim's ref (`attachment:<uuid>`, 122-bit) or its absolute path (`<root>/<batchId 8-80 random>/<name>`), neither of which the discovery tool leaks (`list-pending-chat-attachments.ts:91` is owner-scoped). Read side is owner-scoped, promote side is run-scoped — an asymmetry, documented in-file as accepted. Not a finding; recorded so the next auditor does not re-derive it.
- Daemon principal: `agent-daemon-server.ts:670` `principal = { id: decoded.principalId }` straight from the proxy-supplied `contextRef`. Only the bearer gate stands between a local process and "run tools as any principal id" — this is the real impact statement for SEC-04 (still same-user, still Low).

## 1f. Media import + `platform/http` byte path (`dd187ece`, `b1ce2d0a`, `7b2a2007`, `916eb8b0`; post-freeze fix `103f7ae1` read at HEAD)
Read `features/media-import/{fetch-image,agent-tools,tool-registrations}.ts`, `platform/http/{client,egress-policies,transport.fetch}.ts` in full.
- SSRF guard holds: scheme allowlist + embedded-credential refusal (`client.ts:209-216`); DNS resolved then EVERY address classified, any non-public rejects (`:238-245`); connection pinned to the vetted IP with original Host/SNI (`transport.fetch.ts:46-57`); redirect target re-runs the whole guard and strips `authorization`/`cookie` cross-origin (`client.ts:355-372`); IPv4-mapped IPv6 in every spelling normalised (`:117-154`). Numeric-host spellings (`2130706433`, `0x7f000001`, `0177.0.0.1`) are not IP literals to `isIP`, go through `lookup`, and come back as dotted-decimal → classified → refused. `MEDIA_IMPORT_EGRESS_POLICY`: https-only, empty dev allowlist, 3 hops, 12 MiB. **No new SSRF finding.** MI-01/MI-02 fixed, not re-reported.
- Bytes: served `Content-Type` never read; sniffed type must be in a 5-type still-image allowlist (`fetch-image.ts:84-90, 248-254`); truncation checked before sniff (`:240`). Filename sanitised to `[A-Za-z0-9 -]`, extension from sniffed type (`:182-193`).
- **SEC-05 (Low, CONFIRMED — the refusal-collapse shape the brief asked about):** an egress refusal is a plain `Error` thrown from `client.ts:242` (`egress to '<host>' (<ip>) rejected: resolved address is <class>`); `fetch-image.ts:270-274` deliberately lets it propagate; `tool-registrations.ts:120` classifies only `MediaImportValidationError` as a caller-visible shape rejection. Everything else falls to the executor's generic failure. Attack-path-wise this hides nothing dangerous, but the operator/model sees "internal error" for a security refusal, and the audit row records a crash, not a block. Fix shape: a typed `EgressRefusedError` in `client.ts` that `withSchemaOnRejection`/the executor surface as a refusal.
- **SEC-06 (Low, PLAUSIBLE):** the policy caps are enforced AFTER the transport has buffered the whole body: `transport.fetch.ts:32,65-71` buffers up to `ABSOLUTE_MAX_BYTES` = 100 MiB before `client.ts:348-353` clips to the 12 MiB policy cap. `timeout` (`:52`) is a socket-idle timeout, not a wall clock, so a hostile CDN can hold one `media_import_from_url` call open for as long as it drips bytes and make the process hold ~100 MiB per concurrent import. Disclosed in the transport's own header as a v0 limitation; noting it because the media-import tool is the first agent-supplied-URL consumer of this transport.

## 1g. Executor parity (`08ace3ea`) — verified
`tool-executor-stack.ts:79-85` is the single composition (read-only gate innermost, audit, recovery); `byok-tool-surface.ts:450-454` calls it. Daemon call site to be confirmed by grep (below).

## 1h. `1044e2d5` escapeHtml apostrophes — the fix landed in 4 of 10 copies
Touched: `static-render.ts`, `site-exporter.ts`, `form-render.ts`, `render.ts`. Untouched copies in `apps/website/src`: `assistant/mcp-ui.ts:190`, `public-http/routes/site/store.ts:16`, `public-http/http/site/page-head.ts:194`, `newsletter-confirm.ts:40`, `newsletter-unsubscribe.ts:45`. The two newsletter copies escape only `& < >` but are used only in text-node contexts (`<title>`, `<h1>`, `<p>`) — safe as used. Checked: `mcp-ui.ts:190` and `store.ts:16` already escape `'`; `page-head.ts:194` escapes `& < > "` only, and every sink in that file is double-quoted (no `='${` interpolation exists). **No exposed copy — verified, not a finding.**

## 1j. Assistant proxy principal stamping — verified (no spoof path)
`modules/assistant-daemon-client.ts:40-59` `outboundHeaders` builds the daemon request headers from scratch (`Content-Type`, bearer, `x-tovu-principal-id` = `getAuthedPrincipal(res).id`, `Last-Event-ID`); browser headers are never merged, so a client-sent `x-tovu-principal-id` cannot reach the daemon. `proxyRunStart` (`modules/assistant.ts:117-135`) overwrites `contextRef.principalId` after spreading the client's object. `forwardAttachmentUpload` (`:355-365`) likewise sets the header last. Every `/api/runs|agents|tools|frontend-sessions|attachments` mount sits behind `requireAdminSession` (`:397-514`). NUL-escape commits (`b3553dd9`, `ea5f3a42`) checked with `git diff --text`: `\u0000` in a template literal is the same code unit as the literal byte; no key-format change.

## 1i. Misc verified in this pass (no finding)
- `sites_duplicate_site` (`features/sites/tool-registrations.ts:131-178`): both names validated against `SITE_NAME_PATTERN` before any path join; flag + `switcherCompatible` + `system.write` in that order; target is `path.join(cwd,"sites",targetName)`. `duplicate-site.ts` copies only `layout.ts`'s allowlist (`uploads/themes/plugins/overrides/skills/agent-plugins`), regenerates `config.json` (domain/port reset) and `.site-meta.json` (fresh `siteId`); `content.db` goes through `duplicateContentDb`. `chat.db`, `*.bak`, `restore-point-*.db`, `ops/`, `out/` are excluded by not being on the list. Sound.
- Executor parity: `agent-daemon-server.ts:485` also calls `createAssistantToolExecutor`. Both surfaces share one stack.
- `admin-dev-proxy.ts` (`848ddd09`): `rejectUnauthorized:false` is scoped to a dedicated agent for the Vite upstream only; active only when `TOVU_ADMIN_DEV_PROXY_URL` is set and not SEA. Dev-only surface.
- `site-registry.cjs:140-268` orphan reaper: kills only a pid whose argv contains the row's `siteDir` AND `--port <n>` AND whose ppid is 1; re-identifies before SIGKILL. Fail-closed direction.
- `runner-ipc-stubs.cjs`: every unported `runner:*` verb (working-directory pick/exists/normalize, chat-attachments save, conversations) throws — the preload exposes them but nothing in main acts on renderer-supplied paths.
- `features/pages/permissions.ts`: `pages.edit_html` reaches `admin` (built-in role grant) and any custom policy holding `theme.edit`; `editor`/`viewer` structurally excluded. `wiring.ts` `reconcileGrantsOnBoot:false` only skips grants (fail-closed direction).
- `backfill-reset-admin-password.ts`: password from env or `--password=` (shell-history exposure acknowledged in-file), never echoed; dry run opens read-only. `cleanup-stale-owner-sessions.ts`: deletes only revoked/expired rows; restore point before delete.

## 2. Codex `pending` commits (priority 2)
_(in progress)_

## 3. Rest of window (priority 3)
_(pending)_

## 3. Rest of the window — covered by §1a–1j and the §6 ledger. Every code commit in `4b89cd09..efc6847e` is either read (reviewed/reviewed-light) or covered by the admin sink sweep; no code commit is unaccounted for.

## 3a. INV-05-shape sweep beyond the window (authz against a stale/attacker-influenceable record)
Swept every `rm`/`unlink` sink under `features/**` + admin routes and every count-based guard (`length <= 1`, `isLast*`, `LastRemaining`) in Tovu and `@jini-ai/cms`:
- Count-based guard: exactly one exists — Jini `workspace/delete.ts:61` (INV-03), now shielded in Tovu by the INV-05 identity guard (§1c). No other "last row" guard is reachable from a route.
- Path-keyed deletes reachable from HTTP/tools: `plugins/uninstall.ts` → `onPluginUninstalled` (`plugin-runtime.ts:266-272`: `SAFE_PLUGIN_ID_SEGMENT` + resolved-path containment, fails closed on either); `sites_duplicate_site` (§1i, name-pattern gated, creates only); theme file delete/rename (`themes/explore.ts` delegates to `theme-files.ts`'s containment — pre-window, delegation read, containment function not re-read here); `recovery/restore.ts` (plan/confirm/execute ceremony with `authorizeForHooks` before the lock, `backup.restore` instance-scoped); `api-keys/revoke.ts` → `revokeApiKey` (`assertCallerMayManageApiKeys` + workspace-scoped `findById`). None repeats the D-04/INV-05 shape: each decision is made on the live row/path at call time.
- The only remaining instance of the shape in the window is **SEC-01 (D-04)** on the desktop.

## 3b. The "orphaned Runner launchers with a baked bearer at 0700" question
The generation path is **live, but in Tovu-Runner, not in this repo**: `/Users/la/Programming/Tovu-Runner/src/main/runner-mcp-bridge.ts:225-254` `writeSiteAssistantLauncher` writes `site-assistant-<projectId>.sh` (mode 0700) with `JINI_DAEMON_TOKEN='<token>'` inline. Its `tokens` map (`:257`) is per-Runner-process, so a launcher whose project no longer exists holds a credential that died with the Runner process that minted it; the file is only overwritten for a project id that still exists, never swept. Nothing in Tovu (`apps/desktop`, `apps/website`, `development/scripts`) writes a token to disk. Recommendation for Runner (out of scope here): sweep `site-assistant-*.sh` not matching a live project at boot.

## 3c. Tracked seed database
`git ls-files` shows one tracked `.db`: `sites/tovu-com/content.seed.db` (intentional build input per `layout.ts`). Read-only inspection (`?mode=ro`): `sessions`, `api_keys`, `member_sessions`, `member_magic_tokens`, `gated_mutation_tokens`, `site_assistant_credentials`, `external_mcp_servers`, `admin_execution_credentials`, newsletter confirmation tokens — all **0 rows**. `identity_users` = 4 rows (password hashes present). **Open question §5-Q1:** are all four hashes of the documented default password? Verifying needs an argon2 verify run, which this audit's no-execution rule forbids.

## 3d. Desktop session hygiene (Info)
- `handleDelete` (`project-ipc.cjs:151-167`) stops the child and untracks the row but never calls `endSiteSession` or clears the site's `persist:tovu-site-<sha256(path)>` partition; `openSiteWindow`'s `closed` listener does (`main.cjs:535-545`). After a fleet-mode delete, the partition directory under `userData` keeps the cookie jar (a 30-day session token for a DB that, for an `adopted` row, still exists and is still served on the next open). Same-user only.
- **SEC-08 (Low, PLAUSIBLE, pre-window — present at `4b89cd09:apps/desktop/main.cjs:198-200`):** own-server/attach-mode `createWindow` answers any non-origin `window.open` with `shell.openExternal(target)` for ANY scheme (`file:`, custom handlers). The fleet-mode guest policy added in this window (`registerGuestNavigationPolicy`, `main.cjs:614-641`) correctly restricts external opens to supervised loopback URLs; the older path was not brought up to the same rule. Reachable by any script in the site window (theme JS on the public view, or an admin XSS). Not introduced in this window.

## 4. Findings (severity-ordered, filled as found)

| ID | Sev | Status | Where | One line |
|---|---|---|---|---|
| SEC-01 (=D-04) | High | CONFIRMED | `apps/desktop/src/project-delete-guard.cjs:104-108`, `project-ipc.cjs:164-166` | Erase authorized by a stale path-keyed row; live directory identity never checked |
| SEC-02 (=D-05) | Medium | CONFIRMED | `apps/desktop/src/project-ipc.cjs:151-167` vs `:205-211`; `main.cjs:569-576` | Delete bypasses the per-site serializer Start uses; rm under a booting child |
| SEC-03 | Low | PLAUSIBLE | `apps/website/src/cli/commands/serve.ts:259` | Desktop-spawned `tovu serve` binds all interfaces; admin + daemon proxy reachable from LAN |
| SEC-04 | Low | CONFIRMED | `apps/desktop/src/tovu-server.cjs:299-301` | Daemon bearer token passed via child env, contradicting the boot-token rationale in `desktop-auth.cjs:22-24` |
| SEC-05 | Low | CONFIRMED | `platform/http/client.ts:242` → `media-import/tool-registrations.ts:120` | SSRF/egress refusal surfaces as a generic internal error, not a refusal |
| SEC-06 | Low | PLAUSIBLE | `platform/http/transport.fetch.ts:32,52,65-71` | Body buffered to 100 MiB before the 12 MiB policy cap; idle-timeout only — slow-drip hold on agent-supplied URLs |
| SEC-07 | Info | CONFIRMED | `apps/admin/src/lib/standing-draft-local-backup.ts:38-46` | A server-refused draft is mirrored to `localStorage` and is not cleared on logout — post/page text survives in the browser profile on a shared machine until the next accepted write |
| SEC-08 | Low | PLAUSIBLE (pre-window) | `apps/desktop/main.cjs:324-328` (present at `4b89cd09:198-200`) | Own-server/attach window hands any non-origin `window.open` target, any scheme, to `shell.openExternal`; the fleet guest policy in this window restricts it, the older arm does not |

**Refuted / not reproduced from the codex set:** none of D-04/D-05 was refuted — both confirmed (D-05 downgraded from High to Medium: operator-timed race, no data loss beyond the rm the operator already confirmed). The codex-fixed items (J01, MI-01, MI-02, chat-run death path) were not re-reported.

**Overall threat assessment for the window:** the window's security-bearing additions (boot token, attachment read-back, media import + guarded HTTP client, external-MCP write grants, INV-05 guard, executor parity, escapeHtml sweep) are each correctly gated and fail closed on read. The one High is a desktop data-loss guard that trusts a path-keyed row over the live directory (SEC-01); everything else is Low/Info hardening. No authorization bypass, no SSRF, no secret written to disk, and no secret in the diff was found.

Human sign-off required before any patch: SEC-01 (High), SEC-02 (Medium). The rest can ship under ordinary review.

## 5. Open questions for the owner
- Q1: `sites/tovu-com/content.seed.db` carries 4 `identity_users` rows with password hashes. Are all four the documented default (`tovu-dev`)? If any is a real operator hash, the tracked seed leaks it (argon2 verify was not run — no execution).
- Q2: SEC-03 — should the desktop-spawned `tovu serve` bind `127.0.0.1` instead of all interfaces? `serve.ts:259` is shared with headless deployments, so this is a product decision (a `--host` flag, or the shell passing one).
- Q3: SEC-04 — accept env-borne `TOVU_AGENT_DAEMON_TOKEN` as same-user-only, or move it to the stdout-pipe pattern the boot token uses?
- Q4: Tovu-Runner sweeps stale `site-assistant-*.sh` launchers? (§3b — external repo.)

## 6. Commit ledger (all 243)

Totals: reviewed 63, reviewed-light 19, skimmed (admin sink sweep) 77, skipped (no code / off-limits) 70, out of scope (tests only) 14 = 243.

Status vocabulary: **reviewed** = surviving HEAD code the commit touched was read and its call paths traced (section reference given); **reviewed-light** = the diff or the touched file was read for security relevance only; **skimmed** = admin-renderer-only change covered by the window-wide sink sweep (grep of every added line under `apps/admin/src` for `dangerouslySetInnerHTML|innerHTML|srcdoc|eval(|new Function|document.write|window.open|location.href=|javascript:|localStorage|sessionStorage|document.cookie` — only hits: the SEC-07 localStorage draft backup); **skipped** = no code (docs/todos/reports/comments/assets) or off-limits; **out of scope** = tests only.

Secret-pattern sweep: every added line in `git diff 4b89cd09..efc6847e` (all paths) grepped for Anthropic/OpenAI/GitHub/Slack/AWS key shapes, PEM headers, JWTs, and `key|secret|password|token = "<24+ chars>"` literals, excluding `process.env`/placeholder/fixture lines — **zero hits**.

| Commit | Subject | Status / reason |
|---|---|---|
| `efc6847e` | docs(ads-memory): handoff for session tovu-14 | skipped — docs/report/todos only, no code; secret-pattern sweep of the window diff clean |
| `0bfd3430` | docs(ads-memory): record that commit trailers misattribute the model | skipped — docs/report/todos only, no code; secret-pattern sweep of the window diff clean |
| `b06b4ec7` | docs(todos): close the two entries tonight's work shipped | skipped — docs/report/todos only, no code; secret-pattern sweep of the window diff clean |
| `37ac1943` | feat(admin/settings): show the operator which external MCP tools the assistant refused | reviewed — admin banner (sink sweep) + server half routes/external-mcp/admissions.ts (guardExternalMcpRequest, bearer-proxied) |
| `f02ac28e` | fix(desktop/projects): stop a failed rescan blanking the grid, and unstack the header | reviewed — desktop main-process state read in full at HEAD (main.cjs, project-ipc, project-delete-guard, project-registry, desktop-auth, tovu-server, site-registry, site-dir-store, keyed-serializer, runner-ipc-stubs, preload.mts); renderer sink grep clean |
| `c201d948` | feat(desktop/projects): add a Rescan control so discovery is not only a boot step | reviewed — desktop main-process state read in full at HEAD (main.cjs, project-ipc, project-delete-guard, project-registry, desktop-auth, tovu-server, site-registry, site-dir-store, keyed-serializer, runner-ipc-stubs, preload.mts); renderer sink grep clean |
| `65b8fd74` | feat(desktop/projects): scan for sites on disk at boot, so one made outside the shell appears | reviewed — desktop main-process state read in full at HEAD (main.cjs, project-ipc, project-delete-guard, project-registry, desktop-auth, tovu-server, site-registry, site-dir-store, keyed-serializer, runner-ipc-stubs, preload.mts); renderer sink grep clean |
| `4ede4562` | fix(e2e/types): type the desktop-shell application-menu walk instead of leaving it implicitly any | out of scope — tests/e2e only (read as evidence where cited) |
| `0d9e41d5` | feat(mcp-federation): tell the MODEL which external tools were refused, and why | reviewed — §1d refusal-notice.ts (names re-validated, capped, no config leakage) + daemon wiring diff |
| `e56965a6` | fix(admin/types): clear the 31-error tsc baseline in apps/admin | skimmed — type-only fixes; skimmed — apps/admin renderer only; window-wide grep of every added line for dangerouslySetInnerHTML/innerHTML/srcdoc/eval/window.open/location.href/javascript:/document.cookie/storage found only the localStorage draft backup (SEC-07) |
| `8e973578` | fix(admin/workspace): refuse deleting the server's own bound workspace (INV-05) | reviewed — §1c INV-05 fix verified in routes/workspace/delete.ts |
| `8a4e405d` | fix(admin/menus): log the update-tree catch-all 500 instead of discarding it | reviewed — diff: server-side console.error added in the catch-all; wire body unchanged |
| `b2a46c7e` | feat(desktop/projects): record removals, so the seed guard can ask about the directory | reviewed — desktop main-process state read in full at HEAD (main.cjs, project-ipc, project-delete-guard, project-registry, desktop-auth, tovu-server, site-registry, site-dir-store, keyed-serializer, runner-ipc-stubs, preload.mts); renderer sink grep clean |
| `24bdafc1` | test(media-import): prove the bytes, not that a row exists | out of scope — tests/e2e only (read as evidence where cited) |
| `a346b3ec` | feat(assistant): make media_import_from_url findable, and close the catalog gap | reviewed-light — tool-search keyword/doc2query text only |
| `dd187ece` | feat(media-import): add media_import_from_url, the missing import-by-URL tool | reviewed — §1f media-import fetch-image/agent-tools/tool-registrations — SEC-05 |
| `b1ce2d0a` | feat(platform/http): make the guarded HttpResponse byte-capable | reviewed — §1f platform/http types + client capResponse — SEC-06 |
| `fb5ad748` | docs(todos): mark the non-basic theme shells out of scope | skipped — docs/report/todos only, no code; secret-pattern sweep of the window diff clean |
| `0ea4f887` | docs(ads-memory): record three commits carrying another commit's message | skipped — docs/report/todos only, no code; secret-pattern sweep of the window diff clean |
| `fa70c175` | docs(ads-memory): record that the media-seed fix DID deploy, and the regression since | skipped — docs/report/todos only, no code; secret-pattern sweep of the window diff clean |
| `68ce4909` | docs(todos): close the HTML-Page fallback-shell entry against a live measurement | skipped — docs/report/todos only, no code; secret-pattern sweep of the window diff clean |
| `570e5822` | fix(scripts): stop backfill-external-mcp-aad defaulting --db to the live site database | reviewed — backfill-external-mcp-aad.ts default now a never-created path; resolveExistingDbPath refuses; cleanup script read |
| `b27cdba4` | refactor(admin/menus): move ItemRow's Remove-confirmation logic into MenuEditor.hooks.tsx | SKIPPED — off limits per brief (apps/admin/src/features/menus/MenuEditor.*) |
| `9e77a778` | feat(desktop): restore the tab strip and embedded project workspace | reviewed — desktop main-process state read in full at HEAD (main.cjs, project-ipc, project-delete-guard, project-registry, desktop-auth, tovu-server, site-registry, site-dir-store, keyed-serializer, runner-ipc-stubs, preload.mts); renderer sink grep clean |
| `204e01a7` | feat(desktop): embed a project as a tab, not a new BrowserWindow | reviewed — desktop main-process state read in full at HEAD (main.cjs, project-ipc, project-delete-guard, project-registry, desktop-auth, tovu-server, site-registry, site-dir-store, keyed-serializer, runner-ipc-stubs, preload.mts); renderer sink grep clean |
| `c2e206db` | docs(ads-memory): map Tovu Runner and its connection to Tovu | skipped — docs/report/todos only, no code; secret-pattern sweep of the window diff clean |
| `3bc9a415` | fix(desktop/e2e): stop the e2e suite writing into the real Electron userData dir | reviewed — TOVU_DESKTOP_USER_DATA_DIR override (main.cjs:131-133), E2E-only env; e2e file out of scope |
| `fc3bff7c` | docs(todos): file the single-window desktop shell and two gaps found live | skipped — docs/report/todos only, no code; secret-pattern sweep of the window diff clean |
| `2b69b327` | fix(admin/settings): restore the tsc baseline in the field-spec test | out of scope — tests/e2e only (read as evidence where cited) |
| `f50b8463` | test(admin/settings): add exhaustive route-vs-field-spec coverage for External MCP | out of scope — tests/e2e only (read as evidence where cited) |
| `6df1f9a7` | refactor(admin/hooks): adopt useSettlementGeneration at 8 call sites | skimmed — apps/admin renderer only; window-wide grep of every added line for dangerouslySetInnerHTML/innerHTML/srcdoc/eval/window.open/location.href/javascript:/document.cookie/storage found only the localStorage draft backup (SEC-07) |
| `cc01bce7` | docs(todos): settle the footer menu content-ref-vs-raw-URL question | skipped — docs/report/todos only, no code; secret-pattern sweep of the window diff clean |
| `6bf0fc86` | docs(todos): re-measure the footer dead-links entry against the running site | skipped — docs/report/todos only, no code; secret-pattern sweep of the window diff clean |
| `ae13e739` | fix(admin): add the missing writeAllowedToolNames field spec | reviewed-light — admin field spec; server side routes/external-mcp/put.ts read (§1d) |
| `c171e62b` | feat(admin/hooks): extract shared useSettlementGeneration guard | skimmed — apps/admin renderer only; window-wide grep of every added line for dangerouslySetInnerHTML/innerHTML/srcdoc/eval/window.open/location.href/javascript:/document.cookie/storage found only the localStorage draft backup (SEC-07) |
| `13acf9d3` | docs(ads-memory): explain the AAD backfill scripts and the --db asymmetry | skipped — docs/report/todos only, no code; secret-pattern sweep of the window diff clean |
| `01146403` | fix(admin): drop invalid button-in-anchor nesting on 8 more screens | skimmed — apps/admin renderer only; window-wide grep of every added line for dangerouslySetInnerHTML/innerHTML/srcdoc/eval/window.open/location.href/javascript:/document.cookie/storage found only the localStorage draft backup (SEC-07) |
| `ab022bae` | docs(ads-memory): record boot-orchestration dedup fix report | skipped — docs/report/todos only, no code; secret-pattern sweep of the window diff clean |
| `d1eea4b2` | refactor(boot): dedupe agentDaemonWanted/logCriticalBootFailures; close export's migration-scan gap | reviewed-light — diff: agentDaemonWanted/logCriticalBootFailures dedupe; export now runs migration-reconciliation scan (CLI) |
| `ed397627` | chore(scripts): add operator-invoked cleanup for stale owner sessions | reviewed — cleanup-stale-owner-sessions.ts: deletes only revoked/expired rows, restore point first, dry-run read-only |
| `8d041b52` | refactor(outbox): move the retry-cap terminal-state decision into the worker | reviewed-light — outbox worker/repo: retry cap + terminal state; no egress code in these files |
| `874ee54d` | docs(ads-memory): correct C1 -- the .btn-* fix was never missing | skipped — docs/report/todos only, no code; secret-pattern sweep of the window diff clean |
| `530d6122` | fix(admin): drop invalid button-in-anchor nesting on 3 more back/create links | skimmed — apps/admin renderer only; window-wide grep of every added line for dangerouslySetInnerHTML/innerHTML/srcdoc/eval/window.open/location.href/javascript:/document.cookie/storage found only the localStorage draft backup (SEC-07) |
| `a44148a1` | fix(admin/collections): drop invalid button-in-anchor nesting; correct stale CSS comment | skimmed — apps/admin renderer only; window-wide grep of every added line for dangerouslySetInnerHTML/innerHTML/srcdoc/eval/window.open/location.href/javascript:/document.cookie/storage found only the localStorage draft backup (SEC-07) |
| `4c6a0797` | refactor(auth): delegate mintSessionForPrincipal to Jini's shared minter | reviewed — dev-auth.ts mintSessionForPrincipal at HEAD (§1a) |
| `1044e2d5` | fix(render): escape apostrophes in every escapeHtml/escapeHtmlAttr copy | reviewed — §1h all 10 escapeHtml copies checked; no exposed sink |
| `4c65e392` | fix(admin-voice-input): release the mic if held released mid-permission-prompt | skimmed — apps/admin renderer only; window-wide grep of every added line for dangerouslySetInnerHTML/innerHTML/srcdoc/eval/window.open/location.href/javascript:/document.cookie/storage found only the localStorage draft backup (SEC-07) |
| `f31e6ded` | fix(sites): thread the real boot-resolved site binding through RouteDeps instead of re-deriving it per request | reviewed — routes/system/sites.ts, site-registry includeServingSite, serve.ts binding |
| `00bc4bd6` | fix(admin-security): stop "Make default" from also toggling its own row | skimmed — apps/admin renderer only; window-wide grep of every added line for dangerouslySetInnerHTML/innerHTML/srcdoc/eval/window.open/location.href/javascript:/document.cookie/storage found only the localStorage draft backup (SEC-07) |
| `ee14d334` | test(backfill-execution-aad): refresh fixture tripwire for migration 0058 | out of scope — tests/e2e only (read as evidence where cited) |
| `527077b6` | test(assistant): fix stale audit-detail assertion in byok-tool-surface INCIDENT FIX test | out of scope — tests/e2e only (read as evidence where cited) |
| `80e69410` | docs(reports): mark the AAD backfill dedupe report complete | skipped — docs/report/todos only, no code; secret-pattern sweep of the window diff clean |
| `a68526dd` | refactor(scripts): migrate backfill-external-mcp-aad.ts onto the shared runner | reviewed-light — AAD runner migration (aad-backfill-runner.ts read; dry-run read-only) |
| `a19708d3` | test(scripts): add a characterization test for backfill-external-mcp-aad.ts | out of scope — tests/e2e only (read as evidence where cited) |
| `916eb8b0` | refactor(deps): wire deps.ts onto the shared single-hop HTTPS egress policy | reviewed — §1f egress-policies.ts SINGLE_HOP policy wired in deps.ts |
| `7b2a2007` | refactor(platform/http): extract the shared single-hop HTTPS egress policy | reviewed — §1f egress-policies.ts |
| `384790fa` | docs(reports): admin visual polish — six-combination proof of the Settings light pin | skipped — docs/report/todos only, no code; secret-pattern sweep of the window diff clean |
| `f1b13b38` | test(admin/settings): pin that the Settings page cannot render dark | out of scope — tests/e2e only (read as evidence where cited) |
| `7caa2b71` | fix(post): enqueue entry.published when a post/page is created already published | reviewed — routes/posts/create.ts (command gateway, content.write, outbox drain) |
| `7b138c50` | refactor(scripts): migrate backfill-site-assistant-credential-aad.ts onto the shared runner | reviewed-light — same as a68526dd |
| `0af0748e` | refactor(scripts): migrate backfill-media-provider-credential-aad.ts onto the shared runner | reviewed-light — same as a68526dd |
| `3f0c9915` | refactor(scripts): migrate backfill-execution-credential-aad.ts onto the shared runner | reviewed-light — same as a68526dd |
| `08ace3ea` | fix(assistant): route BYOK's tool executor through the shared read-only-gated stack | reviewed — §1g executor parity (tool-executor-stack.ts, byok-tool-surface.ts, daemon :485) |
| `11aa4708` | fix(identity): register this repo's pages.edit_html grant in the backfill script, without crashing its dry run | reviewed — wiring.ts reconcileGrantsOnBoot diff, pages/permissions.ts, backfill-reset-admin-password.ts |
| `6be26ddb` | test(site-glue): fix REQ-8 assertion that relied on the pre-fix immediate-reclaim bug | out of scope — tests/e2e only (read as evidence where cited) |
| `d3161f2e` | refactor(scripts): migrate backfill-connector-credential-aad.ts onto the shared runner | reviewed-light — same as a68526dd |
| `947c0fa1` | refactor(scripts): migrate backfill-composio-config-aad.ts onto the shared runner | reviewed-light — same as a68526dd |
| `5ae37c8b` | docs(reports): admin visual polish — add the two Settings-pin commits to the commit list | skipped — docs/report/todos only, no code; secret-pattern sweep of the window diff clean |
| `ea8c660b` | docs(reports): admin visual polish — Settings pinned to light, inert-control consequence | skipped — docs/report/todos only, no code; secret-pattern sweep of the window diff clean |
| `89c8c380` | design(admin/settings): pin the page to light and take it fully out of the card | skimmed — apps/admin renderer only; window-wide grep of every added line for dangerouslySetInnerHTML/innerHTML/srcdoc/eval/window.open/location.href/javascript:/document.cookie/storage found only the localStorage draft backup (SEC-07) |
| `5182994a` | refactor(scripts): add shared AAD backfill scaffold, not yet wired to any script | reviewed-light — shared AAD scaffold (aad-backfill-runner.ts head read) |
| `7d7ae169` | fix(members): correct decideTiersAccess's @complexity from O(t) to O(t·a) | skipped — @complexity comment only |
| `0ae3429d` | fix(outbox): stop immediate re-queue, add exponential backoff and an attempt cap | reviewed-light — outbox backoff + attempt cap; no egress code in these files |
| `a9a6e3a9` | fix(export): scheme-check the redirect stub's href sinks | reviewed — site-exporter.ts safeHref (:342-352): scheme allowlist, //host and \ handled by origin compare |
| `d10f6708` | docs(reports): record theme content-template naming drift investigation | skipped — docs/report/todos only, no code; secret-pattern sweep of the window diff clean |
| `2b039738` | test(auth): add the boot-session route's first server-side test; fix a false comment | reviewed-light — test + comment fix in boot-session-token.ts (read at HEAD, §1a) |
| `563e58af` | docs(reports): admin visual polish pass, 2026-09-06 | skipped — docs/report/todos only, no code; secret-pattern sweep of the window diff clean |
| `933c69e9` | design(admin): give the Database and Sites tab rows the icons every other tab row has | skimmed — apps/admin renderer only; window-wide grep of every added line for dangerouslySetInnerHTML/innerHTML/srcdoc/eval/window.open/location.href/javascript:/document.cookie/storage found only the localStorage draft backup (SEC-07) |
| `bc22ff14` | design(admin): apply the form measure to the Users and Integrations create forms | skimmed — apps/admin renderer only; window-wide grep of every added line for dangerouslySetInnerHTML/innerHTML/srcdoc/eval/window.open/location.href/javascript:/document.cookie/storage found only the localStorage draft backup (SEC-07) |
| `b8c321ef` | docs(reports): excess/dead-code review of the 2026-09-01 to 09-03 commits | skipped — docs/report/todos only, no code; secret-pattern sweep of the window diff clean |
| `ad20fc76` | docs(review): add architecture/DI review for 2026-09-01 to 2026-09-03 | skipped — docs/report/todos only, no code; secret-pattern sweep of the window diff clean |
| `9f13f0e3` | design(admin/settings): take the Settings surface out of its card when it renders light | skimmed — apps/admin renderer only; window-wide grep of every added line for dangerouslySetInnerHTML/innerHTML/srcdoc/eval/window.open/location.href/javascript:/document.cookie/storage found only the localStorage draft backup (SEC-07) |
| `262a596b` | docs(reports): bug-hunt review of 2026-09-01 to 09-03 commits (292, unreviewed window) | skipped — docs/report/todos only, no code; secret-pattern sweep of the window diff clean |
| `e332ec33` | fix(desktop): stop minting a fresh 30-day session on every launch | reviewed — desktop main-process state read in full at HEAD (main.cjs, project-ipc, project-delete-guard, project-registry, desktop-auth, tovu-server, site-registry, site-dir-store, keyed-serializer, runner-ipc-stubs, preload.mts); renderer sink grep clean |
| `b357e70c` | docs(architecture): reconcile ADR-INDEX with three missing ADRs and the ADR-047 audit-gap | skipped — docs/report/todos only, no code; secret-pattern sweep of the window diff clean |
| `26985a2d` | design(admin/media): draw the tab strip with the shared TabBar, with icons | skimmed — apps/admin renderer only; window-wide grep of every added line for dangerouslySetInnerHTML/innerHTML/srcdoc/eval/window.open/location.href/javascript:/document.cookie/storage found only the localStorage draft backup (SEC-07) |
| `fe0046dd` | design(admin/sites): make the card's head its status strip; demote the second pill to a footnote | skimmed — apps/admin renderer only; window-wide grep of every added line for dangerouslySetInnerHTML/innerHTML/srcdoc/eval/window.open/location.href/javascript:/document.cookie/storage found only the localStorage draft backup (SEC-07) |
| `ff5513fa` | docs(ads-memory): consolidated handoff from session tovu-8f | skipped — docs/report/todos only, no code; secret-pattern sweep of the window diff clean |
| `645f3221` | design(admin): give single-column forms one shared measure instead of the full column | skimmed — apps/admin renderer only; window-wide grep of every added line for dangerouslySetInnerHTML/innerHTML/srcdoc/eval/window.open/location.href/javascript:/document.cookie/storage found only the localStorage draft backup (SEC-07) |
| `9051e2b5` | fix(site-dir): stop duplicateContentDb wiping every plugin's data from the copy | reviewed — duplicate-content-db.ts: VACUUM INTO ? bound parameter; chat tables purged by frozen name list |
| `96f656cd` | docs(todos): pass 8 additions — record the tovu-8f peer handoff | skipped — docs/report/todos only, no code; secret-pattern sweep of the window diff clean |
| `b3553dd9` | fix(media): escape the last three literal NUL bytes in tracked source | reviewed — --text diff: NUL → \u0000 in a template literal, code-unit identical; behaviour unchanged |
| `5152128e` | docs(ads-memory): close out the todos de-stale report | skipped — docs/report/todos only, no code; secret-pattern sweep of the window diff clean |
| `e99249e2` | docs(todos): de-stale pass 7 — the tail sections | skipped — docs/report/todos only, no code; secret-pattern sweep of the window diff clean |
| `c71c5923` | docs(todos): de-stale pass 6 — Master Build Inventory sections 19-25 | skipped — docs/report/todos only, no code; secret-pattern sweep of the window diff clean |
| `210b3751` | docs(todos): de-stale pass 5 — Master Build Inventory sections 1-18 | skipped — docs/report/todos only, no code; secret-pattern sweep of the window diff clean |
| `ea5f3a42` | fix(repo): escape literal NUL bytes in source so git and grep can read them | reviewed — --text diff: same \u0000 escapes; builtin-role-grants.ts +650 bytes is a comment block, key format unchanged |
| `27ccb328` | fix(admin-security): name every access-token dialog and give ambiguous per-row buttons distinct names | skimmed — apps/admin renderer only; window-wide grep of every added line for dangerouslySetInnerHTML/innerHTML/srcdoc/eval/window.open/location.href/javascript:/document.cookie/storage found only the localStorage draft backup (SEC-07) |
| `00718e89` | fix(admin-database): give every timeline row's Recovery link a distinct accessible name | skimmed — apps/admin renderer only; window-wide grep of every added line for dangerouslySetInnerHTML/innerHTML/srcdoc/eval/window.open/location.href/javascript:/document.cookie/storage found only the localStorage draft backup (SEC-07) |
| `ec725ad8` | fix(admin-recovery): give every row's Restore button a distinct accessible name | skimmed — apps/admin renderer only; window-wide grep of every added line for dangerouslySetInnerHTML/innerHTML/srcdoc/eval/window.open/location.href/javascript:/document.cookie/storage found only the localStorage draft backup (SEC-07) |
| `af8d083a` | docs(todos): de-stale pass 4 — Accomplish, ADR map, research backlogs | skipped — docs/report/todos only, no code; secret-pattern sweep of the window diff clean |
| `0d63cfd8` | fix(site-dir): stop duplicateSite shipping the source site's private databases | reviewed — §1i duplicate-site.ts + layout.ts allowlist |
| `6528ed5a` | docs(todos): restore four sections pass 3 deleted by mistake | skipped — docs/report/todos only, no code; secret-pattern sweep of the window diff clean |
| `ef7fa9c8` | feat(chat-db): detect conversations stranded in content.db by the chat.db split | reviewed — chat-orphan-check.ts: SELECT-only, table names from a frozen list |
| `abfc98fe` | fix(admin-taxonomy): term rows get a real role, valid selected-state, and a clean name | skimmed — apps/admin renderer only; window-wide grep of every added line for dangerouslySetInnerHTML/innerHTML/srcdoc/eval/window.open/location.href/javascript:/document.cookie/storage found only the localStorage draft backup (SEC-07) |
| `e2aafba6` | fix(admin-sites): give every site card's Activate button a per-site accessible name | skimmed — apps/admin renderer only; window-wide grep of every added line for dangerouslySetInnerHTML/innerHTML/srcdoc/eval/window.open/location.href/javascript:/document.cookie/storage found only the localStorage draft backup (SEC-07) |
| `3d702539` | fix(admin-plugins): give repeated Enable/Disable/Inspect buttons a per-row accessible name | skimmed — apps/admin renderer only; window-wide grep of every added line for dangerouslySetInnerHTML/innerHTML/srcdoc/eval/window.open/location.href/javascript:/document.cookie/storage found only the localStorage draft backup (SEC-07) |
| `30288302` | docs(todos): de-stale pass 3 — Active Working Items AW-1..AW-7 | skipped — docs/report/todos only, no code; secret-pattern sweep of the window diff clean |
| `d10486d5` | fix(admin-themes): give repeated Activate/Explore/Download buttons a per-card accessible name | skimmed — apps/admin renderer only; window-wide grep of every added line for dangerouslySetInnerHTML/innerHTML/srcdoc/eval/window.open/location.href/javascript:/document.cookie/storage found only the localStorage draft backup (SEC-07) |
| `6442b34f` | fix(form-render): setInputValueAttr can't span an embedded opposite quote char | reviewed — form-render setInputValueAttr regex diff; escapedValue is pre-escaped |
| `5e593ab4` | docs(todos): de-stale pass 2 — Admin Section Spec Sweep + 2026-08-10 slice | skipped — docs/report/todos only, no code; secret-pattern sweep of the window diff clean |
| `cb3789a9` | fix(gitignore): close chat.db + db-snapshot leak opened by the content.db split | reviewed — .gitignore covers content.db*/chat.db*; git ls-files shows only sites/tovu-com/content.seed.db tracked |
| `df07b37c` | docs(todos): de-stale pass 1 — top-of-file dated entries | skipped — docs/report/todos only, no code; secret-pattern sweep of the window diff clean |
| `fe76057d` | fix(admin-media): give the upload toolbar's file/alt inputs a real accessible name | skimmed — apps/admin renderer only; window-wide grep of every added line for dangerouslySetInnerHTML/innerHTML/srcdoc/eval/window.open/location.href/javascript:/document.cookie/storage found only the localStorage draft backup (SEC-07) |
| `552e806d` | docs(reports): code-inspection bug hunt for the 2026-09-04/05 commits | skipped — docs/report/todos only, no code; secret-pattern sweep of the window diff clean |
| `0b298d86` | feat(assistant): serve a staged chat attachment's bytes back to its uploader | reviewed — §1e read-chat-attachment.ts + get-chat-attachment.ts |
| `3b196ffb` | refactor(assistant): give the chat-attachment upload directory one definition | reviewed — chat-attachment-directory.ts (TOVU_CHAT_ATTACHMENTS_DIR wins; else next to content.db) |
| `efeeb67a` | fix(admin-themes): remove invalid button-in-anchor nesting on ThemeExplore's back control | skimmed — apps/admin renderer only; window-wide grep of every added line for dangerouslySetInnerHTML/innerHTML/srcdoc/eval/window.open/location.href/javascript:/document.cookie/storage found only the localStorage draft backup (SEC-07) |
| `f239866e` | feat(admin-posts): tell the operator when autosave has stopped, instead of nothing | skimmed — apps/admin renderer only; window-wide grep of every added line for dangerouslySetInnerHTML/innerHTML/srcdoc/eval/window.open/location.href/javascript:/document.cookie/storage found only the localStorage draft backup (SEC-07) |
| `e5a434c3` | feat(admin-pages): tell the operator when autosave has stopped, instead of nothing | skimmed — apps/admin renderer only; window-wide grep of every added line for dangerouslySetInnerHTML/innerHTML/srcdoc/eval/window.open/location.href/javascript:/document.cookie/storage found only the localStorage draft backup (SEC-07) |
| `52caa8cc` | docs(reports): scrub a NUL byte the report tool itself introduced; note it as F4 evidence | skipped — docs/report/todos only, no code; secret-pattern sweep of the window diff clean |
| `a9aac85b` | docs(reports): un-corrupt the 2026-09-04..05 excess-code review (literal NUL -> escape text) | skipped — docs/report/todos only, no code; secret-pattern sweep of the window diff clean |
| `ee304d5e` | docs(reports): architecture & DI review of the 2026-09-04/05 commits | skipped — docs/report/todos only, no code; secret-pattern sweep of the window diff clean |
| `ef79b352` | docs(reports): excess/dead-code review of the 2026-09-04..05 commits | skipped — docs/report/todos only, no code; secret-pattern sweep of the window diff clean |
| `6f32d027` | refactor(website): inject the tool-attempt audit sink; drop RouteDeps.contentDbPath | reviewed — diff: audit sink built over the composition root's own db handle; RouteDeps.contentDbPath dropped |
| `0c1a1324` | chore(architecture): register cli/commands/adopt.ts as a composition root | reviewed — .dependency-cruiser.mjs composition-root list |
| `b37864c3` | feat(post-tools): wire expectedVersion through content_post_update, the guard's last unwired arm | reviewed — expectedVersion arm (routes/posts/update.ts read; parseExpectedVersion validates) |
| `2756ac26` | refactor(posts): lift expectedVersion's boundary out of the route so a second arm can share it | reviewed — same as b37864c3 |
| `2bb817f6` | test(admin-seo): make the cleared-field test assert what its name claims | out of scope — tests/e2e only (read as evidence where cited) |
| `62634037` | feat(cli): add `tovu adopt <dir>` -- the missing route from an existing site dir to serve | reviewed — cli/commands/adopt.ts: resolveInstallDirTarget, refuses partial markers, never touches content.db |
| `6998ef9c` | refactor(site-dir): expose the marker-pair classification repairSite already computed | reviewed-light — repair-site.ts classifySiteMarkers |
| `a50458bf` | fix(desktop): reap orphaned tovu serve on every boot mode, never a live sibling's | reviewed — site-registry.cjs orphan reaper (argv+ppid identity, re-identify before SIGKILL) |
| `d033ffb8` | fix(admin-seo): let an operator clear an SEO override, not just blank it | skimmed — apps/admin renderer only; window-wide grep of every added line for dangerouslySetInnerHTML/innerHTML/srcdoc/eval/window.open/location.href/javascript:/document.cookie/storage found only the localStorage draft backup (SEC-07) |
| `90e68e8f` | docs(posts-autosave): correct a route comment that described client logic which never existed | skipped — route comment only |
| `a60e07e8` | fix(admin-autosave): act on putAutosave's `applied` instead of discarding it | skimmed — apps/admin renderer only; window-wide grep of every added line for dangerouslySetInnerHTML/innerHTML/srcdoc/eval/window.open/location.href/javascript:/document.cookie/storage found only the localStorage draft backup (SEC-07) |
| `ed5fae17` | fix(desktop): refuse to rm a project directory the app did not create | reviewed — §1 D-04 (project-delete-guard.cjs) — SEC-01 |
| `72e1e529` | fix(assistant): stop every source edit from destroying staged chat attachments | reviewed — diff: retainAcrossRestarts:true on the disk store; adoption integrity-gated |
| `f3bdd3af` | feat(admin-posts): send the loaded version and surface the 409 without losing the operator's work | skimmed — apps/admin renderer only; window-wide grep of every added line for dangerouslySetInnerHTML/innerHTML/srcdoc/eval/window.open/location.href/javascript:/document.cookie/storage found only the localStorage draft backup (SEC-07) |
| `fb473613` | feat(admin-settings): pass agentHandle to the 11 @jini-ai/ui-mounted Settings tabs | skimmed — agentHandle/aria tagging in apps/admin only; apps/admin renderer only; window-wide grep of every added line for dangerouslySetInnerHTML/innerHTML/srcdoc/eval/window.open/location.href/javascript:/document.cookie/storage found only the localStorage draft backup (SEC-07) |
| `b359e613` | fix(website): add RouteDeps.contentDbPath and make the BYOK tool-audit sink lazy | reviewed — diff: lazy sink (superseded by 6f32d027) |
| `e595312f` | types(admin-api): let api.updatePost carry an optional expectedVersion | skimmed — apps/admin renderer only; window-wide grep of every added line for dangerouslySetInnerHTML/innerHTML/srcdoc/eval/window.open/location.href/javascript:/document.cookie/storage found only the localStorage draft backup (SEC-07) |
| `76d7c739` | fix(desktop): one adoption chokepoint for every site-dir entry point | reviewed — site-dir-store.cjs resolveOrInitSiteDir chokepoint; spawn with args array, no shell |
| `ffc37e59` | docs(ads-memory): refactor review of the 2026-09-06 commits - excess and dead code | skipped — docs/report/todos only, no code; secret-pattern sweep of the window diff clean |
| `b81d57b1` | docs(ads-memory): code-inspection bug hunt over the 2026-09-06 commits | skipped — docs/report/todos only, no code; secret-pattern sweep of the window diff clean |
| `4e64e467` | docs(ads-memory): architecture and DI review of the 2026-09-06 commits | skipped — docs/report/todos only, no code; secret-pattern sweep of the window diff clean |
| `eb678ed3` | feat(voice-input): add a "Disabled for now" tooltip to the mic button | skimmed — apps/admin renderer only; window-wide grep of every added line for dangerouslySetInnerHTML/innerHTML/srcdoc/eval/window.open/location.href/javascript:/document.cookie/storage found only the localStorage draft backup (SEC-07) |
| `9c7d16bf` | feat(posts-route): forward expectedVersion and give the version 409 its own code | reviewed — routes/posts/update.ts 409 VERSION_CONFLICT envelope |
| `a9f84cdc` | refactor(admin-pages): bring PageEditor under the 9/9 complexity ceiling | skimmed — apps/admin renderer only; window-wide grep of every added line for dangerouslySetInnerHTML/innerHTML/srcdoc/eval/window.open/location.href/javascript:/document.cookie/storage found only the localStorage draft backup (SEC-07) |
| `0b7b6de1` | fix(admin-pages): let a newly created page accept hand-authored HTML | skimmed — apps/admin renderer only; window-wide grep of every added line for dangerouslySetInnerHTML/innerHTML/srcdoc/eval/window.open/location.href/javascript:/document.cookie/storage found only the localStorage draft backup (SEC-07) |
| `be45461e` | feat(post): add opt-in optimistic-concurrency check to updatePost | reviewed — same as b37864c3 |
| `f92e835f` | docs(ads-memory): record the f3579456 authorship misattribution | skipped — docs/report/todos only, no code; secret-pattern sweep of the window diff clean |
| `bbd297d8` | fix(desktop): make Projects-screen seeding survive an emptied registry | reviewed — desktop main-process state read in full at HEAD (main.cjs, project-ipc, project-delete-guard, project-registry, desktop-auth, tovu-server, site-registry, site-dir-store, keyed-serializer, runner-ipc-stubs, preload.mts); renderer sink grep clean |
| `013ca04e` | docs(ads): record the outstanding worklist from session tovu-f6 | skipped — docs/report/todos only, no code; secret-pattern sweep of the window diff clean |
| `7afe17a7` | docs(admin): correct pages/posts as done, not permanently excluded | skipped — docs/report/todos only, no code; secret-pattern sweep of the window diff clean |
| `48bf42c8` | fix(admin-posts): tag PostEditor's delete ConfirmDialog with agentHandle | skimmed — agentHandle/aria tagging in apps/admin only; apps/admin renderer only; window-wide grep of every added line for dangerouslySetInnerHTML/innerHTML/srcdoc/eval/window.open/location.href/javascript:/document.cookie/storage found only the localStorage draft backup (SEC-07) |
| `f3579456` | feat(admin-pages): tag PageEditor's remaining view/device controls | skimmed — agentHandle/aria tagging in apps/admin only; apps/admin renderer only; window-wide grep of every added line for dangerouslySetInnerHTML/innerHTML/srcdoc/eval/window.open/location.href/javascript:/document.cookie/storage found only the localStorage draft backup (SEC-07) |
| `3ad87f39` | feat(admin-pages): tag ThemePagesTab and ThemePageDetailsModal controls | skimmed — agentHandle/aria tagging in apps/admin only; apps/admin renderer only; window-wide grep of every added line for dangerouslySetInnerHTML/innerHTML/srcdoc/eval/window.open/location.href/javascript:/document.cookie/storage found only the localStorage draft backup (SEC-07) |
| `2202253d` | feat(admin-pages): tag Pages.tsx's list and tab controls | skimmed — agentHandle/aria tagging in apps/admin only; apps/admin renderer only; window-wide grep of every added line for dangerouslySetInnerHTML/innerHTML/srcdoc/eval/window.open/location.href/javascript:/document.cookie/storage found only the localStorage draft backup (SEC-07) |
| `56f6b46b` | feat(admin-posts): tag Posts.tsx's list controls | skimmed — agentHandle/aria tagging in apps/admin only; apps/admin renderer only; window-wide grep of every added line for dangerouslySetInnerHTML/innerHTML/srcdoc/eval/window.open/location.href/javascript:/document.cookie/storage found only the localStorage draft backup (SEC-07) |
| `6462865e` | docs(admin): correct the Access Tokens live-verification handle names | skipped — docs/report/todos only, no code; secret-pattern sweep of the window diff clean |
| `fe5c8974` | docs(admin): record Phase 4 - ai-assistant, media, and the 8 spot-check screens | skipped — docs/report/todos only, no code; secret-pattern sweep of the window diff clean |
| `c84559e8` | fix(admin-security): tag credential fields and Cancel/trigger buttons | skimmed — agentHandle/aria tagging in apps/admin only; apps/admin renderer only; window-wide grep of every added line for dangerouslySetInnerHTML/innerHTML/srcdoc/eval/window.open/location.href/javascript:/document.cookie/storage found only the localStorage draft backup (SEC-07) |
| `34a69401` | fix(admin-deployment): tag the two remaining external links on Static Site | skimmed — agentHandle/aria tagging in apps/admin only; apps/admin renderer only; window-wide grep of every added line for dangerouslySetInnerHTML/innerHTML/srcdoc/eval/window.open/location.href/javascript:/document.cookie/storage found only the localStorage draft backup (SEC-07) |
| `273b43cb` | fix(admin): tag remaining agent-driveable gaps in sites and collections | skimmed — agentHandle/aria tagging in apps/admin only; apps/admin renderer only; window-wide grep of every added line for dangerouslySetInnerHTML/innerHTML/srcdoc/eval/window.open/location.href/javascript:/document.cookie/storage found only the localStorage draft backup (SEC-07) |
| `935762a5` | fix(admin-media): pass agentHandle to the purge ConfirmDialog | skimmed — agentHandle/aria tagging in apps/admin only; apps/admin renderer only; window-wide grep of every added line for dangerouslySetInnerHTML/innerHTML/srcdoc/eval/window.open/location.href/javascript:/document.cookie/storage found only the localStorage draft backup (SEC-07) |
| `978a7ca3` | feat(admin-ai-assistant): tag AiAssistant screen's Tovu-owned controls | skimmed — agentHandle/aria tagging in apps/admin only; apps/admin renderer only; window-wide grep of every added line for dangerouslySetInnerHTML/innerHTML/srcdoc/eval/window.open/location.href/javascript:/document.cookie/storage found only the localStorage draft backup (SEC-07) |
| `125b8dcc` | docs(admin): record pages/posts as permanently excluded from the agent-tag sweep | skipped — docs/report/todos only, no code; secret-pattern sweep of the window diff clean |
| `8383d732` | docs(admin): record Phase 3 live verification and handoff for the agent-tag sweep | skipped — docs/report/todos only, no code; secret-pattern sweep of the window diff clean |
| `ffc6ce5e` | docs(admin): update agent-tag coverage report through the settings pass | skipped — docs/report/todos only, no code; secret-pattern sweep of the window diff clean |
| `7637876d` | feat(admin-settings): tag the Tovu-owned controls on /admin/settings | skimmed — agentHandle/aria tagging in apps/admin only; apps/admin renderer only; window-wide grep of every added line for dangerouslySetInnerHTML/innerHTML/srcdoc/eval/window.open/location.href/javascript:/document.cookie/storage found only the localStorage draft backup (SEC-07) |
| `feb8a777` | feat(assistant): add chat_list_pending_attachments so the model can find unclaimed uploads | reviewed — §1e list-pending-chat-attachments.ts owner-scoped; proxy stamps header server-side |
| `fb5a1909` | feat(admin-themes): tag the remaining controls on the main Themes screen | skimmed — agentHandle/aria tagging in apps/admin only; apps/admin renderer only; window-wide grep of every added line for dangerouslySetInnerHTML/innerHTML/srcdoc/eval/window.open/location.href/javascript:/document.cookie/storage found only the localStorage draft backup (SEC-07) |
| `98e3021c` | feat(admin-payments): tag the three cross-links on /admin/payments | skimmed — agentHandle/aria tagging in apps/admin only; apps/admin renderer only; window-wide grep of every added line for dangerouslySetInnerHTML/innerHTML/srcdoc/eval/window.open/location.href/javascript:/document.cookie/storage found only the localStorage draft backup (SEC-07) |
| `d52258af` | feat(admin-plugins): tag Plugins, AgentPlugins, and the plugin details modal | skimmed — agentHandle/aria tagging in apps/admin only; apps/admin renderer only; window-wide grep of every added line for dangerouslySetInnerHTML/innerHTML/srcdoc/eval/window.open/location.href/javascript:/document.cookie/storage found only the localStorage draft backup (SEC-07) |
| `10efb899` | fix(autosave): flush the pending standing draft on exit instead of cancelling it | skimmed — apps/admin renderer only; window-wide grep of every added line for dangerouslySetInnerHTML/innerHTML/srcdoc/eval/window.open/location.href/javascript:/document.cookie/storage found only the localStorage draft backup (SEC-07) |
| `eb10f5de` | fix(seo): allow clearing a per-entry SEO override via null | reviewed — routes/seo/put-entry.ts (admin.seo.manage; canonical URL validated in domain) |
| `c08155f2` | feat(admin-redirects): tag every control on /admin/redirects | skimmed — agentHandle/aria tagging in apps/admin only; apps/admin renderer only; window-wide grep of every added line for dangerouslySetInnerHTML/innerHTML/srcdoc/eval/window.open/location.href/javascript:/document.cookie/storage found only the localStorage draft backup (SEC-07) |
| `be0f582a` | feat(admin-integrations): tag the remaining controls on /admin/integrations | skimmed — agentHandle/aria tagging in apps/admin only; apps/admin renderer only; window-wide grep of every added line for dangerouslySetInnerHTML/innerHTML/srcdoc/eval/window.open/location.href/javascript:/document.cookie/storage found only the localStorage draft backup (SEC-07) |
| `b5439d94` | feat(admin-workspace): tag the rename form and delete button | skimmed — agentHandle/aria tagging in apps/admin only; apps/admin renderer only; window-wide grep of every added line for dangerouslySetInnerHTML/innerHTML/srcdoc/eval/window.open/location.href/javascript:/document.cookie/storage found only the localStorage draft backup (SEC-07) |
| `a5dd5e09` | feat(admin-recovery): tag every control on /admin/recovery | skimmed — agentHandle/aria tagging in apps/admin only; apps/admin renderer only; window-wide grep of every added line for dangerouslySetInnerHTML/innerHTML/srcdoc/eval/window.open/location.href/javascript:/document.cookie/storage found only the localStorage draft backup (SEC-07) |
| `c629c0e6` | feat(admin-widgets): tag every control across all four widgets screens | skimmed — agentHandle/aria tagging in apps/admin only; apps/admin renderer only; window-wide grep of every added line for dangerouslySetInnerHTML/innerHTML/srcdoc/eval/window.open/location.href/javascript:/document.cookie/storage found only the localStorage draft backup (SEC-07) |
| `cdb205a5` | feat(admin-dashboard): tag every link on the Overview screen | skimmed — agentHandle/aria tagging in apps/admin only; apps/admin renderer only; window-wide grep of every added line for dangerouslySetInnerHTML/innerHTML/srcdoc/eval/window.open/location.href/javascript:/document.cookie/storage found only the localStorage draft backup (SEC-07) |
| `f7b8af1c` | test(assistant): prove media_promote_chat_attachment is generic, not AVIF-specific | out of scope — tests/e2e only (read as evidence where cited) |
| `6e71742c` | feat(admin): wire agentHandle onto every remaining untagged ConfirmDialog | skimmed — agentHandle/aria tagging in apps/admin only; apps/admin renderer only; window-wide grep of every added line for dangerouslySetInnerHTML/innerHTML/srcdoc/eval/window.open/location.href/javascript:/document.cookie/storage found only the localStorage draft backup (SEC-07) |
| `1d5c0376` | docs(desktop-e2e): correct a comment that promised isolation the suite does not have | out of scope — tests/e2e only (read as evidence where cited) |
| `f281d3a2` | feat(assistant): add media_promote_chat_attachment, bridging chat uploads into the media library | reviewed — §1e promote-chat-attachment.ts run-scoped claim; media.upload gate inherited |
| `e804d16d` | feat(admin-comments): tag every control on /admin/comments | skimmed — agentHandle/aria tagging in apps/admin only; apps/admin renderer only; window-wide grep of every added line for dangerouslySetInnerHTML/innerHTML/srcdoc/eval/window.open/location.href/javascript:/document.cookie/storage found only the localStorage draft backup (SEC-07) |
| `cbbda021` | feat(admin): tag Members' remaining controls and wire ConfirmDialog handles | skimmed — agentHandle/aria tagging in apps/admin only; apps/admin renderer only; window-wide grep of every added line for dangerouslySetInnerHTML/innerHTML/srcdoc/eval/window.open/location.href/javascript:/document.cookie/storage found only the localStorage draft backup (SEC-07) |
| `54051fe3` | fix(desktop): rename the fleet window title from Tovu Runner to Tovu | reviewed — desktop main-process state read in full at HEAD (main.cjs, project-ipc, project-delete-guard, project-registry, desktop-auth, tovu-server, site-registry, site-dir-store, keyed-serializer, runner-ipc-stubs, preload.mts); renderer sink grep clean |
| `7198436e` | feat(admin-seo): tag the per-entry override editor and picker for agent driving | skimmed — agentHandle/aria tagging in apps/admin only; apps/admin renderer only; window-wide grep of every added line for dangerouslySetInnerHTML/innerHTML/srcdoc/eval/window.open/location.href/javascript:/document.cookie/storage found only the localStorage draft backup (SEC-07) |
| `a53c80df` | feat(desktop): make the Projects screen the default front page | reviewed — desktop main-process state read in full at HEAD (main.cjs, project-ipc, project-delete-guard, project-registry, desktop-auth, tovu-server, site-registry, site-dir-store, keyed-serializer, runner-ipc-stubs, preload.mts); renderer sink grep clean |
| `a4efd9c2` | feat(admin-roles): tag the remaining untagged controls on /admin/roles | skimmed — agentHandle/aria tagging in apps/admin only; apps/admin renderer only; window-wide grep of every added line for dangerouslySetInnerHTML/innerHTML/srcdoc/eval/window.open/location.href/javascript:/document.cookie/storage found only the localStorage draft backup (SEC-07) |
| `4f2052f6` | fix(admin-assistant): move the composer mic button next to the "+" | skimmed — apps/admin renderer only; window-wide grep of every added line for dangerouslySetInnerHTML/innerHTML/srcdoc/eval/window.open/location.href/javascript:/document.cookie/storage found only the localStorage draft backup (SEC-07) |
| `79ade955` | feat(admin-database): tag every interactive control on /admin/database | skimmed — agentHandle/aria tagging in apps/admin only; apps/admin renderer only; window-wide grep of every added line for dangerouslySetInnerHTML/innerHTML/srcdoc/eval/window.open/location.href/javascript:/document.cookie/storage found only the localStorage draft backup (SEC-07) |
| `ab5f4b0f` | feat(admin): wire agentHandle through every Placeholder-backed nav page | skimmed — agentHandle/aria tagging in apps/admin only; apps/admin renderer only; window-wide grep of every added line for dangerouslySetInnerHTML/innerHTML/srcdoc/eval/window.open/location.href/javascript:/document.cookie/storage found only the localStorage draft backup (SEC-07) |
| `8cfb8eb9` | docs(admin): audit agentHandle coverage across every nav-listed admin page | skipped — docs/report/todos only, no code; secret-pattern sweep of the window diff clean |
| `27a05955` | docs: handoff for the rotated-out autosave-drafts agent | skipped — docs/report/todos only, no code; secret-pattern sweep of the window diff clean |
| `34694309` | wip(posts): autosave recovery banner in PostEditor, state unverified | skimmed — apps/admin renderer only; window-wide grep of every added line for dangerouslySetInnerHTML/innerHTML/srcdoc/eval/window.open/location.href/javascript:/document.cookie/storage found only the localStorage draft backup (SEC-07) |
| `a4b99c90` | feat(pages): render the standing-draft recovery banner in PageEditor | skimmed — apps/admin renderer only; window-wide grep of every added line for dangerouslySetInnerHTML/innerHTML/srcdoc/eval/window.open/location.href/javascript:/document.cookie/storage found only the localStorage draft backup (SEC-07) |
| `559655cb` | feat(posts): wire standing-draft autosave into usePostEditor; fix slug URLs | reviewed — server half routes/posts/autosave.ts; admin half via sink sweep |
| `0911b45d` | docs: record that both handoff tasks belong to the peer session | skipped — docs/report/todos only, no code; secret-pattern sweep of the window diff clean |
| `3f6097dc` | design(admin/seo): drop the Entry caption and take Pages & posts full width | skimmed — apps/admin renderer only; window-wide grep of every added line for dangerouslySetInnerHTML/innerHTML/srcdoc/eval/window.open/location.href/javascript:/document.cookie/storage found only the localStorage draft backup (SEC-07) |
| `24386850` | docs: handoff for a second session - mic button, and the AVIF upload bridge | skipped — docs/report/todos only, no code; secret-pattern sweep of the window diff clean |
| `05782b71` | feat(pages): wire standing-draft autosave + add the missing unsaved-work guard | skimmed — apps/admin renderer only; window-wide grep of every added line for dangerouslySetInnerHTML/innerHTML/srcdoc/eval/window.open/location.href/javascript:/document.cookie/storage found only the localStorage draft backup (SEC-07) |
| `3a3dea2b` | docs(admin/seo): correct the file header the de-carding made stale | skipped — admin file-header comment only |
| `613ea7e2` | design(admin/seo): take the per-entry panels out of their boxes too | skimmed — apps/admin renderer only; window-wide grep of every added line for dangerouslySetInnerHTML/innerHTML/srcdoc/eval/window.open/location.href/javascript:/document.cookie/storage found only the localStorage draft backup (SEC-07) |
| `9ac963e0` | fix(posts): standing-draft autosave carries title explicitly | reviewed — server half routes/posts/autosave.ts (content.write, conditional UPDATE); admin half via sink sweep |
| `f8fc6b8e` | design(admin/seo): take the defaults form and sitemap panel out of their cards | skimmed — apps/admin renderer only; window-wide grep of every added line for dangerouslySetInnerHTML/innerHTML/srcdoc/eval/window.open/location.href/javascript:/document.cookie/storage found only the localStorage draft backup (SEC-07) |
| `56a0fc09` | feat(admin): shared standing-draft autosave hook + api client functions | skimmed — apps/admin renderer only; window-wide grep of every added line for dangerouslySetInnerHTML/innerHTML/srcdoc/eval/window.open/location.href/javascript:/document.cookie/storage found only the localStorage draft backup (SEC-07) |
| `eba275f4` | feat(posts): standing-draft autosave persistence + HTTP surface (posts+pages) | reviewed — routes/posts/autosave.ts + PostRepoPort.writeAutosave conditional update; no raw SQL |
| `868cfe72` | feat(desktop): route project cards to their own window, strip the webview model | reviewed — desktop main-process state read in full at HEAD (main.cjs, project-ipc, project-delete-guard, project-registry, desktop-auth, tovu-server, site-registry, site-dir-store, keyed-serializer, runner-ipc-stubs, preload.mts); renderer sink grep clean |
| `04806e6b` | feat(media): accept AVIF in the admin upload surfaces, with regression tests | reviewed — Jini content-type-sniffer.ts AVIF by ftyp brand; media-service allowlist; admin accept list |
| `72a8dcb6` | feat(admin-roles): convert /admin/roles to a two-tab screen | skimmed — apps/admin renderer only; window-wide grep of every added line for dangerouslySetInnerHTML/innerHTML/srcdoc/eval/window.open/location.href/javascript:/document.cookie/storage found only the localStorage draft backup (SEC-07) |
| `e94da8f8` | feat(posts): add nullable posts.autosave_json column for standing-draft autosave | reviewed-light — posts.autosave_json column migration |
| `20be2646` | docs(tasks): refresh - 23 commits landed, four agents in flight, five new findings | skipped — docs/report/todos only, no code; secret-pattern sweep of the window diff clean |
| `cbb727db` | fix(desktop): restore own-server boot mode gutted by the boot-token commit | reviewed — desktop main-process state read in full at HEAD (main.cjs, project-ipc, project-delete-guard, project-registry, desktop-auth, tovu-server, site-registry, site-dir-store, keyed-serializer, runner-ipc-stubs, preload.mts); renderer sink grep clean |
| `56e87ae0` | feat(admin-seo): convert /admin/seo to a three-tab screen | skimmed — apps/admin renderer only; window-wide grep of every added line for dangerouslySetInnerHTML/innerHTML/srcdoc/eval/window.open/location.href/javascript:/document.cookie/storage found only the localStorage draft backup (SEC-07) |
| `2c6e0b07` | fix(post-editor): move preview-fallback notice above the frame, mirroring Pages | skimmed — apps/admin renderer only; window-wide grep of every added line for dangerouslySetInnerHTML/innerHTML/srcdoc/eval/window.open/location.href/javascript:/document.cookie/storage found only the localStorage draft backup (SEC-07) |
| `cf05115c` | docs(desktop): handoff for the rotated-out runner-ui-port agent | skipped — docs/report/todos only, no code; secret-pattern sweep of the window diff clean |
| `15548bef` | feat(auth): loopback boot token — the desktop admin comes up with no password | reviewed — §1a boot token end to end |
| `0a1fb89e` | fix(admin-settings): remove the peach background from /admin/settings | skimmed — apps/admin renderer only; window-wide grep of every added line for dangerouslySetInnerHTML/innerHTML/srcdoc/eval/window.open/location.href/javascript:/document.cookie/storage found only the localStorage draft backup (SEC-07) |
| `30e68c54` | fix(admin-editors): compress the action row's band and give it a left anchor | skimmed — apps/admin renderer only; window-wide grep of every added line for dangerouslySetInnerHTML/innerHTML/srcdoc/eval/window.open/location.href/javascript:/document.cookie/storage found only the localStorage draft backup (SEC-07) |
| `de1e1e2e` | test(desktop-e2e): assert the admin comes up authenticated | out of scope — tests/e2e only (read as evidence where cited) |
| `2aa317ab` | feat(desktop): the admin comes up authenticated — no login screen | reviewed — §1a (desktop-auth.cjs, authenticateSiteSession/startSiteBackend) |
| `fb996e8f` | feat(desktop): use the Tovu logo as the app and nav mark | skipped — brand image assets |
| `29a7f036` | feat(admin-editors): move Published/Save/Delete to their own row under the toolbar | skimmed — apps/admin renderer only; window-wide grep of every added line for dangerouslySetInnerHTML/innerHTML/srcdoc/eval/window.open/location.href/javascript:/document.cookie/storage found only the localStorage draft backup (SEC-07) |
| `8e5a9d74` | feat(admin-editors): centre the editor title, move the back link to the far left | skimmed — apps/admin renderer only; window-wide grep of every added line for dangerouslySetInnerHTML/innerHTML/srcdoc/eval/window.open/location.href/javascript:/document.cookie/storage found only the localStorage draft backup (SEC-07) |
| `5178eea5` | design(landing): gold, black and white — the filled pill goes gold in dark mode | skipped — docs/report/todos only, no code; secret-pattern sweep of the window diff clean |
| `717273e8` | docs(tasks): no SITE password at all, and the default-owner-password finding | skipped — docs/report/todos only, no code; secret-pattern sweep of the window diff clean |
| `b3f613a6` | design(landing): cycle the hero verb with kUInetic's word-cycler, as x.ai does | skipped — docs/report/todos only, no code; secret-pattern sweep of the window diff clean |
| `14167454` | test(desktop): first E2E that actually launches apps/desktop, and one RED | out of scope — tests/e2e only (read as evidence where cited) |
| `f66ef907` | design(landing): rebuild the xAI-language homepage sample around x.ai's structure | skipped — docs/report/todos only, no code; secret-pattern sweep of the window diff clean |
| `af67f51e` | docs(desktop): manifest v2 — option-2 scope, N-BrowserWindow model | skipped — docs/report/todos only, no code; secret-pattern sweep of the window diff clean |
| `6a0bd61c` | feat(desktop): port the Runner preload and add TOVU_DESKTOP_UI=runner | reviewed — preload.mts: contextBridge allowlist only; unsandboxed fleet window, no Node in page |
| `4c75f4c0` | feat(desktop): port Tovu-Runner s renderer and shared contracts | reviewed-light — ported renderer + contracts; sink grep (innerHTML/eval/window.open/location) clean; App.tsx webview attrs read |
| `10fb9215` | build(desktop): stand up the renderer build inside apps/desktop | reviewed-light — vite.config.mts: dev server pinned to 127.0.0.1:5175, fs.allow=[here] |
| `098e3466` | docs(desktop): record the Tovu-Runner UI port manifest before any code lands | skipped — docs/report/todos only, no code; secret-pattern sweep of the window diff clean |
| `46513d83` | docs(ads-memory): hand off tovu-c0 — three owner-only commands, and apps/desktop never launched | skipped — docs/report/todos only, no code; secret-pattern sweep of the window diff clean |
| `32af5802` | docs(index): record the reverted HTTP/2 attempt (Node-core crash, reproduced 3x) | skipped — comment block in index.ts recording the reverted HTTP/2 attempt |
| `2cd019cd` | fix(assistant,settings): drop the Connection header on HTTP/2 SSE streams | reviewed — diff: Connection header omitted on HTTP/2 only; no other header change |
| `848ddd09` | fix(admin-dev-proxy): strip hop-by-hop headers before relaying Vite's response | reviewed — admin-dev-proxy.ts: hop-by-hop response strip; dev-only, TLS relax scoped to Vite agent |
| `d131619d` | feat(site-dir): add repairSite — write marker files into a pre-marker-convention site | reviewed-light — repair-site.ts: refuses on existing marker / missing content.db / unmigrated db; writes two JSON files atomically (CLI-only caller) |
| `8a14b56a` | refactor(site-dir): extract readAppliedSchemaIdentity, shared by the boot guard and repair-site | reviewed-light — readAppliedSchemaIdentity extraction (read-only db open) |

