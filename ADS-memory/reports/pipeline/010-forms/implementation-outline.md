# Implementation Outline: forms

- Spec: SPEC-010 v1.0.0 (hash: sha256:d2d727639ef9e1e6131494e2d2dfe90290d917775733b8da72343f8bbfb0d87e)
- ADR: ADR-PIPE-010 (Forms — Implementation Architecture)
- Status: PRODUCED
- Trigger result: Boundary Cross, Contract Change, System Wiring, Data And Persistence, Critical Cross-Boundary Invariant, Parallelization Ambiguity
- Date: 2026-07-13
- Author: Software Architect Agent

> Use this artifact only for post-ADR, pre-tasks structure. It defines module boundaries, public/exported contracts, wiring, data boundaries, and critical invariants. It must not contain pseudo-code, private helper inventories, or task sequencing.

## Trigger Decision Matrix

| Trigger | Applies? | Evidence | Source Trace |
|---|---:|---|---|
| Boundary Cross | yes | Feature crosses `src/forms` (domain) + `src/server/routes/admin/forms` + `src/server/routes/site/forms-submit.ts` (API) + `apps/admin/src/sections/{FormsList,FormEditor}.tsx` (UI) + `src/identity` (permission catalog), `src/mail` (`MailerPort`), `src/integrations` (`enqueueDelivery`) | REQ-01..16 |
| Contract Change | yes | 8 new HTTP endpoints (7 admin + 1 public); 6 new error codes; new `FormDefinitionRepoPort`/`FormSubmissionRepoPort` exported interfaces; new `form.submission.received` domain event | api.spec.md §1, errors.spec.md §2, state.spec.md §1.3 |
| System Wiring | yes | New outbox-driven subscriber (`notify-subscriber.ts`) + a new `bus.subscribe` forwarding line to `integrations`' `enqueueDelivery` — this is exactly the kind of async wiring `orchestrator.spec.md`'s omission justification says doesn't apply to the synchronous core, but does apply here at the composition-root level | REQ-11, REQ-12, ADR-PIPE-010 Decision |
| Data And Persistence | yes | 2 new tables (`form_definitions`, `form_submissions`); no brownfield migration (greenfield) | REQ-01, REQ-10, state.spec.md §1 |
| Brownfield Dependency | no | Forms itself is greenfield; the two dependency-shaped risks (mail adapter location, webhook-fanout wiring gap) are named in ADR-PIPE-010 Context/Consequences, not a migration of Forms' own prior state | spec-manifest.md |
| Reverse-Spec Or Migration | no | Not a reverse-spec extraction; no prior Forms state to migrate | — |
| Critical Cross-Boundary Invariant | yes | INV-04 (honeypot silent-discard), INV-05 (never block on mail/webhook), INV-07 (event emitted iff accepted), INV-09 composite rate-limit key — all span the submission write path + outbox + two independent subscribers | feature.spec.md Invariants |
| Parallelization Ambiguity | yes | Schema + domain core + admin-CRUD write path must land before the public submission path and the two subscribers can be built against real contracts; the `server/app.ts` wiring line is a shared-file convergence point with the parallel Integrations sibling spec | ADR-PIPE-010 Rationale, Consequences/Risks |

## Module Map

| Module/Domain | Owns | Responsibility | Public Contracts | Dependencies | Notes |
|---|---|---|---|---|---|
| `forms` (`src/forms/`) | `form_definitions`/`form_submissions` tables; field-vocabulary/validation rules; the OQ-01 manifest seam | Definition CRUD (via gateway), public submission validation/accept, notification dispatch | C-001..C-012 | `core` (ports/outbox), `identity` (`authorize()`, permission strings), `mail` (`MailerPort` by handle), `integrations` (`enqueueDelivery` forwarding), `infra/db` (schema) | `manifest.ts` is data-only; no domain file imports it back |
| `identity` (existing, extended) | `admin.forms.*` permission strings | Permission catalog registration | Existing `BASE_CATALOG` (unchanged shape) | — | `forms` reads this module (`authorize()`); this module gains no new dependency on `forms` |
| `integrations` (existing, extended by wiring only) | Webhook fan-out (`enqueueDelivery`) | Consumes the new `form.submission.received` topic via one forwarding subscription registered in `server/app.ts` | Existing `enqueueDelivery`/`WebhookSourceEvent` (unchanged signature) | — | No `integrations`-owned file changes; only a composition-root registration line references it |
| `server` routes (existing, extended) | HTTP wiring for `forms`; the webhook-fanout + notify subscriber registrations | Route registration + `authorize()` gating + error mapping + outbox subscriber wiring | C-009..C-012 | `forms`, `identity`, `integrations` | Admin routes mirror `routes/admin/menus/`; public route mirrors `routes/site/analytics-ingest.ts` |
| `apps/admin` UI (existing, extended) | `FormsList.tsx`/`FormEditor.tsx` screens | Renders/edits form definitions and submissions via the admin API | UI-facing, not typed here — see ui.spec.md | `server` routes (via fetch) | Flat files per repo convention |

## File Map

| File Path | Module | Creates / Changes | Public Contracts Housed | Responsibility | Why This Separation Exists | Notes |
|---|---|---|---|---|---|---|
| `src/forms/manifest.ts` | forms | creates | C-001 the manifest data object | THE OQ-01 seam — plain JSON-serializable declaration of Forms' Tier-1-shaped surface | Isolating "what Forms declares" from "how Forms is wired" is the one thing that makes a future real Tier-1 loader retrofit mechanical (ADR-PIPE-010 Decision) | Zero imports of runtime code from this module; Code Review enforces this (ADR-PIPE-010 Enforcement) |
| `src/forms/forms.ts` | forms | creates | C-002 `validateFieldDescriptors`, C-003 `validateSubmissionPayload`, C-004 `isHoneypotTripped` | Pure validation core — field vocabulary, required/maxLength/unregistered-key checks, honeypot trip detection (behavior.spec §5.1) | The "one evaluator" for validation logic, directly unit-testable with zero I/O, mirrors `settings.ts`'s single-evaluator shape | No writes happen here |
| `src/forms/write-service.ts` | forms | creates | C-005 `createFormDefinition`, C-006 `updateFormDefinition`, C-007 `setFormDefinitionStatus` | Admin definition mutations (REQ-01..04), each wrapping the existing `executeCommand` gateway | Keeps Forms' admin audit trail consistent with every other admin mutation (posts/menus/members) rather than inventing a parallel chokepoint | Slug immutability (behavior.spec §1.1) and field-id-removal restriction (behavior.spec §1.2) are enforced here before `execute()` runs |
| `src/forms/submit-service.ts` | forms | creates | C-008 `submitForm` | THE public submission write path (REQ-05..09, REQ-16) — validates, honeypot-checks, rate-limits, persists, enqueues `form.submission.received`, returns without awaiting outbox drainage | Deliberately bypasses `executeCommand` (no actor/permission fits an anonymous visitor — ADR-PIPE-010 Pattern Evaluation); mirrors `analytics-ingest.ts`'s `ingestHit` shape | Calls `void processOutbox(...)` — never `await`s it (INV-05, AC-24) |
| `src/forms/notify-subscriber.ts` | forms | creates | C-009 `registerFormNotifySubscriber` | Outbox subscriber for `form.submission.received` (REQ-12) — loads definition + submission, calls `MailerPort.send()` when `notify.enabled` | The one piece of Forms-owned business logic riding the outbox; kept separate from the webhook-fanout forwarding (which is not Forms-owned logic) | Builds `MailerSendOptions.idempotencyKey` from `submissionId` (ADR-037 amendment 1 requires this) |
| `src/forms/rate-limit-profile.ts` | forms | creates | C-010 `FORMS_SUBMIT_PROFILE`, C-011 `buildFormsRateLimitKey` | Wraps the existing `server/middleware/rate-limit.ts` `createRateLimiter` with Forms' profile + composite key | Reuse over reinvention (Article I) — see ADR-PIPE-010 Pattern Evaluation | Does not reimplement the limiter algorithm |
| `src/forms/ports.ts` | forms | creates | C-012 `FormDefinitionRepoPort`, `FormSubmissionRepoPort` | Persistence seams, rule-of-two | Matches `settings`/`integrations` port convention | Two ports, not one — definitions and submissions have distinct lifecycles (INV-08) |
| `src/forms/repo.memory.ts` | forms | creates | (implements C-012) | In-memory adapters for tests | Rule-of-two adapter #1 | — |
| `src/forms/repo.sqlite.ts` | forms | creates | (implements C-012) | Drizzle/SQLite adapters | Rule-of-two adapter #2 | Reads/writes the 2 tables added to `src/infra/db/schema.ts` |
| `src/forms/errors.ts` | forms | creates | C-013 typed error classes (`FormFieldValidationError`, `FormSlugConflictError`, `FormDefinitionNotFoundError`, `FormSubmissionValidationError`, `FormSubmissionNotFoundError`, `FormRateLimitExceededError`) | Typed domain errors → route-layer error-code mapping | Mirrors `PrincipalNotFoundError`-style typed-error convention | One class per `errors.spec.md` §2 code that originates in this module |
| `src/infra/db/schema.ts` | infra | changes | (Drizzle table defs, not a "contract") | Adds `formDefinitions`/`formSubmissions` Drizzle table definitions | Shared schema file across all features by existing convention | No behavior — pure schema |
| `src/identity/permissions.ts` | identity | changes | (extends `BASE_CATALOG`, not a new contract) | Registers `admin.forms.manage`, `admin.forms.submissions.read`, `admin.forms.submissions.delete` (REQ-15) | Existing single registration point for all permission strings | Verbatim strings from `api.spec.md` §2 |
| `src/server/routes/admin/forms/list.ts` | server | creates | C-014a `registerAdminFormsListRoute` | HTTP wiring for `FORMS_LIST_DEFINITIONS` | One file per endpoint, matches `routes/admin/menus/` | — |
| `src/server/routes/admin/forms/create.ts` | server | creates | C-014b `registerAdminFormsCreateRoute` | HTTP wiring for `FORMS_CREATE_DEFINITION` | Same as above | Calls C-005 |
| `src/server/routes/admin/forms/get-by-id.ts` | server | creates | C-014c `registerAdminFormsGetRoute` | HTTP wiring for `FORMS_GET_DEFINITION` | Same as above | — |
| `src/server/routes/admin/forms/update.ts` | server | creates | C-014d `registerAdminFormsUpdateRoute` | HTTP wiring for `FORMS_UPDATE_DEFINITION`, incl. `status` toggle | Same as above | Calls C-006/C-007 |
| `src/server/routes/admin/forms/list-submissions.ts` | server | creates | C-014e `registerAdminFormsListSubmissionsRoute` | HTTP wiring for `FORMS_LIST_SUBMISSIONS` | Same as above | Cursor pagination per api.spec.md §4 |
| `src/server/routes/admin/forms/get-submission.ts` | server | creates | C-014f `registerAdminFormsGetSubmissionRoute` | HTTP wiring for `FORMS_GET_SUBMISSION` | Same as above | — |
| `src/server/routes/admin/forms/delete-submission.ts` | server | creates | C-014g `registerAdminFormsDeleteSubmissionRoute` | HTTP wiring for `FORMS_DELETE_SUBMISSION` | Same as above | — |
| `src/server/routes/site/forms-submit.ts` | server | creates | C-015 `registerFormsSubmitRoute` | HTTP wiring for the public `FORMS_POST_SUBMIT` | Mirrors `routes/site/analytics-ingest.ts`'s public-route shape exactly | Must be registered before the site `/:slug` catch-all |
| `src/server/app.ts` | server | changes | (wiring only) | Imports + registers the 8 route registrars; adds `bus.subscribe('form.submission.received', ...)` forwarding to `enqueueDelivery`; registers C-009's notify subscriber | Existing single wiring point | **Shared-file convergence risk with the parallel Integrations sibling spec** — see Downstream Handoff Notes |
| `src/server/routes/types.ts` | server | changes | (wiring only) | `RouteDeps` gains `formDefinitionRepo`, `formSubmissionRepo`, `formsRateLimiter` | Existing shared composition-root deps seam | Mirrors the `webhookSubscriptionRepo`/`webhookDeliveryRepo` field-addition precedent |
| `apps/admin/src/sections/FormsList.tsx` | apps/admin | creates | (UI-facing, see ui.spec.md §2.1/§3.1) | Forms list screen | Matches `Members.tsx`/`Menus.tsx` flat-file convention | — |
| `apps/admin/src/sections/FormEditor.tsx` | apps/admin | creates | (UI-facing, see ui.spec.md §2.2-2.5/§3.2-3.5) | Form editor + fields editor + submissions list/detail | Single flat file per repo convention, internally composed of the 5 ui.spec.md components | May split into local sub-files if it exceeds the repo's practical single-file size, same escape hatch ADR-PIPE-007 pre-approved for `Settings.tsx` |
| `apps/admin/src/App.tsx` | apps/admin | changes | (wiring only) | Mounts the Forms routes | Existing per-section route wiring | — |

## Contract Map

| Contract ID / Name | File | Owner Module | Kind | Why Needed | Job | Inputs | Outputs | Validation | Errors | Effect Boundary | Complexity / Resource View | Aggregate-Risk Note | Spec/ADR Trace | Test Seam / Expectation |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| C-001 manifest object | `manifest.ts` | forms | exported data | OQ-01 seam | Declare Forms' Tier-1-shaped surface as data | N/A | `{ pluginId, tier, displayName, fieldTypeVocabulary, capabilities[], adminMenuEntry, webhookTopics[] }` | N/A (data, no runtime validation) | N/A | None — pure data | O(1) | Not applicable — a plain object cannot itself introduce a runtime risk; the risk is drift from a future loader's expected shape (named in ADR-PIPE-010 Consequences) | ADR-PIPE-010 Decision | Code Review: static check that the file contains no function/closure exports |
| C-002 `validateFieldDescriptors` | `forms.ts` | forms | exported function | REQ-02, INV-02 | Reject any field descriptor outside the closed vocabulary or violating limits | `FieldDescriptor[]` | `{ valid: true } \| { valid: false, fieldErrors }` | type ∈ {text,email,textarea,checkbox}; 1-20 fields; label 1-200 chars; maxLength 1-5000 and forbidden for checkbox | none thrown (returns a result) | Pure decision | O(n) over fields | Invariant case: a `checkbox` field with `maxLength` set must always fail (behavior.spec §4) | REQ-02, AC-03, INV-02, behavior.spec §4/§7 | Unit test: one case per behavior.spec §7 boundary row |
| C-003 `validateSubmissionPayload` | `forms.ts` | forms | exported function | REQ-06, INV-01 | Validate a raw submission body against a definition's declared fields | `{ definition: FormDefinitionRecord, body: Record<string, unknown> }` | `{ valid: true, data } \| { valid: false, fieldErrors }` | required fields present; maxLength respected; no keys outside declared fields + `_hp` | none thrown (returns a result) | Pure decision | O(n) over declared fields + body keys | Invariant case: `data`'s keys must always be a subset of the definition's field ids (INV-01) — this function is the sole gate that guarantees it | REQ-06, AC-08/09/10, INV-01 | Unit test: EC-01/02/09, plus every behavior.spec §7 submission-boundary row |
| C-004 `isHoneypotTripped` | `forms.ts` | forms | exported function | REQ-08, INV-04 | Decide whether `_hp` triggers silent-discard | `{ hp: unknown }` | `boolean` | present-and-non-empty-after-trim = true; absent or whitespace-only = false | none (total function) | Pure decision | O(1) | Invariant case: this is the single predicate INV-04's silent-discard path depends on — must never be re-implemented at a second call site | REQ-08, AC-13, INV-04, behavior.spec §5.1, EC-09 | Unit test: empty/absent/whitespace/non-empty cases |
| C-005 `createFormDefinition` | `write-service.ts` | forms | exported function | REQ-01, INV-02/03 | Authorize → validate fields → check slug uniqueness → create via `executeCommand` | `{ workspaceId, actor, name, slug, fields, notify? }` | `{ definition: FormDefinitionRecord }` | C-002 field validation; slug pattern; slug uniqueness (DB unique index, behavior.spec §6.1) | `FormFieldValidationError`→400, `FormSlugConflictError`→409 (DB unique-index violation mapped at this layer) | Explicit side effect: DB write via `executeCommand` (change-set + outbox) | O(1) | **Aggregate-risk:** the slug-uniqueness tie-break relies entirely on the DB unique index, not app logic (behavior.spec §6.1) — a missing index silently reopens EC-04's race | REQ-01..03, AC-01/02/03/04, INV-02/03, EC-04 | Integration test at the real SQLite adapter for the concurrent-create race (EC-04) |
| C-006 `updateFormDefinition` | `write-service.ts` | forms | exported function | REQ-04, behavior.spec §1.1/§1.2 | Authorize → reject `slug` in patch → reject field-id removal → validate → update via `executeCommand` | `{ workspaceId, actor, formId, patch }` | `{ definition: FormDefinitionRecord }` | patch must not include `slug`; patch `fields[]` must be a superset-preserving edit of existing field ids | `FormFieldValidationError`→400, `FormDefinitionNotFoundError`→404 | Explicit side effect: DB write via `executeCommand` | O(n) over fields (superset check) | **Aggregate-risk:** the field-id-removal check is the only thing preventing INV-01 orphaning historical submission data — a bug here corrupts data semantics silently (existing submissions keep referencing a vanished field id with no error surfaced) | REQ-04, behavior.spec §1.1/§1.2, AC-05 | Unit test: patch omitting an existing field id → rejected; patch including `slug` → ignored/rejected consistently |
| C-007 `setFormDefinitionStatus` | `write-service.ts` | forms | exported function | REQ-04, INV-08 | Authorize → flip `active`⇄`disabled` via `executeCommand`; never deletes | `{ workspaceId, actor, formId, status }` | `{ definition: FormDefinitionRecord }` | status ∈ {active, disabled} | `FormDefinitionNotFoundError`→404 | Explicit side effect: DB write via `executeCommand` | O(1) | Invariant case: no code path in this module ever issues a DELETE against `form_definitions` (INV-08) — enforced by the port surface simply not exposing a delete method | REQ-04, AC-05/06, INV-08 | Integration test: disable then submit → `FORMS_DEFINITION_NOT_FOUND` (AC-05); static check that `FormDefinitionRepoPort` has no delete method |
| C-008 `submitForm` | `submit-service.ts` | forms | exported function | REQ-05..09, REQ-16, INV-04/05/07 | Public entrypoint: resolve slug → honeypot check → validate → rate-limit → persist → enqueue event → return, without awaiting side effects | `{ workspaceId, slug, body, sourceIp }` | `{ status: "accepted" }` (identical for genuine-accept and honeypot-discard, REQ-08) | C-004 honeypot check; C-003 payload validation; C-011 rate-limit key check; definition must exist and be `active` | `FormDefinitionNotFoundError`→404 (covers both nonexistent and disabled, REQ-07), `FormSubmissionValidationError`→400, `FormRateLimitExceededError`→429 | Explicit side effect: DB write (submission insert) + outbox enqueue; **fire-and-forget** `void processOutbox(...)` — never awaited in the response path | O(1) plus C-003's O(n) | **Aggregate-risk:** this is the sole function AC-24/INV-05 depend on — an accidental `await` on the outbox drain (or on `MailerPort.send()`) anywhere in this call path silently reintroduces the exact blocking bug REQ-16 forbids | REQ-05..09, REQ-16, AC-07/08/09/10/11/12/13/14/15/24, INV-01/04/05/07, EC-01..09 | Integration test at the real HTTP boundary; AC-24 specifically asserts the response resolves before a stubbed slow/throwing `MailerPort.send()` completes |
| C-009 `registerFormNotifySubscriber` | `notify-subscriber.ts` | forms | exported function | REQ-12 | Subscribe `form.submission.received`; on notify-enabled definitions, call `MailerPort.send()` per recipient | `{ bus, mailer, formDefinitionRepo, formSubmissionRepo }` (called once at boot) | `() => Promise<void>` (an unsubscribe function, per `EventBusPort.subscribe`) | notify.enabled === true; recipients non-empty | Failures logged, not thrown into the bus (a subscriber must not crash `publish()`'s loop over other subscribers) | Explicit side effect: `MailerPort.send()` per recipient (async, decoupled from the HTTP request per REQ-12) | O(r) over recipients | Invariant case: notify-disabled definitions must produce zero `send()` calls (AC-18) — the enabled-check is the sole gate | REQ-12, AC-17/18, EC-06 | Integration test: enabled definition → `MailerPort.send()` invoked with correct recipient/idempotencyKey; disabled → never invoked |
| C-010 `FORMS_SUBMIT_PROFILE` | `rate-limit-profile.ts` | forms | exported constant | REQ-09 | Pin the 5-req/60s profile | N/A | `RateLimitProfile` (imported type from `server/middleware/rate-limit.ts`) | N/A | N/A | N/A (data) | N/A | N/A | behavior.spec §4, api.spec.md §3 | Cross-checked against behavior.spec §4's boundary rows in C-011's test |
| C-011 `buildFormsRateLimitKey` | `rate-limit-profile.ts` | forms | exported function | REQ-09 | Compose the `(ip, formId)` composite key `createRateLimiter` expects | `{ sourceIp, formDefinitionId }` | `string` (`${sourceIp}:${formDefinitionId}`) | N/A (total function) | none | Pure decision | O(1) | Invariant case: two different forms from the same IP must never share a window (api.spec.md §3 note) — the composite key is what prevents cross-form throttling | REQ-09, AC-14, behavior.spec §7 5th/6th-submission rows | Unit test: same IP, two different `formId`s, independent windows |
| C-012 `FormDefinitionRepoPort`, `FormSubmissionRepoPort` | `ports.ts` | forms | exported interfaces | Persistence seam, rule-of-two (Article IV) | Define the storage contracts C-005..C-008 depend on | N/A (interfaces) | N/A | N/A | N/A | N/A | N/A | N/A | ADR-PIPE-010 Module Boundaries, Article IV | Both adapters (`repo.memory.ts`, `repo.sqlite.ts`) run the same contract-test suite; `FormDefinitionRepoPort` exposes no delete method (enforces INV-08 structurally) |
| C-013 typed errors | `errors.ts` | forms | exported error classes | Typed error → route-layer error-code mapping | Carry enough context for each route to build its `errors.spec.md` §3 details payload | constructor args | N/A (Error subclasses) | N/A | N/A | N/A | N/A | N/A | errors.spec.md | Route-layer test per error class asserts the correct HTTP status + code |
| C-014a..g admin route registrars | `routes/admin/forms/*.ts` | server | exported functions | HTTP entrypoints, each `authorize()`-gated per api.spec.md §2 | Parse request → call the matching C-005..C-008/repo-read function → map result/errors per api.spec.md §5/§6 | HTTP request per api.spec.md §4 | HTTP response per api.spec.md §5 | Auth profile per endpoint | Full mapping per api.spec.md §6 | Explicit side effect: HTTP I/O, delegates to C-005..C-008 | O(1) request handling | N/A | REQ-01..04/13..15, api.spec.md | Integration test per endpoint hitting the real route + SQLite adapter |
| C-015 `registerFormsSubmitRoute` | `routes/site/forms-submit.ts` | server | exported function | Public HTTP entrypoint (no auth) | Parse request (slug + body + resolved source IP) → call C-008 `submitForm` → map result/errors | HTTP request (public, untrusted body) | HTTP response per api.spec.md §5 `FORMS_POST_SUBMIT` | None at this layer beyond request parsing bounds — all real validation is C-008's | Full mapping per api.spec.md §6 for `FORMS_POST_SUBMIT` | Explicit side effect: HTTP I/O, delegates to C-008 | O(1) | **Aggregate-risk:** this file resolves `sourceIp` from the request — must reuse `resolveClientIp` (`server/middleware/rate-limit.ts`), not re-derive IP resolution logic a second time | REQ-05, AC-07, api.spec.md | Integration test: no `Authorization` header required (AC-07); untrusted body fields are length-bounded before reaching C-008, mirroring `analytics-ingest.ts`'s `parseBeacon` defensive-coercion pattern |

## Wiring Map

| Flow ID | Source | Transport/Call Type | Target | Payload/Contract | Ordering/Retry/Idempotency | Failure Handling | Trace |
|---|---|---|---|---|---|---|---|
| W-001 | `routes/admin/forms/create.ts` | direct call | `write-service.ts` C-005 | `createFormDefinition` input/output | Not idempotent by design (mirrors `posts/create.ts` — no `Idempotency-Key` dedup for definition creates in v1) | Typed error → HTTP error code (api.spec.md §6) | REQ-01, AC-01 |
| W-002 | `routes/site/forms-submit.ts` | direct call | `submit-service.ts` C-008 | `submitForm` input/output | Not idempotent (a resubmit is a new submission by design — behavior.spec §5 "no duplicate-submission concept") | Typed error → HTTP error code; honeypot-discard and genuine-accept return the identical success shape (REQ-08) | REQ-05..09, AC-07..15 |
| W-003 | `submit-service.ts` C-008 | direct call, fire-and-forget (`void`, not `await`) | `core/events` `processOutbox` | `{ outbox, bus, clock }` | Safe to call more than once; a dropped drain (process crash) just delays delivery to the next drain call, never loses the durably-enqueued row | Errors inside `processOutbox` are caught internally (marks the outbox row `failed`, schedules retry) — never propagate back to the HTTP response, which has already been sent | REQ-16, INV-05, AC-24 |
| W-004 | `core/events` `processOutbox` (via `bus.publish`) | in-process event dispatch | `notify-subscriber.ts` C-009 | `DomainEvent<{workspaceId, formDefinitionId, submissionId}>` | At-least-once (outbox redelivery semantics, ADR-009); C-009 must be safe to run more than once for the same event (mail-lib-owned dedup ledger per ADR-037 fold item 9 covers this, not Forms) | A `MailerPort.send()` failure is logged; the submission row itself is unaffected either way (EC-06) | REQ-12, AC-17/18 |
| W-005 | `server/app.ts` (new registration) | in-process event dispatch | `integrations/delivery.ts` `enqueueDelivery` | `DomainEvent<{workspaceId, formDefinitionId, submissionId}>` narrowed to `WebhookSourceEvent` | Idempotent on `(eventId, subscriptionId)` per ADR-036 §4 — safe under outbox redelivery | Errors surface through the same `bus.publish` try/catch `processOutbox` already has; a fan-out failure never blocks C-009's independent subscription to the same event | REQ-11, AC-16 |
| W-006 | `apps/admin/src/sections/{FormsList,FormEditor}.tsx` | HTTP fetch | `routes/admin/forms/*.ts` | api.spec.md §4/§5 request/response contracts | UI disables save/delete controls during in-flight requests (ui.spec.md §4) | Inline field-error mapping (`FormFieldsEditor`) + banner for other errors, mirrors existing admin-section convention | REQ-13/14, ui.spec.md §3 |

## Data And Side-Effect Boundaries

| Boundary | Owner | Reads | Writes | Side Effects | Consistency / Transaction Rule | Migration / Dual-Write Path |
|---|---|---|---|---|---|---|
| `form_definitions` | forms | `forms.ts`/route layer (list/get), `submit-service.ts` (resolve by slug) | `write-service.ts` C-005/C-006/C-007 only, via `executeCommand` | `change-set.applied` event (generic gateway event, unrelated to `form.submission.received`) | Definition write + its change-set record are one unit of work (`executeCommand`'s existing guarantee); slug uniqueness enforced by a real DB unique index on `(workspace_id, slug)`, not an app-level check (behavior.spec §6.1) | N/A — new table, no legacy data |
| `form_submissions` | forms | route layer (list/get, admin-only) | `submit-service.ts` C-008 (insert only); `routes/admin/forms/delete-submission.ts` (delete only, REQ-14) | `form.submission.received` outbox event enqueued in the same call as the insert (not the same DB transaction as the outbox row necessarily, but both writes happen before the HTTP response — matching this repo's existing outbox-enqueue-then-respond pattern) | Insert is immutable thereafter except for permanent delete (INV-08's submission-side carve-out); no update action exists on this table at all | N/A — new table, no legacy data |
| `identity.principals` / permission catalog | identity (unchanged) | `write-service.ts` C-005..C-007 (read-only, via `authorize()`) | Not written by `forms` | None | N/A — read-only cross-module dependency, same shape every other feature already has | N/A |
| `webhook_subscriptions` / `webhook_deliveries` | integrations (unchanged) | Not read by `forms` | Written only by `integrations`' own `enqueueDelivery` (invoked via the W-005 forwarding line, not by any `forms` code) | Per ADR-036, still only in-memory adapters in this repo — a pre-existing gap, not something this feature changes | N/A — `forms` never touches these tables directly | N/A |

## Observability And Operational Expectations

| Surface / Flow | Required Signals | Correlation / Trace Context | Metrics | Logs | Alert / Runbook Need | Privacy / Secret Constraints | Trace |
|---|---|---|---|---|---|---|---|
| All 8 routes | Structured error responses (errors.spec.md §1 envelope) | `correlationId` on every error payload (deferred/`null` per the existing Article VIII pattern) | None new required (no NFR discovery flagged latency/throughput targets) | Standard request logging (existing server middleware) | N/A — no new production alerting surface beyond existing admin-route conventions | `source_ip` never appears in a public-route response; admin-only visibility | REQ-10, errors.spec.md §1 |
| `submit-service.ts` C-008 | `form.submission.received`'s payload IS the audit signal for what happened on accept | `submissionId`/`formDefinitionId`/`workspaceId` as available correlation ids | None new | Rate-limit rejections and validation failures should log at debug level only (public route — avoid noisy logs from routine bot traffic) | N/A | Submission `data` (visitor-supplied PII) never logged in full — only ids | REQ-11, INV-07 |
| `notify-subscriber.ts` C-009 | A failed `MailerPort.send()` is logged with `errorCode` | `sourceContext: {module: 'forms', ref: formDefinitionId}` (ADR-037 amendment 2 correlation) | None new | Log failures at warn level (expected, non-fatal per EC-06) | N/A | No email body/recipient PII beyond what `MailerPort`'s own adapter already logs (e.g. `ConsoleMailerAdapter`'s existing truncated preview) | REQ-12, EC-06 |

## Critical Invariants

| Invariant ID | Scope | Rule | Reason | Enforcement Surface | Test Expectation | Trace |
|---|---|---|---|---|---|---|
| INV-01 | `forms.ts` C-003, `submit-service.ts` C-008 | `form_submissions.data_json` keys are always a subset of the definition's declared field ids at submission time | Prevents unbounded/unregistered data from ever being persisted | `validateSubmissionPayload` (C-003) is the sole gate before insert | Unit test: a payload with an extra unregistered key is rejected, nothing persisted (AC-09) | feature.spec.md INV-01 |
| INV-04 | `forms.ts` C-004, `submit-service.ts` C-008 | A honeypot-tripped submission never persists a row, never emits the event, never triggers notification, and returns the identical `201` response as a genuine accept | Prevents a scripted bot from distinguishing "silently dropped" from "worked" | `isHoneypotTripped` (C-004) gates all three side effects at one call site in C-008 | Integration test: honeypot-tripped request → `201`, zero DB rows, zero outbox rows (AC-13) | feature.spec.md INV-04, REQ-08 |
| INV-05 | `submit-service.ts` C-008 | The public submission HTTP response must never wait on `MailerPort.send()` or webhook delivery completing | Core reliability promise — a slow/broken mail provider must never degrade the public-facing endpoint | `void processOutbox(...)` (never `await`ed) at the one call site in C-008 | Integration test: a stubbed `MailerPort.send()` that hangs/throws does not delay the HTTP response (AC-24) | feature.spec.md INV-05, REQ-16 |
| INV-07 | `submit-service.ts` C-008 | `form.submission.received` is emitted for every accepted (non-honeypot, non-rate-limited, validation-passing) submission, and only for such submissions | The webhook/notify subscribers' correctness depends on this being neither over- nor under-fired | C-008's single control-flow path: validate → rate-limit → persist → enqueue, in that order, with no other emit site | Integration test: rejected/honeypot/rate-limited submissions never produce an outbox row; accepted ones always do | feature.spec.md INV-07 |
| INV-08 | `ports.ts` C-012 `FormDefinitionRepoPort` | A form definition is never permanently deleted — only `active`⇄`disabled` | Historical submissions must always be able to reference a definition | The port interface itself exposes no delete method — a structural guarantee, not just a convention | Static/type-level check (no delete method exists to call); integration test confirms no delete route exists (AC-06) | feature.spec.md INV-08 |
| INV-09 `[internal-invariant]` composite rate-limit key | `rate-limit-profile.ts` C-011 | The rate-limit window is keyed on `(sourceIp, formDefinitionId)`, never `sourceIp` alone | A visitor submitting to two different forms must not be cross-throttled (api.spec.md §3 note) | `buildFormsRateLimitKey` (C-011) is the single key-construction call site `submit-service.ts` uses | Unit test: same IP, two different `formId`s → independent windows | api.spec.md §3, behavior.spec §4 |

## Brownfield / Migration Mapping (if applicable)

N/A — Forms is greenfield for its own surface. See ADR-PIPE-010's Migration Safety section for why the two dependency-shaped risks (mail adapter location, webhook-fanout wiring gap) are not treated as a migration of Forms' own state.

## Test Expectations

- Contract tests: C-012 `FormDefinitionRepoPort`/`FormSubmissionRepoPort` — one shared contract-test suite run against both `repo.memory.ts` and `repo.sqlite.ts` (matches `settings`/`integrations` precedent).
- Integration tests: W-001 (admin create through the real HTTP route + SQLite adapter, AC-01), W-002 (public submit through the real HTTP route, AC-07..15), W-003/W-004/W-005 (the fire-and-forget outbox drain reaching both the notify subscriber and the webhook-fanout forwarding, AC-16/17/18/24), all 7 admin routes' permission gating (AC-21/22/23).
- Property/invariant tests: INV-01 (submission keys subset of declared fields, across randomized field sets), INV-04 (honeypot trip, across present/absent/whitespace variants), INV-05 (response-before-drain timing, using a deliberately slow/throwing `MailerPort` test double), INV-07 (event emitted iff accepted, across the full accept/reject/honeypot/rate-limit matrix), INV-09 (composite rate-limit key, cross-form independence).
- Characterization tests: N/A — Forms is greenfield, no prior behavior to characterize.
- Explicitly N/A suites with reason: No brownfield/reverse-spec characterization suite (spec_mode: greenfield). No `orchestrator.spec.md`-driven async test suite beyond what's already covered under System Wiring above (`orchestrator.spec.md` itself is omitted per spec-manifest.md — no distinct frontend orchestrator layer).

## Downstream Handoff Notes

- Coordinator task-generation constraints: Sequence schema (`infra/db/schema.ts`) + `forms.ts`/`ports.ts`/adapters + `write-service.ts` ahead of `submit-service.ts` and the two subscribers, which depend on real contracts, not mocks (Article V). The `server/app.ts` wiring task (route registration + the two `bus.subscribe` lines) should be sequenced as one task, not split across parallel slices, because it is the single shared-file convergence point.
- **Explicit cross-spec coordination flag:** `server/app.ts` and `src/server/routes/types.ts` are also touched by the parallel Integrations sibling spec running in this same dispatch round (per the Coordinator's parallel-delivery note). Both this feature's task list and Integrations' task list add lines to the same files (Forms adds a `bus.subscribe('form.submission.received', ...)` line + `RouteDeps` fields; Integrations may add its own topic wiring or `RouteDeps` fields). The Coordinator should either sequence these two tasks (one lands, then the other rebases) or assign both edits to the same task/agent — parallel `[P]` marking of both features' `app.ts` edits is unsafe.
- TDD focus: Certify C-003 (`validateSubmissionPayload`, INV-01) and C-004 (`isHoneypotTripped`, INV-04) first — pure functions, highest test-density payoff. Then C-008 (`submitForm`) as an integration test including the fire-and-forget timing assertion (AC-24) before any route wiring exists to hide behind. C-005/C-006/C-007 (admin CRUD through `executeCommand`) follow the same certification order `posts`/`menus` already established. C-009/W-005 (the two subscribers) last, once the event itself is reliably emitted.
- Programmer architecture audit focus: Verify no import of `repo.memory.ts`/`repo.sqlite.ts` from outside `write-service.ts`/`submit-service.ts`; verify `manifest.ts` has zero function/closure exports; verify `submit-service.ts` contains no `await` on `processOutbox`, `MailerPort.send()`, or anything transitively reaching either; verify the `server/app.ts` webhook-fanout line calls `integrations`' `enqueueDelivery` directly rather than reimplementing any matching/enqueue logic.
- Open risks or ambiguities: None beyond what ADR-PIPE-010's Consequences/Risks section already names (Integrations-sibling `app.ts` convergence; `manifest.ts` shape drift against a not-yet-designed future loader; `source_ip` raw-storage residual PII surface, flagged for Security Agent review).
