# Implementation Outline: site-install-dir

- Spec: SPEC-003 v1.0.0 (hash: sha256:c2547dc39b9e12bc7fb199d3c32e472804a3fea9690014a8000f421db1846142)
- ADR: ADR-PIPE-003 (v1.1.0)
- Status: PRODUCED
- Trigger result: Boundary Cross, Contract Change, Data And Persistence, Brownfield Dependency, Critical Cross-Boundary Invariant, Parallelization Ambiguity
- Date: 2026-07-28
- Revised: 2026-07-28 — see ADR-PIPE-003 v1.1.0 Revision Note (owner override: `commander` adopted in v1). Affected rows only: Module Map's `cli` row, File Map's `src/cli/parse-args.ts` (renamed/repurposed to `src/cli/program.ts`), `src/cli/main.ts`, `src/cli/help.ts`, `src/cli/errors.ts`, and Downstream Handoff Notes. Trigger Decision Matrix, Contract Map, Wiring Map, Data/Side-Effect Boundaries, Observability, Critical Invariants, Brownfield/Migration Mapping, and Test Expectations are unaffected — none concern the CLI-parsing mechanism.
- Author: Software Architect

> Use this artifact only for post-ADR, pre-tasks structure. It defines module boundaries, public/exported contracts, wiring, data boundaries, and critical invariants. It must not contain pseudo-code, private helper inventories, or task sequencing.

## Trigger Decision Matrix

| Trigger | Applies? | Evidence | Source Trace |
|---|---:|---|---|
| Boundary Cross | yes | Feature spans three module boundaries: new `cli/` (transport/dispatch), new `site-dir/` (install-dir domain), and an existing composition-root module (`server/deps.ts`) that must change | ADR-PIPE-003 Module/Service Boundaries |
| Contract Change | yes | New public CLI surface (`api.spec.md` §1) plus a changed exported function signature (`createSqliteRouteDeps`) consumed by `src/index.ts` and 8+ existing test files | REQ-06/REQ-09, ADR-PIPE-003 API/Event Contract Summary |
| System Wiring | no | No queues, jobs, webhooks, or schedulers — every flow is a direct synchronous/async function call within one process | N/A |
| Data And Persistence | yes | New `.site-meta.json`/`config.json` file schemas, the schema-version stamp (index+tag, written atomically), and seed provenance moving from `server/seed.ts` to `templates/starter/seed-content.json` | REQ-01/REQ-02/REQ-05, state.spec.md §2/§7 |
| Brownfield Dependency | yes | `createSqliteRouteDeps`'s legacy zero-argument call shape (REQ-10/AC-13) must survive unmodified; existing consumer inventory: `src/index.ts` + 8 test files listed in ADR-PIPE-003 Migration Safety | REQ-10, ADR-PIPE-003 Migration Safety |
| Reverse-Spec Or Migration | no | This is new-capability brownfield work (adding a CLI to an existing app), not a reverse-spec extraction or phased architecture migration | N/A |
| Critical Cross-Boundary Invariant | yes | INV-01 (no writes outside target dir) and INV-04/INV-05 (schema-stamp monotonicity/atomicity) span `cli`, `site-dir`, and the db layer | feature.spec.md INV-01/INV-04/INV-05 |
| Parallelization Ambiguity | yes | `cli/` and `site-dir/` are independently buildable, but both depend on the one changed `createSqliteRouteDeps` contract landing first — Coordinator needs this made explicit to sequence `tasks.md` safely | ADR-PIPE-003 Rationale |

## Module Map

| Module/Domain | Owns | Responsibility | Public Contracts | Dependencies | Notes |
|---|---|---|---|---|---|
| `cli` | argv parsing (via `commander`, [REVISED v1.1.0]), command dispatch, exit-code/stderr formatting | Turn `process.argv` into a call to `site-dir` or the existing server composition root, and turn any thrown error (including commander's own parsing errors) into the `errors.spec.md` stderr/exit-code contract | C-001, C-002, C-003 | `commander`, `site-dir`, `server/deps.ts`, `server/app.ts` | Never imports Drizzle types directly (Enforcement rule `cli-no-direct-drizzle-imports`). Constructs exactly one `commander` `Command` program (`program.ts`) as the single source of truth for subcommands/options — see ADR-PIPE-003 v1.1.0 Enforcement. |
| `site-dir` | install-dir file schemas, template read, schema guard, workspace resolution, init/boot orchestration | Validate, create, and boot an install dir per state.spec.md, independent of CLI or HTTP concerns | C-004…C-009 | `infra/sqlite/content-db.ts`, `infra/drizzle/meta/_journal.json` (read-only), `templates/starter/*` | Never imports `express` or `cli/**` (Enforcement rule `site-dir-no-server-express-or-cli-imports`) |
| `server` (existing, changed) | HTTP composition root | Build the full `NewsletterRouteDeps` graph and the Express app | C-010 (changed), all pre-existing route-deps contracts (unchanged) | `site-dir/resolve-workspace.ts` (new dependency, one function) | 100% of this module's other ~499 lines are unchanged; see ADR-PIPE-003 Migration Safety |
| `templates/starter` (repo data) | versioned template package | Supply `template.json` + `seed-content.json` as read-only data | none (data, not code) | none | REQ-02/INV-03: no template code executes |

## File Map

| File Path | Module | Creates / Changes | Public Contracts Housed | Responsibility | Why This Separation Exists | Notes |
|---|---|---|---|---|---|---|
| `src/cli/main.ts` | `cli` | creates | C-001 (dispatch entry) | [REVISED v1.1.0] Build the commander program (via `program.ts`), attach command actions, catch typed errors (including commander's own `CommanderError` from `.exitOverride()`), exit with the mapped code | The one process entrypoint for the `tovu` bin — mirrors `src/index.ts`'s existing role for the legacy boot | Never itself opens a db or fs handle |
| `src/cli/commands/init.ts` | `cli` | creates | C-001 (`CLI_INIT`) | Wire a commander action's parsed arguments to `site-dir`'s `initSite`, format the `CLI_INIT` stdout contract | Keeps command-specific stdout wording isolated from dispatch/parsing | Calls C-007 only |
| `src/cli/commands/serve.ts` | `cli` | creates | C-002 (`CLI_SERVE`) | Wire a commander action's parsed arguments to `site-dir`'s `bootSiteDir`, then `server/deps.ts` + `server/app.ts`, then `app.listen` | Keeps the one HTTP-listen side effect isolated from `site-dir` (which has no Express awareness) | Calls C-008 then C-010 |
| `src/cli/help.ts` | `cli` | creates | C-003 (`CLI_HELP`) | [REVISED v1.1.0] Configure commander's help output and `.exitOverride()`/`.configureOutput()` hooks so `--help`/no-args exit 0 and an unknown command exits 2, matching `errors.spec.md`'s `CLI_HELP` contract exactly (commander's own defaults differ and must be overridden) | Small, stable, no dependency on parsing internals | — |
| `src/cli/program.ts` | `cli` | creates [REVISED v1.1.0 — replaces `src/cli/parse-args.ts`] | (internal to `cli`; no cross-module consumer) | Construct and configure the `commander` `Command` program: declares the `init`/`serve` subcommands, the positional `dir` argument, and `--name`/`--port` options; commander itself performs argv tokenization/type coercion | Centralizes the CLI's declarative command schema in one file, per ADR-PIPE-003 v1.1.0's adoption of `commander` (supersedes the v1.0.0 hand-rolled-tokenizer plan) | Omitted from Contract Map: no public contract outside `cli` — same rule as v1.0.0, just a different internal mechanism (commander, not a hand-rolled tokenizer) |
| `src/cli/errors.ts` | `cli` | creates | (internal to `cli`; consumes `site-dir`'s typed errors) | [REVISED v1.1.0] Map a `site-dir`/argv-validation error — now including commander's own parsing/validation errors (e.g. missing required `dir`, invalid `--port` type) surfaced via `.exitOverride()` — to `{code, exitCode, stderrLine}` | Keeps `errors.spec.md`'s exit-code registry in exactly one file | Consumes site-dir error types and commander's `CommanderError`; not itself a contract other modules call |
| `src/site-dir/types.ts` | `site-dir` | creates | (type-only; referenced by C-004…C-009) | `ConfigJson`, `SiteMetaJson`, `TemplateJson`, `TemplateSeedContent` shapes | Single source of truth for the two compatibility-surface file schemas (state.spec.md §2) | ADR-011/012 compatibility surface — additive-only per api.spec.md §7 |
| `src/site-dir/read-site-dir.ts` | `site-dir` | creates | C-004 | Parse + validate `config.json`/`.site-meta.json` for `serve` | Isolated so `bootSiteDir` and any future non-CLI caller share one validator | — |
| `src/site-dir/resolve-workspace.ts` | `site-dir` | creates | C-005 | Resolve the single workspace row from an open db | Shared by `server/deps.ts` (changed) and `bootSiteDir` — single source of truth (REQ-06) | See CIC U-001 |
| `src/site-dir/schema-guard.ts` | `site-dir` | creates | C-006 | Read the runtime's bundled-migration identity; compare to a site's stamp | Isolated so the guard's index+tag comparison logic (RT-005) has exactly one implementation | See CIC U-002 |
| `src/site-dir/init-site.ts` | `site-dir` | creates | C-007 | Orchestrate `init`'s 8-step ordering (BR-01) including cleanup-on-failure | Owns INV-02's commit-marker discipline in one place | See CIC U-003 |
| `src/site-dir/boot-site-dir.ts` | `site-dir` | creates | C-008 | Orchestrate `serve`'s validate→guard→migrate+stamp→resolve ordering (BR-05/BR-06) | Owns BR-06's stamp-write-after-migrate ordering in one place | See CIC U-002 |
| `src/site-dir/read-template.ts` | `site-dir` | creates | C-009 | Read `templates/<id>/*.json`, map `TemplateSeedContent` → `ContentDbSeedData` | Isolated so the mapping between the declarative seed shape and `infra/sqlite/content-db.ts`'s existing `ContentDbSeedData` type has one owner | — |
| `src/server/deps.ts` | `server` | changes | C-010 (changed signature) | Same as today, plus: accept an optional pre-opened db/workspaceId | The only file this feature edits outside its two new modules — see ADR-PIPE-003 Migration Safety | 15 internal `seededWorkspace.id` usages become one resolved variable |
| `templates/starter/template.json` | `templates/starter` | creates | (data, not a code contract) | `id`, `version`, `name`, `defaultConfig` | REQ-02: template is data, not code | Read by C-009 only |
| `templates/starter/seed-content.json` | `templates/starter` | creates | (data, not a code contract) | workspace/entries/presentation, content-equal to `server/seed.ts` | AC-02 byte-parity requirement | Read by C-009 only |

## Contract Map

| Contract ID / Name | File | Owner Module | Kind | Why Needed | Job | Inputs | Outputs | Validation | Errors | Effect Boundary | Complexity / Resource View | Aggregate-Risk Note | Spec/ADR Trace | Test Seam / Expectation |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| C-001 `CLI_INIT` | `src/cli/main.ts`, `src/cli/commands/init.ts` | `cli` | CLI command | Public v1 surface — the create side of "site is a folder" | Turn `tovu init <dir> [--name]` into one `initSite` call and format its result | `{ dir: string; name?: string }` (from argv) | stdout line (name+dir) + exit 0, or mapped stderr+exit code | `--name` 1–200 chars trimmed (else `VALIDATION`) | `VALIDATION`, `INIT_DIR_NOT_EMPTY`, `INTERNAL` | Effectful: process exit, stdout/stderr write | O(1) dispatch; all real cost is inside C-007 | N/A — pure dispatch, no batch/ordering behavior | api.spec.md §1/§4/§5, AC-01/03/04/14 | Integration: spawn `tovu init` against a temp dir |
| C-002 `CLI_SERVE` | `src/cli/main.ts`, `src/cli/commands/serve.ts` | `cli` | CLI command | Public v1 surface — the boot side | Turn `tovu serve <dir> [--port]` into one `bootSiteDir` call, then hand its result to `server/deps.ts` + `server/app.ts` + `app.listen` | `{ dir: string; port?: number }` (from argv) | long-running process; one boot-line log; graceful exit 0 on SIGINT/SIGTERM, or mapped stderr+exit code | `--port` integer 1–65535 (else `VALIDATION`) | `VALIDATION`, `SITE_DIR_INVALID`, `SITE_NEWER_THAN_RUNTIME`, `SITE_CORRUPT`, `PORT_IN_USE`, `INTERNAL` | Effectful: db open, HTTP listen, process signals | O(1) dispatch + one db open/migrate; no per-request cost added | N/A — one-shot boot, no batch | api.spec.md §1/§4/§5, AC-05/06/07/08/09/11 | Integration: spawn `tovu serve` against a temp dir, HTTP request round-trip |
| C-003 `CLI_HELP` | `src/cli/help.ts` | `cli` | CLI command | Public v1 surface — usage/discoverability | Print usage; detect unknown commands | argv (any shape) | stdout usage text (exit 0) or exit 2 on unknown command | none | none (usage is not an error code) | Effectful: stdout write | O(1) | N/A | api.spec.md §1/§6, AC-12 | Integration: spawn `tovu unknown-cmd`, assert exit 2 |
| C-004 `readSiteDir` | `src/site-dir/read-site-dir.ts` | `site-dir` | exported function | `serve`'s first validation gate (BR-05 steps 1–2) must be reusable independent of CLI dispatch | Parse and validate `config.json` + `.site-meta.json` | `{ dir: string }` | `{ config: ConfigJson; meta: SiteMetaJson }` | file present, ≤64 KiB, parseable JSON, `config.name` non-empty | throws `SiteDirInvalidError` naming the failing file + reason | Pure read (fs read, no write) | O(1) — two small file reads | N/A | state.spec.md §5, EC-03, AC-05 | Unit: temp dir with missing/corrupt/oversized files |
| C-005 `resolveWorkspace` | `src/site-dir/resolve-workspace.ts` | `site-dir` | exported function | Shared single source of truth for REQ-06 — used by both `bootSiteDir` and `server/deps.ts`'s changed default path | Return the exactly-one workspace row from an open db | `{ db: ContentDb }` | `WorkspaceRecord` | exactly one row required | throws `SiteCorruptError` (`"zero rows"` / `"multiple rows"`) | Pure read (one SELECT) | O(1) — single indexed/PK query, not a scan | N/A — single-row selector, no cross-record ordering | state.spec.md §5, REQ-06, AC-08/09 | Integration: real SQLite db with 0/1/2 workspace rows |
| C-006 `runtimeSchemaVersion` + guard compare | `src/site-dir/schema-guard.ts` | `site-dir` | exported function | REQ-05/RT-005 — must compare both index and tag, in exactly one implementation | Read the runtime's bundled-migration identity; compare it to a site's stamp | `{ siteMeta: SiteMetaJson }` (compare) / none (read) | `{ index: number; tag: string }` (read) / `"compatible" | "migrate" | "newer-or-divergent"` (compare) | none beyond file read | `SiteNewerThanRuntimeError` (compare only) | Pure read of `src/infra/drizzle/meta/_journal.json` | O(1) — reads one small JSON file, no loop over migrations at request time | N/A | state.spec.md §5/§7, REQ-05, AC-06/07, RT-005 | Unit: fixture `_journal.json` + fixture `SiteMetaJson` combinations (equal/newer/divergent-tag) |
| C-007 `initSite` | `src/site-dir/init-site.ts` | `site-dir` | exported function | Owns BR-01's 8-step ordering + INV-02's cleanup discipline in one place | Create a complete install dir from the starter template | `{ dir: string; name?: string }` | `{ siteId: string; dir: string }` | target absent-or-empty dir; name 1–200 chars trimmed | `InitDirNotEmptyError`, `ValidationError`, `InternalError` (post-cleanup) | Effectful: fs writes (mkdir, file writes), db create+migrate+seed | Bounded — fixed number of fs/db operations per call, no loop over caller input | See CIC U-003 (Failure/Recovery) | BR-01, INV-02, AC-01/03/04/14, EC-01/02/10 | Integration: fault-injection at each of steps 4–7 (temp dir + simulated `ENOSPC`/`EACCES`) |
| C-008 `bootSiteDir` | `src/site-dir/boot-site-dir.ts` | `site-dir` | exported function | Owns BR-05/BR-06's exact ordering (validate → guard → migrate+stamp → resolve) in one place | Validate, guard, migrate, stamp, and resolve workspace for `serve` | `{ dir: string }` | `{ db: ContentDb; workspaceId: string; config: ConfigJson }` | full BR-05 chain | `SiteDirInvalidError`, `SiteNewerThanRuntimeError`, `SiteCorruptError` | Effectful: db open+migrate, `.site-meta.json` stamp rewrite | Bounded — one db open, one migrate() call, one stamp write per boot | See CIC U-002 (ordering/atomicity) | BR-05, BR-06, INV-04/05, AC-06/07/08/09, RT-005 | Integration: real temp install dir at each schema-version relationship (older/equal-matching-tag/equal-divergent-tag/newer) |
| C-009 `readTemplate` | `src/site-dir/read-template.ts` | `site-dir` | exported function | REQ-02 — template is versioned data; this is the one reader | Read `templates/<id>/*.json` and map seed shape to `ContentDbSeedData` | `{ templateId: string }` (`"starter"` in v1) | `{ template: TemplateJson; seed: ContentDbSeedData }` | template files present and parseable | throws `InternalError` (corrupt template — treated as an init-time internal fault, nothing written yet per BR-01 step 3) | Pure read + pure mapping | O(n) over template entries, n bounded by the one starter template's fixed content | N/A | REQ-02, state.spec.md §2, AC-02 | Unit: content-equality assertion vs. `server/seed.ts`'s existing output |
| C-010 `createSqliteRouteDeps` (changed) | `src/server/deps.ts` | `server` | exported function (existing, signature extended) | REQ-06 — must serve both the legacy default path and the new install-dir path from one mechanism | Build the full `NewsletterRouteDeps` graph, resolving `workspaceId` once via C-005 instead of a literal | `(dbPath?: string, overrides?: { db?: ContentDb; workspaceId?: string })` | `NewsletterRouteDeps` | `overrides.db` and `overrides.workspaceId` must be supplied together or not at all | rethrows `SiteCorruptError` from C-005 (uncaught — `cli`'s job to map it, per ADR layering rule) | Effectful: db open+migrate (default path only), full adapter wiring | Unchanged from today's ~500-line body except the one resolution point | See CIC U-001 (characterization parity across 15 usage sites) | REQ-06/REQ-10, AC-08/09/13 | Integration: existing `database-migration-reconciliation-boot.integration.test.ts` + `boot-lifecycle-real-deps.integration.test.ts` must stay green unmodified; new test for the `overrides` branch |

## Wiring Map

| Flow ID | Source | Transport/Call Type | Target | Payload/Contract | Ordering/Retry/Idempotency | Failure Handling | Trace |
|---|---|---|---|---|---|---|---|
| W-001 | `cli/commands/init.ts` | direct call | `site-dir/init-site.ts` (C-007) | `{dir, name?}` → `{siteId, dir}` | one-shot, not retried; not idempotent (second call against the same now-non-empty dir is a distinct `INIT_DIR_NOT_EMPTY` failure, by design) | `cli/errors.ts` maps thrown error → stderr+exit | BR-01, AC-01/03/04 |
| W-002 | `cli/commands/serve.ts` | direct call | `site-dir/boot-site-dir.ts` (C-008) → `server/deps.ts` (C-010) → `server/app.ts` (`createApp`) → `app.listen` | `{dir, port?}` → `{db, workspaceId, config}` → `NewsletterRouteDeps` → Express app → bound listener | strictly sequential; `bootSiteDir`'s migrate step is idempotent via Drizzle's own journal (safe to re-run on a crash-then-restart) | Any step's typed error short-circuits before `app.listen`; `cli/errors.ts` maps it | BR-05, BR-06, BR-07, AC-05/06/07/08/09/11 |
| W-003 | `site-dir/init-site.ts` | direct call | `infra/sqlite/content-db.ts`'s `openContentDb` (existing) | `(dbPath, mappedSeedFromC-009, undefined recovery hook)` | one-shot at init; bypasses `server/deps.ts` entirely — `initSite` never builds route deps | On failure, `initSite` runs cleanup (CIC U-003) before rethrowing | BR-01 step 6, REQ-02 |
| W-004 | `site-dir/boot-site-dir.ts` | direct call | `infra/sqlite/content-db.ts`'s `openContentDb` (existing) | `(dbPath, undefined — no seed, since init already seeded)` then C-005 `resolveWorkspace` | migrate-then-resolve, strictly sequential; migrate is idempotent | Migration failure surfaces as `SiteCorruptError`/`InternalError` per BR-05; workspace-resolution failure surfaces as `SiteCorruptError` | BR-05 steps 3–6 |
| W-005 | `server/deps.ts`'s C-010 (legacy default path, no `overrides`) | direct call | `site-dir/resolve-workspace.ts` (C-005) | `db` (just opened by C-010's own `openContentDb` call) → `WorkspaceRecord` | one-shot per boot; reused mechanism, not a new code path | Uncaught by C-010 — `src/index.ts` (legacy caller) sees the same unhandled-rejection behavior it would see today for any other internal `deps.ts` construction failure (no behavior change) | REQ-06, REQ-10, AC-08/09/13 |

## Data And Side-Effect Boundaries

| Boundary | Owner | Reads | Writes | Side Effects | Consistency / Transaction Rule | Migration / Dual-Write Path |
|---|---|---|---|---|---|---|
| Install-dir filesystem tree (`config.json`, `.site-meta.json`, `uploads/`, `themes/`, `plugins/`, `overrides/`) | `site-dir` | `cli` (via C-004/C-007/C-008 return values, for stdout/log formatting only) | `site-dir` only (`init-site.ts` creates everything; `boot-site-dir.ts` updates only the `.site-meta.json` `schemaVersion`+`schemaTag` fields) | none beyond fs | temp-file+rename atomic JSON writes; commit-marker-last ordering (INV-02, CIC U-003) | N/A — new files only, no dual-write |
| `content.db` (existing SPEC-001/002 schema, unchanged shape) | existing feature repos (unchanged) | `server/deps.ts`'s repo adapters (unchanged), `site-dir/resolve-workspace.ts` | `server/deps.ts`'s repo adapters (unchanged), `site-dir/init-site.ts` (seed insertion only, at init time, via the existing idempotent `seedContentDb` guard) | Drizzle `migrate()` DDL (existing mechanism, ADR-015) | Drizzle's own `__drizzle_migrations` journal (existing, unchanged) | N/A — additive migrations only, ADR-015's existing rule governs this; no new dual-write path |
| `templates/starter/*.json` | `site-dir` (read-only) | `site-dir/read-template.ts` | none at runtime | none | INV-03 — read-only at runtime, enforced by Enforcement's grep check | N/A |
| `.site-meta.json`'s `schemaVersion`+`schemaTag` pair specifically | `site-dir` | `site-dir/schema-guard.ts` | `site-dir/boot-site-dir.ts` only, both fields in one atomic write | none beyond fs | Must be written together or not at all (INV-04) — see CIC U-002 | N/A |

## Observability And Operational Expectations

| Surface / Flow | Required Signals | Correlation / Trace Context | Metrics | Logs | Alert / Runbook Need | Privacy / Secret Constraints | Trace |
|---|---|---|---|---|---|---|---|
| CLI process (`init`/`serve`) | exit code + single stderr line on failure; one stdout success line (`init`); one stdout boot line (`serve`: dir, port, schemaVersion, workspace id) | N/A — single local process, no distributed trace needed | N/A — out of scope for a local CLI in v1 | The structured stderr line (`tovu: <CODE>: <message>`) is itself the log | N/A — local dev/self-hosted, no ops alerting surface in v1 | No secrets/PII in any line — only site name, dir path, port, schema version, workspace id (none of which are sensitive) | api.spec.md §5, errors.spec.md §1, REQ-09 |

## Critical Invariants

| Invariant ID | Scope | Rule | Reason | Enforcement Surface | Test Expectation | Trace |
|---|---|---|---|---|---|---|
| INV-01 | `cli`, `site-dir` (all fs-writing code) | `init` and `serve` must never write any file outside the target install dir | Path-containment is the one real security control for a `LOCAL_PROCESS`-trust CLI (ADR-PIPE-003 security axis, score 3) | `site-dir`'s fs-writing functions (C-007) | Integration: traversal/symlink-escape attempt asserts no file appears outside target | feature.spec.md INV-01, CIC U-004 |
| INV-02 | `site-dir/init-site.ts` (C-007) | A failed `init` must never leave a partial install dir; `.site-meta.json` only exists when every prior step completed | Commit-marker semantics — `serve` trusts the marker's presence completely | C-007 | Integration: fault injection at steps 4–7, assert dir absent (or, if cleanup itself fails, assert the error names the partial dir) | feature.spec.md INV-02, CIC U-003 |
| INV-03 | `templates/starter/**` | Template sources are never written at runtime | REQ-02 — templates are data, not code; no code execution from template content | Enforcement's grep check + `site-dir/read-template.ts`'s read-only access pattern | Unit/audit: no write call in any code path touching `templates/` | feature.spec.md INV-03 |
| INV-04 | `.site-meta.json`'s `schemaVersion`+`schemaTag` pair | Both fields are updated together, only after Drizzle `migrate()` completes; a bumped version beside a stale tag is an illegal state | RT-005 — index-only comparison would falsely pass a divergent-lineage guard | `site-dir/boot-site-dir.ts` (C-008) | Integration: crash-simulated mid-stamp-write, assert next boot sees a consistent (unbumped) stamp, never a torn one | feature.spec.md INV-04, CIC U-002 |
| INV-05 | `.site-meta.json.schemaVersion` | Never exceeds the runtime's schema version after a successful `serve` start, and never decreases | Guarantees `SITE_NEWER_THAN_RUNTIME` remains sound over repeated boots | `site-dir/schema-guard.ts` (C-006) | Integration: re-serve after a successful migration must pass the guard cleanly (AC-07's own round-trip requirement) | feature.spec.md INV-05, CIC U-002 |
| INV-06 | `site-dir`'s exported functions as a whole | The install-dir layout is identical regardless of caller (standalone CLI today, desktop host later) | ADR-011's portability contract — `site-dir` must never assume a TTY or CLI-only caller | `site-dir` module boundary (no `cli`-specific assumptions inside it) | Architectural: `site-dir` has zero imports from `cli/**` (Enforcement rule) | feature.spec.md INV-06 |

## Brownfield / Migration Mapping

| Source Behavior / Contract | Target Module / Contract | Preserve / Change | Characterization Evidence | Migration Safety Note |
|---|---|---|---|---|
| `src/index.ts`'s env-var-driven boot (`TOVU_DB`/`TOVU_CONTENT_DB`/`PORT`) | unchanged — remains the `LEGACY_DEV_BOOT` path | **Preserve**, byte-for-byte | REQ-10/AC-13 | No code in `src/index.ts` changes; it still calls `createSqliteRouteDeps()` with zero arguments |
| `server/deps.ts`'s `seededWorkspace.id` literal (15 usage sites) | `site-dir/resolve-workspace.ts` (C-005), one resolved variable reused 15×  | **Change mechanism, preserve value** | Existing test suite (`database-migration-reconciliation-boot.integration.test.ts`, `boot-lifecycle-real-deps.integration.test.ts`, `settings-principal-check.test.ts`, `settings-register-definitions-op-validation.test.ts`, `newsletter-routes.test.ts`) must stay green unmodified — every one already asserts against `"workspace-local"`, which `resolveWorkspace` will return identically for every existing seeded fixture | Additive optional parameter only; see ADR-PIPE-003 Migration Safety table |
| `server/seed.ts`'s hardcoded seed module | `templates/starter/seed-content.json` (declarative) | **Preserve content, new consumer** | AC-02's byte-parity requirement; a unit test asserts JSON-vs-module content-equality (state.spec.md §7) | Legacy path (`server/app.ts`'s in-memory composition, `server/deps.ts`'s legacy default) keeps using `server/seed.ts` unchanged — the new install-dir path is the only consumer of the new JSON |

## Test Expectations

- Contract tests: C-001/C-002/C-003 (CLI process-level, spawn against temp dirs, per Article V); C-010's `overrides` branch (new) plus its unchanged default branch (existing suites, must stay green).
- Integration tests: W-001…W-005 flows; in particular W-002's full `serve` chain end-to-end (temp install dir → HTTP round-trip) and W-004's four schema-guard relationship cases (older/equal-matching-tag/equal-divergent-tag/newer).
- Property/invariant tests: INV-01 (traversal/symlink-escape), INV-02 (fault injection at each init step), INV-04 (torn-stamp-write simulation), INV-05 (re-serve round-trip).
- Characterization tests: the Brownfield/Migration Mapping table's three rows — all existing `createSqliteRouteDeps` callers must observe identical `workspaceId`/behavior with zero code changes on their side.
- Explicitly N/A suites with reason: no contract-testing-across-teams suite needed (single-repo, single-consumer CLI surface, no externally-versioned SDK in v1).

## Downstream Handoff Notes

- Coordinator task-generation constraints: sequence `tasks.md` so the `server/deps.ts` change (C-010) and `site-dir/resolve-workspace.ts` (C-005) land in the same phase, before any `cli/` task that depends on `bootSiteDir` (C-008) — `cli/` and `site-dir`'s other files (C-001/C-003/C-004/C-006/C-007/C-009) can otherwise proceed in parallel `[P]` slices once C-005/C-010 are certified.
- TDD focus: characterization tests for the Brownfield/Migration Mapping table first (prove no regression before any new behavior is added), then the Critical Invariants' fault-injection/round-trip tests (INV-02/INV-04/INV-05), then the ordinary AC-01…AC-14 contract tests.
- Programmer architecture audit focus: confirm all 15 `seededWorkspace.id` usage sites inside `createSqliteRouteDeps` were replaced with the one resolved variable (CIC U-001); confirm `.site-meta.json`'s stamp write is genuinely atomic (temp-file+rename, both fields in one write, CIC U-002); confirm `site-dir` has zero imports from `cli/**`/`express` (dependency-cruiser Enforcement rule); **[NEW, v1.1.0]** confirm `cli/` depends on `commander` (not any other CLI framework) and that argv parsing/dispatch is entirely delegated to the shared `program.ts`, not reimplemented ad hoc per command; confirm `package.json` adds `"commander": "^15.0.0"` to `dependencies`.
- Open risks or ambiguities: none beyond what Critical Internal Constraints already designates.
