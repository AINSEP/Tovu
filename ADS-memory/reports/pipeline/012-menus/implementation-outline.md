# Implementation Outline: menus-remediation

- Spec: SPEC-012 v1.0.0 (hash: sha256:f1d9a8aa4c8434a7b7dbafd78f696c04f75edcc06bde4250f8aae91a55b4bf9d) — see ADR-PIPE-012 Constitution Check (Article II) for the open spec-amendment-vs-waiver question this outline's Contract Map is written to help resolve either way
- ADR: ADR-PIPE-012 (Menus Remediation — Permission Catalog Rename/Split, SQLite Persistence, Outbox Events, Binding-Index Reconciliation)
- Status: PRODUCED
- Trigger result: Boundary Cross, Contract Change, Data And Persistence, Brownfield Dependency, Critical Cross-Boundary Invariant, Parallelization Ambiguity
- Date: 2026-07-13
- Author: Software Architect Agent

> Use this artifact only for post-ADR, pre-tasks structure. It defines module boundaries, public/exported contracts, wiring, data boundaries, and critical invariants. It must not contain pseudo-code, private helper inventories, or task sequencing.

## Trigger Decision Matrix

| Trigger | Applies? | Evidence | Source Trace |
|---|---:|---|---|
| Boundary Cross | yes | Remediation crosses `src/identity` (permission catalog + migration), `src/navigation` (repo ports + outbox calls), `src/infra/db` (schema), `src/server` (route auth checks + composition roots) | ADR-PIPE-012 Module/Service Boundaries |
| Contract Change | yes | 7 renamed/new permission strings; 2 new SQLite adapter classes; 1 new shared migration function; 5 outbox events actually published for the first time | ADR-PIPE-012 API/Event Contract Summary |
| System Wiring | no | No new queues/webhooks/async jobs — the outbox worker (`processOutbox`) already exists and already polls; this remediation only adds `enqueue()` calls into an existing pipeline | ADR-PIPE-012 Decision |
| Data And Persistence | yes | 2 new Drizzle tables (`menus`, `nav_location_bindings`); a permission-grant fan-out migration | ADR-PIPE-012 Migration Safety |
| Brownfield Dependency | yes | `navigation.manage`'s three call sites (all six route files, `identity/seed.ts`'s `BUILTIN_ADMIN_PERMISSIONS`, `contracts.ts`'s `NAVIGATION_PERMISSIONS`) must all cut over together; `createSqliteRouteDeps()`'s existing in-memory menu wiring is a named brownfield consumer being re-pointed | ADR-PIPE-012 Migration Safety, spec-manifest.md Deviation Log D-1/D-2/D-5/D-9 |
| Reverse-Spec Or Migration | yes | The permission rename is explicitly a breaking grant-data migration per `sweep-crosscutting-decisions-20260710.md` §E; handled under Migration Safety / Brownfield Dependency above | ADR-PIPE-012 Migration Safety |
| Critical Cross-Boundary Invariant | yes | INV-01 (menu version monotonicity) and INV-02 (binding-index uniqueness) must survive the SQLite adapter swap unchanged; a new invariant (grant fan-out is additive-only) spans `identity`+`navigation`+3 sibling features | feature.spec.md INV-01/INV-02 (SPEC-012); ADR-PIPE-012 Migration Safety |
| Parallelization Ambiguity | yes | The permission-migration mechanism must land before any of the six route files' auth checks are cut over, and the SQLite adapters must land before `deps.ts`'s wiring change — without this outline the Coordinator cannot safely mark route-layer and persistence-layer slices `[P]` relative to each other | ADR-PIPE-012 Rationale |

## Module Map

| Module/Domain | Owns | Responsibility | Public Contracts | Dependencies | Notes |
|---|---|---|---|---|---|
| `identity` (existing, extended) | The permission catalog; the new deprecate/fan-out migration mechanism | Register the `admin.menus.*` catalog + deprecated `navigation.manage`; run the additive grant migration once at boot | C-001..C-003 | `core` (ports/types) | `permission-migrations.ts` is intentionally generic — it does not know about `navigation` specifically; `navigation`'s permissions.ts registration calls into it |
| `navigation` (existing, extended) | `MenuRepoPort`/`NavLocationBindingRepoPort`'s second (SQLite) adapter; outbox-enqueue calls in the mutating functions; the binding-index rebuild caller | Persist menus/bindings durably; publish domain events on every mutation; keep the derived index reconcilable | C-004..C-008 | `identity` (`authorize()`, unchanged), `infra/db` (schema), `core` (`OutboxPort`, `IdGeneratorPort`) | No new port beyond the two already declared in `ports.ts`/`repo.memory.ts` — this only completes rule-of-two for both |
| `infra/db` (existing, extended) | Drizzle schema | Add `menus`/`nav_location_bindings` table defs | (schema, not a contract) | — | Shared file every feature's SQLite adapter already extends |
| `server` routes (existing, extended) | HTTP wiring for the six menu routes | Route registration + `authorize()` gating with the action-specific permission + outbox pass-through | C-009a..f | `navigation`, `identity` | Mirrors the existing per-route-file shape exactly — no new file, only edits |
| `server` composition roots (existing, extended) | `createSqliteRouteDeps()` (deps.ts), the in-memory root (app.ts, unchanged) | Wire the new SQLite adapters into the persistent composition root; call the binding-index rebuild once at boot | (wiring only) | `navigation` | The in-memory root is the *other* rule-of-two adapter (dev/tests), not a legacy path — left unchanged |

## File Map

| File Path | Module | Creates / Changes | Public Contracts Housed | Responsibility | Why This Separation Exists | Notes |
|---|---|---|---|---|---|---|
| `src/identity/permission-migrations.ts` | identity | creates | C-001 `registerPermissionMigration`, C-002 `migrateDeprecatedPermissionGrants` | The shared, feature-agnostic deprecate-old/grant-new fan-out mechanism | Generalizes the ad hoc `settings.write`→`settings.definitions.manage` pattern into one tested module instead of four divergent copies (ADR-PIPE-012 Pattern Evaluation) | Deliberately has zero knowledge of `navigation`/`settings`/any specific feature — takes `{from, to, reason}` only |
| `src/identity/permissions.ts` | identity | changes | (extends `BASE_CATALOG` registration; not a new contract) | Register the 7-entry `admin.menus.*` catalog; keep `navigation.manage` registered + marked deprecated; register the Menus `{from, to}` migration pair | Existing single registration point for all permission strings | The doc comment explaining why `navigation.manage`/`integration.manage` predate the frozen convention is updated to point at this ADR instead of describing it as unresolved |
| `src/identity/seed.ts` | identity | changes | (extends `BUILTIN_ADMIN_PERMISSIONS`; calls C-002) | Grant the 6 new `admin.menus.*` CRUD strings directly in seed data; invoke `migrateDeprecatedPermissionGrants()` once after built-in roles are seeded | Existing single seed-data location and existing single boot-time migration call site (mirrors `migrateLegacyPresentationSettings()`'s call site) | `navigation.manage` is removed from the *seed list* (freshly-seeded workspaces get the new strings directly) but stays registered in the catalog |
| `src/navigation/contracts.ts` | navigation | changes | C-003 `NAVIGATION_PERMISSIONS` (values changed, shape unchanged) | Update the already-declared 7-entry permission-name catalog to the `admin.menus.*` values | This constant already existed as the intended target shape (ADR-029 §8) — only the string values were never correct | No other export in this file changes |
| `src/navigation/menu-service.ts` | navigation | changes | C-004 `createMenu`/C-005 `updateMenuTree`/C-006 `assignLocation`/C-007 `deleteMenu` (all four gain an `outbox` dependency; signatures otherwise unchanged) | Enqueue the matching `NAVIGATION_EVENTS` entry after each function's repo write(s) succeed | Keeps event emission next to the write it documents, consistent with `features/workspace/create.ts`'s existing call shape | Each function's existing typed-error behavior on validation/conflict/not-found failure is unchanged — the outbox call only happens on the success path, after the existing repo write(s) |
| `src/navigation/repo.sqlite.ts` | navigation | creates | C-008a `SqliteMenuRepo` (implements `MenuRepoPort`), C-008b `SqliteNavLocationBindingRepo` (implements `NavLocationBindingRepoPort`) | The second rule-of-two adapter for both existing ports | Mirrors `SqlitePostRepo`'s proven shape exactly — same `ContentDb`/Drizzle pattern, same JSON-text-column convention for `doc`/`locations` | Neither class adds any method beyond what its port interface already declares |
| `src/navigation/reconcile.ts` | navigation | creates | C-009 `rebuildNavLocationBindings` | The first real caller of the already-implemented `NavLocationBindingRepoPort.rebuildForWorkspace` | Isolated so it can be invoked once from `deps.ts` at boot and is independently unit-testable against a fixture menu set, mirroring `settings/migration.ts`'s isolation reasoning | Reads every menu's `.locations` field and re-derives the binding index from scratch — never partially updates |
| `src/infra/db/schema.ts` | infra | changes | (Drizzle table defs, not a "contract" per se) | Add `menus` (mirrors `posts` shape: id/workspaceId/slug/title/status/docJson/locationsJson/updatedAt/version) and `nav_location_bindings` (workspaceId/locationKey/menuId/boundAt, composite unique index on workspaceId+locationKey) | Schema is a shared file across all features by existing convention | No behavior — pure schema, same pattern as every prior feature's addition here |
| `src/server/routes/admin/menus/list.ts` | server | changes | (auth check only) | `authorize()` checks `admin.menus.read` instead of `navigation.manage` | One file per endpoint, matches every other route | — |
| `src/server/routes/admin/menus/get-by-id.ts` | server | changes | (auth check only) | `authorize()` checks `admin.menus.read` | Same as above | — |
| `src/server/routes/admin/menus/create.ts` | server | changes | (auth check + outbox pass-through) | `authorize()` checks `admin.menus.create`; passes `deps.outbox`/`deps.idGen` into `createMenu`'s deps bag | Same as above | — |
| `src/server/routes/admin/menus/update-tree.ts` | server | changes | (auth check + outbox pass-through) | `authorize()` checks `admin.menus.update`; passes `deps.outbox` into `updateMenuTree`'s deps bag | Same as above | — |
| `src/server/routes/admin/menus/assign-location.ts` | server | changes | (auth check + outbox pass-through) | `authorize()` checks `admin.menus.assign`; passes `deps.outbox` into `assignLocation`'s deps bag | Same as above | — |
| `src/server/routes/admin/menus/delete.ts` | server | changes | (auth check + outbox pass-through) | `authorize()` checks `admin.menus.delete` always; additionally requires `admin.menus.delete.force` when `?force=true`; passes `deps.outbox` into `deleteMenu`'s deps bag | Same as above — this is the one route with a two-permission check, since it is the one action ADR-029 §8 originally split by trust level | Both checks run before any repo call — a caller with only `.delete` and no `.delete.force` who requests `?force=true` gets `403`, not a silent downgrade to a blocked-purge `409` |
| `src/server/deps.ts` | server | changes | (wiring only) | `createSqliteRouteDeps()` wires `SqliteMenuRepo`/`SqliteNavLocationBindingRepo`; calls `rebuildNavLocationBindings()` once after the SQLite db opens | Existing single SQLite composition root | The in-memory root (`app.ts`) is unchanged — it is the other rule-of-two adapter, used for dev/tests |

## Contract Map

| Contract ID / Name | File | Owner Module | Kind | Why Needed | Job | Inputs | Outputs | Validation | Errors | Effect Boundary | Complexity / Resource View | Aggregate-Risk Note | Spec/ADR Trace | Test Seam / Expectation |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| C-001 `registerPermissionMigration` | `permission-migrations.ts` | identity | exported function | Sibling remediation ADRs (Members/Analytics/Integrations) need one call, not a copy of the mechanism | Record a `{from, to, reason}` rename/split pair for later fan-out | `{ from: string, to: readonly string[], reason: string }` | `void` | `to` non-empty; `from`/`to` strings need not already be catalog-registered at call time (registration order is not enforced) | None thrown — a duplicate `from` registration overwrites the prior entry (idempotent re-registration, matching `PermissionCatalog.register`'s existing overwrite semantics) | Pure decision (in-memory registry mutation, no I/O) | O(1) | None — this is metadata only, no grant is touched until C-002 runs | ADR-PIPE-012 Decision, Pattern Evaluation | Unit test: register two pairs, assert `listPermissionMigrations()` returns both; re-register same `from`, assert overwrite not duplication |
| C-002 `migrateDeprecatedPermissionGrants` | `permission-migrations.ts` | identity | exported function | The actual grant fan-out — the security-relevant half of the mechanism | For every registered `{from, to}` pair, for every policy holding `from`, ensure every string in `to` is also granted to that policy (add missing rows only) | `{ policyPermissions: PolicyPermissionRepoPort, policies: PolicyRepoPort, idGen: IdGeneratorPort, workspaceId: UUID }` | `{ migratedGrantCount: number }` | Never removes a `from` row; never touches a policy that does not hold `from`; skips a `to` string already held (no duplicate row) | None thrown — a single policy's fan-out failure must not be possible by construction (pure additive writes, no partial-failure branch) | Explicit side effect: DB/repo writes (new `policy_permissions` rows only, one per missing `to` string per matching policy) | O(p × m) — p = policies in the workspace, m = registered migration pairs; both small, bounded collections | **Aggregate-risk:** this is the sole path that could either fail-open (wrong `to` list granted) or fail-closed (a policy's fan-out silently skipped) across up to four features at once — the additive-only design bounds the fail-closed direction to "a required new permission is missing," never "an existing permission is removed" | ADR-PIPE-012 Migration Safety, Quality Attribute Scorecard (security axis) | Integration test: seed a policy with only `navigation.manage`, run migration, assert all 6 `admin.menus.*` CRUD strings now present AND `navigation.manage` still present; run again, assert `migratedGrantCount: 0` on the second call (idempotency) |
| C-003 `NAVIGATION_PERMISSIONS` | `contracts.ts` | navigation | exported const | The single source of truth for the 7 permission-string values `identity/permissions.ts` registers and every route file imports | Enumerate the `admin.menus.*` catalog | N/A (const array) | `readonly ["admin.menus.read", "admin.menus.create", "admin.menus.update", "admin.menus.delete", "admin.menus.delete.force", "admin.menus.assign", "admin.menus.manage"]` | N/A | N/A | N/A (data, no behavior) | N/A | N/A | ADR-029 §8, ADR-PIPE-012 Decision | Route-file tests import from this constant rather than re-declaring the literal string, so a future rename cannot desync route checks from the registered catalog |
| C-004 `createMenu` (outbox-extended) | `menu-service.ts` | navigation | exported function (existing, extended) | REQ-01/REQ-02's existing behavior unchanged; adds event emission | Same job as today, plus: on success, enqueue `navigation.menu.created` | Existing `CreateMenuRequired`/`CreateMenuOptional` **plus** `deps.outbox: OutboxPort`, `deps.idGen: IdGeneratorPort` (already present) | Existing `{ menu }` return, unchanged | Existing validation unchanged | Existing `MenuValidationError`/`MenuConflictError` unchanged; no new error type — an outbox enqueue failure propagates as whatever `OutboxPort.enqueue` itself throws (unchanged existing behavior for every other feature calling `enqueue`) | Explicit side effect: adds one outbox-table write after the existing repo write | O(1) additional | Same aggregate-risk class as every other feature's post-write outbox call — not a new risk category | feature.spec.md REQ-01/REQ-02 (SPEC-012); ADR-PIPE-012 D-11 | Existing `menu-service.test.ts` cases updated to pass a fake `OutboxPort`; new assertion: exactly one `navigation.menu.created` event enqueued with `{menuId, slug}` payload on success, zero events enqueued on a validation/conflict rejection |
| C-005 `updateMenuTree` (outbox-extended) | `menu-service.ts` | navigation | exported function (existing, extended) | Same reasoning as C-004 | Same job as today, plus: on success, enqueue `navigation.menu.updated` | Existing input **plus** `deps.outbox` | Existing return, unchanged | Existing validation unchanged | Existing errors unchanged | Explicit side effect: one outbox write after the existing repo write | O(1) additional | Same as C-004 | feature.spec.md REQ-05; ADR-PIPE-012 D-11 | New assertion: exactly one `navigation.menu.updated` event on success; zero on OCC/validation rejection |
| C-006 `assignLocation` (outbox-extended) | `menu-service.ts` | navigation | exported function (existing, extended) | Same reasoning as C-004, but two possible events | Same job as today, plus: on success, enqueue `navigation.location.assigned` for the target menu, **and** `navigation.location.unassigned` for the displaced menu when `displacedMenu !== null` | Existing input **plus** `deps.outbox` | Existing return, unchanged | Existing validation unchanged | Existing errors unchanged | Explicit side effect: one or two outbox writes after the existing repo writes | O(1) additional | Same as C-004; the two-event case (reassignment) must not fire `unassigned` when there was no prior binding | feature.spec.md REQ-06/REQ-07; ADR-PIPE-012 D-11 | New assertion: fresh assign enqueues exactly one `assigned` event; reassignment enqueues one `assigned` + one `unassigned` (for the displaced menu) |
| C-007 `deleteMenu` (outbox-extended) | `menu-service.ts` | navigation | exported function (existing, extended) | Same reasoning as C-004, but event depends on which rung of the ladder fired | Same job as today, plus: trash step enqueues `navigation.menu.updated` (status change only, not a real deletion yet); purge step enqueues `navigation.menu.deleted` | Existing input **plus** `deps.outbox` | Existing return, unchanged | Existing validation unchanged | Existing `MenuLocationBoundError`/`MenuNotFoundError` unchanged — no event on a blocked-purge rejection | Explicit side effect: one outbox write on whichever rung actually mutated state | O(1) additional | Same as C-004; must not double-fire (a blocked purge attempt enqueues nothing) | feature.spec.md REQ-08/REQ-09/REQ-10; ADR-PIPE-012 D-11 | New assertion: first call (trash) enqueues `navigation.menu.updated`; successful purge enqueues `navigation.menu.deleted`; blocked purge (409) enqueues nothing |
| C-008a `SqliteMenuRepo` | `repo.sqlite.ts` | navigation | exported class | Rule-of-two completion for `MenuRepoPort` (D-5) | Persist `NavMenuEntry` rows durably via Drizzle/SQLite | Constructor: `ContentDb`. Methods: identical signatures to `MenuRepoPort` | Identical to `MenuRepoPort`'s existing contract | N/A (adapter has no validation of its own — that stays in `menu-service.ts`) | N/A — surfaces whatever the underlying Drizzle call throws, same as `SqlitePostRepo` | Explicit side effect: SQLite reads/writes | O(1) per operation via indexed `(workspace_id, id)` / `(workspace_id, slug)` lookups (unlike the in-memory adapter's O(n) scan) | None beyond the existing rule-of-two adapter-parity risk (must behave identically to `InMemoryMenuRepo` for every method) | ADR-029 §5 (rule-of-two), ADR-PIPE-012 D-5 | Contract test: run the exact test suite `InMemoryMenuRepo` already passes against this adapter too (mirrors `PostRepoPort`'s dual-adapter test precedent) |
| C-008b `SqliteNavLocationBindingRepo` | `repo.sqlite.ts` | navigation | exported class | Rule-of-two completion for `NavLocationBindingRepoPort` (D-5) | Persist the derived binding index durably, enforcing `UNIQUE(workspace_id, location_key)` at the DB level | Constructor: `ContentDb`. Methods: identical signatures to `NavLocationBindingRepoPort` | Identical to `NavLocationBindingRepoPort`'s existing contract | The unique index is a DB constraint — `upsert` must use `onConflictDoUpdate` targeting it, matching `InMemoryNavLocationBindingRepo`'s replace-not-append semantics | N/A — surfaces whatever Drizzle throws | Explicit side effect: SQLite reads/writes | O(1) per operation via the composite unique index | **Invariant case:** the DB-level unique constraint must make INV-02 (never two rows for the same `(workspaceId, locationKey)`) true even under a concurrent-write race the in-memory adapter's single-threaded-event-loop argument doesn't apply to — this is a genuine strengthening, not just parity | ADR-029 §4 (`UNIQUE(workspace_id, location_key)`), feature.spec.md INV-02 | Contract test: same suite as `InMemoryNavLocationBindingRepo`, plus one new case exercising the DB-level unique constraint directly (two concurrent `upsert` calls for the same key never leave two rows) |
| C-009 `rebuildNavLocationBindings` | `reconcile.ts` | navigation | exported function | First real caller of the already-implemented `rebuildForWorkspace` (D-8) | Read every menu's `.locations` field, compute the resulting binding-index rows, call `rebuildForWorkspace` | `{ menuRepo: MenuRepoPort, bindingRepo: NavLocationBindingRepoPort, clock: ClockPort, workspaceId: UUID }` | `{ rebuiltCount: number }` | None thrown — a menu with an empty `.locations` array simply contributes zero rows | None (best-effort read-then-replace; not partial — `rebuildForWorkspace` replaces the whole workspace's rows in one call) | Explicit side effect: DB read (all menus) + DB write (index replace) | O(n) over menus in the workspace | Invariant case: after this runs, the binding index must exactly equal the union of every menu's `.locations` field — no orphan rows, no missing rows | ADR-029 §Decision-4 ("derived + rebuildable"), ADR-PIPE-012 D-8 | Integration test: seed 3 menus with overlapping/no locations, run rebuild, assert the binding index matches exactly; run twice, assert idempotent (same result both times) |
| C-010a..f route auth-check updates | `routes/admin/menus/*.ts` | server | exported functions (existing, extended) | Each route now checks the action-appropriate permission instead of the single flat one | Same HTTP contract as today (paths/methods/response shapes unchanged) — only the `authorize()` permission argument and, for `delete.ts`, an added second check | Existing HTTP request shapes, unchanged | Existing HTTP response shapes, unchanged | Same error mapping as today; `403` now names the specific missing permission (`admin.menus.create` etc.) instead of always `navigation.manage` | Explicit side effect: HTTP I/O + delegates to C-004..C-007 | O(1) request handling | `delete.ts`'s two-permission check is the one place a caller could previously reach `?force=true` with only the old single permission and can no longer do so with only `admin.menus.delete` — this is an intentional tightening (closes D-1), not a regression, and must be called out in the PR description | feature.spec.md REQ-14/AC-19/AC-20 (SPEC-012, to be superseded by the spec amendment named in ADR-PIPE-012's Article II remediation plan); api.spec.md §2 | Integration test per route: a principal holding only the old `navigation.manage` (pre-migration) is denied post-cutover; a principal holding the correct new action-specific string succeeds; `delete.ts` specifically: `admin.menus.delete` alone succeeds on trash + blocked-purge-409, but `?force=true` without `admin.menus.delete.force` returns `403` |

## Wiring Map

| Flow ID | Source | Transport/Call Type | Target | Payload/Contract | Ordering/Retry/Idempotency | Failure Handling | Trace |
|---|---|---|---|---|---|---|---|
| W-001 | `identity/seed.ts` boot path | direct call, once at startup | `permission-migrations.ts` C-002 | none in / `{ migratedGrantCount }` out | Idempotent — safe on every boot restart (matches `migrateLegacyPresentationSettings`'s W-003 precedent in ADR-PIPE-007) | Per-policy fan-out failure is not expected (pure additive writes); if the underlying repo throws, the boot sequence surfaces it the same way any other seed-time repo failure already does today | ADR-PIPE-012 Migration Safety |
| W-002 | `routes/admin/menus/create.ts` (and update-tree/assign-location/delete.ts) | direct call | `menu-service.ts` C-004/C-005/C-006/C-007 | existing service input **plus** `deps.outbox` | Not idempotent by design — each call enqueues at most the events named in that function's Contract Map row, matching the existing non-idempotent write semantics | If `outbox.enqueue()` throws after the repo write already succeeded, the route's existing catch-all `500` fires — the menu state change itself is NOT rolled back (same accepted non-transactional shape as the existing binding-index write, D-6) | ADR-PIPE-012 D-11, Consequences/Risks |
| W-003 | `server/deps.ts` boot path | direct call, once at startup, after the SQLite db opens | `reconcile.ts` C-009 | `{ workspaceId }` in / `{ rebuiltCount }` out | Idempotent — re-running produces the same binding-index state given the same menu data | Logs and continues on failure (does not abort server boot) — mirrors `migration.ts` C-008's per-row-failure tolerance in ADR-PIPE-007 | ADR-PIPE-012 D-8 |
| W-004 | `core/events/outbox-worker.ts` `processOutbox` (existing, unchanged) | polling, existing mechanism | any future subscriber via `EventBusPort` | `NAVIGATION_EVENTS` entries, now actually present in the outbox table for the first time | Existing retry/backoff semantics (ADR-037 Open, per `sweep-crosscutting-decisions-20260710.md` §E) — unchanged by this ADR | Existing `markFailed`/retry behavior — unchanged | ADR-009 §2 (existing), ADR-PIPE-012 D-11 |

## Data And Side-Effect Boundaries

| Boundary | Owner | Reads | Writes | Side Effects | Consistency / Transaction Rule | Migration / Dual-Write Path |
|---|---|---|---|---|---|---|
| `menus` (new SQLite table) | navigation | `SqliteMenuRepo` (via `menu-service.ts`), `reconcile.ts` C-009 | `SqliteMenuRepo` only, called from `menu-service.ts`'s existing mutating functions | None beyond the row itself | Single-row upsert per write, matching `posts`' existing pattern; no cross-row transaction (unchanged D-6 acceptance) | N/A — no legacy persisted `menus` data exists anywhere (the prior store was in-memory-only and does not survive a restart), so there is nothing to backfill; this is a pure additive schema change |
| `nav_location_bindings` (new SQLite table) | navigation | `SqliteNavLocationBindingRepo`, `reconcile.ts` C-009 | `SqliteNavLocationBindingRepo` only, called from `menu-service.ts`'s `assignLocation`/`deleteMenu` and from `reconcile.ts`'s rebuild | None beyond the row itself | `UNIQUE(workspace_id, location_key)` DB constraint enforces INV-02 at the storage layer (a strengthening vs. the in-memory adapter's single-threaded-argument-only guarantee) | Same as above — no legacy data, pure additive |
| `policy_permissions` (existing, in-memory only today) | identity | `migrateDeprecatedPermissionGrants` C-002, `authorize()` (unchanged) | C-002 (additive-only: new rows for missing `to` strings); existing grant-writing paths unchanged | None beyond the rows themselves | Additive-only by construction — never deletes or mutates an existing row's `permission` value | No dual-write window: since identity has no SQLite adapter yet, C-002 currently only ever sees freshly-seeded, in-process rows; it is built and tested now so it is correct the moment identity's own SQLite adapter lands (see ADR-PIPE-012 Re-evaluation Triggers, Dependency trigger) |
| `identity.permission_catalog` (in-process registry, not a DB table) | identity | Every `authorize()`/`isKnownPermission()` caller | `registerPermission()` (existing), `registerPermissionMigration()` C-001 (new, metadata only) | None (in-memory registry) | `navigation.manage` stays registered (deprecated) until the Point of No Return | N/A |

## Observability And Operational Expectations

| Surface / Flow | Required Signals | Correlation / Trace Context | Metrics | Logs | Alert / Runbook Need | Privacy / Secret Constraints | Trace |
|---|---|---|---|---|---|---|---|
| `menu-service.ts`'s 4 mutating functions | Each successful mutation now emits a `NAVIGATION_EVENTS` entry — the audit/reactivity signal that didn't exist before (D-11) | Event payload carries `menuId`/`slug` or `locationKey`/`menuId` (existing `NavMenuChangedPayload`/`NavLocationChangedPayload` shapes, unchanged) | None new required for this pass (no NFR discovery flagged latency/throughput targets for navigation) | Standard existing outbox-worker logging (unchanged) | N/A — no new production alerting surface beyond the existing outbox worker's own | No secret/PII fields in either event payload | ADR-PIPE-012 D-11, API/Event Contract Summary |
| `permission-migrations.ts` C-002 | The migration's own additive writes ARE the audit signal (each new `policy_permissions` row is visible via the existing `listByPolicyId` read path) | N/A | N/A | Named follow-up (not this pass): log the matched permission string inside `authorize()`'s success paths, needed before the Point of No Return (ADR-PIPE-012 Mitigations Required, operability axis) | N/A — one-time, additive, low-risk | No secret fields | ADR-PIPE-012 Migration Safety |
| SQLite adapters (C-008a/b) | Standard existing SQLite-adapter error surfacing (unchanged pattern from `SqlitePostRepo`) | N/A | N/A | N/A | N/A | N/A | ADR-PIPE-012 D-5 |

## Critical Invariants

| Invariant ID | Scope | Rule | Reason | Enforcement Surface | Test Expectation | Trace |
|---|---|---|---|---|---|---|
| INV-01 (unchanged, re-verified) | `SqliteMenuRepo` via `menu-service.ts` | A menu's `version` must never decrease and must increment by exactly 1 on every successful mutating call, identically whether the in-memory or SQLite adapter is in use | Adapter-parity — the SQLite adapter must not weaken an invariant the in-memory adapter already guarantees | `menu-service.ts` (adapter-independent — the increment logic lives in the service, not the repo) | Contract test: same version-increment assertions from `menu-service.test.ts`, re-run against `SqliteMenuRepo` | feature.spec.md INV-01 |
| INV-02 (strengthened) | `SqliteNavLocationBindingRepo` | The binding index must never hold two rows for the same `(workspaceId, locationKey)` pair — enforced by a DB-level `UNIQUE` constraint, not just adapter-internal replace-not-append logic | Closes a latent gap the in-memory adapter's single-threaded-event-loop argument papers over (a real concurrent-process race, however unlikely in this app's actual deployment shape, is not structurally impossible against a shared SQLite file) | `src/infra/db/schema.ts` (DB constraint) + `SqliteNavLocationBindingRepo.upsert`'s `onConflictDoUpdate` | Integration test: two concurrent `upsert` calls for the same `(workspaceId, locationKey)` never leave two rows in the real SQLite adapter | feature.spec.md INV-02, ADR-029 §4 |
| INV-NEW-01 `[internal-invariant]` grant migration is additive-only | `permission-migrations.ts` C-002 | `migrateDeprecatedPermissionGrants` must never remove or mutate an existing `policy_permissions` row — it may only add rows for a `to` string not already held | The entire safety argument for doing this rename without a dual-write/read-routing window rests on this being strictly additive (see ADR-PIPE-012 Migration Safety) | `permission-migrations.ts` (no delete/update call anywhere in the function's implementation — enforced by construction, not a runtime check) | Integration test: seed a policy with `navigation.manage` plus one unrelated permission, run migration, assert the unrelated permission is untouched and `navigation.manage` itself is still present after migration | ADR-PIPE-012 Migration Safety, Quality Attribute Scorecard (security axis) |
| INV-NEW-02 `[internal-invariant]` no double permission-string dependency | `identity/permissions.ts`, all six route files | No route file may check both `navigation.manage` and an `admin.menus.*` string simultaneously (except `delete.ts`'s deliberate two-permission `.delete`/`.delete.force` pair, which are both *new* strings, not one new + one legacy) | Prevents a half-migrated state where some routes silently still depend on the deprecated string past cutover | Code Review (Enforcement section, ADR-PIPE-012) | Static check / Code Review: grep confirms zero `navigation.manage` string literals remain in `src/server/routes/admin/menus/*.ts` after cutover | ADR-PIPE-012 Migration Safety, Enforcement |

## Brownfield / Migration Mapping (if applicable)

| Source Behavior / Contract | Target Module / Contract | Preserve / Change | Characterization Evidence | Migration Safety Note |
|---|---|---|---|---|
| `navigation.manage` gating all six routes uniformly | `admin.menus.{read,create,update,delete,delete.force,assign}` gating each route by its specific action | Change: intentional tightening (closes D-1) — a principal that previously could force-purge with only `navigation.manage` now needs `admin.menus.delete.force` specifically | `admin-menus-routes.test.ts`'s existing "SPEC-006 REQ-05" test (bare principal denied, owner-wildcard succeeds) is the closest existing characterization; it must be extended per-action, not just re-run as-is | Built-in `admin`/`owner` roles are re-seeded with all six new strings (Migration Safety Backfill plan) so no existing built-in role loses effective access; only a hypothetical custom role holding *only* `navigation.manage` and *only* wanting force-purge denied would see a behavior change — which is exactly the intended fix |
| `InMemoryMenuRepo`/`InMemoryNavLocationBindingRepo` (volatile, dev/tests) | `SqliteMenuRepo`/`SqliteNavLocationBindingRepo` (durable, production composition root only) | Preserve: identical port contract and observable behavior; Change: durability | No prior persisted data exists to characterize against — this is additive, not a behavior-preserving migration of existing rows (see Data And Side-Effect Boundaries) | `createSqliteRouteDeps()` is the only wiring point that changes; `app.ts`'s in-memory root is untouched, so dev/test behavior is unaffected |
| `NavLocationBindingRepoPort.rebuildForWorkspace` (implemented, uncalled) | `reconcile.ts` C-009 (first real caller) | Preserve: existing method contract, unchanged; Change: it is now actually invoked | The method's own existing unit coverage (if any) proves its internal correctness; C-009 only adds a caller | No risk of behavior change to existing menus — rebuild recomputes from each menu's own `.locations` field, which is already the source of truth |

## Test Expectations

- Contract tests: C-008a/C-008b (`SqliteMenuRepo`/`SqliteNavLocationBindingRepo`) — the exact existing `InMemoryMenuRepo`/`InMemoryNavLocationBindingRepo` test suites re-run against both new adapters (matches `PostRepoPort`/`SettingsRepoPort` precedent), plus one new concurrency-specific case for the DB-level unique constraint.
- Integration tests: C-002 (grant fan-out additive/idempotent, INV-NEW-01), C-009 (binding-index rebuild correctness), W-002 (outbox enqueue on each of the four mutating functions' success paths, zero-enqueue on each rejection path), C-010a..f (per-route permission-check cutover, including `delete.ts`'s two-permission case).
- Property/invariant tests: INV-01 (version monotonicity, adapter-independent), INV-02 (binding uniqueness, DB-enforced), INV-NEW-01 (additive-only migration), INV-NEW-02 (no lingering `navigation.manage` dependency, static/Code-Review-enforced rather than a runtime test).
- Characterization tests: None new beyond extending `admin-menus-routes.test.ts`'s existing SPEC-006 REQ-05 test per-action — there is no legacy persisted-data behavior to characterize (menus/bindings have no prior durable state, per the Brownfield/Migration Mapping table).
- Explicitly N/A suites with reason: No dual-write reconciliation test for `menus`/`nav_location_bindings` (no legacy data exists to reconcile against — pure additive schema, unlike SPEC-007's `presentation_settings` migration which did have real rows).

## Downstream Handoff Notes

- Coordinator task-generation constraints: Sequence `permission-migrations.ts` (C-001/C-002) and the SQLite adapters (C-008a/b) ahead of the route-layer cutover (C-010a..f) and the `deps.ts` wiring change — the route checks and the composition-root wiring both depend on real contracts existing first, per Article V (Integration-First Testing). The four `menu-service.ts` outbox-extensions (C-004..C-007) can proceed in parallel with the SQLite adapter work (they depend on `OutboxPort`, which already exists, not on the new adapters). **Before any of this is scheduled as tasks, the Coordinator must resolve ADR-PIPE-012's Article II exception** (SPEC-012 v1.1 amendment vs. explicit waiver) — this outline's Contract Map is written at spec-equivalent granularity specifically so that choice does not block re-reading this document either way.
- TDD focus: Certify C-002 (`migrateDeprecatedPermissionGrants`, INV-NEW-01) first — it is the highest aggregate-risk contract, per ADR-PIPE-012's security-axis mitigation. Then the SQLite adapter contract tests (C-008a/b), then the outbox-extension assertions (C-004..C-007), then the route-layer permission-cutover tests (C-010a..f) last, since those depend on the catalog/migration work being certified first.
- Programmer architecture audit focus: verify zero `navigation.manage` string literals remain in `src/server/routes/admin/menus/*.ts` after cutover (INV-NEW-02); verify `migrateDeprecatedPermissionGrants()` contains no delete/update call against `policy_permissions` (INV-NEW-01, enforced by construction); verify `SqliteMenuRepo`/`SqliteNavLocationBindingRepo` are wired only in `deps.ts`, not duplicated into `app.ts`'s in-memory root; verify `delete.ts`'s two-permission check order (base `admin.menus.delete` checked regardless of `force`, `admin.menus.delete.force` checked additionally only when `force=true` is requested).
- Open risks or ambiguities: None beyond what ADR-PIPE-012's own Consequences/Risks section already names (grant-migration bug risk; outbox-enqueue partial-failure risk inherited from the existing D-6 gap) — both have explicit mitigations recorded there. The one process risk this outline cannot resolve itself is ADR-PIPE-012's Article II exception (spec-amendment-vs-waiver), which is a Coordinator decision, not an architecture one.
