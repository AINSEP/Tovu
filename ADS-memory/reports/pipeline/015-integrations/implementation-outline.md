# Implementation Outline: integrations (webhook-delivery remediation)

- Spec: SPEC-015 v1.0.0 (hash: sha256:f2820497e57ec41382f19b735a4c2f6746f7f78a10f41a3315da79d789aae190)
- ADR: ADR-PIPE-015
- Status: PRODUCED
- Trigger result: Boundary Cross, Contract Change, System Wiring, Data And Persistence, Brownfield Dependency, Critical Cross-Boundary Invariant, Parallelization Ambiguity
- Date: 2026-07-13
- Author: Software Architect Agent

> Use this artifact only for post-ADR, pre-tasks structure. It defines module boundaries, public/exported contracts, wiring, data boundaries, and critical invariants. It must not contain pseudo-code, private helper inventories, or task sequencing.

## Trigger Decision Matrix

| Trigger | Applies? | Evidence | Source Trace |
|---|---:|---|---|
| Boundary Cross | yes | Crosses `src/integrations` (domain), `src/http` (new transport primitive), `src/origin` (egress oracle, read-only dependency), `src/identity` (permission catalog + seed migration), `src/server` (composition root wiring + routes), `src/infra/db` (schema) | GAP-01..GAP-06, GAP-12 |
| Contract Change | yes | New `EventBusPort.subscribeAll` method; new `createHttpClient` factory; new `RouteDeps.originRegistry` field; permission string change on every integrations route's `authorize()` call | ADR-PIPE-015 Decision, API/Event Contract Summary |
| System Wiring | yes | This IS the system-wiring gap: an outbox-style fan-out subscriber (Stage A) + an interval-scheduled worker (Stage B), gated activation in the composition root | GAP-01, ADR-PIPE-015 Migration Safety |
| Data And Persistence | yes | Two new Drizzle tables (`webhook_subscriptions`, `webhook_deliveries` with `payload_json`); repo-port rule-of-two completion; a permission-grant migration | GAP-05, GAP-12, ADR-036 §2 DDL |
| Brownfield Dependency | yes | Five existing admin routes' `authorize()` calls change the permission string they check; `RouteDeps`/`IntegrationsRouteDeps` already declare the fields this ADR fills for real; existing passing tests (`admin-integrations-routes.test.ts`, all four `src/integrations/__tests__/*`) must keep passing against the new adapters | spec-manifest.md Brownfield References |
| Reverse-Spec Or Migration | no | Not a reverse-spec extraction; the permission/data migrations are scoped and handled under Data And Persistence / Brownfield Dependency above | — |
| Critical Cross-Boundary Invariant | yes | INV-P1 (no live delivery traffic before signer+transport+storage are real), INV-P2 (egress fail-closed), INV-P3 (no unguarded HTTP client obtainable), INV-P4 (envelope durability parity) all span module boundaries | ADR-PIPE-015 Migration Safety, Consequences |
| Parallelization Ambiguity | yes | The permission-rename phase and the adapter-build phase are independent and safely parallel; the activation phase is NOT parallel with either — it strictly follows both. Without this outline, Coordinator cannot safely mark `[P]` without risking an out-of-order activation | ADR-PIPE-015 Migration Safety Point of No Return |

## Module Map

| Module/Domain | Owns | Responsibility | Public Contracts | Dependencies | Notes |
|---|---|---|---|---|---|
| `integrations` (`src/integrations/`, existing) | `webhook_subscriptions`/`webhook_deliveries` domain logic, the signer seam, the new scheduler/fan-out wrappers | Signing, delivery orchestration, subscription CRUD (unchanged), now also: durable persistence, real signing, scheduled activation | C-001..C-006 | `http` (`HttpClientPort`), `core` (ports, `EventBusPort`), `origin` (`OriginRegistryPort`, via routes, not this module directly) | No new port added to `integrations/ports.ts` — `KeyringPort`/repo ports are unchanged interfaces, only new adapters |
| `http` (`src/http/`, existing, types/ports-only today) | The guarded transport + its module-private raw adapter | First real implementation of ADR-038's structural guarantee | C-007, C-008 | none (leaf module) | Raw transport is module-private; enforced by an import-boundary canary, not just convention |
| `core` (`src/core/`, existing, extended) | `EventBusPort` | One additive method (`subscribeAll`) | C-009 | none | `integrations` depends on this; this gains no dependency on `integrations` |
| `identity` (`src/identity/`, existing, extended) | Permission catalog, seed-time grant migration | Registers `admin.integrations.manage`; migrates existing grants | (no new port) | none | Mirrors the `settings.write` deprecation precedent exactly |
| `server` routes/composition root (existing, extended) | Wiring for all of the above into the running app | Injects real adapters; owns the gated activation step | C-010a..e (unchanged endpoints, changed deps) | `integrations`, `http`, `origin`, `identity` | `server/deps.ts`'s activation block is the Point of No Return (ADR-PIPE-015 Migration Safety) |
| `infra/db` (`src/infra/db/schema.ts`, existing, extended) | Two new Drizzle table definitions | Schema only, no behavior | — | none | Shared schema file per existing repo convention |

## File Map

| File Path | Module | Creates / Changes | Public Contracts Housed | Responsibility | Why This Separation Exists | Notes |
|---|---|---|---|---|---|---|
| `src/integrations/keyring.env.ts` | integrations | creates | C-001 `EnvOrFileKeyring` | First real `KeyringPort`: env-var override else a generated, machine-local file outside the portable site folder; HKDF derivation | Keeps the secret-material boundary (ADR-024) in one small, directly-testable adapter, separate from signing logic | Never imports anything from `content.db`'s directory conventions — deliberately outside the portable-folder path |
| `src/integrations/signing.keyring.ts` | integrations | creates | C-002 `createKeyringBackedSigner` | Adapts `KeyringPort.deriveSigningSecret` into the existing `WebhookSigner` DI seam | Keeps `signing.ts`'s pure HMAC functions untouched; this file is the only new thing that depends on `KeyringPort` | Replaces `createFixedSecretSigner` only at the composition-root call site — `signing.ts` itself is unmodified |
| `src/integrations/worker.ts` | integrations | creates | C-003 `startWebhookDeliveryWorker` | Stage B activation wrapper: interval-scheduled `processDueDeliveries` calls with an overlap guard and a `stop()` handle | Isolates "when does this run" from "what does one pass do" (already-certified `processDueDeliveries`) — Code Review's activation-gate check (ADR-PIPE-015 Enforcement) targets this file's call site specifically | Never called from `server/app.ts` (hermetic test composition) — only from `server/deps.ts`, and only once Phase 1-2 adapters exist |
| `src/integrations/fanout.ts` | integrations | creates | C-004 `registerWebhookFanout` | Stage A activation wrapper: subscribes to every domain event via `EventBusPort.subscribeAll` and calls `enqueueDelivery` per event | Same isolation rationale as `worker.ts` — the already-certified `enqueueDelivery` logic is untouched; this file is pure wiring | Same activation-gate constraint as `worker.ts` |
| `src/integrations/repo.sqlite.ts` | integrations | creates | C-005 (implements `WebhookSubscriptionRepoPort`), C-006 (implements `WebhookDeliveryRepoPort`, incl. envelope persistence) | Drizzle/SQLite adapters, rule-of-two #2 for both repo ports; folds `DeliveryEnvelopeStore`'s payload into a durable column | Matches `settings`/`post`/`presentation`'s established `repo.sqlite.ts` convention; resolves GAP-05 and GAP-12 together since a delivery worker that can't rehydrate its payload across a restart isn't actually durable | `claimPending`'s SQL query gets a real unique index on `(workspace_id, subscription_id, event_id)`, turning the disclosed O(n) idempotency-scan gap into a real fix as a side effect |
| `src/http/transport.fetch.ts` | http | creates, module-private | (implements `HttpTransportAdapter`, not separately exported) | The dumb, policy-pinned transport: connects only to the pre-resolved/vetted peer, never re-resolves | The whole point of ADR-038's "structural, not documentary" guarantee — a consumer must not be able to import this directly | Enforced by C-008's import-boundary canary; not exported from `src/http/index.ts` |
| `src/http/client.ts` | http | creates | C-007 `createHttpClient` | The only exported constructor; wraps the transport with `EgressPolicy` enforcement (deny-list classification, IDNA normalization, redirect re-verification + auth-header stripping, response/decompressed-byte caps, connect/read timeouts) | Isolates the one place `HttpClientPort` can be constructed, matching ADR-038 amendment 3 | `integrations`, and later Newsletter/Analytics (out of this pass's scope), all call this same factory |
| `src/http/__tests__/import-boundary.test.ts` | http | creates | C-008 `[internal-invariant]` import-boundary canary | Fails the build if `transport.fetch.ts` (or any future raw transport) is imported outside `src/http/` + the named composition-root files | Makes "cannot obtain an unguarded client" a CI-enforced fact, not a comment | Mirrors ADR-022's write-chokepoint canary pattern |
| `src/core/ports.ts` | core | changes | C-009 `EventBusPort.subscribeAll` | Additive method: subscribe a handler to every published event regardless of name | The only way Stage A can be "a core outbox subscriber" without either enumerating every current/future event name or coupling `core/events` to `integrations` | Non-breaking — existing `subscribe(eventName, handler)` callers are unaffected |
| `src/core/events/memory-bus.ts` | core | changes | (implements C-009 for `InMemoryEventBus`) | In-memory implementation of `subscribeAll` | Same file already implements `subscribe`; keeps both dispatch mechanisms in one adapter | — |
| `src/infra/db/schema.ts` | infra | changes | (Drizzle table defs, not a "contract" per se) | Adds `webhookSubscriptions`, `webhookDeliveries` (with `payloadJson`) tables per ADR-036 §2 DDL | Shared schema file, existing convention | No behavior — pure schema; unique index on `(workspace_id, subscription_id, event_id)` added here |
| `src/identity/permissions.ts` | identity | changes | (extends `BASE_CATALOG`/feature registration, not a new contract) | Registers `admin.integrations.manage`; marks `integration.manage` DEPRECATED (doc-comment, not deleted) | Existing single registration point for all permission strings, same file that already carries the `settings.write` DEPRECATED precedent | Mirrors that exact wording convention |
| `src/identity/seed.ts` | identity | changes | (extends seed migration, not a new contract) | `migrateLegacyIntegrationsPermission()` — grants `admin.integrations.manage` to every existing `integration.manage` holder | Existing seed-time migration location, same pattern as the settings.write migration | Idempotent — safe to rerun on every boot |
| `src/server/routes/admin/integrations/create.ts` | server | changes | (consumer re-point, not a new contract) | Drops `permitAllHttpsTargets`; injects `deps.originRegistry.isAllowedEgressTarget` as `isAllowedTarget`; `authorize()` checks `admin.integrations.manage` | Closes GAP-06 at its one call site | The other four routes (`list.ts`/`pause.ts`/`delete.ts`/`deliveries.ts`) only need the permission-string change, not the egress wiring (they don't validate a `targetUrl`) |
| `src/server/routes/admin/integrations/{list,pause,delete,deliveries}.ts` | server | changes | (consumer re-point) | `authorize()` calls switch to `admin.integrations.manage` | Same migration, same file set spec-manifest.md already names | — |
| `src/server/routes/types.ts` | server | changes | (extends `RouteDeps`, not a new contract) | Adds `originRegistry: OriginRegistryPort`; corrects `webhookSigner`'s stale "not consumed by any route yet" doc comment once the worker (not a route) becomes its real consumer | Existing shared composition-root deps file | `IntegrationsRouteDeps` in `routes/admin/integrations/deps.ts` already extends `RouteDeps` — no change needed there once `RouteDeps` itself carries the field |
| `src/server/app.ts` | server | changes | (wiring only) | Hermetic test composition wires in-memory `KeyringPort`/`HttpClientPort` test doubles and an in-memory `OriginRegistry`; does NOT call `registerWebhookFanout`/`startWebhookDeliveryWorker` | Keeps existing hermetic tests deterministic and fast; activation is a `server/deps.ts`-only concern | — |
| `src/server/deps.ts` | server | changes (Phase 4 only) | (wiring only) | Composes the real `EnvOrFileKeyring`, real `createHttpClient(fetchTransport, policy)`, real `OriginRegistry`; registers `registerWebhookFanout` + `startWebhookDeliveryWorker` — **the Point of No Return** | This file already splits "hermetic test composition" (`app.ts`) from "real running server" (`deps.ts`) for media (`LocalFsBlobStore` vs `InMemoryBlobStore`) — same precedent applies here | Gated per ADR-PIPE-015 Migration Safety; Code Review enforcement item targets this file's diff specifically |
| `src/server/seed.ts` | server | changes | (wiring only) | Seeds a dev-capability verified origin + egress allowlist entry at boot, so the real oracle doesn't fail-closed on every fresh dev server | Existing seed boot path already does the analogous thing for identity/workspace bootstrap data | Without this, every egress check fails closed on a fresh clone until an operator manually registers an origin |

## Contract Map

| Contract ID / Name | File | Owner Module | Kind | Why Needed | Job | Inputs | Outputs | Validation | Errors | Effect Boundary | Complexity / Resource View | Aggregate-Risk Note | Spec/ADR Trace | Test Seam / Expectation |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| C-001 `EnvOrFileKeyring` | `keyring.env.ts` | integrations | exported class (implements `KeyringPort`) | GAP-02/03 — signing secrets are currently derived from nothing (empty map) | `activeKey()`/`deriveSigningSecret()`/`derive()` per the frozen `KeyringPort` shape | `{ workspaceId, subscriptionId, version }` (webhook) or `{ workspaceId, purpose, info }` (generic) | `Uint8Array` (raw derived secret) | Root key resolved from `process.env.TOVU_ROOT_KEY` (base64) or a generated, machine-local file; HKDF per ADR-036 §5's info-string shape | Throws if the key file can't be created/read (fail loud, never sign with an absent key) | Explicit side effect: reads env/filesystem; pure HKDF computation otherwise | O(1) per derivation | **Aggregate-risk:** every signature in the system depends on this deriving the same secret deterministically for a fixed root key — a bug here silently invalidates every receiver's verification, not just one delivery | GAP-02, GAP-03, ADR-036 §5 | Unit test: same input → same output; different `version`/`subscriptionId`/`purpose` → different output; missing root key → throws, never returns a zero/placeholder secret |
| C-002 `createKeyringBackedSigner` | `signing.keyring.ts` | integrations | exported function | GAP-02 — `processDueDeliveries` needs a real `WebhookSigner`, not the dev fixed-map stand-in | Adapt `KeyringPort.deriveSigningSecret` + `signPayload` into the `WebhookSigner.signForSubscription` shape | `KeyringPort` instance | `WebhookSigner` | Delegates all signing math to the already-certified `signPayload` (no re-implementation) | Propagates `KeyringPort` errors unchanged | Explicit side effect: one derivation call, then a pure HMAC | O(rawBody length) — unchanged from `signPayload` | N/A — thin adapter over two already-certified units | GAP-02, REQ-16 | Unit test: constructs the exact same signature `createFixedSecretSigner` would for an equivalent secret, proving the swap is behavior-preserving at the signing-math level |
| C-003 `startWebhookDeliveryWorker` | `worker.ts` | integrations | exported function `[internal-invariant]` | GAP-01 Stage B — nothing schedules `processDueDeliveries` today | Run `processDueDeliveries` on a fixed interval, skip a tick if the previous pass hasn't finished, expose `stop()` | `{ deps: ProcessDueDeliveriesDeps, intervalMs, ...ProcessDueDeliveriesOptional }` | `{ stop(): void }` | Overlap guard: a tick that fires while the previous pass is still running is a no-op, not a queued/concurrent second pass | Logs and swallows a per-pass thrown error (a single bad pass must not kill the interval) | Explicit side effect: `setInterval`, delegates to already-certified `processDueDeliveries` | O(1) scheduling overhead; each pass is `processDueDeliveries`'s own complexity | **Why internal-invariant:** the overlap guard is the one new piece of logic this file adds beyond pure delegation — a bug here could double-claim or silently stop running after one uncaught error | GAP-01, ADR-PIPE-015 Migration Safety Point of No Return | Unit test: two ticks scheduled faster than one pass completes → second tick is a no-op; a pass that throws → interval keeps running on the next tick; `stop()` → no further passes fire |
| C-004 `registerWebhookFanout` | `fanout.ts` | integrations | exported function | GAP-01 Stage A — nothing calls `enqueueDelivery` from a live event today | Subscribe to `EventBusPort.subscribeAll`; for each event, call `enqueueDelivery({ event: <narrowed to WebhookSourceEvent> })` | `{ bus: EventBusPort, ...EnqueueDeliveryDeps }` | `Promise<() => Promise<void>>` (unsubscriber, mirrors `subscribe`'s own return shape) | Narrows an arbitrary `DomainEvent` to `WebhookSourceEvent`'s required shape; a malformed/incompatible event (missing `workspaceId`) is skipped, not thrown | Logs and continues past a single event's `enqueueDelivery` failure — one bad event must not stop future fan-out | Explicit side effect: bus subscription, delegates to already-certified `enqueueDelivery` | O(1) per event dispatched | N/A — thin adapter; the fan-out logic itself (topic matching, idempotency) is unchanged, already-certified `enqueueDelivery` | GAP-01, C-009 | Unit test against `InMemoryEventBus`: publishing `workspace.created` (today's real event) results in `enqueueDelivery` being invoked with a correctly-shaped `WebhookSourceEvent`; an event missing `workspaceId` is skipped without throwing |
| C-005 `SqliteWebhookSubscriptionRepo` | `repo.sqlite.ts` | integrations | exported class (implements `WebhookSubscriptionRepoPort`) | GAP-05 — no durable subscription storage today | Same 5 methods as `InMemoryWebhookSubscriptionRepo`, backed by Drizzle/SQLite | Same shapes as the port interface | Same shapes as the port interface | Same behavior contract as the in-memory adapter (shared contract-test suite) | Propagates DB errors; `insert` on a duplicate id throws (matches in-memory behavior) | Explicit side effect: DB reads/writes | O(1) per row op, indexed on `(workspace_id, id)` | N/A — must be behaviorally identical to `repo.memory.ts`, verified by the shared contract-test suite, not a new invariant | GAP-05, ADR-036 §2 | Contract test: both adapters run the identical assertion suite `repo.memory.test.ts` already has |
| C-006 `SqliteWebhookDeliveryRepo` | `repo.sqlite.ts` | integrations | exported class (implements `WebhookDeliveryRepoPort`) + envelope persistence | GAP-05 + GAP-12 — no durable delivery storage AND no durable envelope re-hydration path | Same 7 methods as `InMemoryWebhookDeliveryRepo`, plus reading/writing `payload_json` so `enqueue`/`findById`/`claimPending` durably round-trip the envelope the delivery worker needs | Same shapes as the port interface, `enqueue` additionally persists the envelope | Same behavior contract as the in-memory adapter | Same as C-005 | Explicit side effect: DB reads/writes | O(1) per row op; `claimPending` uses an indexed `(status, next_attempt_at)` query, sorted ascending, capped at `batchSize` | **Aggregate-risk:** this is the row a real, later-process delivery worker depends on to rebuild the exact envelope it signs — a serialization mismatch here silently corrupts every delivered payload | GAP-05, GAP-12, ADR-036 §2, EC-04 | Contract test: enqueue then claim then read back — the envelope's `data` must be byte-identical (JSON round-trip) to what was enqueued; a restart-simulated test (fresh repo instance reading existing rows) proves durability |
| C-007 `createHttpClient` | `client.ts` | http | exported function (the only constructor) | GAP-04/06 — no real guarded transport exists; the only way an `HttpClientPort` may be obtained | Wrap a module-private transport with `EgressPolicy` enforcement before any connection is attempted | `(transport: HttpTransportAdapter, policy: EgressPolicy)` | `HttpClientPort` | Scheme allowlist, resolve-then-connect IP pinning, address-family-complete deny list (incl. IPv4-mapped-IPv6 normalization), IDNA/punycode normalization, credentials-in-URL rejection, redirect re-verification + auth-header stripping cross-origin, response/decompressed-byte caps, connect/read timeouts (all per ADR-038 amendments 1-2) | Any policy violation resolves the request as a transport-level failure (never partially connects) | Explicit side effect: DNS resolution + network I/O, all policy-gated before connecting | O(1) policy evaluation + one network round trip per redirect hop, capped at `policy.maxRedirects` | **Aggregate-risk:** every outbound webhook (and, eventually, Newsletter/Analytics) HTTP call in the system funnels through this one function — a classification bug here is a system-wide SSRF hole, not a local one | GAP-04, GAP-06, ADR-038 | Integration test: a deliberately private/loopback/link-local/metadata target is rejected pre-connect for each address family; a cross-origin redirect strips auth headers and is re-verified against the policy; a policy-widened `devHostAllowlist` capability (not consumer code) is the only way a test reaches `localhost` |
| C-008 import-boundary canary `[internal-invariant]` | `__tests__/import-boundary.test.ts` | http | test (CI-enforced invariant) | Makes ADR-038 amendment 3's "cannot obtain an unguarded client" true, not aspirational | Scan the repo's import graph; fail if `transport.fetch.ts` (or any future raw transport) is imported anywhere outside `src/http/` or the named composition-root files | none (static analysis over the repo) | pass/fail | N/A | Test failure blocks merge | Pure static check, no runtime effect | O(files) once per CI run | **Why internal-invariant:** without this, "module-private" is just a comment a future PR can silently violate | ADR-038 amendment 3, ADR-PIPE-015 Enforcement | Direct test: assert zero matches for an import of the raw transport module outside the allowed path set |
| C-009 `EventBusPort.subscribeAll` | `core/ports.ts` | core | exported interface method (additive) | GAP-01 Stage A needs a way to observe every event without enumerating names | Subscribe a handler to all events regardless of `name` | `(handler: (event: DomainEvent) => Promise<void>)` | `Promise<() => Promise<void>>` (unsubscriber) | None beyond the existing `subscribe` contract's shape | N/A (interface) | N/A (interface, no behavior) | N/A | N/A — this is an additive interface method; the in-memory implementation (`InMemoryEventBus`) is where behavior/complexity lives | GAP-01, C-004 | `InMemoryEventBus.subscribeAll` unit test: publishing events of several different names all reach the same handler; unsubscribing stops delivery |

## Wiring Map

| Flow ID | Source | Transport/Call Type | Target | Payload/Contract | Ordering/Retry/Idempotency | Failure Handling | Trace |
|---|---|---|---|---|---|---|---|
| W-001 | `server/deps.ts` boot (Phase 4 only) | direct call | `fanout.ts` C-004 `registerWebhookFanout` | `{ bus, subscriptionRepo, deliveryRepo, envelopeStore, idGenerator, clock }` | Idempotent per `(eventId, subscriptionId)` — inherited from already-certified `enqueueDelivery` | A per-event failure is logged and does not stop future events from being fanned out | GAP-01, REQ-09 |
| W-002 | `server/deps.ts` boot (Phase 4 only) | direct call, starts an interval | `worker.ts` C-003 `startWebhookDeliveryWorker` | `{ deps: ProcessDueDeliveriesDeps, intervalMs, hooks, requestTimeoutMs, maxAttempts }` | Overlap-guarded — never two concurrent passes; inherited retry/backoff/dead-letter from already-certified `processDueDeliveries` | A per-pass thrown error is logged; the interval keeps running on the next tick | GAP-01, REQ-10..14 |
| W-003 | `worker.ts` (inside a pass) | direct call | `client.ts` C-007 `createHttpClient`'s returned `HttpClientPort.send` | `HttpRequest` (method/url/headers/body/timeoutMs) | No retry inside the transport — `processDueDeliveries` owns retry/backoff, unchanged | Any policy violation or transport error surfaces as a normal failed-attempt outcome (existing `attemptOneDelivery` catch-all) | GAP-04, GAP-06 |
| W-004 | `worker.ts` (inside a pass) | direct call | `signing.keyring.ts` C-002 `createKeyringBackedSigner`'s returned signer | `{ subscription, rawBody, timestampSeconds }` | No re-serialization between signing and sending (INV-04, unchanged) | A derivation failure (e.g. missing root key) surfaces as a normal failed-attempt outcome, same catch-all as W-003 | GAP-02 |
| W-005 | `routes/admin/integrations/create.ts` | direct call | `origin/origin.ts` `OriginRegistry.isAllowedEgressTarget` (via injected `deps.originRegistry`) | `({ workspaceId }, targetUrl)` | Read-only, no retry needed; fails closed (`false`) on any parse failure or unverified origin | A `false` result surfaces as `WebhookSubscriptionValidationError` → HTTP 400, exactly as `permitAllHttpsTargets` returning `false` would have (unchanged error contract) | GAP-06, REQ-03 |
| W-006 | `server/seed.ts` boot path | direct call, once at startup | `origin`'s `OriginSettingRepoPort` (in-memory adapter) | Seeds a dev-capability verified origin + egress allowlist entry for the local workspace | Idempotent — safe if boot runs seeding more than once | If seeding fails, every egress check fails closed (loud, not silent) until fixed | ADR-PIPE-015 Consequences (risk mitigation) |
| W-007 | `identity/seed.ts` boot path | direct call, once at startup | `migrateLegacyIntegrationsPermission()` | none in / grant-count out | Idempotent — reruns are a harmless no-op re-grant | Per-principal failure is logged and skipped, does not abort boot | GAP (permission rename), Migration Safety |

## Data And Side-Effect Boundaries

| Boundary | Owner | Reads | Writes | Side Effects | Consistency / Transaction Rule | Migration / Dual-Write Path |
|---|---|---|---|---|---|---|
| `webhook_subscriptions` | integrations | `subscriptions.ts`, admin routes (via repo port) | `subscriptions.ts`'s exported functions only (via the repo port — no new write path added) | None new | Existing INV-01/INV-07 (unchanged) — one row per write, no revision ledger (ADR-036 §2 explicitly narrows this table out of the revisioned audit path) | N/A — no legacy data; table starts empty |
| `webhook_deliveries` (+ `payload_json`) | integrations | `delivery.ts`'s `enqueueDelivery`/`processDueDeliveries` (via repo port) | Same two functions only | None new | `claimPending`'s status-flip-on-claim (unchanged); the new `payload_json` column is written at `enqueue` time in the same statement as the row insert, never as a separate write (closes GAP-12 without a second write path) | N/A — no legacy data; table starts empty |
| `identity` permission grants | identity (unchanged owner) | `authorize()` (unchanged) | `migrateLegacyIntegrationsPermission()` (new, additive grant only) | None | Additive-only migration; never revokes the old grant | Existing `integration.manage` grants are preserved unchanged; `admin.integrations.manage` is granted alongside |
| `core/origin` verified-origin + allowlist settings | origin (unchanged owner, out of this feature's file scope) | `OriginRegistry` (read-only from `integrations`' perspective) | `server/seed.ts`'s new dev-capability seed call only | None new | Unchanged — in-memory only; this ADR does not add durability here (named risk) | N/A — pre-existing gap, not created by this ADR |

## Observability And Operational Expectations

| Surface / Flow | Required Signals | Correlation / Trace Context | Metrics | Logs | Alert / Runbook Need | Privacy / Secret Constraints | Trace |
|---|---|---|---|---|---|---|---|
| `worker.ts` C-003 each pass | Structured summary already computed by `processDueDeliveries` (`processed/delivered/failed/dead`) | None new required this pass (no NFR discovery flagged a correlation-id requirement here) | None new required | Log the pass summary at `info` level; log a per-pass thrown error at `error` level with the pass otherwise continuing | N/A this pass — a `dead`-count alert is named as a follow-up, not built here (matches SPEC-015's own Art. VIII partial finding) | Never log `rawBody`/signing secrets — only counts and delivery ids | GAP-01, Art. VIII |
| `client.ts` C-007 every send | Policy-rejection reason (which deny-list rule tripped) surfaced in the failed-attempt's `lastError`, not swallowed silently | None new | None new required this pass | A policy rejection produces a descriptive `lastError` string (e.g. "egress denied: private address") so an operator debugging a stuck subscription can tell "your endpoint is unreachable" from "your endpoint is in a denied address range" | N/A this pass | Never log request/response bodies beyond the existing truncated `bodyText` cap | GAP-04, GAP-06 |
| `keyring.env.ts` C-001 first-boot key generation | A one-time boot log line stating the generated key file's path (never its contents) | N/A | N/A | Log at `warn` level on first generation ("no TOVU_ROOT_KEY set; generated a new root key at <path> — back this up") | Operator runbook need: document key-loss consequences (every signature changes; receivers must re-copy) — named follow-up per ADR-PIPE-015 Overall Weaknesses | Never log the key material itself, only its file path and `keyId` | GAP-02, GAP-03 |

## Critical Invariants

| Invariant ID | Scope | Rule | Reason | Enforcement Surface | Test Expectation | Trace |
|---|---|---|---|---|---|---|
| INV-P1 | `server/deps.ts` composition root | `registerWebhookFanout`/`startWebhookDeliveryWorker` must never be called unless the real `KeyringPort`, real `HttpClientPort`, and both SQLite repo adapters are already composed in the same `RouteDeps` | Prevents the exact "half-finished cutover" risk the dispatch named — live traffic must never hit a fake signer, permit-all egress, or in-memory-only storage | `server/deps.ts` (composition-root review), Code Review Agent checklist item | Integration/manual test: attempting to start the worker/fanout against `createFixedSecretSigner`/`permitAllHttpsTargets`/in-memory repos is only possible in the test composition (`app.ts`), never in `deps.ts` | ADR-PIPE-015 Migration Safety Point of No Return |
| INV-P2 | `origin/origin.ts` `isAllowedEgressTarget` (consumed via W-005) | A target URL is allowed only if it is same-origin with a verified origin, or on the workspace's explicit egress allowlist — any parse failure, ambiguity, or unverified-origin state fails closed (`false`), never throws to become an unhandled 500 | Closes GAP-06's live SSRF exposure | `subscriptions.ts`'s existing `validateTargetUrl`, unchanged call shape | Already covered by `origin`'s own test suite (`normalize-origin-candidate.test.ts`, `origin.test.ts`); this pass adds one integration test at `create.ts`'s route boundary asserting a private-IP target is rejected end-to-end | GAP-06, ADR-040 |
| INV-P3 `[internal-invariant]` | `src/http/` module boundary | No file outside `src/http/` or the named composition-root files may import `transport.fetch.ts` (or construct an `HttpTransportAdapter` directly) | The only way "cannot obtain an unguarded client" is true rather than aspirational | C-008 import-boundary canary | CI test fails the build on any violating import | ADR-038 amendment 3 |
| INV-P4 | `webhook_deliveries.payload_json` (C-006) | The envelope written at `enqueue` time and the envelope read back at claim/attempt time must be byte-identical (JSON round-trip, no lossy re-serialization) | A delivery worker running in a later process/tick must sign and send the exact original payload — this is what makes GAP-12's resolution real, not cosmetic | `repo.sqlite.ts`'s `enqueue`/`claimPending`/`findById` | Contract test: enqueue then simulate a fresh process (new repo instance, same DB file) reading the row back — envelope equality assertion | GAP-12, EC-04 |

## Brownfield / Migration Mapping (if applicable)

| Source Behavior / Contract | Target Module / Contract | Preserve / Change | Characterization Evidence | Migration Safety Note |
|---|---|---|---|---|
| `authorize()` checks against `integration.manage` (every existing route + `admin-integrations-routes.test.ts`) | `authorize()` checks against `admin.integrations.manage` | Change: permission string only — the authorization *behavior* (who is allowed) is preserved via the seed-time dual-grant migration | `admin-integrations-routes.test.ts`'s existing `"a principal without integration.manage is denied 403..."` test is updated to assert against the new string and must still pass | Migration must run (`migrateLegacyIntegrationsPermission()`) before route tests assert against the new string, mirroring SPEC-007's settings.write precedent |
| `createFixedSecretSigner(new Map())` wired in `server/app.ts`/`deps.ts` | `createKeyringBackedSigner(EnvOrFileKeyring)` wired in `server/deps.ts` only | Change: real signing material; `app.ts`'s hermetic test composition keeps a fixed/fake signer (test doubles are appropriate there) | `signing.test.ts`'s existing round-trip assertions (`signPayload`/`verifySignature`) are unchanged — this migration only changes where the secret bytes come from, not the signing math | No compatibility risk — no receiver has ever received a real signed delivery yet (GAP-01 means nothing has been sent), so there is no "old signature format" to preserve |
| `permitAllHttpsTargets` injected in `create.ts` | `deps.originRegistry.isAllowedEgressTarget` injected in `create.ts` | Change: real fail-closed check replacing permit-all | `subscriptions.test.ts`'s existing `"createSubscription rejects a target the injected allowlist check disallows"` test already exercises the *shape* of this seam — it just needs the real oracle wired at the route layer, not a new test pattern invented | Existing subscriptions created under permit-all (none exist in production — in-memory only, GAP-05) carry no risk of being retroactively invalidated; a fresh dev server must have a seeded origin (W-006) or every create request 400s |
| No scheduler/subscriber exists (`enqueueDelivery`/`processDueDeliveries` called only from unit tests) | `registerWebhookFanout` + `startWebhookDeliveryWorker` called from `server/deps.ts` | Change: from "never invoked in production" to "invoked continuously" — this is GAP-01's entire resolution | N/A — there is no prior production behavior to characterize; the delivery worker has never run against real traffic | This is the Point of No Return (ADR-PIPE-015 Migration Safety) — gated on Phase 1-2 completing first |

## Test Expectations

- Contract tests: C-005/C-006 — one shared contract-test suite run against `repo.memory.ts` and `repo.sqlite.ts` for both `WebhookSubscriptionRepoPort` and `WebhookDeliveryRepoPort` (matches `settings`/`post`/`presentation` precedent).
- Integration tests: W-003/W-004 (a real guarded `HttpClientPort` + real signer exercised together against a local test receiver, asserting a valid, verifiable `Tovu-Signature`); W-005 (a private-IP target rejected end-to-end at the `create.ts` route boundary); the permission-rename's route-layer tests (all five routes) re-asserted against `admin.integrations.manage`.
- Property/invariant tests: INV-P2 (fail-closed on every parse-failure/ambiguity class ADR-040's own test suite already enumerates), INV-P3 (import-boundary canary), INV-P4 (envelope round-trip durability).
- Characterization tests: None required — GAP-01's activation has no prior production behavior to preserve (nothing has ever been delivered); the permission-rename's characterization is the existing, already-passing route test suite re-pointed at the new string.
- Explicitly N/A suites with reason: No multi-instance/concurrent-claimer test suite — this ADR's `setInterval` scheduling is explicitly single-process (see Pattern Evaluation); a multi-instance test suite is a scale-trigger follow-up, not required now.

## Downstream Handoff Notes

- Coordinator task-generation constraints: The permission-rename phase (identity/permissions.ts, identity/seed.ts, the five route files' `authorize()` calls) and the adapter-build phase (`keyring.env.ts`, `signing.keyring.ts`, `http/client.ts`, `http/transport.fetch.ts`, `repo.sqlite.ts`, `core/ports.ts`'s `subscribeAll`) are safely `[P]` — parallel and independent. The activation phase (`fanout.ts`, `worker.ts`, and their registration in `server/deps.ts` + `server/seed.ts`'s origin-seeding) must be sequenced strictly after BOTH prior phases complete and are reviewed — it is not `[P]` with either.
- TDD focus: Certify C-001 (`EnvOrFileKeyring`) and C-007 (`createHttpClient`)/C-008 (import-boundary canary) first — they are the highest aggregate-risk contracts (a bug in either is a system-wide security defect, not a local one). Then C-005/C-006 (durability contract parity with `repo.memory.ts`). C-003/C-004 (the activation wrappers) last, since they are thin, low-risk delegations to already-certified logic — but their overlap-guard/error-swallowing behavior (C-003) and event-narrowing behavior (C-004) still need direct unit coverage before Phase 4.
- Programmer architecture audit focus: Verify no import of `transport.fetch.ts` outside `src/http/`/`server/deps.ts`/`server/app.ts`; verify `server/app.ts` never calls `registerWebhookFanout`/`startWebhookDeliveryWorker`; verify all five integrations routes check `admin.integrations.manage`; verify `create.ts` no longer references `permitAllHttpsTargets` (should be deletable once the wiring lands); verify the SQLite adapters' `enqueue` writes `payload_json` in the same statement as the delivery row insert.
- Open risks or ambiguities: Root-key loss/rotation operator story is named but not fully specified (ADR-PIPE-015 Overall Weaknesses) — recommend a short follow-up decision before any non-local deployment. `core/origin`'s own persistence gap (no SQLite adapter) is inherited, not fixed, by this pass — flagged as a dependency risk, not silently absorbed into this feature's file scope. A Red-Team pass over this ADR is recommended before implementation begins (Planning Preflight Evidence notes Red-Team was not run for the parent as-built spec).
