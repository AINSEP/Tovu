# Implementation Outline: plugin-system

- Spec: SPEC-005 v1.0.0 (hash: sha256:0464c86642d343d3633cf6ee204af7b96d7b0716b1af17b49cdc99f236266c68)
- ADR: ADR-005-ARCH (`reports/pipeline/005-plugin-system/adr.md`)
- Status: PRODUCED
- Trigger result: Boundary Cross, Contract Change, System Wiring, Data And Persistence, Brownfield Dependency, Critical Cross-Boundary Invariant, Parallelization Ambiguity (all yes — see matrix). Reverse-Spec Or Migration: no.
- Date: 2026-07-28
- Author: Software Architect

## Trigger Decision Matrix

| Trigger | Applies? | Evidence | Source Trace |
|---|---:|---|---|
| Boundary Cross | yes | Feature spans a new package (`packages/sdk`), a new feature module (`src/features/plugin-runtime/`), the existing `src/features/post/` module, `src/core/commands` (gateway), `src/server/routes/admin/*`, and `src/infra/db/schema.ts` | REQ-01…11; ADR §Module/Service Boundaries |
| Contract Change | yes | New `@tovu/sdk` public package contract (REQ-08); new HTTP endpoints (REQ-10); new optional `BeforeSaveHookPort` on existing `CreatePostDeps`/`UpdatePostDeps`; `PostRecord` gains `ext` (REQ-06/11) | REQ-05/06/08/10/11 |
| System Wiring | yes | Boot-time `module.register()` hook registration must complete before any plugin `import()` path is reachable; dynamic `import()` of prebuilt ESM from an install-dir path | ADR "SDK Resolution Mechanism"; BR-01 |
| Data And Persistence | yes | New `posts.ext` column + new `plugin_activations` table (Drizzle, additive); `ext` write must land in the same transaction/write as the SPEC-002 entry save (BR-06) | state.spec.md §7; BR-06/BR-08 |
| Brownfield Dependency | yes | `updatePost`/`createPost` (`src/features/post/post.ts`) are existing, widely-tested functions gaining a new optional dependency; `toAdminPostResponse` (`src/server/http/admin/posts.ts`) gains additive `ext`; `update.ts`'s `captureInverse` must snapshot pre-edit `ext` | REQ-05/06/11; BR-08 |
| Reverse-Spec Or Migration | no | This is a greenfield subsystem wrapped around existing brownfield touch-points, not a source→target behavior remapping | N/A |
| Critical Cross-Boundary Invariant | yes | Hook-then-single-write atomicity spans `plugin-runtime` + `post.ts` + `core/commands`; revert must never re-fire the hook, spanning `post.ts` + `core/commands`/`revert.ts` | BR-06/BR-07/BR-08; AC-17 |
| Parallelization Ambiguity | yes | The SDK package, the loader/capability/hook-registry core, the `word-count` plugin, the HTTP routes, and the DB migration look independently sliceable but all share the `PluginSdk`/`BeforeSaveHookPort`/`PluginManifest` contracts defined here — Coordinator cannot safely mark them `[P]` without this map | This document, in full |

## Module Map

| Module/Domain | Owns | Responsibility | Public Contracts | Dependencies | Notes |
|---|---|---|---|---|---|
| `packages/sdk` (`@tovu/sdk`) | The plugin-author public contract | Define + export the types/constants/identity-helper a plugin author's `server/index.mjs` is built against | C-001…C-004 | None (leaf package; no dependency on `src/features/plugin-runtime`) | Must never import anything from `src/` — it is consumed by plugin code, not by core |
| `src/features/plugin-runtime` | Manifest validation, discovery, load pipeline, capability SDK construction, hook composition, activation state | Everything REQ-01…09 requires except the HTTP surface and the `post.ts` integration point | C-005…C-013 | `@tovu/sdk` (types only, for shared shape reference), `src/infra/db` (via `repo.sqlite.ts` only, per ADR-015 boundary rule) | Deliberately distinct from `src/features/plugins/` (ADR-023 `dataModule`, unrelated) |
| `src/features/post` | Content-entry create/update/read, now including the hook-merge step before persistence | Own the one write path the hook attaches to; stay ignorant of plugin-runtime internals beyond the `BeforeSaveHookPort` contract | C-014 (changed), `BeforeSaveHookPort` (new) | None new (the port is defined here; the implementation is injected at the composition root) | Existing module; changes must be additive/optional per Migration Safety |
| `src/server/boot` | Boot-time SDK resolution hook registration | Register `module.register()` exactly once, before any plugin-loading code path is reachable | C-015 | Node `node:module` built-in only | New file; ordering constraint is CIC-designated (U-002) |
| `src/server/routes/admin/plugins` + `src/server/http/admin/plugins.ts` | REQ-10 HTTP surface | Thin route handlers + DTO mapping, mirroring `admin/posts/*` exactly | C-016, C-017 | `plugin-runtime` (discovery/activation), `core/commands` (gateway) | No new pattern — parity with existing admin routes |
| `src/infra/db` | `posts.ext` column, `plugin_activations` table | Schema-of-record; Drizzle-generated migration | (schema only, no function contract) | N/A | Additive only, per ADR-015 rule 3 (generated migrations, never hand-written) |

## File Map

| File Path | Module | Creates / Changes | Public Contracts Housed | Responsibility | Why This Separation Exists | Notes |
|---|---|---|---|---|---|---|
| `packages/sdk/package.json` | `packages/sdk` | creates | — (package manifest) | Declares `@tovu/sdk`'s `exports` map, blocking deep imports (ADR-005 rule 1) | The `exports` map is the actual enforcement mechanism for "public API = SDK exports only" — it must live in a real package boundary, not a `src/` file | `type: module`; `main`/`types` point at built output |
| `packages/sdk/src/index.ts` | `packages/sdk` | creates | C-001 `definePlugin`, C-002 `PluginDefinition`/`PluginSdk` types, C-003 hook-point constant, C-004 capability tokens | The entire plugin-author-facing runtime + type surface | This is the literal thing REQ-08's snapshot test pins — it must be one clearly-bounded file/entry, not scattered | No re-exports of anything from `src/` — leaf module |
| `src/features/plugin-runtime/manifest.ts` | `plugin-runtime` | creates | C-005 `PluginManifest` type, C-006 `validateManifest()` | Static manifest validation, BR-02 order, collect-all errors | Validation is a distinct, independently-testable concern from loading (mirrors SPEC-004's theme validator) | Consumed by both `discovery.ts` (list-time) and `loader.ts` (enable-time re-validation, BR-05) |
| `src/features/plugin-runtime/discovery.ts` | `plugin-runtime` | creates | C-007 `discoverPlugins()` | Enumerate built-ins + install-dir plugins, produce `PluginDiscoveryRecord[]` (TB-01 order) | Discovery (read-only enumeration) is a different lifecycle phase from loading (side-effecting `import()`) — REQ-02/03 treat them as distinct steps | Legacy mode (no install dir) returns built-ins only (EC-09) |
| `src/features/plugin-runtime/loader.ts` | `plugin-runtime` | creates | C-008 `loadPlugin()` | BR-01's exact ordered pipeline: integrity → sdkRange → import → capability-SDK invoke → hook attach | This is the single most security-critical unit in the feature — isolating it as its own file makes the CIC-designated ordering constraint (U-001) auditable as one unit | Must call `capability-sdk.ts` and `hook-registry.ts`, never construct either inline |
| `src/features/plugin-runtime/capability-sdk.ts` | `plugin-runtime` | creates | C-009 `buildCapabilityScopedSdk()` | Build the per-load `PluginSdk` handle; ungranted surfaces are `CAPABILITY_DENIED`-throwing stubs, never absent/`undefined` | REQ-04's "SDK object exposes only the granted surface" plus AC-05's specific error code demand a dedicated builder, not ad hoc conditionals inline in the loader | CIC-designated (U-003) |
| `src/features/plugin-runtime/hook-registry.ts` | `plugin-runtime` | creates | C-010 `runBeforeSave()` (implements `BeforeSaveHookPort`) | Compose all enabled plugins' `content.entry.beforeSave` filters in TB-01 order, merge `ext` per-plugin-namespace, fail-closed on any error | This is the concrete adapter for the port `post.ts` depends on — keeping it in `plugin-runtime` (not `post.ts`) is what keeps `post.ts` plugin-ignorant | CIC-designated (U-004) |
| `src/features/plugin-runtime/activation.ts` | `plugin-runtime` | creates | C-011 `setPluginEnabled()`, C-012 `getActivation()`, `PluginActivationRepoPort` | Gateway-backed enable/disable feature functions, mirroring `presentation.ts`'s `setActiveTheme`/`getPresentationSettings` | Parity with the existing, proven activation pattern in this codebase — no new shape invented | `PluginActivationRepoPort` is the contract `repo.memory.ts`/`repo.sqlite.ts` satisfy |
| `src/features/plugin-runtime/repo.memory.ts`, `repo.sqlite.ts` | `plugin-runtime` | creates | C-013 (adapter pair for `PluginActivationRepoPort`) | Persistence adapters, rule-of-two from day one | Every other feature-owned repo port in this codebase ships both adapters together (Article IV) | `repo.sqlite.ts` is the only file besides `schema.ts`/`content-db.ts` allowed to import Drizzle (ADR-015 rule 2) |
| `src/features/plugin-runtime/built-ins/word-count/` | `plugin-runtime` | creates | (no new public contract — consumes C-001…C-004 like any plugin) | REQ-09's bundled dogfood plugin, exercised through the exact same validator/loader path as a site-installed plugin | Proves the loop end to end; must not take a shortcut path that a real plugin couldn't also take, except skipping the tarball/integrity-file step (ADR §Decision item 4) | Tokenization per REQ-09's now-pinned algorithm (RT-005) |
| `src/server/boot/plugin-sdk-resolver.ts` | `server/boot` | creates | C-015 `registerPluginSdkResolver()` | Registers the `module.register()` hook exactly once, before the load pipeline is reachable | Boot-sequencing is a distinct concern from the loader itself; isolating it makes the CIC-designated ordering constraint (U-002) a single auditable call site | Called from `src/index.ts`, not from `app.ts`/`deps.ts` (mirrors the existing `runBootGateOrExit` placement rationale — the one real top-level boot path) |
| `src/server/routes/admin/plugins/list.ts`, `set-enabled.ts` | server routes | creates | C-016 (route registrars) | `PLUGINS_LIST`/`PLUGIN_SET_ENABLED` HTTP surface, gateway-wrapped for the mutation | Mirrors `admin/posts/create.ts`/`update.ts` exactly — no new routing pattern | `set-enabled.ts` follows `create.ts`'s `executeCommand` wrapping shape |
| `src/server/http/admin/plugins.ts` | server http | creates | C-017 `toAdminPluginResponse()` | DTO mapping for the list/enable response shapes | Mirrors `admin/posts.ts`'s `toAdminPostResponse()` | — |
| `src/features/post/post.ts` | `post` (existing) | changes | C-014 (changed): `PostRecord.ext`; `BeforeSaveHookPort` (new port, defined here); `CreatePostDeps.beforeSaveHook?`/`UpdatePostDeps.beforeSaveHook?` (new, optional) | Own the hook-merge step immediately before the single `repo.save()` call | This is the one place BR-06's "same transaction" requirement can be satisfied without a second write | Optional dependency — absent ⇒ no-op merge, per behavior.spec.md §10 |
| `src/features/post/repo.memory.ts`, `repo.sqlite.ts` | `post` (existing) | changes | (no new contract — `PostRecord.ext` flows through existing `save`/`toRecord`) | Persist/read the new `ext` field | Same JSON-column handling `bodyJson` already uses | `repo.sqlite.ts`: `ext: text("ext", {mode:"json"})` column, JSON.stringify/parse like `bodyJson` |
| `src/server/http/admin/posts.ts` | server http (existing) | changes | `toAdminPostResponse()` (changed, additive) | Expose `ext` on `AdminPostEnvelope` only when non-empty (REQ-11) | Additive-only DTO change — no existing field renamed/removed | — |
| `src/server/routes/admin/posts/update.ts` | server routes (existing) | changes | (no new contract — `captureInverse`'s returned object gains one field) | Snapshot pre-edit `ext` alongside `title`/`slug`/`bodyJson`/`status` (BR-08) | Required for AC-17's atomic revert guarantee | One-line addition to the existing `captureInverse` closure |
| `src/infra/db/schema.ts` | `infra/db` (existing) | changes | (schema only) | Add `posts.ext` column + `pluginActivations` table | Drizzle is the schema of record (ADR-015); migration is generated, never hand-written | Run `drizzle-kit generate` after editing |
| `package.json` (root) | repo root | changes | (no function contract) | Add `"workspaces": ["packages/*"]`; add `"engines": {"node": ">=20.6.0"}` | Required for `packages/sdk` to exist as a real installable package and for `module.register()` to be available | Programmer must smoke-test `apps/admin`'s separate `npm --prefix` install/build still works unchanged (ADR Consequences) |

## Contract Map

| Contract ID / Name | File | Owner Module | Kind | Why Needed | Job | Inputs | Outputs | Validation | Errors | Effect Boundary | Complexity / Resource View | Aggregate-Risk Note | Spec/ADR Trace | Test Seam / Expectation |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| C-001 `definePlugin` | `packages/sdk/src/index.ts` | `packages/sdk` | exported function (SDK public API) | Plugin-author registration entry point (REQ-08) | Wrap a `PluginDefinition` for type inference; identity at runtime | `PluginDefinition` (`{ setup(sdk) => void }`) | The same object, typed as `Plugin` | None (pure identity) | None | Pure, no I/O | O(1) | N/A | REQ-08, ADR Decision item 2 | Public-API snapshot test (REQ-08/AC-10) |
| C-002 `PluginDefinition`/`PluginSdk` types | `packages/sdk/src/index.ts` | `packages/sdk` | exported types | Shape of the capability-scoped handle a plugin's `setup` receives | Define `content?: {read, extend}`, `addFilter?` surfaces, each optional per capability | — (compile-time only) | — | — | — | — | — | N/A | REQ-04/05/08; ADR-024 §3 (serializable-only) | Type-level; exercised by the snapshot test's type-check step |
| C-003 hook-point constant | `packages/sdk/src/index.ts` | `packages/sdk` | exported constant | `HOOK_CONTENT_ENTRY_BEFORE_SAVE = "content.entry.beforeSave"` | Single source of truth for the hook name, both plugin- and core-side | — | string literal | — | — | Pure | O(1) | N/A | REQ-05 | Snapshot test |
| C-004 capability tokens | `packages/sdk/src/index.ts` | `packages/sdk` | exported constants | `CONTENT_READ`/`CONTENT_EXTEND`/`HOOKS_ATTACH` | Single source of truth for the v1 capability vocabulary | — | string literals | — | `CAPABILITY_UNKNOWN` if a manifest uses any other string (validated in `manifest.ts`, not here) | Pure | O(1) | N/A | REQ-04 | Snapshot test |
| C-005 `PluginManifest` | `src/features/plugin-runtime/manifest.ts` | `plugin-runtime` | exported type | Typed shape of `tovu.plugin.json` | Mirror state.spec.md §2 exactly | — | — | — | — | — | — | N/A | REQ-01 | Type-level, exercised by C-006's tests |
| C-006 `validateManifest()` | `src/features/plugin-runtime/manifest.ts` | `plugin-runtime` | exported function | Static, collect-all manifest validation (BR-02) | Given a parsed manifest + folder name, return every applicable error or none | `{ manifest: unknown, folderName: string, builtInIds: string[] }` | `{ errors: ValidationError[] }` (empty ⇒ valid pending integrity/sdkRange) | Package-level → manifest → identity → capabilities → hooks → fields, in that order, collecting all (BR-02) | `MANIFEST_MISSING`/`MALFORMED`/`ID_FOLDER_MISMATCH`/`ID_DUPLICATE`/`SHADOWS_BUILT_IN`/`CAPABILITY_UNKNOWN`/`HOOK_UNKNOWN`/`FIELD_PATH_INVALID`/`QUERYABLE_UNSUPPORTED_V1`/`ENGINE_UNSUPPORTED` (errors.spec.md §3) | Pure (no I/O — takes parsed JSON, not a file path) | O(fields) per manifest — bounded, no loop over external input | Adversarial case: a manifest with every rule violated at once must report ALL of them (BR-02's collect-all, not first-failure) | REQ-01/04/05/06; BR-02/BR-03; AC-07/AC-08/AC-12 | Fixture-plugin corpus (valid/tampered/incompatible/bad-hook/bad-field/queryable) per spec-manifest.md's TDD read set |
| C-007 `discoverPlugins()` | `src/features/plugin-runtime/discovery.ts` | `plugin-runtime` | exported function | Enumerate all plugins (built-in + site) with status | Read built-in registry + scan install-dir, call `validateManifest()` per candidate, order per TB-01 | `{ installDir?: string, builtIns: BuiltInPluginSource[] }` | `PluginDiscoveryRecord[]` | Delegates to C-006; does not itself re-implement validation rules | None thrown — errors are recorded per-record in `errors[]`, never rejected as a whole | Read-only filesystem I/O (no `import()` — discovery never executes plugin code) | O(n) over installed plugin count, one filesystem stat/read per candidate | N/A | REQ-02/03; EC-08/EC-09; TB-01 | Integration test with a fixture install-dir tree |
| C-008 `loadPlugin()` | `src/features/plugin-runtime/loader.ts` | `plugin-runtime` | exported function | Execute BR-01's ordered pipeline for one plugin (boot + enable-time) | integrity → sdkRange → `import()` → `buildCapabilityScopedSdk()` → invoke `setup()` → attach hooks via `hook-registry.ts` | `{ record: PluginDiscoveryRecord, manifest: PluginManifest, entryPath: string }` | `{ loaded: true } \| { loaded: false, reason: ErrorCode }` | Short-circuits at first pipeline failure (BR-01) — never partially registers | `INTEGRITY_FAILED`/`SDK_RANGE_UNSATISFIED`/`CODE_ENTRY_MISSING` | Effectful: file I/O (integrity hash), dynamic `import()` (arbitrary code execution) | O(file count) for integrity hashing; `import()` cost is plugin-defined and unbounded — no budget stated (ADR scalability axis gap) | See CIC U-001 (verify-before-import ordering) | REQ-03; BR-01; INV-04; AC-03/AC-04 | Integration test: tampered artifact ⇒ assert `import()` never invoked (spy/counter), no partial registration |
| C-009 `buildCapabilityScopedSdk()` | `src/features/plugin-runtime/capability-sdk.ts` | `plugin-runtime` | exported function | Build the per-plugin `PluginSdk` handle | Project only declared-capability surfaces as real closures; every other surface is a `CAPABILITY_DENIED`-throwing stub | `{ pluginId: string, capabilities: string[], coreDeps: {...} }` | `PluginSdk` handle | — | `CAPABILITY_DENIED` (thrown lazily, on ungranted-surface invocation, not at build time) | Pure construction (no I/O at build time; effects happen when the plugin later calls a granted method) | O(capability count) — trivially small | See CIC U-003 (stub-vs-absent distinction) | REQ-04; BR-04-cap; AC-05; EC-06; INV-02 | Unit test: call every capability surface with 0/1/2/3 granted capabilities, assert exact error code on ungranted calls |
| C-010 `runBeforeSave()` (`BeforeSaveHookPort` impl) | `src/features/plugin-runtime/hook-registry.ts` | `plugin-runtime` | exported function (implements the port declared in `post.ts`) | Compose all enabled plugins' filters, merge `ext`, fail-closed | `entry: Readonly<ContentEntryDraftSnapshot>` (plain, serializable) | `Promise<JsonObject>` (the merged, namespaced `ext` patch) | Per-plugin `FIELD_PATH_INVALID`/`FIELD_TYPE_MISMATCH` validation (BR-06) before merge | `PLUGIN_HOOK_FAILED` (thrown, mapped to 500 by the calling route) on any filter throw, `CAPABILITY_DENIED`, or invalid write (BR-07) | Effectful only insofar as it invokes plugin code; performs no persistence itself (the caller, `post.ts`, persists the merged result) | O(enabled-plugin count) per save, each plugin's own cost unbounded (no budget — same scalability gap as C-008) | See CIC U-004 (fail-closed-before-persist) | REQ-05/06; BR-04/BR-06/BR-07; TB-01; AC-01/AC-05/AC-06/AC-07; EC-06/EC-10 | Integration test at the `updatePost`/route level (Article V) |
| C-011 `setPluginEnabled()` | `src/features/plugin-runtime/activation.ts` | `plugin-runtime` | exported function | Enable/disable through the gateway | `{ workspaceId, pluginId, enabled, discovery: PluginDiscoveryRecord[] }` | `{ activation: PluginActivationRecord }` | `status "valid"` required to enable (BR-05) | `PluginNotFoundError`/`PluginInvalidError`/`PluginIncompatibleError` (mapped by the route to 404/422/422) | Effectful: calls loader (enable) / unloads (disable); the route wraps this in `executeCommand` for the gateway/change-set | O(1) plus C-008's cost on enable | N/A (parity with `presentation.ts`'s `setActiveTheme`) | REQ-07; BR-05; AC-02/AC-13 | Integration test mirroring `presentation.test.ts`'s enable/disable shape |
| C-012 `getActivation()` | `src/features/plugin-runtime/activation.ts` | `plugin-runtime` | exported function | Read current activation state for projection into `PLUGINS_LIST` | `{ workspaceId, pluginId }` | `PluginActivationRecord \| null` | — | — | Read-only | O(1) | N/A | state.spec.md §5 `getActivation` | Unit test |
| C-013 `PluginActivationRepoPort` + adapters | `src/features/plugin-runtime/{activation,repo.memory,repo.sqlite}.ts` | `plugin-runtime` | port + 2 adapters | Persistence contract (Article IV rule-of-two) | `save`/`findByWorkspaceAndPlugin`(-equivalent)/`listAll` | per-method | per-method | — | — | Adapter I/O | O(1) per call | N/A | REQ-07; state.spec.md §7 | Contract test (both adapters satisfy the same port test suite, mirrors `PresentationSettingsRepoPort`'s existing test shape) |
| C-014 `PostRecord.ext` + `BeforeSaveHookPort` | `src/features/post/post.ts` | `post` | changed type + new port (defined here, implemented by C-010) | The integration seam between core content and the plugin system | `runBeforeSave(entrySnapshot): Promise<JsonObject>` | merged `ext` object | — | Propagates `PLUGIN_HOOK_FAILED` unchanged (post.ts does not catch it — the route does) | Called once, immediately before the single `deps.repo.save(post)` call in both `createPost` and `updatePost` | O(1) call-site overhead; actual cost is C-010's | See CIC U-004/U-005 | REQ-05/06/11; BR-06/BR-08; AC-01/AC-17 | Integration test: existing `post.test.ts` fixtures must pass unchanged with `beforeSaveHook` absent (regression seam) |
| C-015 `registerPluginSdkResolver()` | `src/server/boot/plugin-sdk-resolver.ts` | `server/boot` | exported function, called once from `src/index.ts` | Register the `module.register()` hook redirecting `@tovu/sdk`/`@tovu/*` before any plugin `import()` path is reachable | none (reads the runtime's own bundled SDK path) | none (side effect: process-global hook registration) | Must be idempotent-safe if accidentally called twice (should no-op or throw clearly, not double-register) | — | Effectful, process-global, boot-time only | O(1) | See CIC U-002 (registration-before-reachability ordering) | ADR "SDK Resolution Mechanism" | Integration/process-level test only — not unit-testable in isolation (ADR testability axis note) |
| C-016 `PLUGINS_LIST`/`PLUGIN_SET_ENABLED` route registrars | `src/server/routes/admin/plugins/{list,set-enabled}.ts` | server routes | exported `ContentRouteRegistrar`-shaped functions | REQ-10 HTTP surface | Mirror `admin/posts/{create,update}.ts` exactly: guard workspaceId, call `executeCommand` (set-enabled only), map errors to status codes | HTTP req/res | HTTP response per api.spec.md §5 | Route-level guard (`PLUGIN_NOT_FOUND` before gateway, per errors.spec.md §4) | 404/422/409/500 per api.spec.md §6 | Effectful (HTTP + gateway call) | O(1) plus C-011's cost | N/A | REQ-10; AC-11 | Integration test at the HTTP layer (Article V) |
| C-017 `toAdminPluginResponse()` | `src/server/http/admin/plugins.ts` | server http | exported function | DTO mapping | `PluginDiscoveryRecord[]` (+ `PluginActivationRecord` projection) | api.spec.md §5 shape | — | — | Pure | O(n) | N/A | REQ-10 | Unit test, mirrors `toAdminPostResponse` |

## Wiring Map

| Flow ID | Source | Transport/Call Type | Target | Payload/Contract | Ordering/Retry/Idempotency | Failure Handling | Trace |
|---|---|---|---|---|---|---|---|
| W-001 | `src/index.ts` (boot) | direct call | `registerPluginSdkResolver()` (C-015) | none | Must run before any route is registered or any request can trigger plugin discovery/enable (CIC U-002) | Throws on double-registration; boot fails loudly rather than silently double-hooking | ADR SDK Resolution Mechanism; CIC U-002 |
| W-002 | admin route `set-enabled.ts` (C-016) | direct call via `executeCommand` (gateway) | `setPluginEnabled()` (C-011) → `loadPlugin()` (C-008) on enable | `{workspaceId, pluginId, enabled}` | Gateway order: authorize → idempotency check → captureInverse → execute → record (existing `executeCommand` contract, unchanged) | `DuplicateCommandError`/`ForbiddenError` handled by the route exactly like `admin/posts/*`; `PluginInvalidError`/`PluginIncompatibleError` mapped to 422 before the gateway is entered (BR-05) | REQ-07; BR-05; api.spec.md §6 |
| W-003 | `post.ts`'s `createPost`/`updatePost` | direct call (optional dep) | `runBeforeSave()` (C-010) via `BeforeSaveHookPort` | `Readonly<ContentEntryDraftSnapshot>` | Must complete (resolve or throw) before the single `deps.repo.save(post)` call in the same function invocation (CIC U-004) | A thrown `PLUGIN_HOOK_FAILED` propagates out of `createPost`/`updatePost` uncaught by them; the calling route catches it and returns 500, matching the existing error-propagation pattern already used for `PostValidationError` etc. | BR-06/BR-07; AC-01/AC-05; EC-10 |
| W-004 | `src/server/routes/admin/posts/update.ts`'s `captureInverse` | direct call (existing gateway seam) | `deps.postRepo.findById` (existing) | reads `priorPost.ext` (new field, already present on the existing read) | Runs BEFORE `execute()` per `executeCommand`'s documented order — already true structurally, only the returned object's field list changes | Unchanged failure handling — `captureInverse` returning `null` already means "not found," `execute()` throws 404 | BR-08; AC-17 |
| W-005 | revert path (`core/commands/revert.ts`, existing) | direct call (existing) | `PostRepoPort.save()` directly (raw restore, NOT `updatePost`) | the stored `inversePayload` (now including `ext`) | Must NOT call `updatePost`/`createPost` (which would re-run W-003/C-010) — this is the defining ordering constraint of CIC U-005 | Existing revert error handling, unchanged | BR-08; AC-17; CIC U-005 |
| W-006 | plugin's `server/index.mjs` (dynamic `import()`, inside C-008) | ESM dynamic import, specifier `@tovu/sdk` | resolved via the W-001 hook to `packages/sdk`'s built output | N/A (module resolution, not a data payload) | Only reachable after W-001 has completed (process-wide) | If W-001 never ran, this import falls through to ordinary Node resolution and likely fails to resolve (`ERR_MODULE_NOT_FOUND`) — an observable, loud failure, not a silent misresolution, given `<install-dir>` is not an ancestor of the runtime's own `node_modules` | ADR SDK Resolution Mechanism; CIC U-002 |

## Data And Side-Effect Boundaries

| Boundary | Owner | Reads | Writes | Side Effects | Consistency / Transaction Rule | Migration / Dual-Write Path |
|---|---|---|---|---|---|---|
| `posts.ext` column | `src/features/post` (repo adapters) | `post.ts`'s hook-merge step, `toAdminPostResponse`, `core/commands` revert restore | `post.ts`'s `createPost`/`updatePost` only (via the single `repo.save()` call); revert restore writes it too (raw, no hook re-fire) | None beyond the row write itself | Written in the exact same `repo.save()` call as every other `PostRecord` field — no separate transaction, no dual-write (BR-06) | N/A — additive column, default `{}` |
| `plugin_activations` table | `src/features/plugin-runtime` (repo adapters) | `discovery.ts` (status projection), `activation.ts` | `activation.ts`'s `setPluginEnabled()` only, via the gateway | Gateway change-set row + (on enable) `loadPlugin()`'s side effect of importing/registering the module | One applied change set per transition (INV-05); disable/enable never touches `ext` (INV-03) | N/A — new table |
| Plugin install-dir (`plugins/<id>/<version>/`) | filesystem, read-only from this feature's perspective | `discovery.ts`, `loader.ts` | Only `tovu plugin build`/install tooling (out of this ADR's scope — REQ-02 install layout, not this feature's write path) | Dynamic `import()` executes plugin code as a read of the filesystem artifact | N/A (no transaction — filesystem is not transactional; integrity hash is the correctness check, per BR-01) | N/A |

## Observability And Operational Expectations

| Surface / Flow | Required Signals | Correlation / Trace Context | Metrics | Logs | Alert / Runbook Need | Privacy / Secret Constraints | Trace |
|---|---|---|---|---|---|---|---|
| `loadPlugin()` (C-008) failure | Structured error per validation/integrity/sdkRange failure | Plugin id + error code | N/A (no metrics infra introduced by this feature) | One structured log line per discovery-time failure, code + file + message (errors.spec.md format) | N/A this slice — quarantine/alerting is OQ-06, explicitly deferred | No secrets in plugin manifests are expected; do not log full manifest contents if a future field could carry one | REQ-03; behavior.spec.md §2; VIII |
| `runBeforeSave()` (C-010) fail-closed | One structured error per thrown/denied/invalid hook execution | Plugin id + cause (thrown error message or `CAPABILITY_DENIED`/`FIELD_*` code) | N/A | Logged once per failure (EC-10), correlated to the failed save's `changeSetId` when the save would otherwise have produced one | Recommended runbook note (non-blocking, RT-004): "a site-wide save outage traces to one enabled plugin; disable via `PATCH .../plugins/:id`" — worth adding to operator-facing docs when OQ-02's UI ships | Plugin filters receive entry content, not credentials — no new secret-handling surface | BR-07; VIII |
| `PLUGIN_SET_ENABLED` / `PLUGIN_ENABLE` change sets | `change-set.applied`/`.reverted` events (existing gateway mechanism, unchanged) | `changeSetId` (existing correlation id) | N/A | N/A beyond the existing gateway audit trail | N/A | N/A | REQ-07; Constitution Article VIII |

## Critical Invariants

| Invariant ID | Scope | Rule | Reason | Enforcement Surface | Test Expectation | Trace |
|---|---|---|---|---|---|---|
| INV-001 | `plugin-runtime/loader.ts` (C-008) | Integrity + `sdkRange` checks must both pass before any `import()` of the plugin's code | A tampered or incompatible plugin must never execute, even transiently at module-eval time | C-008's internal ordering | Integration test (see CIC U-001) | INV-04; BR-01; AC-03/AC-04 |
| INV-002 | `[internal-invariant]` `server/boot/plugin-sdk-resolver.ts` (C-015) | The `module.register()` hook must be registered before any code path that could trigger a plugin `import()` is reachable | A plugin loaded before the hook is registered could fall through to ordinary Node resolution and pick up a locally-planted, spoofed `@tovu/sdk` | Boot sequencing in `src/index.ts` | Integration/process-level test (see CIC U-002) | ADR SDK Resolution Mechanism |
| INV-003 | `post.ts` / `core/commands/revert.ts` boundary | The revert/restore code path must never invoke `createPost`/`updatePost` (and therefore never `runBeforeSave()`) | A revert is a pre-image restore, not a genuine save (BR-08) — re-firing the hook would recompute or corrupt `ext` using whichever plugins happen to be enabled at revert time, not at original-save time | `revert.ts`'s call target (must be a raw `repo.save()`, not the feature function) | Integration test (see CIC U-005) | BR-08; AC-17 |

## Test Expectations

- Contract tests: `PluginActivationRepoPort` (C-013, both adapters against one shared test suite, mirrors `PresentationSettingsRepoPort`); `@tovu/sdk` public-API snapshot test (C-001…C-004, REQ-08/AC-10).
- Integration tests: `PLUGINS_LIST`/`PLUGIN_SET_ENABLED` at the HTTP layer (C-016); `word-count` enable → save → `ext` written → disable → revert (AC-01/AC-02/AC-13/AC-17) exercised through the real `updatePost` write path, not a mock; the fixture-plugin corpus (valid/tampered/incompatible/bad-hook/bad-field/queryable) exercised through `discoverPlugins()`+`loadPlugin()` together.
- Property/invariant tests: INV-001 (verify-before-import ordering), INV-002 (resolver-registration-before-reachability), INV-003 (revert never re-fires hook) — all three via the companion Critical Internal Constraints artifact's designated verification surfaces.
- Characterization tests: N/A — no reverse-spec/migration surface in this feature.
- Explicitly N/A suites with reason: UI component tests → `ui.spec.md` omitted this slice (OQ-02); orchestrator/async-flow tests → no orchestrator layer this slice (spec-manifest.md A-05).

## Downstream Handoff Notes

- Coordinator task-generation constraints: `packages/sdk` (C-001…004) and `plugin-runtime`'s manifest/discovery/loader/capability-sdk/hook-registry core (C-005…010) can be built in parallel with the DB migration (`schema.ts`) and the HTTP routes (C-016/017), since all three depend only on the contracts fixed in this document, not on each other's internals. `word-count` (built-ins) depends on C-001…010 all existing first (sequential, not parallel). The `post.ts` integration (C-014, W-003) and the revert-path fix (W-005) should be sequenced as their own phase, gated by TDD certifying CIC U-004/U-005 first, given their brownfield/regression sensitivity.
- TDD focus: prioritize CIC-designated units (U-001…U-005 in the companion artifact) before general coverage — these are the load-bearing security/correctness units, not incidental glue.
- Programmer architecture audit focus: confirm `src/features/plugin-runtime/` never gets confused with or merged into `src/features/plugins/`; confirm the `BeforeSaveHookPort` dependency is genuinely optional (existing `post.test.ts` fixtures pass unmodified); confirm the revert path (W-005) calls a raw repo write, not `updatePost`.
- Open risks or ambiguities: the `plugin.tier` manifest field gap and the ABI-freeze doc-sync note are both recorded in the ADR's Risks section, not here — they are spec/doc housekeeping, not implementation-outline structural gaps.
