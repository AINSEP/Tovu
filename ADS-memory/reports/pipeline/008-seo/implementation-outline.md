# Implementation Outline: seo

- Spec: SPEC-008 v1.0.0 (hash: sha256:5647a1176b49a39923b174865ecebeec4115078ec0625c6a58087815c4e0e128)
- ADR: ADR-PIPE-008 (SEO — Implementation Architecture)
- Status: PRODUCED
- Trigger result: Boundary Cross, Contract Change, Data And Persistence, Brownfield Dependency, Critical Cross-Boundary Invariant, Parallelization Ambiguity
- Date: 2026-07-13
- Author: Software Architect Agent

> Use this artifact only for post-ADR, pre-tasks structure. It defines module boundaries, public/exported contracts, wiring, data boundaries, and critical invariants. It must not contain pseudo-code, private helper inventories, or task sequencing.

## Trigger Decision Matrix

| Trigger | Applies? | Evidence | Source Trace |
|---|---:|---|---|
| Boundary Cross | yes | Feature crosses `src/seo` (domain), `src/server/http/site` + `src/server/routes/{admin/seo,site}` (render + API), `apps/admin/src/sections/{Seo,PostEditor}.tsx` (UI), `src/features/post`, `src/features/settings`, `src/identity` (three existing modules extended) | REQ-01, REQ-06, REQ-08, REQ-09, REQ-11 |
| Contract Change | yes | 8 new HTTP endpoints; new `SeoQueryPort` implementation; new `page-head.ts` registry contracts; new outbox event names; new `SettingValueSchema` variant | api.spec.md §1, ADR-PIPE-008 Decision §2/§3/§5 |
| System Wiring | yes | Outbox event subscriptions (3 new event names) drive sitemap cache invalidation; boot-time idempotent settings-definition registration (`seoReady`) | ADR-PIPE-008 Decision §3/§5 |
| Data And Persistence | yes | New nullable `posts.seo_ext_json` column; 7 new `site.seo.*` setting definitions; in-module sitemap cache | REQ-01, REQ-02, REQ-11, state.spec.md §1 |
| Brownfield Dependency | yes | Extends 3 existing, real, already-shipping modules (`features/post`'s `updatePost`, `features/settings`'s schema vocabulary, `server/http/site/render.ts`'s render path) that ADR-032's original stub assumed were either unnecessary to touch or already provided the needed surface | ADR-PIPE-008 Context (5 architecture-discovered gaps) |
| Reverse-Spec Or Migration | no | Not a reverse-spec extraction; brownfield touches are additive extensions, not a migration (see ADR-PIPE-008 Migration Safety — no data to migrate) | — |
| Critical Cross-Boundary Invariant | yes | INV-03 (no raw HTML crossing the head seam), INV-04/05 (sitemap eligibility), INV-07 (no local origin authority), INV-08 (workspace-prefixed cache key), INV-09 (single read surface) all span the write chokepoint + head-fold + sitemap cache | feature.spec.md Invariants |
| Parallelization Ambiguity | yes | Schema column, settings-vocabulary extension, and the `post.ts` outbox extension must land before the SEO module's write-service/settings/sitemap code and the API routes can be built against real contracts; the `page-head.ts` registry must exist before `render.ts`/`pages.ts` are touched or before SEO's own contributor is written | ADR-PIPE-008 Rationale |

## Module Map

| Module/Domain | Owns | Responsibility | Public Contracts | Dependencies | Notes |
|---|---|---|---|---|---|
| `seo` (`src/seo/`) | `posts.seo_ext_json` reads/writes (via `post`'s repo); `site.seo.*` setting reads/writes (via `settings`); the in-module sitemap cache; SEO's own `page.head`/`seo.sitemap.collect` contributor | Effective-meta evaluator, per-entry write chokepoint, settings mapping, sitemap/robots build + cache, analysis | C-001..C-011 | `post` (`PostRepoPort`), `routing` (`urlFor`), `settings` (`getEffective`, write-service), `media` (read-only repos), `identity` (`authorize()`) | No new port anywhere; `SeoQueryPort` (existing stub) is the one public read surface (INV-09) |
| `server/http/site` (existing, extended) | The `page.head` registry; head-element serialization | Ordered fold of all registered head contributors, dedup/tie-break, sanitize-to-markup | C-012..C-014 | none (pure, no I/O) | Core-owned seam, not SEO-owned — a future non-SEO contributor registers here directly |
| `server/routes/{admin/seo,site}` (new + extended) | HTTP wiring for SEO | Route registration + `authorize()` gating + error mapping + `PageHeadContext` construction | C-015..C-022 | `seo`, `identity`, `server/http/site` | Mirrors `routes/admin/settings/` one-file-per-endpoint shape |
| `post` (existing, extended) | Outbox event emission on publish/update/unpublish transitions | Detect the 4-row transition table, enqueue at most one event per `updatePost()` call | C-023 | `core` (`OutboxPort`) | No behavior change to any existing caller that ignores the new events |
| `settings` (existing, extended) | The new `{type:"json"}` schema variant | Accept an opaque JSON value as a setting value | C-024 | none | Used exactly once by this feature (`site.seo.robots_rules`) |
| `identity` (existing, extended) | `admin.seo.manage` permission string | Catalog registration only | — | none | Mirrors `navigation.manage`/`integration.manage`'s registration call shape |
| `apps/admin` UI (existing, extended) | `Seo.tsx` screen; `PostEditor.tsx`'s embedded SEO panel | Renders/edits per-entry + site-level SEO via the admin API | C-025 (UI-facing, not typed here — see ui.spec.md) | `server/routes/admin/seo` (via fetch) | `Seo.tsx` mirrors `Appearance.tsx`; the entry panel is embedded, not standalone (ui.spec.md §6) |

## File Map

| File Path | Module | Creates / Changes | Public Contracts Housed | Responsibility | Why This Separation Exists | Notes |
|---|---|---|---|---|---|---|
| `src/seo/seo.ts` | seo | creates | C-001 `getEntryMeta`, C-002 `analyzeEntry` | Pure effective-meta evaluator + analysis, the "one evaluator" every consumer (admin preview, public render, analyze) shares (REQ-05) | Isolating the pure evaluator lets TDD certify every precedence rule (behavior.spec.md §1.1) as direct unit tests with no I/O | Never writes anything — reads only, via injected `PostRepoPort`/settings/routing/media deps |
| `src/seo/page-head-contributor.ts` | seo | creates | C-003 `seoPageHeadHook` | SEO's own `PageHeadHook` implementation — maps `getEntryMeta`'s result into ordered, deduped `HeadElement[]` per behavior.spec.md §2.1's priority bands | Kept separate from `seo.ts` so the contributor's registration (a side effect at boot) is isolated from the pure evaluator it wraps | Registered into `page-head.ts`'s registry at `server/app.ts` boot — never imported directly by `render.ts` |
| `src/seo/write-service.ts` | seo | creates | C-004 `setEntrySeoOverrides` | THE per-entry write chokepoint (REQ-01/02/03); authorize -> validate -> merge -> `postRepo.save` -> direct cache-invalidation on a noindex/canonical-affecting write | Isolating the chokepoint lets Code Review enforce "only this file calls `postRepo.save` for `seo_ext_json`" as a file-boundary check | Reuses `PostRepoPort` directly — no new repo/port |
| `src/seo/settings.ts` | seo | creates | C-005 `getSeoSettings`, C-006 `setSeoSettings`, C-007 `ensureSeoSettingDefinitions` | Maps the 7 `site.seo.*` ledger definitions to/from the `SeoSettings` shape; validates `titleTemplate`/`robotsRules` before calling the ledger's `write-service.set` | Keeps every ADR-028 namespace/key/schema decision (ADR-PIPE-008 Decision §3) in one file, distinct from the ledger itself | `defaultRobots` composed/decomposed here — the ledger never sees an object for that field |
| `src/seo/sitemap.ts` | seo | creates | C-008 `buildSitemap`, C-009 `buildRobots`, C-010 `regenerateSitemapCache` [internal-invariant cache map], C-011 `invalidateSitemapCache` | Cache-backed sitemap/robots build; the empty `seo.sitemap.collect` registry (OQ-01) | Isolates the one stateful (in-process cache) surface in this module from the otherwise-pure `seo.ts`/`settings.ts` | Cache key always `ws:{workspaceId}:seo:sitemap` (INV-08); `invalidateSitemapCache` is idempotent (a repeat call for an already-clear key is a no-op) |
| `src/seo/media.ts` | seo | creates | C-013 `resolveSeoImageRef` (numbering continued below with page-head module) | Media-ref-to-URL resolution for `ogImage`/`twitterImage`, fail-soft on any lookup miss (EC-07) | Isolated so `seo.ts`'s evaluator stays synchronous-shaped and this file owns the one place SEO reads `media`'s repos | Never generates a rendition — only resolves an already-generated one; omits the field otherwise |
| `src/seo/errors.ts` | seo | creates | typed error classes (`SeoFieldValidationError`, `SeoInvalidCanonicalUrlError`, `SeoSettingsValidationError`, `SeoEntryNotFoundError`) | Typed domain errors -> route-layer error-code mapping | Mirrors `settings/errors.ts`'s one-class-per-code convention | — |
| `src/server/http/site/page-head.ts` | server/http/site | creates | C-012 `registerPageHeadContributor`, C-014 `foldPageHead`, `serializeHeadElements` [internal-invariant dedup/tie-break] | The render-seam registry (ADR-PIPE-008 Decision §2) — ordered fold, fail-closed-per-contributor, dedup, serialize | Core-owned so a future non-SEO contributor has a natural home; keeps `render.ts` itself free of registry bookkeeping | Mirrors `routing.ts`'s `phaseRegistry`/`namedRoutes` shape (in-module array + typed register/reset) |
| `src/server/http/site/render.ts` | server/http/site | changes | (consumes C-014's output, not a new contract) | `pageShell()` accepts a pre-serialized head-elements string; suppresses its hardcoded `<title>` when the fold already produced one | Minimal, additive-shaped change to a real working file — the splice point is the only touch | No change to `renderDocNode`/component registry/Liquid rendering |
| `src/server/routes/site/pages.ts` | server/routes/site | changes | (wiring only) | Builds `PageHeadContext` (incl. `canonicalUrl` via `routing.urlFor`), calls `foldPageHead()` + `serializeHeadElements()` before `renderSite()`, on both the home and `:slug` routes | Existing composition point for the public render path | Home route has no `entry`; `PageHeadContext.entry` is `undefined` there (SEO's contributor still emits site-level tags) |
| `src/server/routes/site/sitemap.ts` | server/routes/site | creates | C-015 `registerSeoSitemapRoute` | HTTP wiring for `SEO_GET_SITEMAP` | One file per public endpoint, matches `media-rendition.ts`'s standalone-file convention | Returns `text/xml`, no auth |
| `src/server/routes/site/robots.ts` | server/routes/site | creates | C-016 `registerSeoRobotsRoute` | HTTP wiring for `SEO_GET_ROBOTS` | Same as above | Returns `text/plain`, no auth |
| `src/server/routes/admin/seo/get-entry.ts` | server/routes/admin/seo | creates | C-017 `registerAdminSeoGetEntryRoute` | HTTP wiring for `SEO_GET_ENTRY_META` | One file per endpoint, matches every other `routes/admin/<feature>/<action>.ts` | — |
| `src/server/routes/admin/seo/put-entry.ts` | server/routes/admin/seo | creates | C-018 `registerAdminSeoPutEntryRoute` | HTTP wiring for `SEO_PUT_ENTRY_META`, incl. `SEO_FIELD_VALIDATION_ERROR`/`SEO_INVALID_CANONICAL_URL` mapping | Same as above | — |
| `src/server/routes/admin/seo/get-entry-analyze.ts` | server/routes/admin/seo | creates | C-019 `registerAdminSeoGetEntryAnalyzeRoute` | HTTP wiring for `SEO_GET_ENTRY_ANALYZE` | Same as above | — |
| `src/server/routes/admin/seo/get-settings.ts` | server/routes/admin/seo | creates | C-020 `registerAdminSeoGetSettingsRoute` | HTTP wiring for `SEO_GET_SETTINGS` | Same as above | Awaits `deps.seoReady` first (mirrors `settings/get-effective.ts` awaiting `settingsReady`) |
| `src/server/routes/admin/seo/put-settings.ts` | server/routes/admin/seo | creates | C-021 `registerAdminSeoPutSettingsRoute` | HTTP wiring for `SEO_PUT_SETTINGS`, incl. `SEO_SETTINGS_VALIDATION_ERROR` mapping | Same as above | Awaits `deps.seoReady` first |
| `src/server/routes/admin/seo/post-sitemap-regenerate.ts` | server/routes/admin/seo | creates | C-022 `registerAdminSeoPostSitemapRegenerateRoute` | HTTP wiring for `SEO_POST_SITEMAP_REGENERATE` | Same as above | Returns `202` |
| `src/infra/db/schema.ts` | infra | changes | (Drizzle column def, not a contract per se) | Adds nullable `seoExtJson: text("seo_ext_json")` to the `posts` table | Schema is the existing shared file every feature's storage changes go through | No index added in this pass (ADR-PIPE-008 Decision §7 / OQ-02) |
| `src/identity/permissions.ts` | identity | changes | (extends the registration call block, not a new contract) | Registers `admin.seo.manage` | Existing single registration point for all permission strings | Mirrors the `navigation.manage`/`integration.manage` block immediately above it |
| `src/features/settings/types.ts` | settings | changes | C-024 `SettingValueSchema` `{type:"json"}` variant | Extends the value-schema vocabulary by exactly one variant | Existing single source of truth for the schema union | Additive — no existing schema value changes shape |
| `src/features/settings/settings.ts` | settings | changes | (extends `validateValueAgainstSchema`, not a new contract) | Adds the `case "json": return true;` branch | Existing single source of truth for schema validation | — |
| `src/features/post/post.ts` | post | changes | C-023 `updatePost` (extended signature/behavior) | Emits `entry.published`/`entry.updated`/`entry.unpublished` per the ADR-PIPE-008 Decision §5 transition table | Existing single write path for post/page status changes | `UpdatePostDeps` gains `outbox: OutboxPort`; every existing call site (routes/admin/posts/update.ts) must be updated to pass it |
| `src/server/routes/types.ts` | server | changes | (extends `RouteDeps`, not a new contract) | Adds `seoReady: Promise<void>` | Existing single composition-root dependency bag | Mirrors `settingsReady`'s exact shape/convention |
| `src/server/app.ts` | server | changes | (wiring only) | Registers the 8 SEO route registrars; subscribes `SeoEventSubscriptions` to the 3 entry events; calls `ensureSeoSettingDefinitions()` at boot; registers SEO's `PageHeadHook` contributor | Existing single wiring point | Mirrors the exact fire-and-forget pattern `settingsReady`/`identityReady` already use |
| `apps/admin/src/sections/Seo.tsx` | apps/admin | creates | (UI-facing contracts per ui.spec.md, not duplicated here) | The `/admin/seo` site-settings screen | Matches `Appearance.tsx`'s flat-file, no-props convention | — |
| `apps/admin/src/sections/PostEditor.tsx` | apps/admin | changes | (embeds the SEO panel, not a new top-level contract) | Adds the `SeoEntryPanel` as a tab/section within the existing editor | Existing host file for per-entry editing (ui.spec.md §6: SEO panel is never mounted standalone) | — |
| `apps/admin/src/App.tsx` | apps/admin | changes | (wiring only) | Mounts `<Seo />` | Existing per-section route wiring | — |
| `apps/admin/src/lib/api.ts` | apps/admin | changes | (adds the 6 admin-route client functions, not duplicated here) | Client wrappers for the 6 admin SEO endpoints | Existing single API client file | — |

## Contract Map

| Contract ID / Name | File | Owner Module | Kind | Why Needed | Job | Inputs | Outputs | Validation | Errors | Effect Boundary | Complexity / Resource View | Aggregate-Risk Note | Spec/ADR Trace | Test Seam / Expectation |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| C-001 `getEntryMeta` | `seo.ts` | seo | exported function (`SeoQueryPort` method) | REQ-05's one-evaluator requirement | Resolve the effective `SeoMeta` for one entry (override ▸ site default ▸ derived, per field) | `{ workspaceId, entryId }` | `SeoMeta` (never partial) | Entry must exist | `SeoEntryNotFoundError` | Reads: `PostRepoPort`, `getSeoSettings`, `routing.urlFor`, `resolveSeoImageRef` | O(1) — one post read + one settings read + up to 2 media lookups | Invariant case: `effective.title` must never resolve empty (INV-02-equivalent, behavior.spec.md §1.1) even when override/default/derived all appear absent (falls to `entry.title` through the template) | REQ-04/05, AC-05..09, behavior.spec.md §1.1 | Unit test per field-precedence row (override-wins, default-wins, derived-fallback) against fixture posts + settings |
| C-002 `analyzeEntry` | `seo.ts` | seo | exported function (`SeoQueryPort` method) | REQ-14 | Compute score + issues list over C-001's resolved `SeoMeta` | `{ workspaceId, entryId }` | `SeoAnalysis` (`score` always a number, `issues` may be `[]`) | Entry must exist | `SeoEntryNotFoundError` | Pure decision over C-001's result (no additional I/O) | O(1) | N/A | REQ-14, AC-28 | Unit test: missing-title/description -> issue present; nothing missing -> `score: 100, issues: []` |
| C-003 `seoPageHeadHook` | `page-head-contributor.ts` | seo | exported `PageHeadHook` (registered, not called directly) | REQ-06/07 | Map C-001's `SeoMeta` into ordered `HeadElement[]` per the fixed priority bands (title=100, meta-desc=110, canonical=120, robots=130, og=140-149, twitter=150-159, jsonld=900) | `PageHeadContext` | `HeadElement[]` | N/A (mapping only; C-001 already validated) | Never throws for a normal case; a thrown internal error is caught by C-014's fold, not by this function itself | Pure mapping (delegates the one read to C-001) | O(1) | Invariant case: JSON-LD `@type` mapping (`page`->`WebPage`, `post`->`Article`, override via `schemaType`) plus `BreadcrumbList` inclusion when `ctx` supplies an ancestor chain | REQ-07, AC-12/13/14/15 | Unit test per content-type/override combination; integration test via C-014's fold at the real render boundary |
| C-004 `setEntrySeoOverrides` | `write-service.ts` | seo | exported function (the chokepoint) | REQ-01/02/03 | Authorize -> validate (registered-key-only, length/URL-scheme) -> merge into `posts.seo_ext_json` -> save -> conditionally invalidate the sitemap cache | `{ workspaceId, entryId, patch, callerPrincipalId }` | `{ overrides: SeoExtFields, effective: SeoMeta }` (post-write, via C-001) | `admin.seo.manage` required; patch keys ⊆ registered `SeoExtFields` keys; string fields ≤500 chars; URL fields ≤2048 chars + safe scheme | `FORBIDDEN`, `UNAUTHENTICATED`, `SeoFieldValidationError`, `SeoInvalidCanonicalUrlError`, `SeoEntryNotFoundError` | Explicit side effect: `PostRepoPort.save` write; conditional cache-invalidation call | O(1) | **Aggregate-risk:** the sole path INV-01/INV-06 depend on — a bug here either persists an unregistered key or bypasses authorization for every caller | REQ-01/02/03/15, AC-01..04/29/30, INV-01/06 | Integration test at the real SQLite `PostRepoPort` adapter boundary (AC-01/03/04 are P1) |
| C-005 `getSeoSettings` | `settings.ts` | seo | exported function | REQ-11 | Read all 7 `site.seo.*` values via `getEffective`, recompose `defaultRobots` from its 2 decomposed booleans | `{ workspaceId }` | `SeoSettings` (no `baseUrl` field — INV-07) | N/A (read-only; ledger totality guarantees a value) | none | Reads: `settings.getEffective` ×8 (7 keys, `defaultRobots` needs 2) | O(1) — bounded reads | N/A | REQ-11/12, AC-24, INV-07 | Unit test against `InMemorySettingsRepo` fixtures |
| C-006 `setSeoSettings` | `settings.ts` | seo | exported function | REQ-11 | Validate (`titleTemplate` contains `%s` at least once; `robotsRules.length <= 50`; per-rule shape) -> decompose `defaultRobots` -> call the ledger's `write-service.set` per changed key | `{ workspaceId, patch: Partial<SeoSettings>, callerPrincipalId }` | `SeoSettings` (post-write, via C-005) | `titleTemplate` must contain `%s`; `robotsRules.length <= 50`; each rule's `userAgent` required | `SeoSettingsValidationError`, `FORBIDDEN` (via the ledger's own `settings.workspace.write` check) | Explicit side effect: N calls to `settings.write-service.set`, each its own `setting_revisions` row | O(k) where k = number of changed keys (≤8) | **Aggregate-risk:** a partial-write bug here (some keys written, one rejected) would leave `SeoSettings` internally inconsistent — must validate ALL fields before writing ANY | REQ-11, AC-24/25, behavior.spec.md §7 (titleTemplate/robotsRules boundary cases) | Integration test: invalid `titleTemplate` -> zero settings changed (AC-25); each boundary case (500/501, 50/51 rules) from behavior.spec.md §7 |
| C-007 `ensureSeoSettingDefinitions` | `settings.ts` | seo | exported function | Boot-time prerequisite for C-005/C-006 to resolve at all | Idempotently register the 7 `site.seo.*` definitions (skip-if-already-registered, mirrors `migration.ts`'s `ensureCoreDefinition`) | `{ workspaceId, systemPrincipalId }` | `void` | N/A | none (best-effort per definition, logged not thrown, mirrors `migrateLegacyPresentationSettings`) | Explicit side effect: DB writes via `settings.write-service.registerDefinitions` | O(1) — 7 fixed definitions | Invariant case: rerun on every boot must be a no-op (idempotent) | REQ-11, ADR-PIPE-008 Decision §3 | Integration test: call twice, assert only 7 `setting_definitions` rows exist, not 14 |
| C-008 `buildSitemap` | `sitemap.ts` | seo | exported function (`SeoQueryPort` method) | REQ-08/10 | Cache-checked build of `SitemapEntry[]` — published, non-noindex posts/pages, plus any `seo.sitemap.collect` contributions | `{ workspaceId }` | `SitemapEntry[]` (never `null`, `[]` when empty — EC-04) | N/A (read-only) | none surfaced (an internal query failure maps to `INTERNAL_ERROR` at the route layer) | Reads: `PostRepoPort.list` (app-filtered for `noindex`, per ADR-PIPE-008 Decision §7/OQ-02), the in-module cache | O(n) over the workspace's posts (no expression index in v1 — see ADR-PIPE-008 Re-evaluation Triggers) | Invariant case: never includes a non-`published` or effective-`noindex` entry (INV-04/05) regardless of cache state | REQ-08/10, AC-16/17/22/23, INV-04/05 | Integration test at the real `/sitemap.xml` route + SQLite adapter |
| C-009 `buildRobots` | `sitemap.ts` | seo | exported function (`SeoQueryPort` method) | REQ-09 | Compose `RobotsPolicy` from `SeoSettings.robotsRules` + computed `sitemapUrls` | `{ workspaceId }` | `RobotsPolicy` (`sitemapUrls: []` when `sitemapEnabled` is `false`) | N/A | none | Reads: C-005 | O(1) | N/A | REQ-09, AC-20/21, EC-09 | Unit test: `sitemapEnabled` true/false cases |
| C-010 `regenerateSitemapCache` `[internal-invariant]` cache map | `sitemap.ts` | seo | exported function | REQ-13 | Force-rebuild the cached sitemap entry now, bypassing the cache-hit path | `{ workspaceId }` | `void` | `admin.seo.manage` required (checked at the route layer, not here — mirrors other chokepoints' layering) | none | Explicit side effect: overwrites the in-module cache Map entry | O(n) — same cost as a cache-miss `buildSitemap` | Invariant case: concurrent regenerate calls for the same workspace must converge to one deterministic cache entry, never a torn/duplicated write (EC-08) — enforced by the Map's atomic single-key overwrite semantics (JS single-threaded event loop, no lock needed) | REQ-13, AC-27, EC-08 | Integration test: two concurrent regenerate calls -> one final cache value, no error |
| C-011 `invalidateSitemapCache` | `sitemap.ts` | seo | exported function | REQ-10, INV-08 | Clear the workspace's cache entry (idempotent) | `{ workspaceId }` | `void` | N/A | none | Explicit side effect: deletes a Map key | O(1) | Invariant case: a repeat call for an already-clear key is a no-op, never an error (idempotency required by outbox retry semantics) | REQ-10, INV-08, EC-05 | Unit test: call twice on the same key, assert no throw, cache stays clear |
| C-012 `registerPageHeadContributor` | `page-head.ts` | server/http/site | exported function | Boot-time registration for any `page.head` contributor (SEO now, others later) | Append a `PageHeadHook` to the ordered in-module registry | `PageHeadHook` | `void` | N/A | Explicit side effect: mutates module-level array | O(1) | N/A | REQ-06, ADR-PIPE-008 Decision §2 | Unit test: register 2 contributors, assert both fold |
| C-013 `resolveSeoImageRef` | `media.ts` | seo | exported function | EC-07 | Resolve an `ogImage`/`twitterImage` field (media ref or absolute URL) to an absolute URL, or `undefined` on any miss | `{ workspaceId, ref }` | `string \| undefined` | N/A (fail-soft by design) | never throws | Reads: `MediaRepoPort`, `AssetRenditionRepoPort`, `TransformDefinitionRepoPort` (read-only) | O(1) — bounded lookups | Invariant case: a deleted/never-generated asset must omit the field, never break the rest of the head render (EC-07) | REQ-01, EC-07 | Unit test: valid ref -> URL; deleted asset -> `undefined`; already-absolute URL -> pass-through |
| C-014 `foldPageHead` `[internal-invariant]` dedup/tie-break | `page-head.ts` | server/http/site | exported function | REQ-06/16, INV-03 | Await every registered contributor (ascending `priority`), drop a throwing contributor's output, dedup by `HeadElementKey` (last-writer-wins by priority, later-registration tie-break), return the final ordered array | `PageHeadContext` | `HeadElement[]` | N/A | Never throws (a contributor's throw is caught and dropped, REQ-16) | Pure orchestration (delegates I/O to contributors) | O(c log c) over registered contributors (sort) + O(e) over emitted elements (dedup) | **Why internal-invariant:** the exact last-writer-wins-by-priority + later-registration tie-break rule (behavior.spec.md §5/§6.1) is security/correctness-relevant for INV-03 (only tagged descriptors ever reach serialization) — a divergent implementation would silently change which of two colliding tags wins | REQ-06/16, AC-10/11/31, INV-03, behavior.spec.md §5/§6.1 | Direct unit test for the collision + tie-break cases; integration test via the real `pageShell()` render path |
| `serializeHeadElements` | `page-head.ts` | server/http/site | exported function | INV-03 | Turn the final `HeadElement[]` into escaped `<head>` tag markup — the one place IR becomes a string | `HeadElement[]` | `string` (HTML fragment) | Every text/attribute value is escaped | N/A | Pure (reuses `render.ts`'s `escapeHtml`) | O(e) over elements | Invariant case: a `jsonld` element's `data` must be `JSON.stringify`-serialized inside a `<script type="application/ld+json">` tag, never string-concatenated as raw markup | REQ-06, INV-03 | Unit test: one case per `HeadElement` kind |
| C-015 `registerSeoSitemapRoute` | `routes/site/sitemap.ts` | server/routes/site | exported route registrar | REQ-08 | HTTP wiring for `GET /sitemap.xml` -> C-008, XML serialization | HTTP request (no auth) | `text/xml` body, `200` | none (public) | `INTERNAL_ERROR` -> `500` | Explicit side effect: HTTP I/O + delegates to C-008 | O(1) request handling | N/A | REQ-08, AC-16/17/18, api.spec.md | Integration test hitting the real route |
| C-016 `registerSeoRobotsRoute` | `routes/site/robots.ts` | server/routes/site | exported route registrar | REQ-09 | HTTP wiring for `GET /robots.txt` -> C-009, text serialization | HTTP request (no auth) | `text/plain` body, `200` | none (public) | `INTERNAL_ERROR` -> `500` | Explicit side effect: HTTP I/O + delegates to C-009 | O(1) | N/A | REQ-09, AC-19/20/21, api.spec.md | Integration test hitting the real route |
| C-017..C-022 admin route registrars | `routes/admin/seo/*.ts` | server/routes/admin/seo | exported functions | HTTP entrypoints, each `admin.seo.manage`-gated | Parse request -> call the matching C-001..C-011 function -> map result/errors to api.spec.md §5/§6 contracts | HTTP request per api.spec.md §4 | HTTP response per api.spec.md §5 | Auth profile `SEO_ADMIN` (api.spec.md §2) | Full mapping per api.spec.md §6 | Explicit side effect: HTTP I/O + delegates to seo module functions | O(1) request handling | N/A | REQ-01/11/13/14/15, api.spec.md | Integration test per endpoint hitting the real route + SQLite adapter |
| C-023 `updatePost` (extended) | `features/post/post.ts` | post | exported function (existing, extended) | REQ-10's cache-invalidation signal source | Same existing validate/persist behavior, PLUS: compare `existing.status` to `input.status` and enqueue at most one outbox event per the 4-row transition table | `UpdatePostRequired` (deps gains `outbox: OutboxPort`) | `{ post: PostRecord }` (unchanged) | Same existing validation, unchanged | Same existing errors, unchanged | Explicit side effect: existing `repo.save` PLUS a new `outbox.enqueue` call | O(1) | **Aggregate-risk:** every other module's `entry.*` subscription (SEO's cache invalidation now, any future consumer later) depends on this transition table being exactly right — a misclassified transition silently breaks sitemap freshness with no visible error | REQ-10, AC-22/23, EC-05, ADR-PIPE-008 Decision §5 | Direct unit test for all 4 transition-table rows in isolation, before any SEO-side subscription test |
| C-024 `SettingValueSchema` `{type:"json"}` | `features/settings/types.ts` + `settings.ts` | settings | exported type variant + validator branch | REQ-11's `robotsRules` storage need | Accept an opaque JSON value as a setting's value (ledger does not validate internal shape) | N/A (type-level + one validator branch) | N/A | Ledger-level: none (accepts any JSON value); shape/length is SEO's own responsibility (C-006) | N/A | N/A (type/validator only) | O(1) | N/A | REQ-11, ADR-PIPE-008 Decision §3 | Unit test: `validateValueAgainstSchema({type:"json"}, anyJsonValue)` returns `true` for object/array/scalar/null-if-nullable |

## Wiring Map

| Flow ID | Source | Transport/Call Type | Target | Payload/Contract | Ordering/Retry/Idempotency | Failure Handling | Trace |
|---|---|---|---|---|---|---|---|
| W-001 | `routes/admin/seo/put-entry.ts` | direct call | `write-service.ts` C-004 | `setEntrySeoOverrides` input/output | Not idempotent by design (each call is a fresh write) — UI disables save during in-flight (ui.spec.md `SeoEntryPanel`) | Typed error maps 1:1 to an HTTP error code (api.spec.md §6) | REQ-01, AC-01/04 |
| W-002 | `write-service.ts` C-004 | direct call (conditional) | `sitemap.ts` C-011 `invalidateSitemapCache` | `{ workspaceId }` | Called only when the write changes a sitemap-eligibility-affecting field (`noindex`, or any field that changes `canonical`) — not on every write | No-op if the cache was already clear (idempotent) | REQ-10, INV-04/05 |
| W-003 | `features/post/post.ts` `updatePost` C-023 | `OutboxPort.enqueue` | outbox (existing `InMemoryOutbox`/SQLite outbox) | `DomainEvent<{entryId, contentType}>` named `entry.published`\|`entry.updated`\|`entry.unpublished` | Enqueued in the same call as the post save (not the same DB transaction — matches this repo's existing outbox pattern, e.g. `workspace/create.ts`) | Outbox delivery retry is the existing `processOutbox` mechanism's responsibility; SEO's subscription handler (W-004) must be idempotent regardless | REQ-10, ADR-PIPE-008 Decision §5 |
| W-004 | outbox dispatcher (existing `EventBusPort`) | `bus.subscribe` handler | `sitemap.ts` C-011 `invalidateSitemapCache` (via a `SeoEventSubscriptions` handler registered in `server/app.ts`) | `DomainEvent<{entryId, contentType}>` | Idempotent (ADR-009) — a duplicate delivery is a no-op | Handler never throws; a failure to invalidate leaves the cache stale until the next event or manual regenerate (documented fallback) | REQ-10, EC-05, feature.spec.md Dependencies |
| W-005 | `routes/site/pages.ts` (home + `:slug`) | direct call | `page-head.ts` C-014 `foldPageHead` | `PageHeadContext` | Called once per page render, after content/theme resolution, before `renderSite()` | A fold that produces `[]` (e.g. SEO's contributor not registered) still renders a valid (if minimal) page — `render.ts`'s existing `<title>` fallback covers that case | REQ-06, AC-10/11 |
| W-006 | `page-head.ts` C-014 | direct call | `seo/page-head-contributor.ts` C-003 (as one registered `PageHeadHook`) | `PageHeadContext` -> `HeadElement[]` | Runs in ascending-`priority` order alongside any other registered contributor | A throw from C-003 is caught by C-014's fold and dropped — the page still renders (REQ-16, EC-06) | REQ-16, AC-31, EC-06 |
| W-007 | `apps/admin/src/sections/{Seo,PostEditor}.tsx` | HTTP fetch | `routes/admin/seo/*.ts` | api.spec.md §4/§5 request/response contracts | UI disables save/regenerate controls during in-flight requests (ui.spec.md §4) | `ErrorBanner` renders recoverable errors (ui.spec.md §3) | REQ-01/11/13, ui.spec.md |
| W-008 | `server/app.ts` boot | direct call, once at startup | `settings.ts` C-007 `ensureSeoSettingDefinitions` | none in / `void` out | Idempotent — safe on every boot restart | Per-definition failure is logged and does not abort boot (mirrors `migrateLegacyPresentationSettings`) | REQ-11, ADR-PIPE-008 Decision §3 |

## Data And Side-Effect Boundaries

| Boundary | Owner | Reads | Writes | Side Effects | Consistency / Transaction Rule | Migration / Dual-Write Path |
|---|---|---|---|---|---|---|
| `posts.seo_ext_json` | seo (via `post`'s existing `PostRepoPort`) | `seo.ts` (C-001/C-002), route layer | `write-service.ts` C-004 only | none beyond the row write itself (no revision ledger for this field — `posts.version` bump is the existing post-level audit signal) | Single-row write via existing `PostRepoPort.save` — no new transaction semantics | N/A — new nullable column, no legacy data (ADR-PIPE-008 Migration Safety) |
| `setting_definitions` / `setting_values_workspace` (namespace `site.seo`) | settings (existing ledger, reused unmodified) | `settings.ts` C-005 (via `getEffective`) | `settings.ts` C-006/C-007 (via the ledger's own `write-service.set`/`registerDefinitions`) | `setting_revisions` row per changed key (existing ledger guarantee, INV-01 of SPEC-007, unchanged) | Inherited unchanged from ADR-028/SPEC-007 — value write + revision are one transaction | N/A — new definitions registered idempotently at boot, no legacy `site.seo.*` data exists |
| in-module sitemap cache (`Map<string,string>`, keyed `ws:{workspaceId}:seo:sitemap`) | seo (`sitemap.ts`) | `buildSitemap`/`buildRobots` (C-008/C-009) | `regenerateSitemapCache`/`invalidateSitemapCache` (C-010/C-011) | none external — process-local, lost on restart (documented, acceptable — REQ-13 exists for manual recovery) | Single-key overwrite, no partial-write risk (JS event-loop atomicity) | N/A — no persistence, nothing to migrate |
| `EventBusPort`/`OutboxPort` (existing, reused) | `post` (producer), `seo` (consumer) | N/A | `post.ts` C-023 enqueues; `seo`'s subscription handler (W-004) reads | 3 new event names (`entry.published`/`entry.updated`/`entry.unpublished`) added to the existing bus | Existing outbox reliability guarantees (unchanged) | N/A |
| `media` repos (`MediaRepoPort`/`AssetRenditionRepoPort`/`TransformDefinitionRepoPort`) | media (unchanged, read-only reuse) | `seo/media.ts` C-013 | none (seo never writes media) | none | N/A — read-only cross-module dependency | N/A |

## Observability And Operational Expectations

| Surface / Flow | Required Signals | Correlation / Trace Context | Metrics | Logs | Alert / Runbook Need | Privacy / Secret Constraints | Trace |
|---|---|---|---|---|---|---|---|
| All 8 admin/public SEO routes | Structured error responses (errors.spec.md §1 envelope) | `correlationId: null` (deferred, matches SPEC-001/SPEC-007 precedent); `workspaceId`/`entryId` in `details` are the available correlation identifiers | None new required (no NFR flagged latency/throughput targets) | Standard request logging (existing server middleware) | N/A — no new production alerting surface beyond existing admin-route conventions | No secret/PII fields anywhere in SEO's data model | REQ-10 (Article VIII), errors.spec.md §1 |
| `write-service.ts` C-004 | The `posts.version` bump on every write IS the minimal audit signal (no dedicated revision ledger for this field, unlike settings) | none dedicated | N/A | N/A | N/A | N/A | Article VIII |
| `post.ts` C-023's new outbox events | Each event is the audit signal for a publish/update/unpublish transition (existing outbox durability guarantees apply) | `aggregateId`/`workspaceId` on the `DomainEvent` (existing shape) | N/A | N/A | N/A | N/A | ADR-PIPE-008 Decision §5 |
| `server/app.ts` boot (`ensureSeoSettingDefinitions`) | Log a summary line on failure (per-definition, non-fatal) for operator visibility, mirroring `migrateLegacyPresentationSettings`'s `console.error` convention | N/A | N/A | Log line per failed definition registration | N/A — one-time, low-risk, 7 fixed rows | N/A | ADR-PIPE-008 Decision §3 |

## Critical Invariants

| Invariant ID | Scope | Rule | Reason | Enforcement Surface | Test Expectation | Trace |
|---|---|---|---|---|---|---|
| INV-01 | `write-service.ts` C-004 | `posts.seo_ext_json`, when non-null, must always be a subset of the registered `SeoExtFields` keys | An unregistered key persisted would silently widen the write surface beyond what any consumer validates | `write-service.ts` (validation runs before any `postRepo.save` call) | Integration test: attempt a write with an unregistered key, assert rejection AND that the existing row is unchanged (AC-04) | feature.spec.md INV-01 |
| INV-03 | `page-head.ts` C-014/`serializeHeadElements` | `page.head` output delivered to the render seam must never contain a raw HTML string — only tagged `HeadElement` descriptors, serialized (escaped) at exactly one point | The single largest theme-injection surface (ADR-020 §2/§6) — a plugin/contributor must never be able to smuggle markup | `page-head.ts` (the only file that turns IR into a string) | Unit test: every `HeadElement` kind serializes to escaped markup; a contributor cannot return a string directly (type system enforces this) | feature.spec.md INV-03, AC-10 |
| INV-04/INV-05 | `sitemap.ts` C-008 | `SitemapEntry[]` never includes an entry with effective `noindex === true` or `status !== "published"` | Search-engine-facing correctness — an indexable-looking sitemap must never leak drafts or noindexed content | `buildSitemap` (app-side filter, per ADR-PIPE-008 Decision §7) | Integration test: workspace with a mix of draft/published/noindex posts -> only the eligible ones appear (AC-16/17) | feature.spec.md INV-04/05 |
| INV-07 | `seo/settings.ts`, `seo/seo.ts` | No SEO code path may read or write a `seo.base_url`-shaped setting; every absolute URL comes from `routing.urlFor`'s `canonicalUrl` | Single-origin-authority — ADR-032's own Round-3 audit fold closed exactly this drift | `SeoSettings`'s type shape (no `baseUrl` field exists to accidentally read) + `seo.ts`'s exclusive use of `routing.urlFor` | Static/type-level check (no `baseUrl` field compiles) + integration test: an entry's canonical/OG/sitemap URLs all derive from the routing-resolved origin (AC-26) | feature.spec.md INV-07, AC-26 |
| INV-08 | `sitemap.ts` cache Map | The sitemap cache key must always be `ws:{workspaceId}:seo:sitemap` — never shared across workspaces | Cross-workspace data leakage would be a severe correctness bug (ADR-007) | `sitemap.ts` (the only file that constructs the cache key) | Unit test: two different workspaceIds produce two independent cache entries | feature.spec.md INV-08 |
| INV-09 | `seo/write-service.ts`, `seo/seo.ts`, `seo/sitemap.ts` | `getEntryMeta`/`renderHead`/`buildSitemap`/`buildRobots`/`analyzeEntry` are reachable ONLY through `SeoQueryPort` — no route/hook/future-AI-tool binding reads persisted SEO data through a second, parallel handler | "No back door" dogfood rule (ADR-032 §4/ADR-027 INV-6 precedent) — a second read path could drift from the one evaluator's precedence rules | Code Review architecture check: every admin/public route calls into `seo.ts`/`sitemap.ts`'s exported functions, never `PostRepoPort`/settings directly for SEO data | Code Review checklist item + AC-09's cross-consumer-identical-values test as the functional proof | feature.spec.md INV-09, AC-09 |
| INV-010 `[internal-invariant]` transition-table correctness | `features/post/post.ts` C-023 | The 4-row status-transition table (ADR-PIPE-008 Decision §5) must be implemented exactly — no transition emits more than one event, and a draft-to-draft edit emits none | A misclassified transition silently breaks REQ-10's cache-invalidation contract with no visible symptom until a stale sitemap is noticed | `post.ts`'s `updatePost` (isolated, directly testable status-comparison logic) | Direct unit test for all 4 rows, before any SEO-side subscription test depends on it | REQ-10, AC-22/23, ADR-PIPE-008 Decision §5 |

## Brownfield / Migration Mapping (if applicable)

| Source Behavior / Contract | Target Module / Contract | Preserve / Change | Characterization Evidence | Migration Safety Note |
|---|---|---|---|---|
| `src/seo/{ports,types}.ts` (ADR-032 design-only stub, zero call sites) | `src/seo/{seo,write-service,settings,sitemap,media,errors}.ts` (this feature's real implementation) | Change: the stub's `entries.fields.ext.seo.*` storage assumption, `SeoPermission`'s 5-string vocabulary, `seo.base_url`, and `RobotsPolicy`'s stored-vs-computed conflation are all corrected per `state.spec.md` §5 and this ADR — the stub's *shapes* (`SeoExtFields`, `HeadElement`, `SeoMeta`) are preserved unchanged | `state.spec.md` §5 "Stub Mismatches" (4 documented) + ADR-PIPE-008 Context (5 architecture-discovered) | No data to preserve — the stub has zero adapters/call sites today, so there is nothing running to keep compatible |
| `src/features/post/post.ts` `updatePost` (existing, event-free) | Same function, extended (C-023) | Change: gains outbox-event emission; existing validate/persist behavior is unchanged | Existing `post.test.ts` suite (must continue passing unmodified) | Every existing call site of `updatePost` must be updated to pass an `outbox: OutboxPort` in `deps` — a compile-time-enforced change, not a silent behavior drift |
| `src/server/http/site/render.ts` `pageShell()` (existing, fixed `<head>`) | Same function, extended to accept a pre-serialized head string | Change: additive parameter; when absent/empty, behavior is identical to today (fixed `<title>`, no other head tags) | Existing `render.test.ts` suite (must continue passing unmodified for any caller that doesn't pass head elements) | Backward-compatible by construction — `pages.ts` is the only caller, updated in the same change |

## Test Expectations

- Contract tests: C-004 (`setEntrySeoOverrides`) and C-006 (`setSeoSettings`) against real adapters (`repo.sqlite.ts` for posts, the existing settings SQLite adapter) — not just in-memory.
- Integration tests: W-001 (PUT entry through the real HTTP route + SQLite `PostRepoPort`, AC-01/03/04), W-003/W-004 (publish/unpublish through `updatePost` -> outbox -> sitemap cache invalidation, AC-22/23), W-005/W-006 (the real `pageShell()` render path with SEO's contributor registered, AC-10/11/31), all 8 admin/public routes (AC-16..21/24..27/29/30).
- Property/invariant tests: INV-03 (no raw string ever reaches `serializeHeadElements`'s input except via a tagged `HeadElement`), INV-04/05 (sitemap eligibility across a matrix of status × noindex combinations), INV-08 (cache key uniqueness across ≥2 workspaces), INV-09 (every admin/public route traced to a `seo.ts`/`sitemap.ts` call, no direct `PostRepoPort` bypass), INV-010 (the 4-row transition table, all rows).
- Characterization tests: None required beyond the existing `post.test.ts`/`render.test.ts` suites continuing to pass unmodified for their pre-existing cases (this is the closest thing to a characterization check this brownfield extension needs — see Brownfield/Migration Mapping).
- Explicitly N/A suites with reason: No orchestrator test suite — `orchestrator.spec.md` is omitted (spec-manifest.md — no distinct frontend orchestrator layer). No dual-write/backfill test suite — Migration Safety confirms there is no data migration in this feature.

## Downstream Handoff Notes

- Coordinator task-generation constraints: Sequence (1) the `posts.seo_ext_json` column + `SettingValueSchema` `{type:"json"}` variant + `post.ts`'s outbox extension + `page-head.ts`'s registry, ahead of (2) the `seo` module's own files (`seo.ts`/`write-service.ts`/`settings.ts`/`sitemap.ts`/`media.ts`), ahead of (3) the API routes and `render.ts`/`pages.ts` wiring, ahead of (4) the admin UI. Phase (1)'s four pieces are mutually independent and safe to mark `[P]`; phase (2)'s five files depend on all of phase (1) but are largely independent of each other (`write-service.ts` needs `errors.ts`; `sitemap.ts` needs nothing from `settings.ts`/`media.ts`) so several can run `[P]` once phase (1) lands. C-014 (`foldPageHead`)'s dedup/tie-break logic must be certified by TDD before C-003 (SEO's contributor) or `render.ts`/`pages.ts` are wired to depend on it (mirrors ADR-PIPE-007's C-005-before-C-003/C-004 sequencing precedent).
- TDD focus: Certify C-023 (`updatePost`'s transition table) and C-014 (`foldPageHead`'s dedup/tie-break) FIRST — both are aggregate-risk, both are pure/isolated, and multiple downstream pieces (the SEO module, `render.ts`, `pages.ts`) depend on their exact behavior being locked before integration. Then C-004 (the write chokepoint) and C-006 (settings write). Route-layer tests last, once the domain layer is certified.
- Programmer architecture audit focus: Verify no route/hook reads `posts.seo_ext_json` or the `site.seo.*` settings other than through `seo.ts`/`write-service.ts`/`settings.ts` (INV-09). Verify every existing `updatePost` call site was updated to supply `outbox` in deps (a compile error otherwise — confirm it's fixed, not suppressed). Verify `render.ts`'s existing Liquid-template tests and `post.test.ts`'s existing suite still pass unmodified. Verify the `{type:"json"}` schema variant is used exactly once.
- Open risks or ambiguities: None beyond what's already captured in ADR-PIPE-008's Consequences/Risks section (transition-table correctness; head-fold dedup/tie-break correctness; the `json` schema-variant scope) — all three have explicit mitigations recorded there. One item intentionally deferred past this pass, per spec Open Questions: OQ-01 (`seo.sitemap.collect` stays a live-but-empty registry) and OQ-02 (no expression index in v1) are both resolved as "not built now" decisions in ADR-PIPE-008 Decision §7, not open ambiguities for TDD/Programmer to re-litigate.
