# Handoff: Tovu SPEC-001 gateway — working spike + external audit to reconcile

Generated: 2026-07-07T05:01:08Z
Source agent/session: Claude Code (Claude Opus 4.8), Tovu repo — ran the AI-Dev-Shop pipeline for SPEC-001 through Spec→Red-Team→Architect→(tasks)→exploratory implementation. Personas loaded this session: Coordinator, Red-Team, Spec Agent, Software Architect, Programmer.
Target: fresh Claude Code session, same machine/repo.

## Next-Agent Prompt

> Read `AI-Dev-Shop/AGENTS.md` and perform its Mandatory Startup. Then read
> `ADS-project-knowledge/.local-artifacts/handoff/20260707T050108Z-handoff.md` and continue from Next Steps.
> Priority: read the external audit report `ADS-project-knowledge/reports/external-audit/runs/20260707T043122Z-external-audit-report.md`
> and reconcile the codebase + specs with it. The v1 specs were edited to apply audit blockers F1–F4 but NOT
> re-validated (hashes are stale); and the SPEC-001 gateway has a working-but-exploratory implementation that
> does NOT yet meet the new atomicity requirement (AC-17/EC-08/BR-04). Load the relevant persona before claiming its name.

## Current State

**SPEC-001 (admin-command-gateway) ran the whole pipeline this session and has a WORKING but exploratory (not test-certified) implementation.** The other specs (002–005) are spec+Red-Team only — no Architect/outline/code.

- Pipeline for 001: Red-Team (0 BLOCKING·6 ADVISORY·1 CF) → **R2 revision** (RT-001/002/006 + `tovu/→src/` path fix) → **constitution bootstrapped** (`ADS-project-knowledge/governance/constitution.md`, owner did NOT ratify — flagged) → **Planning Preflight PASS** → **ADR-018 ACCEPTED** (`reports/pipeline/001-.../adr.md`, registered in `reports/architecture/ADR-INDEX.md`) + **implementation-outline.md** → **tasks.md** (41 tasks) → owner chose **implement-first, skip tests** → gateway wired + verified live.
- **Working demo** (in-memory): `PORT=3999 TOVU_DB=memory node --import tsx src/index.ts`; login `admin`/`tovu-dev`; edit post `post-home` in workspace `workspace-local` → change set recorded → list/get (inversePayload hidden over HTTP) → revert restores + bumps version. DUPLICATE_COMMAND / REVERT_CONFLICT / CHANGE_SET_INVALID_STATUS all verified. `npm run typecheck` clean.
- On the default **:3000 (SQLite)** server: posts/workspaces/presentation persist to `content.db`; **change-sets are in-memory** (reset on restart) — the SQLite change-set adapter is deferred (Red-Team RT-004).

**Two things the next session MUST reconcile (this is the "fix it" the owner asked for):**

1. **The external audit edited specs 001/003/004/005 (+002) to apply blockers F1–F4, but did NOT re-validate — hashes are STALE.** SPEC-001 validator now returns **VIOLATION**: canonical feature hash is `sha256:768af10e49fcde7a6e8d9dc0f4b1193c0302201a480afc01d9fe20fafe25e5d6`, recorded is my `sha256:0230e96c…c7a0`. ADR-018 / implementation-outline / tasks were all authored against `0230e96c`, so they now cite a stale hash.
2. **The exploratory code does NOT meet the new atomicity requirement (audit F4 → SPEC-001 REQ-01/BR-04/EC-08/AC-17).** `executeCommand` in `src/core/commands/command.ts` runs `mutation.execute()` and THEN `changeSets.insert()` as two separate steps. The revised spec now requires the feature mutation + change-set header + item to commit as **one unit of work** — an insert failure must roll back the feature mutation. My code leaves the mutation applied (a "mutation without a record" = the exact INV-01 case the audit closed). This needs a real fix + the AC-17 injected-failure test.

## Completed Work (this session)

- Red-Teamed SPEC-001 → `reports/pipeline/001-admin-command-gateway/red-team-findings.md`; applied RT-001/002/006 as R2; fixed `tovu/→src/` path drift across the package.
- Bootstrapped the constitution (8 articles, from de-facto spec usage) → `governance/constitution.md` (owner ratification still pending).
- Coordinator Planning Preflight PASS; wrote **ADR-018** + **implementation-outline.md**; generated **tasks.md** (41 tasks, phases 0–5 + polish); registered ADR-018 in the ADR-INDEX.
- Implemented the SPEC-001 gateway (exploratory, owner-directed skip-tests): NEW `src/core/commands/{revert.ts, appliers.ts, index.ts}`, `src/server/http/admin/change-sets.ts`, `src/server/routes/admin/change-sets/{list,get,revert}.ts`; MODIFIED `src/core/commands/{change-set.ts, command.ts}` (added item `entityVersionAtApply` + `captureEntityVersion` hook), `src/server/routes/admin/posts/update.ts` (rewired through gateway), `src/server/routes/types.ts` + `app.ts` + `deps.ts` (added `changeSets` dep). Verified end-to-end via curl.
- Recorded everything in `reports/pipeline/001-admin-command-gateway/pipeline-state.md` (stage ledger, Red-Team, preflight, ADR approval, exploratory-spike + hash-stale + code-gap flags).

## Active Files And Artifacts

- **AUDIT (read first):** `ADS-project-knowledge/reports/external-audit/runs/20260707T043122Z-external-audit-report.md` — self-contained; "Applied Fixes — Implement Against These" lists every new/changed ID. Companion raw data in `.local-artifacts/external-audit/` (packet + codex stdout; gemini FAILED).
- Spec package (audit-edited, hash-stale): `ADS-project-knowledge/specs/001-admin-command-gateway/` — REQ-01 (atomicity), AC-17, EC-08 (feature.spec.md); AC-02/AC-10 (ext-under-revert); plus BR-04 (behavior.spec.md), traceability AC-17/EC-08.
- ADR + plan (cite stale hash 0230e96c — update after re-validate): `reports/pipeline/001-admin-command-gateway/{adr.md, implementation-outline.md, tasks.md, pipeline-state.md, red-team-findings.md}`.
- Code: `src/core/commands/*`, `src/server/routes/admin/change-sets/*`, `src/server/routes/admin/posts/update.ts`, `src/server/http/admin/change-sets.ts`, `src/server/{app.ts,deps.ts,routes/types.ts}`.
- `ADS-project-knowledge/governance/constitution.md` (bootstrapped, unratified).

## Decisions And Constraints (owner-approved this session)

- Fold Red-Team RT-001/002/006 into SPEC-001 R2, then Architect (done).
- Bootstrap constitution before Architect (done; not ratified).
- Approve ADR-018 → generate tasks + TDD (ADR approved; TDD then **skipped by owner** in favor of implement-first).
- **Implement-first, skip tests** — code is VibeCoder-grade / NOT test-certified; departs Constitution Art. II. Promotion = run TDD to certify tests over the working code. `implementation-outline.md` is the contract of record.
- Presentation path (REQ-05) deferred until the `version` field + backfill land.

## Risks And Open Questions

- **Hash-stale specs** (001/003/004/005 +002 touched by audit) — re-validate `--update-hash`, Red-Team re-affirm the audit deltas, then update ADR-018 + ledger hashes. Do this BEFORE more code so downstream cites the real hash.
- **Atomicity code gap** (AC-17/EC-08/BR-04) — the in-memory `ChangeSetRepoPort`/gateway has no transaction; a real unit-of-work (or compensating rollback of the feature mutation) is needed. The SQLite adapter (RT-004) makes this a real DB transaction.
- **ext-under-revert** (audit F1 → AC-02/AC-10, SPEC-005 BR-08/AC-17): revert must restore pre-edit `ext` verbatim and NOT re-fire `content.entry.beforeSave`. Not exercised by the current post-only path (posts carry no `ext` yet), but load-bearing once SPEC-002 content-entries + SPEC-005 plugins land — the post reverter in `appliers.ts` will need to include `ext` in the inverse pre-image.
- **Test-First debt** — nothing is test-certified. Owner accepted this for the spike.
- **Audit coverage was single-external-auditor** (codex only; gemini transport-failed) — not two-auditor-confirmed (see the report's Auditor Matrix / Degraded Coverage).
- Constitution not ratified; `PROJECT_MEMORY.md` + `src/INFO.md` referenced by the repo's `AGENTS.md` do not exist (per-module `INFO.md` are the real convention).

## Suggested Skills / Personas

- `AI-Dev-Shop/agents/spec/skills.md` — re-validate + re-hash the audit-edited specs (validator `--phase spec --update-hash`; DoD Sign-Off row Role must be exactly `Spec Agent`).
- `AI-Dev-Shop/agents/red-team/skills.md` — re-affirm the audit deltas (F1–F4) against the new hashes.
- `AI-Dev-Shop/agents/programmer/skills.md` — fix the gateway atomicity gap (AC-17/EC-08/BR-04).
- `AI-Dev-Shop/agents/tdd/skills.md` — promote the exploratory code by certifying tests over it (`tasks.md` phases 1–5).

## Next Steps

1. Boot per `AI-Dev-Shop/AGENTS.md`. Read the audit report (above) fully.
2. **Re-validate the audit-edited specs** (001 first): `python3 AI-Dev-Shop/framework/spec-providers/speckit/validators/validate_spec_package.py ADS-project-knowledge/specs/001-admin-command-gateway --phase spec --update-hash` → then 003/004/005 (+002). Red-Team re-affirm the F1–F4 deltas. Update ADR-018 `Spec:` line + `pipeline-state.md` hashes to the new SPEC-001 hash (`768af10e…` before re-hash; run the validator to get the committed value).
3. **Fix the code atomicity gap** in `src/core/commands/command.ts` (+ in-memory adapter) so the feature mutation + change-set commit atomically; add the AC-17 injected-failure test.
4. **Promote** the exploratory code: run TDD against SPEC-001 (new hash) to certify the `tasks.md` suite over the working implementation; converge.
5. Then optional: wire the **presentation path** (version field + backfill + reverter), and the **SQLite change-set adapter** (RT-004) which also delivers the real transaction for step 3.
6. Consider the audit's deferred backlog (F5 + verifier advisories) and structural gaps (transaction model, cross-spec ID-namespace, concurrency, upgrade/compat, auth thread-through) — post-skeleton, load-bearing soon.

## Handoff Contract

- Inputs used: this session's work (Red-Team/R2/constitution/ADR-018/outline/tasks/exploratory code + live curl verification); the external audit report + its Applied-Fixes section; validator runs; `git status`; the SPEC-001 package + brownfield source.
- Output summary: a fresh session can reconcile the audit (re-validate/re-hash specs, re-affirm Red-Team) and close the two concrete gaps (spec-hash staleness, gateway atomicity), then promote the working gateway via TDD.
- Risks: hash-stale specs; non-atomic gateway vs revised spec; ext-under-revert unimplemented (not yet exercised); no test certification; single-external-auditor audit; unratified constitution.
- Suggested next assignee: Coordinator → Spec Agent (re-validate) → Red-Team (re-affirm) → Programmer (atomicity) → TDD (promote).
