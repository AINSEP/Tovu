# Handoff: Tovu — auth fix + theme wide-screen bands + content pages + ADR-019 (theme bundles) + SPEC-004 drift reconcile

Generated: 2026-07-07T18:15:00Z
Source agent/session: Claude Code (Claude Opus 4.8), Tovu repo, Coordinator (mixed Review/Pipeline + VibeCoder spike).
Target: fresh Claude Code session, same machine/repo.

## Next-Agent Prompt

> Read `AI-Dev-Shop/AGENTS.md` and perform its Mandatory Startup. Then read
> `ADS-project-knowledge/.local-artifacts/handoff/20260707T181500Z-handoff.md` and continue from Next Steps.
> Context: theme system is a VibeCoder spike (exploratory, not test-certified beyond the SPEC-001 AC-17 slice).
> Priority: (1) decide the unreproduced "content page won't scroll" report; (2) fix the content-page wide-screen
> layout (TODO §4); (3) work the SPEC-004 drift OPEN items (C3–C7). Load the relevant persona before claiming its name.

## Current State

- **Backend runs**; in-memory demo server is LIVE on **:3999** (`PORT=3999 TOVU_DB=memory node --import tsx src/index.ts`,
  active theme `tovu-official`). Owner's `:3000` untouched.
- Backend `npm run typecheck` clean; `npm test` **15/15**.
- Nothing committed this session (all changes unstaged). `themes/` and `src/features/theme/` are still untracked (new).
- **Theme packages are cached at boot** — editing anything under `themes/**` requires a **server restart** to take
  effect. This bit us repeatedly; it is the prime suspect for the owner's "page won't scroll / didn't fix it" reports
  (a stale server serving an old theme snapshot). Filed as a dev hot-reload TODO.

## Completed Work (this session)

- **Auth logout bug FIXED + verified** — `src/server/middleware/dev-auth.ts` now uses a **stateless HMAC-signed cookie**
  (`{userId, exp}` signed with `TOVU_SESSION_SECRET`, dev default) with a **7-day `Max-Age`**, replacing the in-memory
  `Map` that `tsx watch` wiped on every restart. Verified end-to-end: cookie minted by one instance authenticates on a
  fresh instance (survives restart), survives tab close, tamper→401. Typecheck + 15/15.
- **Theme wide-screen "bands" fix (HOME only)** — `themes/tovu-official/`: added `--surface: #E7E9F3` + `--maxw: 75rem`
  tokens; full-bleed alternating band backgrounds on `.feature-grid` + `.section--media-left` (`styles.css`). Verified
  in-browser at 2560px. **Caveat:** this only touched the HOME template. **Content/post pages were NOT fixed** — see C8.
- **Content pages created** (`src/server/seed.ts`, rewritten with tiny doc-authoring helpers) — 8 published entries that
  render at `/:slug` through the active theme: `welcome` (kept — a route test depends on `post-home`/`welcome`), plus
  site pages `about`, `how-themes-work`, `how-plugins-work`, `plugin-api`, `self-hosting` (Coming Soon), and two generic
  non-Tovu posts `the-weight-of-type`, `slow-mornings`. All render 200. Cross-theme verified: the same page renders
  totally differently in `tovu-official` / `column` / `signal` (proof of content-is-data / theme-is-arrangement).
- **Nav wiring** — `themes/tovu-official/templates/post.json` nav + footer now link the real pages (Docs/Writing
  dropdowns). Home nav still uses on-page anchors (they work).
- **ADR-019 (PROPOSED)** — `reports/architecture/ADR-019-theme-bundles-plugin-dependencies.md`: theme bundles (a
  declarative theme may declare plugin deps `requires`, installed in one consented step); NO JS-capable theme class;
  behavior stays in the plugin plane. Extends ADR-010. Indexed in ADR-INDEX.md. **Needs owner ACCEPT.**
- **SPEC-004 → v1.1.0** (`content_hash a5289e06`, validator PASS) — added OQ-06 (theme bundles → ADR-019) AND folded in
  drift corrections **C1** (built-in ids `paper/atlas/glassmorphic` → `tovu-official/column/signal`, `tovu-official`
  fallback) and **C2** (optional `fonts[]` manifest key). Reconciled across feature/behavior/state/traceability/spec-dod.
  Pipeline-state ledger updated.
- **Spec-drift findings report** — `reports/pipeline/004-declarative-theme-system/spec-drift-findings.md` (C1–C8, routed).
- **TODO.md** (repo root) — §1 mobile drawer bug, §2 visual-regression-testing learning path (Playwright
  `toHaveScreenshot`), §3 ADR-019 (done), §4 content-page wide-screen layout (the "won't respond when stretched" bug).
- **Playwright allowlist** — added server-wide `mcp__plugin_playwright_playwright` to `.claude/settings.local.json`
  (stops the per-tool approval prompts). `.playwright-mcp/` gitignored.

## Active Files And Artifacts

- Auth: `src/server/middleware/dev-auth.ts`.
- Themes: `themes/{tovu-official,column,signal}/` (`tokens.json`, `styles.css`, `templates/{home,post}.json`);
  renderer `src/server/http/site/render.ts`; theme feature `src/features/theme/theme.ts` (shallow validity only).
- Content: `src/server/seed.ts` (helpers + 8 entries). Content model = `PostRecord` (no `kind`, no create API).
- Specs/ADRs: `specs/004-declarative-theme-system/*` (v1.1.0), `reports/architecture/ADR-019-*`, `ADR-INDEX.md`,
  `reports/pipeline/004-.../{pipeline-state,spec-drift-findings}.md`.
- `TODO.md` (repo root).

## Decisions And Constraints (owner-approved / established)

- Declarative themes = pure DATA (ADR-010); behavior lives in PLUGINS; theme bundles are packaging, not a trust class
  (ADR-019). Nav is theme-authored.
- Specs are hashed ground truth; re-hash via
  `python3 AI-Dev-Shop/framework/spec-providers/speckit/validators/validate_spec_package.py <pkg> --update-hash --phase spec`.
- "Fix upstream intent, not downstream drift" — spec/code conflicts go to the owning spec (see drift report), not patched.
- Theme edits require a server restart (no hot reload).

## Risks And Open Questions

- **"Content page won't scroll" — NOT reproduced.** On `:3999`/`tovu-official`, `/how-themes-work` scrolls the full
  874px to the footer at 1366×768 and 2560×1400; no theme has an `overflow:hidden`/`100vh` clamp. Prime suspect =
  stale theme cache on the owner's server (needs restart) OR browser-specific. Needs owner's exact URL/port + browser.
- **C8 (real):** content-page (`entry-content`) is left-anchored on wide screens — `.wrap` centered at 75rem but
  `.prose` is `max-width:42rem` with no auto margins. Fix ≈ center the entry column. In TODO §4.
- **C3–C7 OPEN** (drift report): C3 template-set mismatch (themes ship `post.json`, spec wants `entry.json` min); C4 no
  `kind` → pages leak into blog feed on list themes; C5 no content-create API; C6 no theme validation/CSS sanitization
  (security-critical, top Architect item); C7 no inline `link` mark (content can't link).
- Theme spike is not test-certified; no visual-regression suite yet (TODO §2). Constitution bootstrapped, unratified.
- ADR-019 is PROPOSED (needs owner ACCEPT); theme-bundle capability needs its own spec slice (pairs SPEC-005).

## Suggested Skills

- `verify` / `run` — drive the app to reproduce the scroll report and confirm any fix.
- The Speckit validator (above) for any spec edit.
- Playwright MCP (now allow-listed) for cross-theme / multi-width screenshots.

## Next Steps

1. Boot per `AI-Dev-Shop/AGENTS.md`; read this handoff. Get the owner's exact URL/port + browser for the scroll report;
   if it's their own server, **restart it** (theme cache) and re-check.
2. Fix **C8** (center the entry/content column across all three themes) — the real "doesn't respond when stretched" bug.
   Then stand up TODO §2's Playwright visual-regression suite covering `/about` at 1280/1920/2560 per theme.
3. Optional content: add a "themes need plugins for behavior" seam on `/how-themes-work` linking the plugin pages
   (owner asked whether themes+plugins content should connect — yes).
4. Work the drift OPEN items in order: C3 (Architect decision) → C6 (validation pipeline) → C4/C5 (SPEC-002 `kind` +
   create API) → C7 (`link` mark).
5. Get owner ACCEPT on ADR-019; if yes, draft the theme-bundle spec slice.

## Handoff Contract

- Inputs used: this session (auth fix, theme bands, content seed, nav wiring, ADR-019, SPEC-004 v1.1.0 + drift report,
  TODO updates); `git status --short`; typecheck + `npm test` (15/15); live curl + Playwright verification on :3999;
  prior handoff `20260707T164909Z-handoff.md`.
- Output summary: a fresh session can resolve the scroll report, fix content-page wide layout, build the VRT suite, and
  work the SPEC-004 drift items — with auth fixed, content seeded, and the theme-bundle decision recorded.
- Risks: unreproduced scroll report; content-page wide layout (C8); theme spike not test-certified; theme cache needs
  restart; C3–C7 open; ADR-019 pending accept.
- Suggested next assignee: Programmer (C8 + auth-adjacent) → VibeCoder (VRT + content) → Software Architect (C3/C6, SPEC-002).
