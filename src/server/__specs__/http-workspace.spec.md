# Spec: Workspace HTTP Route

This file is retained as the original seed spec for the workspace route.

The detailed server version of this route now lives in:

- `20-workspaces/create-route.spec.md`
- `20-workspaces/create-route-errors.spec.md`

Use those files as the canonical transport specs going forward.
