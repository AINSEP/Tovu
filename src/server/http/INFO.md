# http Overview

Owns HTTP-specific helpers that are still transport-layer concerns.

This folder is the intended home for response serialization, correlation-header helpers, and other HTTP-only shaping code that should stay out of feature modules.
