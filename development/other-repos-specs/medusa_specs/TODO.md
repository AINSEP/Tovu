# Medusa Specs TODO

- Add a smaller admin-extension authoring note if Tovu wants a stricter extension SDK model.
- Add a narrower route-by-route DTO and payload inventory only if Tovu needs Medusa HTTP parity instead of architectural guidance.
- Add a small design-system note for `@medusajs/ui` only if Tovu needs the admin component layer itself.
- Add a telemetry/operations note only if Tovu needs Medusa's runtime-operability posture as a reference.
- Optionally capture Medusa Cloud public docs separately, but keep hosted internals explicitly inferential.
