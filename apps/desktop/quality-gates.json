{
  "_comment": "The desktop quality gates. THIS FILE IS THE ONLY PLACE A GATE MAY BE TURNED OFF — never by deleting a workflow step, commenting out a line, or removing a script. `scripts/check-gates.mjs` runs every gate with `enabled` not false, and PRINTS EVERY GATE ON EVERY RUN including the off ones, so a disabled gate is a visible, dated decision rather than an absence nobody notices. Setting `enabled: false` requires `disabledReason` and `disabledOn` (YYYY-MM-DD) or the runner fails; a gate left off longer than `maxDisabledDays` fails until the date is re-confirmed. A `scripts/check-*.mjs` that no gate below runs also fails the runner — see `src/quality-gates.js`'s header for why each of those rules exists, and which real gate in this repo rotted for want of it.",
  "_comment_2026_09_12_coverage_numbers": "Coverage percentages produced by this harness come from node's own `--experimental-test-coverage` lcov, whose LF counts EVERY line in a file including comments and blanks (verified: keyed-serializer.js LF:51 / wc -l 51; App.hooks.ts 1272/1272). This codebase is exceptionally comment-dense, so these line percentages read HIGHER than a statement-based tool would report and are NOT comparable to any c8, vitest or istanbul number — including `apps/admin`'s vitest coverage. Do not put a desktop number and an admin number in the same table and conclude anything from the difference.",
  "maxDisabledDays": 30,
  "gates": [
    {
      "id": "tree-quiet",
      "run": "node scripts/check-tree-quiet.ts",
      "enabled": true,
      "_comment": "The PRIMARY half of the packaging safety gate (2026-09-12): refuses to let `npm run package` proceed while apps/desktop is still being written to. It runs first in this list on purpose — a dirty-tree refusal is cheap and should catch nearly everything before the expensive build/stage/electron-builder steps even start. The BACKSTOP half — byte-for-byte verification of the finished app.asar against source — cannot live in this manifest at all: `npm run gates` runs before `electron-builder` in the `package` script, so there is no artifact yet for a gate here to inspect. That half is `scripts/verify-package.mjs`, appended as the final step of `package.json`'s `package` script instead. See ADS-memory/reports/2026-09-12-packaging-asar-corruption.md and src/tree-quiet.js's header for the full mechanism this protects against."
    },
    {
      "id": "tests",
      "run": "npm test",
      "enabled": true
    },
    {
      "id": "typecheck",
      "run": "npm run typecheck",
      "enabled": true
    },
    {
      "id": "coverage",
      "run": "node scripts/check-coverage.ts",
      "enabled": true
    },
    {
      "id": "complexity",
      "run": "node scripts/check-complexity.ts",
      "enabled": true
    },
    {
      "id": "js-backslide",
      "run": "node scripts/check-js-backslide.ts",
      "enabled": true,
      "_comment": "The TypeScript-only backslide guard (2026-09-12): fails on any .js/.mjs/.cjs file anywhere under apps/desktop, tracked or untracked, outside the one named exception (src/speech/preload-speech.cjs — a sandboxed Electron preload that cannot load TypeScript). See ADS-memory/reports/2026-09-12-desktop-typescript-migration-plan.md, Phase 5 item 2, and src/js-backslide-guard.ts's header for why the exception is an exact path, not a directory or pattern."
    }
  ]
}
