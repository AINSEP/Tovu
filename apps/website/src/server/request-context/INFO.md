# request-context Overview

Owns normalized request-context assembly for the server transport boundary.

This folder is the intended home for request ID propagation, auth/scope normalization, and any helper code that builds the transport-safe `RequestContext`.
