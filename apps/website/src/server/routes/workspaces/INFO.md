# workspaces Route Overview

Owns workspace-related server routes.

This folder is the intended home for `POST /workspaces` and future workspace transport handlers once the current monolithic `app.ts` wiring is split into route modules.
