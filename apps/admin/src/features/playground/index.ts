/**
 * @file Public surface of the `playground` feature.
 *
 * `panels.tsx` imports from HERE, never from a file inside this folder — same convention as
 * every other feature (see `features/themes/index.ts`).
 */
export { Playground } from "./Playground";
