# features/dashboard

The admin landing screen — the ungrouped, top-row **Dashboard** entry.

| file | what it is |
|---|---|
| `Dashboard.tsx` | Recent-posts summary and quick links. |
| `index.ts` | The only surface `panels.tsx` may import. |

## Notes for anyone editing here

`Dashboard.tsx` has a unit test (`__tests__/Dashboard.unit.test.tsx`).
