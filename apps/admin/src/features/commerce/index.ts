/**
 * @file Public surface of the Commerce admin feature.
 *
 * App-level composition imports from this module so the feature can grow internal UI, model, and
 * API segments later without exposing those implementation paths.
 */
export { Payments } from "./Payments";
