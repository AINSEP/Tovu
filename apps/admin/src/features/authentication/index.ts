/**
 * @file Public surface of the `authentication` feature. The admin panel registry imports only
 * this boundary; provider metadata and presentation details remain private to the slice.
 */
export { Authentication } from "./Authentication";
