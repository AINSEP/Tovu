# apps/admin

> Placeholder — structure draft; refine after admin IA research

The Tovu admin shell (Next). The current working slice lives at ../../nextjs (dashboard, posts list/editor with TipTap, appearance/presentation) and moves here in Phase 0. The sections/ tree below is the target information architecture — a placeholder per section a typical CMS admin needs (WordPress screenshot reference: wordpress-admin-dashboard.png at repo root). IA is DRAFT until the admin research task in todos.md (study Directus/Ghost/Payload/Strapi/WP admin structures) is done. Sections marked (registry) are rendered from kernel/surfaces descriptors so plugins can contribute panels; sections marked (plugin) ship with a bundled plugin, not core.
