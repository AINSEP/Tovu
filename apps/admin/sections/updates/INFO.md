# admin/updates

> Placeholder — backlog

Core updates: version status, preflight checks, staged update with last-known-good rollback (UF-01, lib/recovery + workflows). Separate from extension updates deliberately — different blast radius.
