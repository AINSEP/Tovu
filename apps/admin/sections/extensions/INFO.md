# admin/extensions

> Placeholder — Phase 2 (plugin lifecycle) + Phase 4

Plugins: installed list w/ enable/disable, add from library, per-plugin settings pages (registry), dependency + capability display from manifests (ADR-004), UPDATE flow with preflight + one-click rollback (UF-01: pointer-flip + no-DDL make this real). Trust/provenance badges.
