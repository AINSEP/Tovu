# admin/content

> Placeholder — exists as thin slice (posts) in ../../nextjs

Content entries per registered type (posts, pages, custom types from schema registry): list views w/ filters + bulk actions, editor (TipTap), revisions + restore, trash, scheduled publishing. WP Posts+Pages generalized — one section driven by the schema registry rather than hardcoded types.
