# Handoff: Tovu v1 spec suite — Red-Team done + fixes applied; ready for Software Architect

Generated: 2026-07-07T04:55:00Z
Source agent/session: Claude Code (Claude Opus 4.8), Tovu repo — booted AI-Dev-Shop framework, operated as Spec Agent + Red-Team (both personas loaded this session)
Target: fresh Claude Code session, same machine/repo

## Next-Agent Prompt

> Read `AI-Dev-Shop/AGENTS.md` and perform its Mandatory Startup. Then read
> `ADS-project-knowledge/.local-artifacts/handoff/20260707T045500Z-handoff.md` and continue from Next Steps.
> The v1 spec suite (SPEC-001…005) is validated, Red-Teamed, and all Red-Team fixes are applied + re-validated.
> The next pipeline stage is **Software Architect** (via Coordinator Planning Preflight). Before that, the owner
> wanted to **start wiring** — the first implementable unit is **SPEC-001 (the command gateway)**; check its
> readiness first. Load the relevant persona (`AI-Dev-Shop/agents/<agent>/skills.md`) before claiming its name.

## Current State

- **All five v1 spec packages are validator-PASS, Red-Teamed, and RT-fixes applied.** Strict Speckit packages in `ADS-project-knowledge/specs/`:
  - SPEC-001 `001-admin-command-gateway/` — pre-existing; approved; **NOT yet wired into routes** (implementation prerequisite for 002/004/005). Has a `reports/pipeline/001-*/pipeline-state.md` from an earlier session — check its stage before wiring.
  - SPEC-002 `002-content-entry-authoring/` — hash `sha256:b0127bc0…4675`
  - SPEC-003 `003-site-install-dir/` — hash `sha256:99cf109e…a899`
  - SPEC-004 `004-declarative-theme-system/` — hash `sha256:ba45edcd…4139`
  - SPEC-005 `005-plugin-system/` — hash `sha256:894425c4…a096` (thin walking-skeleton slice)
- **Human spec checkpoint: APPROVED by owner (Leon Aburime) 2026-07-07** for all five → Red-Team. Recorded in each `reports/pipeline/<NNN>/pipeline-state.md`.
- **Red-Team complete for 002/003/004/005** (Red-Team persona loaded). Findings in `reports/pipeline/<NNN>/red-team-findings.md`. Verdicts: 002 (0B·6A·1CF), 003 (0B·5A·1CF), 004 (0B·4A·1CF), 005 (**1B**·4A·1CF).
- **All Red-Team fixes applied and re-validated PASS.** SPEC-005's one BLOCKING finding (RT-001) is resolved. Every package now cleared for Software Architect.
- Validator command (run from repo root, per package):
  `python3 AI-Dev-Shop/framework/spec-providers/speckit/validators/validate_spec_package.py ADS-project-knowledge/specs/<pkg> --phase spec --update-hash`
  Gotcha: the DoD Sign-Off Block MUST contain a row whose Role cell is exactly `Spec Agent` or the validator fails.

## Completed Work (this session)

1. Booted the AI-Dev-Shop framework correctly (AGENTS.md Mandatory Startup; Spec Agent persona at `agents/spec/skills.md` — the prior handoff's `agents/spec-agent/` path was wrong).
2. **SPEC-004**: wrote the missing `spec-dod.md`, validated the package.
3. **Persona sign-off audit** (owner chose retroactive audit): re-audited 002/003 vs the Spec Agent guardrails; corrected sign-offs to reflect pre-persona-load origin.
4. **Drizzle adoption** (owner chose adopt+revise): revised SPEC-002 (REQ-11 + state §7 → `schema.ts` edit + `drizzle-kit generate`) and SPEC-003 (REQ-05 + `runtimeSchemaVersion` + §7 → Drizzle `migrate()`) as revision **R1**; re-validated.
5. **ADR-015** written + indexed: Drizzle as SQL layer behind the repo ports; exit strategy; rule-of-two via shared schema.
6. **SPEC-005** written from scratch (8 files, thin slice per owner: artifact loader + one hook + `ext.{pluginId}` + minimal `@tovu/sdk` + `word-count` dogfood). Deferred surface = OQ-01…OQ-09b with a prominent "come back later" block.
7. **Red-Teamed 002/003/004/005**; wrote findings + pipeline-state ledgers.
8. **Applied ALL Red-Team fixes** (owner chose "apply all my recs") across 002/003/004/005 + ADR-015; re-validated all PASS.

## Red-Team fixes applied — the notable ones (details in each `red-team-findings.md`)

- **SPEC-005 RT-001 (was BLOCKING):** `content.entry.beforeSave` now returns an **`ext`-delta only** (`(Readonly<entry>, ctx) => ExtPatch`) — a filter can no longer mutate `title`/`slug`/`status`/`bodyJson` outside the capability model. Core-field transformation deferred to **OQ-09** (a separate future capability+hook that re-validates through SPEC-002).
- **SPEC-004 RT-001/RT-005:** CSS sanitization is now a **positive allowlist** (parse, don't regex); `@import`, external/`data:` `url()`, `@font-face` external `src`, `javascript:`/`expression()`/`-moz-binding` forbidden. Agent Directives require a vetted CSS parser + an Architect build-vs-adopt decision. **This is the suite's top security boundary.**
- **SPEC-003 RT-005:** the install-dir compatibility stamp is now **index + tag** (new `.site-meta.json.schemaTag`) so divergent runtime lineages at the same migration index are detected — not just count. Noted in ADR-015 consequences.
- **SPEC-002 RT-001/RT-002:** DB `UNIQUE(workspace_id, slug)` violation now maps to `SLUG_CONFLICT` (not a 500); a derived slug hitting a reserved word auto-suffixes instead of hard-failing.

## Active Files And Artifacts

- `ADS-project-knowledge/specs/00{1,2,3,4,5}-*/` — the five packages (001 is upstream dependency + format precedent).
- `ADS-project-knowledge/reports/pipeline/00{1,2,3,4,5}-*/` — `pipeline-state.md` (stage ledgers, checkpoint approval) + `red-team-findings.md` (002–005).
- `ADS-project-knowledge/reports/architecture/ADR-015-*` (Drizzle) + ADR-003/004/005 (plugin system) + ADR-INDEX.md.
- Governing docs: `tovu-v2-design.md` (§3 kernel registries, §3.5 tiers), `docs/research/competitor-analysis.md` (line 770 = Drizzle adoption; plugin guidance), `docs/research/strapi-deep-dive.md`.
- Brownfield code the specs pin to: `src/features/post/*`, `src/features/presentation/*`, `src/infra/db/schema.ts` (Drizzle), `src/infra/sqlite/content-db.ts`, `drizzle/`, `src/server/http/site/render.ts`, `src/headless/contracts.ts`, `apps/admin/src/**`.

## Decisions And Constraints (owner-approved this session)

- Retroactive Spec-Agent persona audit of 002/003 (not accept-as-Direct).
- Adopt Drizzle as the substrate + revise 002/003 (→ ADR-015).
- SPEC-005 = thin walking-skeleton slice; "come back later" for the full extension surface.
- SPEC-005 dogfood = trivial `word-count` plugin; minimal `@tovu/sdk`.
- Apply ALL Red-Team recommended resolutions (done).
- Constitution (`ADS-project-knowledge/governance/constitution.md`) STILL not bootstrapped — every spec applies toolkit-default articles + the Art. VI no-auth EXCEPTION. Flagged for Coordinator bootstrap.

## Risks And Open Questions

- **SPEC-001 is a hard prerequisite and not wired into routes.** 002/004/005 all execute through the gateway. It's the natural first wiring target; verify its pipeline stage before pointing code at it.
- **Red-Team CONSTITUTION_FLAGs remain for the Software Architect** (not fixed in specs — they're Architect prep): 004 **CSS sanitizer build-vs-adopt (top priority, security)**; 002 slug-library (Art. I); 003 CLI-framework growth trigger (Art. I).
- Constitution not bootstrapped — do this before the Architect leans on it.
- SPEC-005 is deliberately minimal; OQ-01…OQ-09b track everything deferred (admin UI, AI-tool bridge, queryable ext fields, signatures, sandbox, deps, core-field mutation hook, version switching).
- The owner wants to "start wiring." Framework discipline: 002–005 shouldn't be coded before Architect→TDD, but SPEC-001 (gateway) is legitimately wireable now as the dependency root.

## Suggested Skills / Personas

- `AI-Dev-Shop/agents/software-architect/skills.md` — next stage (Implementation Outline / ADR complexity justifications for the CONSTITUTION_FLAGs).
- `AI-Dev-Shop/agents/coordinator/skills.md` — Planning Preflight gate before Architect dispatch.
- `AI-Dev-Shop/skills/handoff/SKILL.md` — this doc.

## Next Steps

1. Boot per `AI-Dev-Shop/AGENTS.md`. Confirm `<AI_DEV_SHOP_ROOT>` + framework active.
2. Decide direction with the owner: (a) **Software Architect** on the batch (start with SPEC-004 — the CSS-sanitizer build-vs-adopt ADR is the highest-risk decision), and/or (b) **start wiring SPEC-001 (the command gateway)** — check `reports/pipeline/001-*/pipeline-state.md` for its readiness first.
3. Consider **bootstrapping the constitution** (`framework/templates/bootstrap/constitution-template.md`) before the Architect stage.
4. Run Coordinator Planning Preflight before any Software Architect dispatch (framework gate).

## Handoff Contract

- Inputs used: this session's conversation; `AI-Dev-Shop/{AGENTS.md, agents/spec, agents/red-team, framework/spec-providers/speckit/**, framework/templates/red-team-template.md}`; the five spec packages + validator runs; ADR-003/004/005/006/010/011/012; `tovu-v2-design.md`; `competitor-analysis.md`; brownfield source (`schema.ts`, `content-db.ts`, repos).
- Output summary: five validated + Red-Teamed + fixed spec packages, ADR-015, full pipeline-state + red-team-findings ledgers; all cleared for Software Architect except the outstanding CONSTITUTION_FLAGs which are Architect prep, not blockers.
- Risks: SPEC-001 unwired (prerequisite); constitution not bootstrapped; CSS-sanitizer decision outstanding; SPEC-005 minimal by design.
- Suggested next assignee: AI Dev Shop Coordinator → Software Architect (or Programmer for SPEC-001 gateway wiring, if the owner prioritizes that).
