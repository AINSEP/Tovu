Codex(Audit Peer): 
```json
{
  "threat_model_accepted": true,
  "rejection_reason": "",
  "auditor_scope_check": "Audited HEAD against the supplied ADR-047/SPEC-043 packet and live repo reads for src/widgets, src/core/entry-refs, src/features/entries/{field-validation,write-service}.ts, src/navigation/resolver.ts, schema/permission excerpts, and widgets-related tests. Graph MCP tools were requested by repo policy but unavailable, so I used direct rg/nl reads. I did not run tests/typecheck because the current sandbox is read-only.",
  "ledger_updates": [],
  "findings": [
    {
      "id": "WIDGETS-AUDIT-001",
      "severity": "blocker",
      "in_scope_domain": "10",
      "rationale": {
        "checked": "SPEC-043 REQ-44/45, AC-30, ADR-047 Amendment 6, implementation outline C-007, and live exports under src/widgets.",
        "expected": "A server-side, versioned document-mutation service for insert/remove/reorder of widgetEmbed nodes, using the same chokepoint and guardrails as the editor path.",
        "observed": "src/widgets/embed-service.ts is absent. The only live reference is a doc comment in src/widgets/embed-validation.ts:6-9 naming embed-service as the intended caller; rg finds no insertWidgetEmbed/removeWidgetEmbed/reorderWidgetEmbeds exports.",
        "why_it_matters": "The domain layer does not implement the agent-native inline-embed mutation path the debate explicitly added. AC-30 cannot be executed without inventing a new path or bypassing the domain layer.",
        "recommended_fix": "Add src/widgets/embed-service.ts with insertWidgetEmbed/removeWidgetEmbed/reorderWidgetEmbeds, require widgets.place, validate host/target workspace/type/status, apply validateWidgetEmbedMutation, and persist through an entries chokepoint that can version bodyJson.",
        "confidence": "high"
      }
    },
    {
      "id": "WIDGETS-AUDIT-002",
      "severity": "blocker",
      "in_scope_domain": "3",
      "rationale": {
        "checked": "Force-purge behavior, EntryRefsRepoPort, and entry_refs source replacement/removal semantics.",
        "expected": "A force-purged source entry should retract or otherwise correctly flag its outgoing entry_refs rows so where-used/safe-delete answers reflect the current graph.",
        "observed": "EntryRefsRepoPort explicitly has removeBySource for when the source itself was force-purged at src/core/entry-refs/ports.ts:35-36, but purgeWidgetInstance only updates status to purged at src/widgets/write-service.ts:327-345 and never calls removeBySource or onWritten. A contact-form/menu widget can therefore remain as a config-field source ref after force-purge.",
        "why_it_matters": "An allowed widgets.delete.force actor can create stale where-used/safe-delete rows for the target entry. That is a concrete reference-integrity violation under INV-06/INV-09’s derived-index contract.",
        "recommended_fix": "Inside force purge, either call entryRefsRepo.removeBySource in the same transaction as the purge status write, or define purged entries as retained reference sources and update findByTarget/where-used semantics to exclude or label purged sources consistently.",
        "confidence": "medium"
      }
    },
    {
      "id": "WIDGETS-AUDIT-003",
      "severity": "high",
      "in_scope_domain": null,
      "rationale": {
        "checked": "REQ-04/41 and exported functions in src/widgets/write-service.ts plus permission catalog.",
        "expected": "A widgets.read-gated read/list/revision-history domain API.",
        "observed": "widgets.read is registered at src/identity/permissions.ts:530, but src/widgets/write-service.ts exports only create/update/trash/purge at lines 104, 165, 242, and 304; there is no read/list/history service enforcing widgets.read.",
        "why_it_matters": "The future route/AI layer would need to read widgets through lower-level entry repos and reimplement authorization. That is not a current mutation bypass, so I am not marking it blocker under the frozen allowlist, but it is a clear REQ-04/41 implementation gap.",
        "recommended_fix": "Add readWidgetInstance/listWidgetInstances/listWidgetRevisions functions that require widgets.read and return parsed widget read models through the domain layer.",
        "confidence": "high"
      }
    },
    {
      "id": "WIDGETS-AUDIT-004",
      "severity": "medium",
      "in_scope_domain": "3",
      "rationale": {
        "checked": "Tests under src/widgets/__tests__ and src/core/entry-refs/__tests__.",
        "expected": "Contract/integration coverage for both memory and SQLite adapters for EntryRefsRepoPort and WidgetRegionBindingRepoPort, as promised by the outline.",
        "observed": "rg finds no SqliteEntryRefsRepo or SqliteWidgetRegionBindingRepo usage in the widgets/entry-refs tests; only in-memory adapters are used.",
        "why_it_matters": "The production adapters look straightforward by inspection, and migrations exist, but reference-integrity and derived-binding claims are not exercised at the real SQLite boundary.",
        "recommended_fix": "Add shared adapter contract tests for replaceForSource/rebuildForWorkspace/findByTarget and binding upsert/rebuild uniqueness against both memory and SQLite.",
        "confidence": "high"
      }
    },
    {
      "id": "WIDGETS-AUDIT-005",
      "severity": "medium",
      "in_scope_domain": "5",
      "rationale": {
        "checked": "recent-entries resolver cost behavior.",
        "expected": "Bounded query behavior matching Amendment 2’s no-unbounded-scan intent.",
        "observed": "src/widgets/resolvers/recent-entries.ts:35 calls listByWorkspace with no type/filter/limit, then sorts every published entry in memory at lines 36-38.",
        "why_it_matters": "The output is clamped, and resolver timeout contains page-render impact, but the resolver still performs an unbounded workspace scan on normal page render.",
        "recommended_fix": "Add or use an EntryListPort method that can query published entries with limit/order/type filters, then clamp at the query boundary as well as the IR boundary.",
        "confidence": "medium"
      }
    },
    {
      "id": "WIDGETS-AUDIT-006",
      "severity": "low",
      "in_scope_domain": "7",
      "rationale": {
        "checked": "resolverId dispatch.",
        "expected": "resolverId indexes a resolver-id-keyed closed map.",
        "observed": "src/widgets/resolvers/index.ts:146 casts resolverId to WidgetTypeKey and indexes a type-keyed map. Today all resolverIds equal type keys, so behavior is safe now.",
        "why_it_matters": "A future shared resolverId or renamed resolver silently degrades to resolver-error despite satisfying the data contract.",
        "recommended_fix": "Key CORE_RESOLVERS by resolverId, or type resolverId as WidgetTypeKey if same-as-type is the intended invariant.",
        "confidence": "medium"
      }
    }
  ],
  "out_of_scope_fatal_warnings": [],
  "score": 6.8,
  "score_rationale": "The implemented CRUD, region mutation, resolver isolation, owner/onWritten shared-module changes, menu resolver export, and contact-form non-duplication mostly hold, but the server-side embed command is absent and force-purge leaves outgoing refs stale.",
  "blocking_gate": "FAIL — validated unresolved blockers exist and score is below 8.5",
  "closure": "COVERAGE_COMPLETE — round-1 coverage finished under TM-adr047-widgets-audit-001; see blocking_gate for the pass/fail outcome."
}
```

What looks solid: implemented mutations check `widgets.create/update/place/delete/delete.force` before writes; `bindWidgetArea` is plausibly the documented system-seeding exception; `owner` and `onWritten` are additive defaults for existing entries callers; `resolveMenuDoc` is a safe additive navigation export; the `fieldsJson.ext.widgets.payload` storage deviation is internally consistent between write, extraction, and resolution; `contact-form` imports only the forms read-side port.

Edit guidance: fix `embed-service.ts` first, because it likely needs one more additive entries chokepoint change to update `bodyJson` under version precondition. Then fix purge reference cleanup in [write-service.ts](/Users/la/Desktop/Programming/Tovu/src/widgets/write-service.ts:304). A minimal direction is: when `input.force` is true, run `entryRefsRepo.removeBySource({ workspaceId, sourceEntryId: input.widgetInstanceId })` in the same transaction as the purge status update, or move purge to an `onWritten`/transaction-capable helper that updates both rows atomically.