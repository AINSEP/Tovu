# ADR-046 Swarm Debate — Round 2 (Informed)

You are continuing the ADR-046 production-readiness debate from Round 1. Round 1 converged: all participants independently recommended Candidate A (staged hardening) over B (feature-by-feature patching, rejected), C (DI container, rejected — violates 46 prior accepted decisions' established typed-constructor convention), and D (process split, rejected — solves a scale problem this single-node SQLite product doesn't have). That candidate-level choice is settled; do not re-litigate it in Round 2.

A fourth participant (an in-host adversarial reviewer, added at the user's explicit request specifically to stress-test the convergence rather than let it stand unchallenged) then found something real: `src/core/commands/command.ts` (lines 176-177) documents BR-04, an already-accepted invariant — outbox enqueue is contractually OUTSIDE the domain-write transaction today ("an enqueue failure propagates but never rolls back the committed change set"). ADR-046 Phase 1 proposes "write domain state and outbox rows in the same database transaction" as if it were routine adapter work, but it's actually a change to that accepted invariant, requiring a real design decision nobody had flagged. The Coordinator independently verified this claim directly against the source — it's accurate.

## Full Round 1 responses (verbatim)

### Primary (Claude Code host session)
Candidate A (staged hardening). High confidence. Reasoning: introducing a DI container (C) or process split (D) would be the first framework-level abstraction shift across 46 prior accepted decisions, for a problem that doesn't need it; feature-by-feature patching (B) is the observed status quo and is why composition roots are fragile — a background session's mid-edit of `deps.ts` broke `tsc` repo-wide during this same evening, a live instance of exactly that failure mode. Not fully settled: whether ADR-046's strict-serial sequencing (inventory fully first) is right, or whether the outbox — the single highest-severity known gap — should start in parallel.

### agy (Gemini 3.1 Pro High)
[As sent in Round 1 — see context packet CTX-adr046-production-readiness-2026-07-16.md for full text if needed] Candidate A, high confidence. Rejects B/C/D on established-convention and proportionality grounds. Self-objection: formal lifecycle machinery may be disproportionate ceremony for a solo developer; strict sequential ordering delays the outbox fix. Would reconsider a B/A hybrid if the concurrent-agent-editing workflow changed.

### Codex (gpt-5.6-terra, reasoning=xhigh)
Candidate A, refined as staged/capability-gated. Confidence ~0.9. Concrete refinement: migrate composition incrementally behind stable façades rather than replacing both roots at once; **start the durable outbox as soon as its contract is pinned, in parallel with the rest of the inventory** — do not wait for the entire inventory if the outbox's scope is bounded, since it's the highest-severity known data-loss risk.

### Fable subagent (adversarial addition)
Concurred on candidate choice (A, ~0.9) but found:
1. The BR-04/transaction-seam conflict above (the headline finding, code-verified).
2. Nothing user-critical actually rides the outbox in production TODAY (a `ConsoleMailerAdapter` stub, contained webhook delivery) — the "urgent data-loss risk" framing is future-tense, materializing only once a real consumer ships.
3. Proposed resolving sequencing neither strictly-serial nor Codex's parallel-now, but **demand-paged**: resolve the transaction-seam design question early (cheap), but gate the outbox's durability *implementation* to the first real production consumer shipping, enforced structurally via Phase 0's fail-closed rule (no production mailer/webhook-egress may enable without its durable outbox path existing).
4. Phase 1's 7-capability durability table may be push/uniform-sweep rather than pull — several rows back walking-skeleton features (webhooks/media/analytics, from auto-drafted overnight-sweep ADRs) with no shipping commitment yet.
5. `dependency-cruiser` (Phase 4) should baseline alongside Phase 0, not wait until after Phase 3's composition churn — it's cheap, independent, and most valuable during heavy file movement.
6. The `deps.ts`-broke-`tsc` anecdote all three Round-1 participants cited as evidence for the composition split is actually evidence for **agent-workspace isolation** (worktrees) — and Phase 3's own migration window (dual composition paths behind a façade) will temporarily *increase* the number of composition files concurrent agents touch, briefly amplifying the exact failure mode being cited against it.

### Coordinator's Round 2 position (frozen before reading this round's peer responses)
Agree demand-paged is right for the outbox specifically — the finding is a verified transaction-boundary conflict, not a guess, so premature implementation risks building against an unresolved design. Working through the other 6 Phase-1 capability rows with the same lens Fable applied to the outbox: **change sets are likely already fine** — `command.ts`'s own BR-04 text states change-set inserts are ALREADY inside the domain-mutation transaction today (only outbox enqueue is the documented exception), so that row probably isn't a hidden landmine. **Analytics likely lower-risk** — ADR-046's own Phase 1 table already offers an explicit escape hatch for it ("either add a durable sink, or label best-effort/local"). Hypothesis, not conclusion: **webhooks and media metadata are the two most likely to hide something similar** — webhooks because ADR-046's own table describes real HMAC/replay/idempotency complexity beyond a simple adapter swap; media because a same-evening spec pass (SPEC-021) already found 12 named implementation gaps in that exact subsystem, including things like no `entry_refs` where-used index and no per-generation storage-key epochs, some of which might interact with "transactional metadata updates" the same way BR-04 interacted with the outbox. **Members** also worth a specific check given real session/consent-lifecycle invariants (ADR-021, ADR-030's consent record) that a generic "SQLite adapter" framing might not account for.

## Your task for Round 2

1. **Independently verify or refute the Coordinator's hypothesis above.** For each of the 6 remaining Phase-1 capability rows (change sets, webhooks, members, origin registry, media metadata/transform registry, analytics), check whether ADR-046's proposed fix ("add a SQLite adapter and transaction participation," etc.) conflicts with any existing accepted invariant/contract the same way BR-04 conflicted with the outbox fix. You have read access to the repo — actually check, don't just reason abstractly. Relevant starting points: `src/core/commands/command.ts` (the write chokepoint, already read for the outbox finding), `src/features/members/**`, `src/features/integrations/**` or wherever webhook subscription/delivery logic lives, `src/media/**` (already has `ADS-project-knowledge/specs/021-media-assets/feature.spec.md` as a same-evening reference), `src/origin/**`, and any `write-service.ts`/chokepoint-shaped files in these areas.
2. **State your position on demand-paged (Fable) vs. parallel-now (Codex's original Round 1 position) for the outbox sequencing specifically**, now that you've seen the BR-04 finding. Has your position changed? Why or why not?
3. **State whether you found anything new** beyond what's already listed above — a different capability with a hidden conflict, a problem with the demand-paged proposal itself, or a reason Phase 0's fail-closed rule can't actually structurally enforce "no production mailer/webhook-egress without durable outbox" the way Fable proposed.
4. Give your confidence and what would change your mind, same as Round 1.

This is the last round before synthesis — be concrete and cite file/line evidence where you found something, not just architectural intuition.
