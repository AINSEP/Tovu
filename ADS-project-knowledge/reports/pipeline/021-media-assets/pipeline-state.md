# Pipeline State: FEAT-021 Media Assets

| Field | Value |
|---|---|
| feat_id | FEAT-021-media-assets |
| spec_id | SPEC-021 |
| stage | spec (Spec Agent dispatch only — no Software Architect/`/plan` dispatch performed this run) |
| spec_provider | speckit |
| provider_version_ref | github/spec-kit @ 2c2fea8783f33085652b8c87e839bae84a6eb78d |
| provider_native_root | specs/ |
| provider_output_root | ADS-project-knowledge/specs/021-media-assets |
| spec_path | ADS-project-knowledge/specs/021-media-assets |
| spec_entrypoint_path | ADS-project-knowledge/specs/021-media-assets/feature.spec.md |
| spec_readiness_artifact | ADS-project-knowledge/specs/021-media-assets/spec-dod.md |
| spec_support_paths | api.spec.md, state.spec.md, ui.spec.md, behavior.spec.md, errors.spec.md, traceability.spec.md, spec-manifest.md |
| spec_naming | standard (deviates from the Coordinator dispatch's stated default of `prefixed` — see spec-manifest.md's header note: a direct grep of every existing spec-manifest.md found only 001-005 use `prefixed`; every folder from 006 onward, including the two most recent, 014 and 015, uses `standard`) |
| spec_mode | brownfield (lightweight as-built backfill of a walking-skeleton ADR-027 implementation; no System Design blueprint was produced or needed — small/no-blueprint change per Spec Agent workflow step 5) |
| spec_hash | sha256:8517330957578346a96c9e5f48ede2e8f388c882469889e662f33ca471ac3bc0 |
| spec_hash_verified_at | 2026-07-15 (provider-local validator, `--phase spec --update-hash`, exit 0, zero warnings on final run) |
| planning_preflight_status | NOT STARTED |
| planning_preflight_checked_at | — |
| validator_result | PASS (`--phase spec`, exit 0; first run flagged 7 mechanical `spec-dod.md` issues — a non-canonical status string, three under-justified NA rows, two status cells containing the literal word "FAIL" despite an intended PASS-as-EXCEPTION, and a non-ISO-8601 sign-off date — all fixed; second run passed clean) |
| red_team_status | NOT STARTED |
| red_team_spec_hash | |
| governing_adr | ADR-027 (Media / Assets Subsystem), ACCEPTED 2026-07-09 (2-round swarm debate + round-1 audit TM-media-001 FAIL -> blockers folded -> round-2 re-audit PASS, 6 clause-gap amendments folded); extends ADR-022/ADR-025; relates ADR-012/006/007/021/009/023/026/024/020 |
| pipeline_adr | None produced this run — Spec Agent dispatch only, per Coordinator instruction ("no design gap, no code gap, a documentation/formalization gap") |
| governance_adr_promotion | Not evaluated this run — no architecture work was performed |
| research_artifact | N/A — no research performed; this is a direct read-the-code-against-the-ADR pass |
| implementation_outline_path | Not produced this run |
| tasks_path | Not generated this run — Spec Agent dispatch stops after spec-dod gate per dispatch instruction; a `/plan` dispatch (targeting OQ-01's authz remediation first) is the recommended next step |
| implementation_progress | Real and shipped: upload/list/update/trash/purge CRUD (library + 5 admin routes + admin UI), the audited-shape two-phase journaled blob-GC protocol, named append-only transform registry + bounded/single-flight rendition resolution, the public frozen-URL-contract rendition route, 2 real `BlobStorePort` adapters, 2 real `ImageTransformerPort` adapters. NOT implemented: per-action authorization on any admin media route (headline finding, OQ-01), the ADR-027 §7 flat `media.*` permission set (only `media.write`, unused, is registered — OQ-02), origin-isolated serving, `MediaIngressPolicy` (SSRF/pixel-bomb/magic-byte), the generic `entries` content-type model, `entry_refs` where-used, per-generation storage epochs, any scheduler driving GC/eager-generation automatically, theme/plugin transform declaration, out-of-process generation, the `BlobStorePort` presign surface + S3 adapter, `blob_kind` remote seam, video pipeline states, structured error envelopes/domain events. See `feature.spec.md`'s Scope section for the full named-GAP list. |

## Notes

- **2026-07-15 Spec Agent dispatch (this entry).** Read ADR-027 in full, all 18 files under `src/media/` (including `INFO.md`, which already discloses most scope gaps), the 6 `src/media/__tests__/*.test.ts` files, all 5 admin route files under `src/server/routes/admin/media/`, `src/server/routes/site/media-rendition.ts`, `src/server/http/admin/media.ts`, both server-level test files (`admin-media-routes.test.ts`, `media-rendition-route.test.ts`), `apps/admin/src/sections/Media.tsx`, the relevant slice of `apps/admin/src/lib/api.ts`, `src/identity/permissions.ts`, `src/server/middleware/dev-auth.ts`, a comparison read of `src/server/routes/admin/menus/delete.ts` (the sibling-section authz pattern), the pre-existing generic `src/server/__specs__/50-media/media-api.spec.md` design note, `ADS-project-knowledge/governance/constitution.md`, and every existing `spec-manifest.md` in the repo (to verify the naming-convention claim in the dispatch instruction). Wrote the full Speckit-compatible spec package (`feature.spec.md`, `api.spec.md`, `state.spec.md`, `ui.spec.md`, `errors.spec.md`, `behavior.spec.md`, `traceability.spec.md`, `spec-manifest.md`, `spec-dod.md`) at `ADS-project-knowledge/specs/021-media-assets/`. `orchestrator.spec.md` is OMITTED (see `spec-manifest.md`) — no client-side orchestration abstraction exists, matching the SPEC-007/009/015 precedent.
- **Headline finding, not previously disclosed anywhere in the code's own extensive self-documentation:** none of the five admin media routes (`list.ts`/`upload.ts`/`update.ts`/`trash.ts`/`delete.ts`) call `deps.authorize(...)` or check any permission before performing their operation — confirmed by direct read of all five files plus a repo grep for `authorize`/`permission` across them (zero matches). Every sibling admin section shipped since SPEC-006/ADR-021 landed (menus, integrations, seo, redirects) enforces `deps.authorize(...)` on its write routes; media does not. Separately, `src/identity/permissions.ts` registers exactly one media-related permission (`media.write`, owner `"core"`), which is referenced nowhere else in the repo (confirmed by grep) — it is dead/unused, and does not match ADR-027 §7's flat `media.*` set anyway. Recorded as `feature.spec.md` REQ-39/REQ-40, a Constitution Article VI EXCEPTION, and OQ-01/OQ-02 — flagged as the highest-priority follow-up, not silently fixed (this is a spec-writing pass, read-only against `src/media/`, the ADR, and everything else per the dispatch's scope discipline instruction).
- **`[CONTRACT VS IMPLEMENTATION]` markers used throughout the package** (per the Spec Agent workflow's evidence-preservation gate for brownfield specs) fall into two groups:
  1. **Already self-disclosed by the code** (named in `src/media/INFO.md` and individual file headers before this spec pass began): bespoke `MediaRecord` table vs. the ADR-027 §2 generic-entries model; no origin-isolated serving; no `MediaIngressPolicy`; no `entry_refs` where-used index; no per-generation storage epochs; no scheduler; no theme/plugin transform declarer; no eager/out-of-process generation; no presign surface; no `blob_kind` remote seam; no video pipeline states. These are formalized into named GAP identifiers (GAP-ENTRIES, GAP-ORIGIN, GAP-INGRESS, GAP-ENTRYREFS, GAP-EPOCHS, GAP-SCHEDULER, GAP-DECLARER, GAP-OUTOFPROCESS, GAP-PRESIGN, GAP-REMOTE) in `feature.spec.md`'s Scope section, not re-discovered.
  2. **Newly found by this spec pass, not previously disclosed anywhere in the code:** the total absence of per-action authorization (GAP-AUTHZ, REQ-39/REQ-40) and the stale "no byte-serving route" comment in `Media.tsx` now that `media-rendition.ts` exists (GAP-UI-STALE, REQ-42) — plus the absence of any structured error envelope/domain-event emission (GAP-OBSERVABILITY), which `INFO.md` also never named.
- **No `[NEEDS CLARIFICATION]` markers were needed.** Every question this spec pass surfaced (authz remediation priority, permission-set scope, GAP prioritization) was resolvable as a properly-owned, dated Open Question rather than a blocking ambiguity — none of them change what this spec documents (real, already-shipped behavior), only what should be built next. See `feature.spec.md`'s OQ-01/OQ-02/OQ-03.
- **Naming-convention correction, disclosed rather than silently applied.** The Coordinator dispatch instructed `spec_naming: prefixed` "without asking," citing "every existing spec folder in this repo uses the prefixed convention" as settled fact. A direct grep of all 15 existing `spec-manifest.md` files found this premise is false: `prefixed` is used only by `001`-`005`; `standard` is used by all 10 folders from `006-identity-and-authorization` onward, including the two most recently written (`014-analytics`, `015-integrations`). This spec uses `standard` — the actual current convention — and records the correction here and in `spec-manifest.md` rather than following the dispatch's incorrect premise or silently overriding it without a trace.
- **Validator run and passed clean.** `python3 AI-Dev-Shop/framework/spec-providers/speckit/validators/validate_spec_package.py ADS-project-knowledge/specs/021-media-assets --phase spec --update-hash` first flagged 7 mechanical issues in `spec-dod.md`: `B-04`'s status cell used a non-canonical `PASS (pending run)` string; `C-14`/`F-03`/`F-04` were `NA` but their justification lived only in a sibling row's Notes cell rather than their own; `G-06`/`G-08` used status cells containing the literal word `FAIL` (`"FAIL, then justified as an intentional as-built EXCEPTION"`) even though the intent was a documented PASS-as-disclosed-EXCEPTION; and the Sign-Off Block's Spec Agent date was `2026-07-15` rather than full ISO-8601 UTC. All seven fixed (each `NA` row given its own concrete justification sentence; `G-06`/`G-08` changed to plain `PASS` with the EXCEPTION nuance moved entirely into their Notes cells; the sign-off date changed to `2026-07-15T00:00:00Z`). Second run: **PASS, zero warnings**, `sha256:8517330957578346a96c9e5f48ede2e8f388c882469889e662f33ca471ac3bc0`.
- **Next steps:** bring this spec package to the human/Coordinator for Planning Preflight review, including a decision on OQ-01 (authz remediation priority — the recommended first follow-up), OQ-02 (permission-set scope), and OQ-03 (GAP prioritization for everything else); then, if a follow-up implementation pass is wanted, a dedicated `/plan` dispatch. Recommend updating `ADR-INDEX.md`'s Media row (line 36) to reference SPEC-021 once this package is approved — not done as part of this dispatch (scope discipline: read-only against everything outside the two designated spec/report folders).
