# Implementation Outline: settings-core-ledger

- Spec: SPEC-007 v0.3.1 (hash: sha256:fc322f69fe3cc586dbe9823c4d3d6d5225e419d538be554bdfe88873a40a501b)
- ADR: ADR-PIPE-007 (Settings Core-Only — Implementation Architecture)
- Status: PRODUCED
- Trigger result: Boundary Cross, Contract Change, Data And Persistence, Brownfield Dependency, Critical Cross-Boundary Invariant
- Date: 2026-07-11
- Author: Software Architect Agent

> Use this artifact only for post-ADR, pre-tasks structure. It defines module boundaries, public/exported contracts, wiring, data boundaries, and critical invariants. It must not contain pseudo-code, private helper inventories, or task sequencing.

## Trigger Decision Matrix

| Trigger | Applies? | Evidence | Source Trace |
|---|---:|---|---|
| Boundary Cross | yes | Feature crosses `src/features/settings` (domain) + `src/server/routes/admin/settings` (API) + `apps/admin/src/sections/Settings.tsx` (UI) + `src/identity` (permission catalog + `PrincipalRepoPort` read) | REQ-10, REQ-11, REQ-13 |
| Contract Change | yes | 5 new admin HTTP endpoints; new `PRINCIPAL_NOT_FOUND` error code; new `SettingsRepoPort` exported interface | api.spec.md §1, errors.spec.md §2 |
| System Wiring | no | No queues, webhooks, or async jobs — the write chokepoint and resolver are synchronous ordinary core code (spec-manifest.md orchestrator.spec.md omission justification) | spec-manifest.md |
| Data And Persistence | yes | 5 new tables (ADR-028 §2 DDL); brownfield migration of `presentation_settings` into the new ledger | REQ-01, REQ-08, ADR-028 §2 |
| Brownfield Dependency | yes | `PresentationSettingsRepoPort` retirement with 3 named existing consumers (`appliers.ts`, `seed.ts`, `navigation/ports.ts`) | spec-manifest.md Brownfield References |
| Reverse-Spec Or Migration | no | Not a reverse-spec extraction; the brownfield migration is scoped and handled under Data And Persistence / Brownfield Dependency above | — |
| Critical Cross-Boundary Invariant | yes | INV-01 (no value row without same-tx revision), INV-02 (resolver totality), INV-07 (fail-closed authorize-first), INV-09 (principal workspace match) all span the chokepoint + repo + resolver | feature.spec.md Invariants |
| Parallelization Ambiguity | yes | Schema/chokepoint work must land before API routes and UI can be built against real contracts; without this outline the Coordinator can't safely mark API/UI slices `[P]` relative to the domain-layer slice | ADR-PIPE-007 Rationale |

## Module Map

| Module/Domain | Owns | Responsibility | Public Contracts | Dependencies | Notes |
|---|---|---|---|---|---|
| `settings` (`src/features/settings/`) | The 5 ADR-028 tables; all setting definitions and values | Registry, resolver, single write chokepoint, purge, legacy-value migration | C-001..C-010 | `core` (ports/types), `identity` (`authorize()`, `PrincipalRepoPort`), `infra/db` (schema) | No `SettingsPort` service abstraction — `settings.ts` is the one evaluator (ADR-028 §8) |
| `identity` (existing, extended) | `settings.*` permission strings; unchanged `PrincipalRepoPort` | Permission catalog registration; principal existence/workspace lookup (reused, not extended) | Existing `PrincipalRepoPort.findById` (unchanged signature) | — | `settings` reads this module; this module gains no new dependency on `settings` |
| `server` routes (existing, extended) | HTTP wiring for `settings` | Route registration + `authorize()` gating + error mapping | C-006..C-010 | `settings`, `identity` | Mirrors `routes/admin/menus/` shape exactly |
| `apps/admin` UI (existing, extended) | `Settings.tsx` screen | Renders/edits settings via the admin API | C-011 (UI-facing, not typed here — see ui.spec.md) | `server` routes (via fetch) | Single flat file per repo convention |

## File Map

| File Path | Module | Creates / Changes | Public Contracts Housed | Responsibility | Why This Separation Exists | Notes |
|---|---|---|---|---|---|---|
| `src/features/settings/settings.ts` | settings | creates | C-001 `getEffective`, C-002 `registerDefinitions` (pure validation half) | Resolver + definition-registry pure logic | The "one evaluator" (ADR-028 §8) — no separate `SettingsPort`; mirrors `identity/authorize.ts`'s single-evaluator shape | No writes happen here — see `write-service.ts` |
| `src/features/settings/write-service.ts` | settings | creates | C-003 `SettingsWriteService.set`, C-004 `.clear`, C-005 `deriveRequiredPermission` [internal-invariant] | THE write chokepoint (REQ-04); self-vs-other permission derivation (REQ-06/behavior.spec §1.3); REQ-13 target-principal check | Isolating the chokepoint in its own file lets Code Review enforce "repo write methods imported only here" as a file-boundary check, not a convention | Repo write methods are imported here only — never re-exported |
| `src/features/settings/purge-service.ts` | settings | creates | C-006 `purgeTenantSettings` | Ledgered tenant/principal teardown (REQ-07) | Distinct high-privilege operation (`settings.definitions.manage`-gated per ADR-028 §7), kept separate from the per-key chokepoint | Loops over `write-service.ts`'s internals in a single transaction — not via repeated public `clear()` calls (must share the one tx) |
| `src/features/settings/ports.ts` | settings | creates | C-007 `SettingsRepoPort` | Persistence seam, rule-of-two | Matches `presentation`/`identity` port convention | Deliberately excludes any principal-lookup method — `identity.PrincipalRepoPort` is imported directly by `write-service.ts` |
| `src/features/settings/repo.memory.ts` | settings | creates | (implements C-007) | In-memory adapter for tests | Rule-of-two adapter #1 | — |
| `src/features/settings/repo.sqlite.ts` | settings | creates | (implements C-007) | Drizzle/SQLite adapter | Rule-of-two adapter #2 | Reads/writes the 5 tables added to `src/infra/db/schema.ts` |
| `src/features/settings/migration.ts` | settings | creates | C-008 `migrateLegacyPresentationSettings` | One-time brownfield value migration (REQ-08) | Isolated so it can be invoked once from `seed.ts` and is trivially unit-testable against a fixture `presentation_settings` row set | Idempotent — safe to call more than once (upsert semantics) |
| `src/features/settings/errors.ts` | settings | creates | C-009 typed error classes incl. `PrincipalNotFoundError` | Typed domain errors → route-layer error-code mapping | Mirrors `PresentationSettingsNotFoundError`/`PresentationSettingsValidationError` convention | One class per errors.spec.md §2 code that originates in this module |
| `src/infra/db/schema.ts` | infra | changes | (Drizzle table defs, not a "contract" per se) | Adds 5 Drizzle table definitions per ADR-028 §2 DDL | Schema is a shared file across all features by existing convention | No behavior — pure schema |
| `src/identity/permissions.ts` | identity | changes | (extends `BASE_CATALOG`, not a new contract) | Registers `settings.*` permission strings (REQ-06) | Existing single registration point for all permission strings | `settings.write` marked deprecated per ADR-028 §7 (removal is a follow-up, not this feature) |
| `src/identity/seed.ts` | identity | changes | (extends seed migration, not a new contract) | Grants `settings.definitions.manage` to existing `settings.write` holders (ADR-028 §7) | Existing seed-time migration location | — |
| `src/server/routes/admin/settings/register-definitions.ts` | server | creates | C-010a `registerAdminSettingsRegisterDefinitionsRoute` | HTTP wiring for `SETTINGS_REGISTER_DEFINITIONS` | One file per endpoint, matches every other `routes/admin/<feature>/<action>.ts` | — |
| `src/server/routes/admin/settings/get-effective.ts` | server | creates | C-010b `registerAdminSettingsGetEffectiveRoute` | HTTP wiring for `SETTINGS_GET_EFFECTIVE` | Same as above | — |
| `src/server/routes/admin/settings/set.ts` | server | creates | C-010c `registerAdminSettingsSetRoute` | HTTP wiring for `SETTINGS_SET`, incl. `PRINCIPAL_NOT_FOUND`→404 mapping | Same as above | — |
| `src/server/routes/admin/settings/clear.ts` | server | creates | C-010d `registerAdminSettingsClearRoute` | HTTP wiring for `SETTINGS_CLEAR`, incl. `PRINCIPAL_NOT_FOUND`→404 mapping | Same as above | — |
| `src/server/routes/admin/settings/reset.ts` | server | creates | C-010e `registerAdminSettingsResetRoute` | HTTP wiring for `SETTINGS_RESET` | Same as above | — |
| `src/server/app.ts` | server | changes | (wiring only) | Imports + registers the 5 route registrars | Existing single wiring point | — |
| `src/server/seed.ts` | server | changes | (wiring only) | Invokes `migrateLegacyPresentationSettings()` at boot | Existing seed boot path | — |
| `src/core/commands/appliers.ts` | core | changes | (consumer re-point, not a new contract) | Reads active theme via `getEffective` instead of `PresentationSettingsRepoPort` | Named brownfield consumer #1 | — |
| `src/navigation/ports.ts` | navigation | changes | (consumer re-point) | Same re-point | Named brownfield consumer #2 | — |
| `apps/admin/src/sections/Settings.tsx` | apps/admin | creates | (UI-facing contracts per ui.spec.md, not duplicated here) | The Settings admin screen | Matches `Members.tsx`/`Menus.tsx` flat-file convention | — |
| `apps/admin/src/App.tsx` | apps/admin | changes | (wiring only) | Mounts `<Settings />` | Existing per-section route wiring | — |

## Contract Map

| Contract ID / Name | File | Owner Module | Kind | Why Needed | Job | Inputs | Outputs | Validation | Errors | Effect Boundary | Complexity / Resource View | Aggregate-Risk Note | Spec/ADR Trace | Test Seam / Expectation |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| C-001 `getEffective` | `settings.ts` | settings | exported function | Resolver is the read-path contract every consumer (UI, other features, appliers.ts) depends on | Resolve one setting's effective value under strict layer precedence | `{ key: string, scopeContext: { workspaceId?, principalId? } }` | `{ value, sourceLayer, defVersion }` (never throws for a live key) | Alias-transparent; coerces stale `def_version` in memory, no write-back | None thrown for a live key — tombstoned key returns typed-absent (EC-10) | Pure decision (no I/O beyond the repo read) | O(1) — reads ≤3 layer rows + 1 definition row via cache | Invariant case: all-absent-layers path must return the validated default, never undefined (INV-02) | REQ-03, AC-04/05/06/21, INV-02 | Unit test against `repo.memory.ts` fixtures for every layer-precedence pair + tombstone + stale-version cases |
| C-002 `registerDefinitions` (validation half) | `settings.ts` | settings | exported function | Namespace-fence + schema validation must be pure/testable independent of the write tx | Validate a definition against owner-kind namespace fencing, scope bitmask, secret rejection | `{ definitions: DefinitionInput[] }` | `{ valid: true } \| { valid: false, error }` | `owner_kind` ↔ namespace fence (REQ-02); `secret !== true` (REQ-09/INV-08); `scopes` 1..7 | `DefinitionInvalidError`, `SecretNotSupportedError` | Pure decision | O(n) over definitions in the call | Invariant case: a site-owned def declaring the global scope bit must always be rejected (INV-05) | REQ-02, REQ-09, AC-02/03/15, INV-05, INV-08 | Unit test: one case per CHECK constraint in ADR-028 §2 DDL |
| C-003 `SettingsWriteService.set` | `write-service.ts` | settings | exported function (the chokepoint) | THE single value-mutation entry point (REQ-04) | Authorize → validate → write value + revision in one tx | `{ key, scope, value, scopeContext: { workspaceId?, principalId? } }` | `{ value, revisionSeq }` | `authorize()` first (fail-closed); scope ∈ definition's `scopes`; value validates against schema; REQ-13 principal check when scope=user and principalId≠caller | `FORBIDDEN`, `SCOPE_NOT_ALLOWED`, `VALUE_VALIDATION_FAILED`, `PRINCIPAL_NOT_FOUND`, `DEFINITION_TOMBSTONED` | Explicit side effect: DB write (value row + revision row, same tx) | O(1) — single-row upsert + single-row insert | **Aggregate-risk:** this is the sole path INV-01 depends on — a bug here breaks the "no value row without a revision" guarantee for every caller | REQ-04, REQ-13, AC-07/08/24, INV-01, INV-07, INV-09 | Integration test at the DB-transaction boundary (AC-07/AC-08/AC-24 are P1/P2 — verified at the real SQLite adapter, not just in-memory) |
| C-004 `SettingsWriteService.clear` | `write-service.ts` | settings | exported function | Symmetric chokepoint entry for clears | Same authorize/validate/write shape as `set`, writing `state='cleared'` | `{ key, scope, scopeContext }` | `{ revisionSeq }` | Same authorize + REQ-13 principal check as `set` | `FORBIDDEN`, `PRINCIPAL_NOT_FOUND` | Explicit side effect: DB write | O(1) | Same aggregate-risk class as C-003 | REQ-04, REQ-13, INV-01, INV-09 | Integration test mirroring C-003's |
| C-005 `deriveRequiredPermission` `[internal-invariant]` | `write-service.ts` | settings | internal pure function | The self-vs-other authorization rule (behavior.spec §1.3) must be a single source of truth, not re-implemented at the route layer | Given a scope + target principalId + caller's own principalId, return the exact permission string to check | `{ scope, targetPrincipalId?, callerPrincipalId }` | One of `settings.global.write \| settings.workspace.write \| settings.user.self.write \| settings.user.write` | scope=user: `targetPrincipalId` omitted or === caller → `.self.write`; differs → `.write` | None (total function) | Pure decision | O(1) | **Why internal-invariant:** a divergence between this function's logic and any route-layer re-implementation is a fail-open authorization bug (Red-Team RT-003) | REQ-06, AC-25/26, behavior.spec §1.3 | Direct unit test for both branches (AC-25 negative, AC-26 positive) — must be certified before C-003/C-004 are wired to call it |
| C-006 `purgeTenantSettings` | `purge-service.ts` | settings | exported function | Ledgered tenant/principal teardown (REQ-07) — the only sanctioned way to remove value rows | Authorize once → enumerate affected rows → append redacted `op='purge'` revision per row → delete rows, one tx | `{ workspaceId \| principalId }` | `{ purgedCount }` | `settings.definitions.manage` required (ADR-028 §7) | `FORBIDDEN` | Explicit side effect: DB writes (redacted revisions + deletes), single tx | O(n) over affected value rows | **Aggregate-risk:** must never delete a row without first appending its purge revision in the same tx (INV-06) | REQ-07, AC-12/13, INV-06 | Integration test: purge a workspace with N values, assert N redacted revisions + N deletions, all-or-nothing on failure |
| C-007 `SettingsRepoPort` | `ports.ts` | settings | exported interface | Persistence seam, rule-of-two (ADR-006/IV) | Define the storage contract `write-service.ts`/`settings.ts` depend on | N/A (interface) | N/A (interface) | N/A | N/A | N/A (interface, no behavior) | N/A | N/A | ADR-028 §8, Article IV | Both adapters (`repo.memory.ts`, `repo.sqlite.ts`) run the same contract-test suite |
| C-008 `migrateLegacyPresentationSettings` | `migration.ts` | settings | exported function | One-time brownfield value migration (REQ-08) | Read every `presentation_settings` row, write it through `SettingsWriteService.set` as `core.presentation.activeThemeId`, and seed `theme.{themeId}` definitions | none (reads all workspaces) | `{ migratedCount }` | Idempotent — reruns overwrite with the same value harmlessly | None (best-effort per row; a single row failure must not abort the others — logs and continues) | Explicit side effect: DB reads (legacy table) + DB writes (via C-003) | O(n) over workspaces | Invariant case: post-migration, `getEffective('core.presentation.activeThemeId', …)` must equal the pre-migration `PresentationSettingsRepoPort` value for every workspace (AC-14) | REQ-08, AC-14 | Integration test seeded with fixture `presentation_settings` rows, asserting AC-14's equivalence |
| C-009 `PrincipalNotFoundError` (+ siblings) | `errors.ts` | settings | exported error class | Typed error → route-layer `PRINCIPAL_NOT_FOUND` mapping (REQ-13) | Carry enough context (`principalId`, `workspaceId`) for the route to build the error payload | constructor args | N/A (Error subclass) | N/A | N/A | N/A | N/A | N/A | REQ-13, errors.spec.md | Route-layer test asserts this error class maps to HTTP 404 `PRINCIPAL_NOT_FOUND` |
| C-010a..e route registrars | `routes/admin/settings/*.ts` | server | exported functions | HTTP entrypoints, each `authorize()`-gated per api.spec.md §2 | Parse request → call the matching settings function → map result/errors to the api.spec.md §5/§6 response/error contracts | HTTP request per api.spec.md §4 | HTTP response per api.spec.md §5 | Auth profile per endpoint (api.spec.md §2) | Full mapping per api.spec.md §6, incl. new `PRINCIPAL_NOT_FOUND` 404 rows | Explicit side effect: HTTP I/O + delegates to C-001/C-003/C-004/C-006 | O(1) request handling (delegates complexity to the called contract) | N/A | REQ-10, AC-16, api.spec.md | Integration test per endpoint hitting the real route + SQLite adapter |

## Wiring Map

| Flow ID | Source | Transport/Call Type | Target | Payload/Contract | Ordering/Retry/Idempotency | Failure Handling | Trace |
|---|---|---|---|---|---|---|---|
| W-001 | `routes/admin/settings/set.ts` | direct call | `write-service.ts` C-003 | `SettingsWriteService.set` input/output | Not idempotent by design (each call appends a new revision) — client-side double-submit is a UI concern (ui.spec.md `ValueEditor` disables during in-flight save) | Any thrown typed error maps 1:1 to an HTTP error code (api.spec.md §6) | REQ-04, AC-07/08 |
| W-002 | `write-service.ts` (REQ-13 check) | direct call | `identity/ports.ts` `PrincipalRepoPort.findById` | `{ workspaceId, id }` → `PrincipalRecord \| null` | Read-only, no retry needed | `null` → throws `PrincipalNotFoundError` (C-009), caught and mapped by the calling route | REQ-13, AC-24 |
| W-003 | `server/seed.ts` boot path | direct call, once at startup | `migration.ts` C-008 | none in / `{ migratedCount }` out | Idempotent — safe if boot runs migration more than once (e.g. dev restarts) | Per-row failure is logged and skipped, does not abort the boot sequence | REQ-08 |
| W-004 | `apps/admin/src/sections/Settings.tsx` | HTTP fetch | `routes/admin/settings/*.ts` | api.spec.md §4/§5 request/response contracts | UI disables the save control during in-flight requests (ui.spec.md §4) | `ErrorBanner` renders recoverable errors with retry (ui.spec.md `onRetry`) | REQ-11, ui.spec.md §3 |
| W-005 | `core/commands/appliers.ts`, `navigation/ports.ts` | direct call (post-migration) | `settings.ts` C-001 `getEffective` | `{ key: 'core.presentation.activeThemeId', scopeContext }` | Read-only | N/A — `getEffective` is total, never throws for a live key (INV-02) | REQ-08, Migration Safety |

## Data And Side-Effect Boundaries

| Boundary | Owner | Reads | Writes | Side Effects | Consistency / Transaction Rule | Migration / Dual-Write Path |
|---|---|---|---|---|---|---|
| `setting_definitions` | settings | `settings.ts` resolver, route layer (list) | `write-service.ts` only (via C-002/C-003 registration path) | `setting_revisions` row appended same-tx on every change | Definition change + its revision row are one transaction (INV-01) | N/A — new table, no legacy data |
| `setting_values_global` / `_workspace` / `_user` | settings | `settings.ts` resolver | `write-service.ts` C-003/C-004 only; `purge-service.ts` C-006 for deletes | `setting_revisions` row appended same-tx | Value write + revision are one transaction (INV-01); value-table FKs are `ON DELETE RESTRICT`, never `CASCADE` | `setting_values_global` seeded once from legacy `presentation_settings` via C-008 (Migration Safety) |
| `setting_revisions` | settings | Any `settings.read.revisions`-permitted caller (reserved for future API per ADR-028 §7 RD2-02 — no reader ships in this pass) | `write-service.ts`, `purge-service.ts` only, always same-tx with the row it documents | None beyond the row itself | Append-only; never cascade-deleted (INV-06) | N/A |
| `presentation_settings` (legacy) | presentation (being retired) | `migration.ts` C-008 (read-only, once) | Unchanged — still owned by `presentation` until the follow-up deletion (ADR-PIPE-007 Migration Safety Point of No Return) | None new | Unchanged | Source of the one-time backfill into `setting_values_global` |
| `identity.principals` | identity (unchanged) | `write-service.ts` W-002 (read-only, via `PrincipalRepoPort`) | Not written by `settings` | None | N/A — read-only cross-module dependency | N/A |

## Observability And Operational Expectations

| Surface / Flow | Required Signals | Correlation / Trace Context | Metrics | Logs | Alert / Runbook Need | Privacy / Secret Constraints | Trace |
|---|---|---|---|---|---|---|---|
| All 5 admin routes | Structured error responses (errors.spec.md §1 envelope) | `correlationId` on every error payload | None new required for this pass (no NFR discovery flagged latency/throughput targets) | Standard request logging (existing server middleware) | N/A — no new production alerting surface beyond existing admin-route conventions | `secret:true` definitions are hard-rejected (INV-08) — no secret value ever reaches a log or revision row in this subset | REQ-10, errors.spec.md §1 |
| `write-service.ts` C-003/C-004 | Every write's `setting_revisions` row IS the audit signal (actor, seq, op, before/after) | `actor` field on every revision row | N/A | N/A (the ledger is the durable record, not a log line) | N/A | `before_json`/`after_json` on a purge (C-006) are redacted, never plaintext | INV-01, Article VIII |
| `migration.ts` C-008 boot-time run | The migrated rows' own `setting_revisions` entries prove the migration ran (no separate migration-specific telemetry needed) | N/A | N/A | Log a summary line (`migratedCount`) at boot for operator visibility | N/A — one-time, low-risk, small row count | N/A | Migration Safety |

## Critical Invariants

| Invariant ID | Scope | Rule | Reason | Enforcement Surface | Test Expectation | Trace |
|---|---|---|---|---|---|---|
| INV-01 | `write-service.ts` C-003/C-004, `purge-service.ts` C-006 | A value/definition row must never exist without a same-transaction revision row | Chokepoint integrity — the entire audit/never-brick guarantee depends on this | `write-service.ts` (module boundary — repo write methods package-private) | Integration test: assert exactly 1 value row + 1 revision row per write, same tx, on the real SQLite adapter | ADR-028 §4, feature.spec.md INV-01 |
| INV-02 | `settings.ts` C-001 `getEffective` | Must never throw or return undefined for a live key — worst case is the validated default | Resolver totality — every consumer (UI, `appliers.ts`, `navigation/ports.ts`) depends on this never erroring | `settings.ts` (pure function, directly testable) | Unit test: all-layers-absent case returns the definition default, no throw | feature.spec.md INV-02, AC-05 |
| INV-07 | `write-service.ts` C-003/C-004, `purge-service.ts` C-006, all route registrars | `authorize()` runs before any value is disclosed or mutated (fail-closed) | Security boundary — an unauthorized caller must never see or change a value | `write-service.ts` (authorize-first ordering), route layer (auth profile per endpoint) | Integration test: unauthorized caller gets `FORBIDDEN` with zero DB rows written (AC-08) | ADR-021, feature.spec.md INV-07 |
| INV-09 `[internal-invariant]` REQ-13 target-principal check | `write-service.ts` C-003/C-004, W-002 | `setting_values_user` row never written for a `principal_id` whose own `workspace_id` doesn't equal the request's `workspaceId` | Closes Red-Team RT-001 — prevents writing a value keyed to a principal that isn't actually in the target workspace (ADR-007 structural scoping) | `write-service.ts`, single call site to `identity.PrincipalRepoPort.findById` (W-002) | Integration test: set at scope=user with a `principalId` from a different workspace → `PRINCIPAL_NOT_FOUND`, zero rows written (AC-24) | feature.spec.md INV-09, REQ-13, AC-24 |

## Brownfield / Migration Mapping (if applicable)

| Source Behavior / Contract | Target Module / Contract | Preserve / Change | Characterization Evidence | Migration Safety Note |
|---|---|---|---|---|
| `PresentationSettingsRepoPort.findByWorkspaceId` (read active theme) | `settings.ts` C-001 `getEffective('core.presentation.activeThemeId', …)` | Preserve exact returned value per workspace (AC-14) | `src/features/presentation/__specs__/presentation-settings.spec.md` (superseded spec, behavior preserved as one setting per spec-manifest.md) | Migration must complete (C-008) before `appliers.ts`/`navigation/ports.ts` are re-pointed — see ADR-PIPE-007 Migration Safety |
| `PresentationSettingsRepoPort.save` (set active theme) | `write-service.ts` C-003 `set('core.presentation.activeThemeId', …)` | Change: now goes through the chokepoint (gets a revision) instead of a raw upsert | N/A — new behavior is strictly additive (an audit trail that didn't exist before) | No compatibility risk — old callers are re-pointed, not left running in parallel |
| Theme presets (hardcoded `ALLOWED_THEME_IDS` in `presentation.ts`) | `theme.{themeId}` settings definitions | Change: becomes a registered definition per theme, discovered at startup (per ADR-028 §1) | `src/features/theme` discovery (existing, referenced by `presentation.ts`'s own comments) | Backfilled by C-008 alongside the active-theme value migration |

## Test Expectations

- Contract tests: C-007 `SettingsRepoPort` — one shared contract-test suite run against both `repo.memory.ts` and `repo.sqlite.ts` (matches `presentation`/`identity` precedent).
- Integration tests: W-001 (SET through the real HTTP route + SQLite adapter, AC-07/08), W-002 (REQ-13 principal check, AC-24), W-003 (migration boot path, AC-14), all 5 admin routes (AC-16 permission-gating).
- Property/invariant tests: INV-01 (no orphan value row), INV-02 (resolver totality across all layer-presence combinations), INV-06 (purge-before-delete), INV-09 (principal workspace match).
- Characterization tests: AC-14's before/after equivalence check against fixture `presentation_settings` rows (this is the closest thing to a characterization test this brownfield migration needs — the legacy behavior is simple enough that no broader characterization suite is warranted).
- Explicitly N/A suites with reason: No orchestrator/async test suite — `orchestrator.spec.md` is omitted (spec-manifest.md — synchronous core code, no queue/job/webhook surface in this subset).

## Downstream Handoff Notes

- Coordinator task-generation constraints: Sequence schema (`infra/db/schema.ts`) + `settings.ts`/`write-service.ts`/`ports.ts`/adapters ahead of the API routes and UI — those depend on real contracts, not mocks, per Article V (Integration-First Testing). C-005 (`deriveRequiredPermission`) must be certified by TDD before `write-service.ts`'s C-003/C-004 are wired to call it (see ADR-PIPE-007 Quality Attribute Scorecard, security axis). The migration (C-008) and the brownfield re-point (`appliers.ts`, `navigation/ports.ts`) must land in the same task/PR — no partial cutover (Enforcement section, ADR-PIPE-007).
- TDD focus: Certify C-003/C-004 (the chokepoint) and C-005 (permission derivation) first — they're the highest aggregate-risk contracts. Then C-001 (resolver totality, INV-02) and C-006 (purge, INV-06). Route-layer tests last, once the domain layer is certified.
- Programmer architecture audit focus: Verify no import of `repo.memory.ts`/`repo.sqlite.ts` from outside `write-service.ts`; verify all 3 named brownfield consumers are re-pointed in the same PR as the migration; verify `PrincipalNotFoundError` maps to `PRINCIPAL_NOT_FOUND` 404 at every route that can raise it (`set`, `clear`).
- Open risks or ambiguities: None beyond what's already captured in ADR-PIPE-007's Consequences/Risks section (self-vs-other derivation consistency; incomplete migration cutover) — both have explicit mitigations recorded there.
