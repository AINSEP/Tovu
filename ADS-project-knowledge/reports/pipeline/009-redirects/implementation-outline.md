# Implementation Outline: redirects

- Spec: SPEC-009 v1.0.0 (hash: sha256:666f3726f38eda3cc6274ebb3bfd1ee5642bcfeffb2eb80bc3f48e89fc3986ef)
- ADR: ADR-PIPE-009 (Redirects — Implementation Architecture)
- Status: PRODUCED
- Trigger result: Boundary Cross, Contract Change, Data And Persistence, Brownfield Dependency, Critical Cross-Boundary Invariant, Parallelization Ambiguity
- Date: 2026-07-13
- Author: Software Architect Agent

> Use this artifact only for post-ADR, pre-tasks structure. It defines module boundaries, public/exported contracts, wiring, data boundaries, and critical invariants. It must not contain pseudo-code, private helper inventories, or task sequencing.

## Trigger Decision Matrix

| Trigger | Applies? | Evidence | Source Trace |
|---|---:|---|---|
| Boundary Cross | yes | Feature crosses `src/redirects` (domain), `src/routing` (forward-chain registration + slug-capture slot, modified), `src/origin` (open-redirect oracle, newly wired), `src/server/routes/{admin/redirects,site/pages}` (API + real site route), `apps/admin` (UI), `src/identity` (permission catalog) | REQ-08, REQ-09, REQ-12, REQ-15, REQ-18 |
| Contract Change | yes | 7 new admin HTTP endpoints; new `REDIRECT_TARGET_NOT_ALLOWED` error code; new `RedirectRepoPort` adapters; two new `src/routing` exports (`runPreContentPhase`/`runPostContentPhase`) | api.spec.md §1, errors.spec.md §2, ADR-PIPE-009 Decision B |
| System Wiring | no | No queues/webhooks beyond the existing ADR-009 outbox lane already used identically by every other feature (`redirect.created`/`.updated`/`.tombstoned`/`.hit`); no new async orchestration layer (spec-manifest.md orchestrator.spec.md omission) | spec-manifest.md |
| Data And Persistence | yes | 3 new tables (`redirects`, `redirect_revisions`, `redirect_hits`, ADR-033 §2); no brownfield row migration (net-new tables, no existing data) | REQ-06, state.spec.md §1 |
| Brownfield Dependency | yes | Stale `SlugChangeCapture` stub deletion in `src/redirects/types.ts`; first-time composition-root wiring of `src/origin`/`src/routing`; additive touch to `src/routing/routing.ts`; the content-chokepoint (`post.ts`) gap named as explicitly out of scope | spec-manifest.md Brownfield References, ADR-PIPE-009 Migration Safety |
| Reverse-Spec Or Migration | no | Not a reverse-spec extraction; the brownfield items above are scoped and handled under Brownfield Dependency | — |
| Critical Cross-Boundary Invariant | yes | INV-01 (no rule row without same-tx revision), INV-02 (no request observes a moved entry before its redirect exists), INV-03 (no redirect emitted to an unvalidated location — read-path oracle), INV-04 (one-hop chain), INV-07 (single write chokepoint) all span the chokepoint + repo + phase-handler + routing/content boundary | feature.spec.md Invariants |
| Parallelization Ambiguity | yes | Schema/chokepoint/matcher/capture/routing-wiring work must land before admin API routes, the site-route wiring, and the UI can be built against real contracts; without this outline the Coordinator can't safely mark those slices `[P]` relative to the domain-layer slice | ADR-PIPE-009 Rationale |

## Module Map

| Module/Domain | Owns | Responsibility | Public Contracts | Dependencies | Notes |
|---|---|---|---|---|---|
| `redirects` (`src/redirects/`) | The 3 ADR-033 tables; rule lifecycle, matching, hit counting, slug-capture | Chokepoint, bounded matcher, hit sink, capture implementation, phase-handler adapter | C-001..C-013 | `core` (ports/types), `routing` (phase registration + capture slot), `origin` (open-redirect oracle), `identity` (`authorize()`) | No `RedirectService` facade beyond the chokepoint module itself — mirrors `settings`'s "one evaluator" shape; `RedirectMatcher`/`RedirectHitSink` stay internal seams, not ports (ADR-033 ADR-006 accounting) |
| `routing` (existing, extended) | Forward-chain phase registry; `SlugChangeCapture` slot; NEW: two phase-runner exports | Registration + lookup surface `redirects` consumes; the interleaving seam Decision B adds | `runPreContentPhase`, `runPostContentPhase` (NEW), existing exports unchanged | — | `redirects` reads/writes this module's registries; this module gains no new dependency on `redirects` |
| `origin` (existing, newly wired) | Verified canonical origin + open-redirect/egress oracles | `isAllowedRedirectTarget` — the single oracle for both `redirects`' write and read paths | Existing `OriginRegistryPort` (unchanged signature) | — | First-time composition-root wiring happens as part of this feature (ADR-PIPE-009 Context item 4) |
| `identity` (existing, extended) | `admin.redirects.manage` permission string | Permission catalog registration | Existing `PermissionDescriptor` catalog (unchanged shape) | — | Follows the frozen `admin.<section>.<action>` convention, no deviation |
| `server` routes (existing, extended) | HTTP wiring for `redirects`; the real forward-resolution site route | Route registration + `authorize()` gating + error mapping; `GET /:slug` phase interleaving | C-011, C-012 (route registrars); site-route wiring (not a typed contract, see Wiring Map W-006/W-007) | `redirects`, `identity`, `routing` | Admin routes mirror `routes/admin/menus/` shape exactly; site-route change is surgical (two call sites in one existing handler) |
| `apps/admin` UI (existing, extended) | `Redirects.tsx` + `RedirectEditor.tsx` | Renders/edits redirect rules via the admin API | UI-facing, not typed here — see ui.spec.md | `server` routes (via fetch) | Two flat files per `Menus.tsx`/`MenuEditor.tsx` convention |

## File Map

| File Path | Module | Creates / Changes | Public Contracts Housed | Responsibility | Why This Separation Exists | Notes |
|---|---|---|---|---|---|---|
| `src/redirects/types.ts` | redirects | changes | (adds `RedirectTargetNotAllowedError`; removes stale `SlugChangeCapture`) | Deletes the pre-ADR-039 stub; adds the one new error class this spec introduces | Everything else in this file is already correct and stub-complete (ADR-033-governed) | See ADR-PIPE-009 Migration Safety — this deletion + the two files below land in the same PR |
| `src/redirects/ports.ts` | redirects | unchanged | (existing) `RedirectRepoPort`, `RedirectMatcher`, `RedirectHitSink`, `RedirectResolver`, hook types | Already fully specifies every seam this feature needs | No changes required — confirmed by direct read | — |
| `src/redirects/redirects.ts` | redirects | creates | C-001 `createRedirect`, C-002 `updateRedirect`, C-003 `tombstoneRedirect`, C-004 `importRedirects` | THE write chokepoint (REQ-06); write-time pattern + open-redirect validation, one-hop collapse, loop rejection | Isolating the chokepoint in its own file lets Code Review enforce "repo write methods imported only here (or via `ports.internal.ts`)" as a file-boundary check | Opens its own `BEGIN IMMEDIATE`/`COMMIT` per write, matching `settings/repo.sqlite.ts`'s established manual-transaction pattern |
| `src/redirects/capture.ts` | redirects | creates | C-005 `RedirectSlugChangeCapture` (implements routing's `SlugChangeCapture`) | In-tx auto-capture on slug change (REQ-15/16/17) | Isolated so its "no own transaction" discipline (ADR-PIPE-009 Decision A) is a one-file, one-reviewer-checklist-item property | MUST NOT open its own `BEGIN`/`COMMIT` — relies on the caller (content's chokepoint, once wired) already holding the connection's open transaction; calls `ports.internal.ts`'s shared insert helper directly |
| `src/redirects/phase-handler.ts` | redirects | creates | C-006 `RedirectResolver.resolve` (impl), C-007 `registerRedirectsPhaseHandlers` | Adapts `RedirectRequest`/`RedirectResolution` to/from `RouteResolvePhaseOutcome`; owns the read-path open-redirect oracle call | The single highest-risk contract in this feature (open-redirect on the read path) gets its own file for focused review + test-first certification | Calls `RedirectRepoPort.lookupExact`/`lookupLongestPrefix`/`listDynamic`, `RedirectMatcher.match`, then `OriginRegistryPort.isAllowedRedirectTarget` on the fully-interpolated location before ever returning a `redirect` outcome (C-013) |
| `src/redirects/matcher.ts` | redirects | creates | C-008 `RedirectMatcher` (impl: `match`, `validatePattern`) | Bounded, pure matching + pattern validation (REQ-07,19,20,22) | Pure logic, independently unit-testable without any repo/DB dependency | No backtracking engine; `matchType:'regex'` always fails `validatePattern` in v1 (REQ-22/INV-05) |
| `src/redirects/hit-sink.ts` | redirects | creates | C-009 `RedirectHitSink` (impl), C-010 `registerRedirectHitOutboxHandler` | Off-hot-path hit aggregation (REQ-21) | Isolated so the async outbox subscriber wiring is one file, matching every other feature's async-handler convention | `record()` is idempotent-tolerant (best-effort); never called synchronously from the phase-handler's response path |
| `src/redirects/ports.internal.ts` | redirects | creates | C-011-internal `insertRedirectAndRevision` `[internal-invariant]` | Package-private shared insert helper — the one place that actually issues the `INSERT`s for `redirects`/`redirect_revisions` | Shared by `redirects.ts` (which wraps it in its own tx) and `capture.ts` (which does not) — this is Decision A's load-bearing seam | Never exported from `index.ts`; imported only by `redirects.ts` and `capture.ts` |
| `src/redirects/repo.memory.ts` | redirects | creates | (implements `RedirectRepoPort`) | In-memory adapter for tests | Rule-of-two adapter #1 | — |
| `src/redirects/repo.sqlite.ts` | redirects | creates | (implements `RedirectRepoPort`) | Drizzle/SQLite adapter | Rule-of-two adapter #2 | Reads/writes the 3 tables added to `src/infra/db/schema.ts`; enforces the `(workspace_id, from_pattern)` exact-match uniqueness at the application layer (SQLite/Drizzle can't express a partial unique index through the builder — same documented limitation `settings/repo.sqlite.ts` already carries) |
| `src/redirects/index.ts` | redirects | creates | Public barrel | Mirrors `routing`/`origin` barrel shape | Deep imports into `src/redirects/` internals are not supported outside this barrel | `ports.internal.ts` is deliberately NOT re-exported here |
| `src/redirects/INFO.md` | redirects | creates | (docs, not a contract) | Module purpose doc | Matches every sibling Tier-2 library's own `INFO.md` | — |
| `src/routing/routing.ts` | routing | changes | C-012 `runPreContentPhase`, C-013 `runPostContentPhase` (NEW exports) | Thin wrappers around the existing module-private `runPhase()` | The additive fix for Decision B — lets a caller interleave content lookup between phases | `resolve()` itself is UNCHANGED; its own existing test suite must still pass unmodified |
| `src/routing/index.ts` | routing | changes | (barrel export only) | Barrel-exports the two new functions | — | — |
| `src/infra/db/schema.ts` | infra | changes | (Drizzle table defs, not a "contract" per se) | Adds `redirects`, `redirect_revisions`, `redirect_hits` (ADR-033 §2 DDL) | Schema is a shared file across all features by existing convention | No behavior — pure schema |
| `src/identity/permissions.ts` | identity | changes | (extends the registered catalog, not a new contract) | Registers `admin.redirects.manage` (REQ-12) | Existing single registration point for all permission strings | Follows the frozen `admin.<section>.<action>` convention (sweep-crosscutting-decisions-20260710.md line 71) — no deviation from what the spec directs |
| `src/server/http/admin/redirects.ts` | server | creates | (DTOs + `RedirectRouteDeps`/`RedirectRouteRegistrar`, not individually numbered — mirrors `http/admin/menus.ts`) | Serializes `RedirectRecord`/`RedirectHitStats` into stable admin JSON envelopes; declares the route-deps superset (`redirectRepo`, `originRegistry`) | Mirrors `server/http/admin/menus.ts`'s exact pattern | — |
| `src/server/routes/admin/redirects/list.ts` | server | creates | C-014a `registerAdminRedirectListRoute` | HTTP wiring for `LIST_REDIRECTS` | One file per endpoint, matches every other `routes/admin/<feature>/<action>.ts` | — |
| `src/server/routes/admin/redirects/get-by-id.ts` | server | creates | C-014b `registerAdminRedirectGetRoute` | HTTP wiring for `GET_REDIRECT` | Same as above | — |
| `src/server/routes/admin/redirects/create.ts` | server | creates | C-014c `registerAdminRedirectCreateRoute` | HTTP wiring for `CREATE_REDIRECT`, incl. `REDIRECT_TARGET_NOT_ALLOWED`/`REDIRECT_VALIDATION_ERROR`/`REDIRECT_LOOP_DETECTED`/`REDIRECT_CONFLICT` mapping | Same as above | — |
| `src/server/routes/admin/redirects/update.ts` | server | creates | C-014d `registerAdminRedirectUpdateRoute` | HTTP wiring for `UPDATE_REDIRECT` | Same as above | — |
| `src/server/routes/admin/redirects/tombstone.ts` | server | creates | C-014e `registerAdminRedirectTombstoneRoute` | HTTP wiring for `TOMBSTONE_REDIRECT` | Same as above | — |
| `src/server/routes/admin/redirects/import.ts` | server | creates | C-014f `registerAdminRedirectImportRoute` | HTTP wiring for `IMPORT_REDIRECTS` (207 multi-status) | Same as above | — |
| `src/server/routes/admin/redirects/hits.ts` | server | creates | C-014g `registerAdminRedirectHitsRoute` | HTTP wiring for `LIST_REDIRECT_HITS` | Same as above | — |
| `src/server/routes/site/pages.ts` | server | changes | (wiring only — Decision B) | `GET /:slug` calls `runPreContentPhase()` before its existing content lookup, and `runPostContentPhase()` inside its existing `PostNotFoundError` catch branch; emits the actual 301/302/307/308 on a `redirect` outcome | This is the real forward-resolution entry point in this repo (confirmed by direct read — it does not consult `routing` at all today) | See Wiring Map W-006/W-007 for the exact before/after call sites |
| `src/server/deps.ts` / `src/server/app.ts` | server | changes | (wiring only) | First-time `origin`/`routing` composition-root wiring; wires `redirects` repo/matcher/hitSink; registers the phase-handler + capture at boot | Existing single wiring point for every feature | Seeds a `dev-capability` verified origin for the seeded workspace (mirrors how every other dev-mode capability is seeded in `server/seed.ts`) |
| `src/server/routes/types.ts` | server | changes | (extends `RouteDeps` minimally, or — mirroring `MenuRouteDeps`'s precedent — a local `RedirectRouteDeps` extension in `http/admin/redirects.ts` instead) | `redirectRepo`/`originRegistry` availability to route handlers | Follows the exact precedent `server/http/admin/menus.ts`'s own doc comment sets: avoid touching the shared collision-prone `routes/types.ts` when a local extension suffices | Decided in favor of the local-extension pattern (matches the more recent `menus`/`settings` precedent over older direct-`RouteDeps`-edits) |
| `apps/admin/src/lib/api.ts` | apps/admin | changes | (adds Redirect types + client methods, not a new contract per se) | `listRedirects`/`getRedirect`/`createRedirect`/`updateRedirect`/`tombstoneRedirect`/`importRedirects`/`getRedirectHits` | Mirrors the existing `Menu` block exactly | — |
| `apps/admin/src/sections/Redirects.tsx` | apps/admin | creates | (UI-facing contracts per ui.spec.md, not duplicated here) | List screen; houses `RedirectRow`, `RedirectHitBadge`, `ErrorBanner` as in-file components (exported for reuse by `RedirectEditor.tsx`), matching `settings`'s single-file-houses-many-components precedent scaled down to two files | Matches `Menus.tsx`'s list-screen role | `RedirectImportDialog` also lives here (list-level action) |
| `apps/admin/src/sections/RedirectEditor.tsx` | apps/admin | creates | (UI-facing contracts per ui.spec.md) | Create/edit form; imports `RedirectHitBadge`/`ErrorBanner` from `Redirects.tsx` | Matches `MenuEditor.tsx`'s editor-screen role | — |
| `apps/admin/src/App.tsx` | apps/admin | changes | (wiring only) | Mounts `<Redirects />`/`<RedirectEditor />` at `#/redirects`, `#/redirects/new`, `#/redirects/:id` | Existing per-section route wiring | — |

## Contract Map

| Contract ID / Name | File | Owner Module | Kind | Why Needed | Job | Inputs | Outputs | Validation | Errors | Effect Boundary | Complexity / Resource View | Aggregate-Risk Note | Spec/ADR Trace | Test Seam / Expectation |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| C-001 `createRedirect` | `redirects.ts` | redirects | exported function (chokepoint) | The single manual-create entry point (REQ-01) | Validate pattern + target, collapse/reject loop, write rule + revision in one tx | `CreateRedirectInput` | `{ record: RedirectRecord }` | `RedirectMatcher.validatePattern`; `OriginRegistryPort.isAllowedRedirectTarget` when `toTarget` is absolute; loop-collapse/reject via `findByFromPattern` | `RedirectValidationError`, `RedirectTargetNotAllowedError`, `RedirectConflictError`, `RedirectLoopError` | Explicit side effect: DB write (record + revision, same tx) via `ports.internal.ts` | O(1) — single lookup + single upsert + single insert | **Aggregate-risk:** shares the write-path oracle call with C-002/C-004 — a bug here means an unsafe target could be persisted (though still caught again at read time by C-006/C-013) | REQ-01,06,07,08,13,14,22, AC-01/02/03/09/10/11/16/17/27, INV-01,04,05,07 | Integration test at the DB-transaction boundary (AC-08 forces a mid-transaction failure and asserts neither row persists) |
| C-002 `updateRedirect` | `redirects.ts` | redirects | exported function (chokepoint) | Symmetric chokepoint entry for updates (REQ-04) | Same validation/write shape as `createRedirect`, applied to a subset of mutable fields | `UpdateRedirectInput` | `{ record: RedirectRecord }` | Same as C-001, plus existing-record lookup | `RedirectNotFoundError` + same set as C-001 | Explicit side effect: DB write | O(1) | Same aggregate-risk class as C-001 | REQ-04,07,08,13,14, AC-06,09,10,11,16,17, INV-01,04,05,07 | Integration test mirroring C-001's |
| C-003 `tombstoneRedirect` | `redirects.ts` | redirects | exported function (chokepoint) | Soft-delete (REQ-05) | Flip `status:'disabled'`, append tombstone revision, same tx | `{ workspaceId, id, actorId }` | `{ record: RedirectRecord }` | Rule must exist and not already be tombstoned | `RedirectNotFoundError` | Explicit side effect: DB write | O(1) | Low — no validation/loop logic to get wrong, only a status flip + append | REQ-05, AC-07, INV-01,07 | Integration test: tombstoned rule stops matching (via C-006) but still appears in `list()` when `status` filter is omitted |
| C-004 `importRedirects` | `redirects.ts` | redirects | exported function (chokepoint) | Batch-create reusing the same chokepoint, no bypass (REQ-26) | Loop over items, call `createRedirect`'s validation/write path per item; collect per-item success/failure | `{ workspaceId, actorId, rules: CreateRedirectInput[] }` (1..500 items) | `{ created: RedirectRecord[], failed: Array<{ index, code, message }> }` | Batch size 1..500 (rejected above/below at the route layer, `VALIDATION_ERROR`, before this function runs); each item independently validated | Per-item errors collected, not thrown (partial success is expected, REQ-26/AC-31) | Explicit side effect: N independent DB writes, NOT one shared transaction across items (EC-08) | O(n) over batch size | Low-medium — must not accidentally short-circuit remaining items on one item's failure | REQ-26, AC-31, EC-08 | Integration test: 3-item batch with one invalid pattern → 2 written (each with own revision), 1 rejected with the same `REDIRECT_VALIDATION_ERROR` a manual create would produce |
| C-005 `RedirectSlugChangeCapture.onSlugChange` | `capture.ts` | redirects | exported class method (implements routing's `SlugChangeCapture`) | The in-tx auto-capture entry point (REQ-15,16,17) | Insert an `active`, `source:'auto_slug_change'`, `matchType:'exact'` rule + revision, assuming an already-open ambient transaction | `SlugChangeCaptureInput` (routing-owned: `workspaceId, entryId, oldPath, newPath, actor, changeSetId`) | `Promise<void>` | Idempotent by `changeSetId` (checks for an existing auto rule with the same `changeSetId` before inserting, REQ-17/AC-20) | Any unexpected error propagates uncaught (EC-05) — this function performs NO transaction control and NO error translation of its own | **Explicit side effect, NO transaction of its own** — calls `ports.internal.ts`'s `insertRedirectAndRevision` directly against the ambient connection | O(1) | **Aggregate-risk: highest in this file.** This function's correctness depends entirely on never opening its own `BEGIN`/`COMMIT` (ADR-PIPE-009 Decision A) — a violation here would silently break atomicity with the (not-yet-wired) content rename | REQ-15,16,17, AC-18,20, EC-05, INV-01,02 | Unit test: call `onSlugChange()` directly inside a manufactured open transaction (no HTTP layer needed) and assert the row + revision exist before that outer transaction commits; a second test asserts retrying the same `changeSetId` does not duplicate the rule (AC-20) |
| C-006 `RedirectResolver.resolve` (phase-handler impl) | `phase-handler.ts` | redirects | exported function (implements `RedirectResolver`) | The resolution entry point the routing-chain adapter (C-007) calls per phase | Look up exact/prefix/dynamic candidates for the request phase, run `RedirectMatcher.match`, then validate the fully-interpolated location before returning a match | `RedirectRequest` (`{ workspaceId, path, phase }`) | `RedirectResolution` | Applies phase eligibility (`override`-only in `pre_content`) before querying; see C-013 for the mandatory read-path oracle gate | None thrown — a failed oracle check or a repo miss both resolve to `{ matched: false }` (REQ-10) | Reads via `RedirectRepoPort.lookupExact`/`lookupLongestPrefix`/`listDynamic`; calls C-013 before returning any match | O(1) exact/prefix (index-backed) + O(k) dynamic scan, k = capped dynamic-set size (500) | Shares C-013's aggregate-risk (see below) | REQ-09,10,18,19,20, AC-12,13,14,21,22,23,24,25, INV-03,04 | Integration test at the `RedirectRepoPort` + `RedirectMatcher` boundary, seeded with competing rule sets per behavior.spec.md §1 |
| C-007 `registerRedirectsPhaseHandlers` | `phase-handler.ts` | redirects | exported function | Composition-root wiring entry — registers the two `RouteResolvePhaseHandler` adapters into `routing`'s registry | Translate `RedirectRequest`/`RedirectResolution` to/from `RouteResolvePhaseOutcome`; call `registerResolvePhase('pre_content', …)` and `registerResolvePhase('post_content', …)` once at boot | `{ deps: { repo, matcher, originRegistry } }` | `void` | N/A (registration only) | N/A | Registration side effect only (module-level state in `routing`) | O(1) | Low — pure adapter/translation, no business logic of its own | REQ-18, Dependencies table (`routing` integration) | Called once in `server/app.ts`'s boot sequence; a unit test asserts the adapter's outcome translation (`matched:true` → `{kind:'redirect', location, statusCode}`) is correct in isolation |
| C-008 `RedirectMatcher` (impl: `match`, `validatePattern`) | `matcher.ts` | redirects | exported object/functions (implements `RedirectMatcher`) | Pure, bounded matching + write-time pattern validation | `match`: pick the highest-precedence candidate per behavior.spec.md §1.1/§2.1, interpolate wildcard captures + prefix tails. `validatePattern`: reject invalid/unsafe patterns for a given `matchType`, including a hard reject of `matchType:'regex'` | `match`: `{ request, rules }`; `validatePattern`: `{ matchType, fromPattern }` | `match`: `RedirectResolution`; `validatePattern`: `{ ok: true } \| { ok: false, reason }` | Length bounds (1-2048), `priority` range (0-1000) enforced by callers per behavior.spec.md §4; `validatePattern` itself checks matchType-specific pattern well-formedness and hard-rejects `regex` | N/A (total functions, never throw) | Pure — no I/O | O(n) in `rules.length` for `match` (bounded by the capped dynamic-set size passed in); O(1) for `validatePattern` | Low — pure, exhaustively unit-testable, no hidden state | REQ-07,19,20,22, behavior.spec.md §1,2,4,7, AC-02,09,23,24,27, INV-05 | Unit test: one case per precedence pair (behavior.spec.md §1.1), one per tie-break level (§2.1), one per boundary in the Limits table (§4/§7) |
| C-009 `RedirectHitSink` (impl: `record`, `getStats`, `listStats`) | `hit-sink.ts` | redirects | exported class (implements `RedirectHitSink`) | Off-hot-path hit aggregation (REQ-21) | Upsert `redirect_hits.hitCount`/`lastHitAt` on `record`; read aggregate stats | `record`: `{ workspaceId, redirectId, at }`; `getStats`/`listStats`: `{ workspaceId, redirectId? }` | `record`: `void`; `getStats`: `RedirectHitStats \| null`; `listStats`: `RedirectHitStats[]` | None (best-effort) | Failure is caught and logged by the outbox subscriber (C-010), never rethrown to the request path | Explicit side effect: DB write (non-revisioned sidecar, explicitly narrows INV-01's revision discipline per ADR-033 §2) | O(1) per call | Low — a dropped increment loses a statistic, never a redirect (INV-06 equivalent) | REQ-21, AC-26, INV-06 (feature.spec.md numbering) | Integration test: force the sink to throw, assert the already-returned HTTP redirect response is unaffected (AC-26) |
| C-010 `registerRedirectHitOutboxHandler` | `hit-sink.ts` | redirects | exported function | Wires the `redirect.hit` outbox event to C-009's `record` | Subscribe to `redirect.hit` via the existing `EventBusPort`/outbox convention (mirrors `app.ts`'s `bus.subscribe('workspace.created', …)` pattern) | `{ deps: { bus, hitSink } }` | `void` | N/A (registration only) | N/A | Registration side effect only | O(1) | Low | REQ-21, ADR-009 lane 2 | Called once in `server/app.ts`'s boot sequence |
| C-011-internal `insertRedirectAndRevision` `[internal-invariant]` | `ports.internal.ts` | redirects | package-private function (NOT exported from `index.ts`) | The one place that actually issues `INSERT`s against `redirects`/`redirect_revisions` — shared by C-001-004 (which wrap it in their own tx) and C-005 (which does not) | Insert/update the record row + append the revision row, using whatever connection/handle is passed in — performs NO transaction control itself | `{ db, record, revision }` | `void` | Caller has already validated everything; this function does not re-validate | N/A (propagates any DB-level error uncaught) | Explicit side effect: DB write, but transaction-agnostic by design | O(1) | **Aggregate-risk: this is the sole path INV-01 depends on** — a bug here breaks "no rule row without a revision" for every caller, and (via C-005) breaks the atomicity guarantee with the future content-chokepoint wiring | INV-01, ADR-PIPE-009 Decision A | Direct unit test with a fake `db` handle asserting exactly one record write + one revision write per call, in call order; a second test calls it twice inside one manufactured outer transaction (simulating C-001 then C-005 sharing a connection) and asserts both persist together |
| C-012 `runPreContentPhase` | `routing/routing.ts` | routing | exported function (NEW) | The Decision B seam — lets a caller run only the `pre_content` phase | Normalize `path`, run only the `pre_content` registry, return the first non-null outcome or `null` | `(path: string, ctx: RouteResolveContext)` | `Promise<RouteResolvePhaseOutcome \| null>` | None beyond normalization | N/A (delegates to registered handlers; does not itself throw) | Reads module-private `phaseRegistry.pre_content` | O(h) in registered `pre_content` handlers | Low — thin wrapper around already-tested logic | REQ-18, ADR-PIPE-009 Decision B | Unit test in `routing`'s own suite: identical outcome to calling `resolve()` when no `post_content` rule would ever be reached; a new test asserts calling this alone does NOT also run `post_content` (the property `resolve()` couldn't offer) |
| C-013 `runPostContentPhase` | `routing/routing.ts` | routing | exported function (NEW) | The Decision B seam — lets a caller run only the `post_content` phase after its own content lookup has failed | Normalize `path`, run only the `post_content` registry, return the first non-null outcome or `null` | `(path: string, ctx: RouteResolveContext)` | `Promise<RouteResolvePhaseOutcome \| null>` | None beyond normalization | N/A | Reads module-private `phaseRegistry.post_content` | O(h) in registered `post_content` handlers | **This function's caller (the real site route) is where REQ-10's "never serve a redirect to an unvalidated location" guarantee actually surfaces to a real HTTP response — the single highest-risk contract in this whole feature.** The phase-handler (C-006) must have already run the read-path oracle check before this function's return value reaches the site route's redirect-emission code. | REQ-09,10,18, AC-12,13,14,21,22, INV-03, ADR-PIPE-009 Decision B | Integration test: the real `GET /:slug` route, seeded with a wildcard rule whose capture would interpolate to a disallowed cross-origin location, asserts a 404 (or fallthrough) is returned, never a 30x to the disallowed target (AC-12/AC-14) — this is the feature's single most important test |
| C-014a..g route registrars | `routes/admin/redirects/*.ts` | server | exported functions | HTTP entrypoints, each `authorize()`-gated per api.spec.md §2 (`admin.redirects.manage`) | Parse request → call the matching `redirects.ts`/`hit-sink.ts` function → map result/errors to api.spec.md §5/§6 response/error contracts | HTTP request per api.spec.md §4 | HTTP response per api.spec.md §5 | `ADMIN_SESSION` auth profile (api.spec.md §2) | Full mapping per api.spec.md §6, incl. `REDIRECT_TARGET_NOT_ALLOWED` (400, write path only) | Explicit side effect: HTTP I/O + delegates to C-001..004/C-009 | O(1) request handling (delegates complexity to the called contract) | N/A — routing is a thin pass-through, matching every other admin route in this repo | REQ-01-05,12,23,26, api.spec.md | Integration test per endpoint hitting the real route + SQLite adapter |

## Wiring Map

| Flow ID | Source | Transport/Call Type | Target | Payload/Contract | Ordering/Retry/Idempotency | Failure Handling | Trace |
|---|---|---|---|---|---|---|---|
| W-001 | `routes/admin/redirects/create.ts` (and update/tombstone/import) | direct call | `redirects.ts` C-001..C-004 | chokepoint input/output per Contract Map | Not idempotent by design (each call may append a new revision); `import` (C-004) processes items independently, no cross-item transaction | Any thrown typed error maps 1:1 to an HTTP error code (api.spec.md §6) | REQ-01,04,05,26, AC-01,06,07,31 |
| W-002 | `redirects.ts` C-001/C-002 | direct call | `origin` `OriginRegistryPort.isAllowedRedirectTarget` | `(ctx: RedirectTargetContext, url: string)` → `boolean` | Read-only, no retry needed | `false` → throws `RedirectTargetNotAllowedError`, caught and mapped by the calling route to `REDIRECT_TARGET_NOT_ALLOWED` (400) | REQ-08, AC-10,11 |
| W-003 | `phase-handler.ts` C-006 | direct call | `origin` `OriginRegistryPort.isAllowedRedirectTarget` | Same signature as W-002, called against the FULLY-INTERPOLATED `location`, not the stored `toTarget` template | Read-only | `false` → C-006 returns `{ matched: false }` (never throws, never surfaces as an HTTP error — REQ-10) | REQ-09,10, AC-12,13,14, INV-03 |
| W-004 | `server/app.ts` boot | direct call, once at startup | `phase-handler.ts` C-007 `registerRedirectsPhaseHandlers` | none in / `void` out | Idempotent-by-convention (registered once; a second boot-time call would double-register — guarded the same way `routing`'s own `resetRoutingRegistrationsForTests` implies registries are boot-time-singleton) | A registration failure at boot is a hard startup failure (fail loud, not silent) | REQ-18, ADR-PIPE-009 Context item 4 |
| W-005 | `server/app.ts` boot | direct call, once at startup | `capture.ts` C-005's owning module → `routing`'s `registerSlugChangeCapture()` | `SlugChangeCapture` instance in / `void` out | Idempotent-by-convention, same as W-004 | Startup failure, not silent | REQ-15, ADR-039 §4 |
| W-006 | `server/routes/site/pages.ts` `GET /:slug`, BEFORE its existing content lookup | direct call | `routing` C-012 `runPreContentPhase` | `(path, ctx)` → `RouteResolvePhaseOutcome \| null` | Read-only; runs unconditionally before `getPublishedPostBySlug` | A `redirect`/`resolved`/`not_found` outcome short-circuits the handler (emits the 30x, or the routing-decided response) BEFORE any content lookup runs; `null` falls through to the existing `Promise.all([getPublishedPostBySlug, …])` unchanged | REQ-18, AC-22, ADR-PIPE-009 Decision B |
| W-007 | `server/routes/site/pages.ts` `GET /:slug`, INSIDE the existing `catch (err instanceof PostNotFoundError)` branch | direct call | `routing` C-013 `runPostContentPhase` | Same shape as W-006 | Read-only; runs only after the real content lookup has already thrown `PostNotFoundError` | A `redirect` outcome emits the 30x instead of the existing hard-coded 404 response; `null` falls through to the existing 404 response unchanged | REQ-18, AC-21, EC-06, ADR-PIPE-009 Decision B |
| W-008 | `phase-handler.ts` C-006 (on a match) | outbox enqueue | `redirect.hit` event (ADR-009 lane 2) | `{ workspaceId, redirectId, at }` | Fire-and-forget from the response path — MUST NOT be awaited synchronously before the redirect response is sent | Enqueue failure is logged, never delays or blocks the redirect response (REQ-21/INV-06) | REQ-21, AC-26 |
| W-009 | outbox dispatcher (existing `processOutbox`) | event delivery | `hit-sink.ts` C-010's subscriber → C-009 `record` | `redirect.hit` payload | Idempotent-tolerant (a duplicate delivery double-increments in the worst case — acceptable per ADR-033 §7's "best-effort" posture; not specified as required-idempotent by any AC) | Handler failure is caught by the existing outbox retry/dead-letter convention, never surfaces to a request | REQ-21 |
| W-010 | `capture.ts` C-005 (called by the FUTURE content chokepoint, not yet wired — see Migration Safety) | direct call, ambient shared connection | `ports.internal.ts` C-011-internal | `{ db, record, revision }` | Must run strictly inside the caller's already-open transaction bracket; no transaction control here | Any throw propagates uncaught to the caller (the content chokepoint), which must abort its own transaction (REQ-16/EC-05) | REQ-15,16,17, ADR-PIPE-009 Decision A |
| W-011 | `apps/admin/src/sections/{Redirects,RedirectEditor}.tsx` | HTTP fetch | `routes/admin/redirects/*.ts` | api.spec.md §4/§5 request/response contracts | UI disables the relevant action control during in-flight requests (ui.spec.md §4 loading states) | `ErrorBanner` renders recoverable errors (ui.spec.md §3.3 validation-error event) | REQ-23,24,25, ui.spec.md |

## Data And Side-Effect Boundaries

| Boundary | Owner | Reads | Writes | Side Effects | Consistency / Transaction Rule | Migration / Dual-Write Path |
|---|---|---|---|---|---|---|
| `redirects` | redirects | `phase-handler.ts` C-006 (lookupExact/lookupLongestPrefix/listDynamic), route layer (list/get) | `redirects.ts` C-001-004 (own tx) and `capture.ts` C-005 (ambient tx) ONLY, both via `ports.internal.ts` C-011-internal | `redirect_revisions` row appended same-tx on every write | Rule row + its revision are one transaction (INV-01); C-005's writes share the CALLER's transaction, not its own (Decision A) | N/A — new table, no legacy data |
| `redirect_revisions` | redirects | Not read by any route in this pass (audit-only, matches settings' reserved-future-API precedent) | `ports.internal.ts` C-011-internal ONLY, always same-tx with the row it documents | None beyond the row itself | Append-only; never rewritten or deleted | N/A |
| `redirect_hits` | redirects | Route layer (`LIST_REDIRECT_HITS`), `hit-sink.ts` C-009 `getStats`/`listStats` | `hit-sink.ts` C-009 `record` ONLY, via the async outbox handler (W-009), never synchronously from the request path | None | Deliberately NON-transactional with `redirects`/`redirect_revisions` — explicitly narrows ADR-022 INV-3 (ADR-033 §2, ADR-027 precedent) | N/A |
| `origin`'s verified-origin store | origin (unchanged) | `redirects.ts` (W-002), `phase-handler.ts` (W-003), both read-only | Not written by `redirects` | None | N/A — read-only cross-module dependency; this feature's composition-root wiring seeds the dev-capability origin once at boot, not a per-request write | Seeded once via `InMemoryOriginSettingRepo` at boot, mirroring how `server/seed.ts` seeds every other dev-mode fixture |
| `routing`'s phase registry + `SlugChangeCapture` slot | routing (unchanged data shape; two new read-only-to-callers exports) | `phase-handler.ts` C-007 registers into it (write-once at boot); `pages.ts` reads via C-012/C-013 | Registration only happens at boot (W-004/W-005); no per-request writes | None | Module-level singleton state, boot-time-only mutation | N/A |

## Observability And Operational Expectations

| Surface / Flow | Required Signals | Correlation / Trace Context | Metrics | Logs | Alert / Runbook Need | Privacy / Secret Constraints | Trace |
|---|---|---|---|---|---|---|---|
| All 7 admin routes | Structured error responses (errors.spec.md §1 envelope) | `correlationId` on every error payload | None new required (no NFR discovery flagged latency/throughput targets) | Standard request logging (existing server middleware) | N/A — no new production alerting surface beyond existing admin-route conventions | None — redirect rules carry no secret/PII fields | REQ-23, errors.spec.md §1 |
| `redirects.ts` C-001-004 | Every write's `redirect_revisions` row IS the audit signal (actor, seq, tombstoned, recordedAt) | `changeSetId`-equivalent not applicable to manual writes; `actorId` on every revision row | N/A | N/A (the ledger is the durable record) | N/A | N/A | INV-01, Article VIII |
| `phase-handler.ts` C-006/C-013 read-path oracle rejection (REQ-09/10) | This is a **security-relevant fail-closed path** — every rejection should be logged (not surfaced to the client, per REQ-10) so an operator can see attempted open-redirect exploitation attempts against their rules | `redirectId` + rejected `location` in the log line | A future metric ("redirect.read_path_rejected count") is a plausible follow-up, not required by any AC in this pass | Log the rejected candidate at `warn` level — distinct from a normal 404 | Owner: Programmer (add the log line); no alert wiring required this pass, named as a future operability improvement | Never log the redirect target if it could contain injected credentials/PII in a query string — log the normalized host only, not the full raw candidate, mirroring `origin`'s own posture of never trusting raw input in logs | REQ-09,10, INV-03 |
| `capture.ts` C-005 | The `redirect.created` outbox event (fired post-commit, per ADR-039's transactional-outbox clarification) is the audit signal proving the auto-capture ran | `changeSetId` is the correlation id (REQ-17) | N/A | N/A | N/A | N/A | REQ-15,17 |
| `hit-sink.ts` C-009/C-010 | `redirect.hit` handler failures are logged, never surfaced as a redirect-serving failure (INV-06 equivalent) | `redirectId` | N/A | Log-and-continue on handler failure | N/A | N/A | REQ-21, AC-26 |

## Critical Invariants

| Invariant ID | Scope | Rule | Reason | Enforcement Surface | Test Expectation | Trace |
|---|---|---|---|---|---|---|
| INV-01 | `redirects.ts` C-001-004, `capture.ts` C-005, via `ports.internal.ts` C-011-internal | A `redirects` row must never exist without a same-transaction `redirect_revisions` row | Chokepoint integrity — the audit/never-brick guarantee depends on this | `ports.internal.ts` (module boundary — the only function that issues these `INSERT`s) | Integration test: assert exactly 1 record row + 1 revision row per write, same tx, on the real SQLite adapter, for BOTH a manual create (C-001) AND a manufactured-transaction call to `capture.ts` (C-005) | feature.spec.md INV-01 |
| INV-02 | `capture.ts` C-005 (once wired by the future content chokepoint) | No request may ever observe a moved entry's new location without its `auto_slug_change` redirect already existing | Never-break-links is the feature's core guarantee | `capture.ts`'s no-own-transaction discipline (Decision A) | Unit test: `onSlugChange()` called inside a manufactured open transaction, assert the redirect row is queryable via a query on that SAME transaction handle before commit | feature.spec.md INV-02, AC-18 |
| INV-03 | `phase-handler.ts` C-006/C-013 | A redirect resolution must never emit an HTTP redirect to a `location` that has not passed `isAllowedRedirectTarget` against the fully-interpolated target, immediately before the response is sent | The single highest-risk security contract in this feature — an unvalidated wildcard capture is a live open-redirect vector | `phase-handler.ts` (C-006 must call the oracle before returning any `matched:true` outcome); site-route wiring (W-006/W-007) must not have any path that emits a 30x without going through C-006 first | Integration test: the real `GET /:slug` route with a wildcard rule crafted to interpolate an authority-mutating location, asserting 404/fallthrough, never a 30x (AC-12/14) — this is the single most important test in this feature | feature.spec.md INV-03, REQ-09,10 |
| INV-04 | `redirects.ts` C-001/C-002 | A rule's resolved chain must never require more than one hop | Bounded resolution cost, immune to redirect loops by construction | `redirects.ts` (collapse-on-write via `findByFromPattern`) | Integration test: A→B→C collapses to A→C at write time (AC-16); B→A rejected when A→B exists (AC-17) | feature.spec.md INV-04, REQ-13,14 |
| INV-07 | `redirects.ts`, `capture.ts`, `ports.internal.ts` | Only these files may write `redirects`/`redirect_revisions`; no route handler writes them directly | Chokepoint discipline (ADR-022 §4a, ADR-033 §2) | Code Review import-boundary check (Enforcement, ADR-PIPE-009) | No automated CI canary in this pass (matches this repo's current settings/media precedent of Code-Review-enforced, not CI-enforced, chokepoint boundaries) | feature.spec.md INV-07 |

## Brownfield / Migration Mapping (if applicable)

| Source Behavior / Contract | Target Module / Contract | Preserve / Change | Characterization Evidence | Migration Safety Note |
|---|---|---|---|---|
| `src/redirects/types.ts`'s stale `SlugChangeCapture` (flat payload: `workspaceId, entryId, fromPath, toPath, actorId, pluginId`, no method) | `src/routing/types.ts`'s `SlugChangeCapture`/`SlugChangeCaptureInput` (method-bearing: `onSlugChange(input)`, fields `oldPath, newPath, actor, changeSetId`) | Change: delete the stale stub entirely, import the routing-owned shape instead — NOT a field-rename, a full replacement (the two shapes are structurally incompatible: one is a data payload, the other is an interface with a method) | Confirmed via direct read of both files; confirmed via `grep` that nothing outside `src/redirects/types.ts` imports the stale name | Must land in the same PR as `capture.ts`'s creation and its `registerSlugChangeCapture()` registration — see ADR-PIPE-009 Migration Safety |
| `routing.resolve()`'s bundled pre_content+post_content execution | `runPreContentPhase`/`runPostContentPhase` (new, additive) | Preserve: `resolve()` itself is unchanged, still callable by any future caller with no interleaving need | `routing`'s own existing `__tests__/routing.test.ts` must pass unmodified after the additive change | No compatibility risk — purely additive; no existing caller of `resolve()` exists in this repo yet to break |
| `src/server/routes/site/pages.ts`'s `GET /:slug` (currently: direct `getPublishedPostBySlug` call, no routing-chain consultation at all) | Same handler, now interleaved with C-012/C-013 | Change: the handler gains two new call sites (before its content lookup, and inside its `PostNotFoundError` catch); its existing success/error response shapes for the content-found and genuine-404 cases are unchanged | Direct read of the current handler (recorded verbatim in ADR-PIPE-009 Context) | No dual-write risk — this is additive branching inside one existing handler, not a data migration |
| N/A — content chokepoint (`post.ts` `updatePost`) wiring | N/A | Explicitly NOT changed by this feature (see ADR-PIPE-009 Migration Safety "Known accepted gap") | N/A | Owed to the content-lib owner as a follow-up feature; Coordinator must schedule it |

## Test Expectations

- Contract tests: `RedirectRepoPort` (C-001-004's persistence dependency) — one shared contract-test suite run against both `repo.memory.ts` and `repo.sqlite.ts` (matches `settings`/`presentation`/`identity` precedent).
- Integration tests: W-001 (admin CRUD/import through the real HTTP route + SQLite adapter, AC-01/06/07/08/31), W-002/W-003 (open-redirect oracle on both write and read paths, AC-10/11/12/13/14 — **W-003/INV-03 is the single highest-priority integration test in this feature**), W-006/W-007 (the real `GET /:slug` site route, AC-21/22/23/24, EC-04/06), W-004/W-005 (boot-time registration doesn't throw and is idempotent-by-convention).
- Property/invariant tests: INV-01 (no orphan rule row, both C-001-004's own-tx path and C-005's ambient-tx path), INV-02 (C-005's atomicity via a manufactured-transaction unit test, since the real content-chokepoint caller doesn't exist yet), INV-03 (see above), INV-04 (one-hop collapse across a 3+ rule chain).
- Unit tests: `matcher.ts` C-008 — one test per behavior.spec.md §1.1/§2.1/§4/§7 row (precedence pairs, tie-break levels, every documented boundary value); `capture.ts` C-005's idempotency (AC-20).
- Characterization tests: N/A — no existing behavior is being preserved under a rewrite (net-new feature; the only brownfield item, the stale type deletion, has zero runtime callers to characterize, confirmed by `grep`).
- Explicitly N/A suites with reason: No orchestrator/async-job test suite beyond the standard outbox-handler pattern (`orchestrator.spec.md` omitted, spec-manifest.md); no end-to-end "rename a real page through the real admin UI" test in this feature's own suite — that scenario's full wiring depends on the not-yet-built content-chokepoint follow-up (ADR-PIPE-009 Migration Safety), so AC-18 is certified against a direct `onSlugChange()` call instead, and this gap is named explicitly rather than silently left untested-and-unexplained.

## Downstream Handoff Notes

- Coordinator task-generation constraints: Sequence schema (`infra/db/schema.ts`) + `src/redirects/{types.ts,ports.internal.ts,matcher.ts,repo.memory.ts,repo.sqlite.ts,redirects.ts}` + the `src/routing/routing.ts` additive change ahead of the phase-handler (`phase-handler.ts`), which itself must land before the site-route wiring (`pages.ts`) and the admin API routes — those depend on real contracts, not mocks, per Article V. The stale `SlugChangeCapture` deletion, `capture.ts`, and its `registerSlugChangeCapture()` call must be one task/PR, not split across tasks (ADR-PIPE-009 Migration Safety — no partial cutover). The `origin`/`routing` composition-root wiring (W-004/W-005 plus the dev-capability origin seed) must land before ANY test that exercises `phase-handler.ts` end-to-end.
- TDD focus: Certify C-011-internal (`insertRedirectAndRevision`) and C-005 (`capture.ts`'s ambient-tx discipline) first — highest aggregate-risk for INV-01/INV-02. Then C-006/C-013 together (the read-path oracle gate, INV-03 — the single highest-priority contract in this feature, given the same "certify before wiring" treatment SPEC-007 gave its permission-derivation function). Then C-001-004 (the write chokepoint). Route-layer and UI tests last, once the domain layer is certified.
- Programmer architecture audit focus: Verify no import of `repo.memory.ts`/`repo.sqlite.ts` from outside `redirects.ts`/`capture.ts` (via `ports.internal.ts`); verify `capture.ts` never issues its own `BEGIN`/`COMMIT`; verify `phase-handler.ts`'s C-006 cannot return a `matched:true` redirect outcome without having called `isAllowedRedirectTarget` on the final interpolated location in every code path (not just the common case); verify `routing`'s own test suite is unmodified and green after the two new additive exports land; verify the stale `SlugChangeCapture` deletion, `capture.ts`, and its registration call are in the same PR.
- Open risks or ambiguities: None beyond what ADR-PIPE-009's Consequences/Risks section already names explicitly (the content-chokepoint follow-up gap; the future-second-in-tx-participant re-evaluation trigger for `SlugChangeCapture`'s eventual generalization). Both have named owners and triggers there — this outline does not introduce any new unresolved ambiguity.
