# Implementation Outline: members (remediation)

- Spec: SPEC-013 v1.0.0 (hash: sha256:4223c9dd7808b2a49977075b897ed237e979ce83b611ac6b4a963dd6b82eb5b1)
- ADR: ADR-PIPE-013 (Members Remediation)
- Status: PRODUCED
- Trigger result: Boundary Cross, Contract Change, Data And Persistence, Brownfield Dependency, Critical Cross-Boundary Invariant, Parallelization Ambiguity
- Date: 2026-07-13
- Author: Software Architect Agent

> Use this artifact only for post-ADR, pre-tasks structure. It defines module boundaries, public/exported contracts, wiring, data boundaries, and critical invariants. It must not contain pseudo-code, private helper inventories, or task sequencing.

## Trigger Decision Matrix

| Trigger | Applies? | Evidence | Source Trace |
|---|---:|---|---|
| Boundary Cross | yes | Remediation crosses `src/members` (domain), `src/server/routes/admin/members` + new `src/server/routes/members` (API, two distinct trust boundaries), `src/server/middleware/rate-limit.ts` (cross-cutting middleware), `src/origin`/`src/mail`/`src/identity` (consumed ports), `apps/admin/src/sections/Members.tsx` (UI) | ADR-PIPE-013 Decision §1-3, §7 |
| Contract Change | yes | 2 new public HTTP endpoints; 3 existing endpoints gain a required `authorize()` precondition; new `MemberConsentRepoPort` interface; 3 new `RateLimitProfile` constants; new `tovu_member_session` cookie | ADR-PIPE-013 API/Event Contract Summary |
| System Wiring | no | No queues, webhooks, schedulers, or async jobs — every new surface is a synchronous route handler or ordinary function call | — |
| Data And Persistence | yes | 7 new Drizzle table definitions (`members`, `member_tiers`, `member_subscriptions`, `member_sessions`, `member_magic_tokens`, `member_consents`, `member_revisions`); a `repo.sqlite.ts` adapter for 6 ports | ADR-PIPE-013 Decision §4-5, Migration Safety |
| Brownfield Dependency | yes | 4 existing, already-shipped, already-tested admin routes are modified (3 gain `authorize()`, 1 gains a shared rate-limit check); `Members.tsx` and `apps/admin/src/lib/api.ts`'s 3 existing methods are extended-in-place | spec-manifest.md Brownfield References, ADR-PIPE-013 Migration Safety |
| Reverse-Spec Or Migration | no | Not itself a reverse-spec extraction (SPEC-013 already is one); this outline is the forward gap-closing work that spec documented the need for | — |
| Critical Cross-Boundary Invariant | yes | INV-06 (constant anti-enumeration response) must survive the new rate-limit and origin-fallback logic; the member/admin session-cookie separation (ADR-030 §3) spans the new public route family and the existing admin route family; consent's request→confirm-only path to `granted` spans `consent-service.ts` and any future caller | feature.spec.md INV-06, ADR-030 §3, ADR-PIPE-013 Enforcement |
| Parallelization Ambiguity | yes | The authz fix (item 1), the rate-limit+public-routes slice (items 2-3), the consent module (item 4), the SQLite adapters (item 5), and the UI wiring (item 7) touch mostly-disjoint files but share `deps.ts`/`RouteDeps`-shaped wiring points — without this outline the Coordinator cannot safely mark them `[P]` relative to each other | ADR-PIPE-013 Module/Service Boundaries |

## Module Map

| Module/Domain | Owns | Responsibility | Public Contracts | Dependencies | Notes |
|---|---|---|---|---|---|
| `members` (`src/members/`) | All 6 member-domain tables (incl. new `member_consents`); the write chokepoint; the new consent chokepoint | Mutation + resolver logic for members, tiers, subscriptions, sessions, magic-link tokens, and consent | C-001..C-008 | `core` (ports/types), `identity` (`authorize()`, consumed by route layer not this module), `mail` (`MailerPort`), `origin` (`OriginRegistryPort`, new consumption) | `consent-service.ts` is a second chokepoint file, deliberately separate from `write-service.ts` (distinct actor model — see ADR-PIPE-013 Decision §4) |
| `server` admin routes (existing, extended) | HTTP wiring for the 4 existing admin member routes | Route registration + (now-uniform) `authorize()` gating + error mapping | C-009..C-012 | `members`, `identity` | Mirrors the unchanged shape of every other `routes/admin/<feature>/<action>.ts` |
| `server` public member routes (NEW) | HTTP wiring for the 2 new unauthenticated member routes | Route registration + rate-limit gating + cookie issuance | C-013, C-014 | `members`, `server/middleware/rate-limit` | Deliberately excludes `identity`'s `requireAdminSession` — this family must stay unauthenticated |
| `server/middleware/rate-limit` (existing, extended) | 3 new `RateLimitProfile` constants | Rate-limit policy data only — no new counting logic | C-015 | none (pure data) | `createRateLimiter`/`resolveClientIp` themselves are unchanged |
| `apps/admin` UI (existing, extended) | `Members.tsx` screen | Renders + now also mutates via 3 already-existing API client methods | C-016 (UI-facing, not typed here — see ui.spec.md) | `server` routes (via fetch, unchanged `api.ts`) | Single flat file per repo convention |

## File Map

| File Path | Module | Creates / Changes | Public Contracts Housed | Responsibility | Why This Separation Exists | Notes |
|---|---|---|---|---|---|---|
| `src/members/types.ts` | members | changes | C-001 `MemberConsentRecord`, `ConsentPurpose`, `ConsentStatus`, `ConsentEvidence` types | Domain shapes for D1c | Matches the existing types-only convention this file already follows | No behavior — types only |
| `src/members/ports.ts` | members | changes | C-002 `MemberConsentRepoPort` | Persistence seam for consent, rule-of-two | Matches the existing 5-port convention in this file | Package-private write access enforced at `consent-service.ts`'s boundary, same discipline as `write-service.ts` |
| `src/members/repo.memory.ts` | members | changes | (implements C-002) `InMemoryMemberConsentRepo` | In-memory adapter #1 | Matches the existing 5-adapter convention in this file | — |
| `src/members/repo.sqlite.ts` | members | creates | (implements 5 existing ports + C-002) | Drizzle/SQLite adapter #2 for all 6 ports | Closes REQ-18; mirrors `SqlitePostRepo`/`settings/repo.sqlite.ts` exactly | Not wired into `app.ts` boot path this pass (ADR-PIPE-013 Decision §5) — built and contract-tested only |
| `src/members/consent-service.ts` | members | creates | C-003 `requestConsent`, C-004 `confirmConsent`, C-005 `revokeConsent`, C-006 `checkConsent` | THE consent chokepoint (D1c) | Kept separate from `write-service.ts` because the actor model differs (`originModule` attribution vs. member/operator) — mixing them would blur two different authorization stories in one file | Repo write access to `member_consents` is imported here only |
| `src/members/write-service.ts` | members | changes | C-007 `requestSignInLink` (modified: absolute-URL + fallback) | Upgrades the magic-link URL from relative to absolute via `OriginRegistryPort`, with graceful fallback | Isolating the origin-resolution call inside the existing function (not a new file) because it's a small, localized change to one existing function's link-building step, not a new chokepoint | No other function in this file changes |
| `src/members/index.ts` | members | changes | (barrel additions for C-001..C-006) | Public surface export | Matches ADR-009 §1's "module public contract is its `index.ts`" rule already followed here | — |
| `src/infra/db/schema.ts` | infra | changes | (Drizzle table defs, not a "contract" per se) | Adds 7 table definitions (6 new domain tables + the widened `member_revisions.entity_kind` enum) | Schema is a shared file across all features by existing convention | No behavior — pure schema |
| `src/server/middleware/rate-limit.ts` | server | changes | C-008 `MAGIC_LINK_PER_EMAIL`, C-009 `MAGIC_LINK_PER_IP`, C-010 `MAGIC_LINK_COMPLETE_ATTEMPT` (all `RateLimitProfile` constants) | Policy data for the new rate limits | Existing single file already holds `LOGIN_STRICT`; new profiles belong alongside it, not in a new file | `createRateLimiter`/`resolveClientIp` unchanged |
| `src/server/routes/admin/members/list.ts` | server | changes | (no new contract — adds a precondition to the existing route) | Adds `authorize({permission:'member.manage'})` before listing | One file per endpoint, matches existing convention | Must reuse `disable.ts`'s exact call shape |
| `src/server/routes/admin/members/get-by-id.ts` | server | changes | same | Adds `authorize()` before returning a member | Same as above | — |
| `src/server/routes/admin/members/request-magic-link.ts` | server | changes | same, plus consumes C-011 (rate-limit check, email-only key) | Adds `authorize()` + the shared per-email rate-limit check | Same as above | Shares the `MAGIC_LINK_PER_EMAIL` limiter instance with the new public route (see Wiring Map) |
| `src/server/routes/admin/members/deps.ts` | server | changes | `MembersRouteDeps` gains rate-limiter instances + `memberConsentRepo` | Composition-boundary glue | Existing file already assembles the shared deps bundle for this route family | — |
| `src/server/routes/members/sign-in.ts` | server | creates | C-012 `registerPublicMemberSignInRequestRoute` | HTTP wiring for the new public sign-in request endpoint, rate-limited (2 checks) | New unauthenticated route family — must not import `requireAdminSession` | — |
| `src/server/routes/members/complete-sign-in.ts` | server | creates | C-013 `registerPublicMemberCompleteSignInRoute` | HTTP wiring for the new public complete-sign-in endpoint; sets `tovu_member_session` cookie | Same family as above | Must never read or write the `tovu_session` (admin) cookie name |
| `src/server/routes/members/deps.ts` | server | creates | `MemberPublicRouteDeps` | Narrower deps bundle for the unauthenticated family (no `authorize`/session deps) | Mirrors the existing `routes/admin/members/deps.ts` composition pattern, deliberately narrower | — |
| `src/server/app.ts` | server | changes | (wiring only) | Imports + registers the 2 new public routes outside `/api/admin`'s `requireAdminSession` middleware; wires `MemberConsentRepoPort` + rate-limiter instances into route deps | Existing single wiring point | Boot-time repo adapters remain in-memory (ADR-PIPE-013 Decision §5) |
| `apps/admin/src/sections/Members.tsx` | apps/admin | changes | (UI-facing contracts per ui.spec.md, not duplicated here) | Adds disable/resend-link/detail-expand row actions calling the 3 existing `api.ts` methods | Matches the existing flat-file convention | No change to `apps/admin/src/lib/api.ts` needed for these 3 actions |

## Contract Map

| Contract ID / Name | File | Owner Module | Kind | Why Needed | Job | Inputs | Outputs | Validation | Errors | Effect Boundary | Complexity / Resource View | Aggregate-Risk Note | Spec/ADR Trace | Test Seam / Expectation |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| C-001 `MemberConsentRecord` et al. | `types.ts` | members | exported types | Domain shape for D1c | N/A (types only) | N/A | N/A | N/A | N/A | N/A | N/A | N/A | ADR-PIPE-013 Decision §4 | Type-level only; exercised by C-002..C-006's tests |
| C-002 `MemberConsentRepoPort` | `ports.ts` | members | exported interface | Persistence seam, rule-of-two (Article IV) | Define the storage contract `consent-service.ts` depends on | N/A (interface) | N/A | N/A | N/A | N/A (interface) | N/A | N/A | ADR-030 §6 (D1c fold), Article IV | Both adapters run the same contract-test suite |
| C-003 `requestConsent` | `consent-service.ts` | members | exported function | Entry point into the pending state | Create a `pending` consent row for `(memberId, purpose)`, attributed to `originModule` | `{workspaceId, memberId, purpose, evidence, originModule}` | `{consent: MemberConsentRecord}` | `purpose` matches `ConsentPurpose` shape; `memberId` resolves in-workspace | `MemberNotFoundError` | Explicit side effect: DB write (value + revision, same tx) | O(1) | **Aggregate-risk:** the only entry point that may create a consent row — a bug here is the entry point for the "cannot assert granted directly" invariant | Crosscutting sweep §B (D1c) | Integration test: assert exactly 1 value row + 1 revision row, `status='pending'` |
| C-004 `confirmConsent` | `consent-service.ts` | members | exported function | The ONLY path to `status='granted'` | Transition an existing `pending` row to `granted`, recording confirmation evidence | `{workspaceId, memberId, purpose, evidence, originModule}` | `{consent: MemberConsentRecord}` | Row must exist and be `pending` | `MemberNotFoundError` (no pending row for that purpose) | Explicit side effect: DB write | O(1) | **Aggregate-risk:** this function, and only this function, may set `status='granted'` — must never be reachable except after `requestConsent` | Crosscutting sweep §B ("cannot assert granted directly") | Unit/integration test: confirming with no prior `requestConsent` call throws, never silently creates a granted row |
| C-005 `revokeConsent` | `consent-service.ts` | members | exported function | Symmetric teardown | Transition `granted → revoked` (idempotent no-op if already revoked) | `{workspaceId, memberId, purpose}` | `{consent: MemberConsentRecord}` | Row must exist | `MemberNotFoundError` | Explicit side effect: DB write | O(1) | Same discipline as `disableMember`'s idempotency | Crosscutting sweep §B | Integration test: revoking twice is a no-op, not an error |
| C-006 `checkConsent` | `consent-service.ts` | members | exported function | Read path for future consumers (e.g. Newsletter) | Return the current status for `(memberId, purpose)` | `{workspaceId, memberId, purpose}` | `{status: ConsentStatus \| 'none'}` | None (total function) | None thrown | Pure read | O(1) | None — read-only | Crosscutting sweep §B ("OR add members.consent.check") | Unit test: no row for that purpose returns `'none'`, not an error |
| C-007 `requestSignInLink` (modified) | `write-service.ts` | members | exported function (existing, modified) | Absolute-URL magic link with safe fallback | Resolve `OriginRegistryPort.canonicalOrigin`; build an absolute link; fall back to the existing relative path on `OriginNotVerifiedError` | unchanged input shape + implicit `origin` dep added | unchanged `{delivered: true}` | unchanged (email format) | unchanged; `OriginNotVerifiedError` caught internally, never surfaced to the caller | Explicit side effect: adds one read call to `OriginRegistryPort` before the existing mail-send | O(1) | **Invariant case:** INV-06's constant response must hold even when origin resolution fails — the fallback path must be silent to the caller, logged only | ADR-PIPE-013 Decision §3, feature.spec.md INV-06 | Unit test: origin-verified workspace gets absolute link; origin-unverified workspace gets today's relative link, same `{delivered:true}` response either way |
| C-008 `MAGIC_LINK_PER_EMAIL` | `rate-limit.ts` | server | exported const | Rate-limit policy for the public + admin request-magic-link surfaces | windowSeconds=3600, max=5, burst=0 | N/A (data) | N/A | N/A | N/A | N/A (pure data) | N/A | N/A | ADR-030 OQ-8, ADR-PIPE-013 Decision §2-3 | Boundary test: 6th request in the window for the same email is denied |
| C-009 `MAGIC_LINK_PER_IP` | `rate-limit.ts` | server | exported const | Rate-limit policy, IP-keyed, public route only | windowSeconds=3600, max=20, burst=0 | N/A (data) | N/A | N/A | N/A | N/A | N/A | N/A | Same | Boundary test: 21st request in the window for the same IP is denied |
| C-010 `MAGIC_LINK_COMPLETE_ATTEMPT` | `rate-limit.ts` | server | exported const | Defense-in-depth on the complete-sign-in route | windowSeconds=60, max=20, burst=5 | N/A (data) | N/A | N/A | N/A | N/A | N/A | N/A | Same | Boundary test: 26th attempt in 60s from one IP is denied |
| C-011 request-magic-link route (modified) | `routes/admin/members/request-magic-link.ts` | server | exported route registrar | Close REQ-10's gap + apply rate limiting to the admin-triggered path | `authorize()` then `MAGIC_LINK_PER_EMAIL.check(email)` then `requestSignInLink` | HTTP request | HTTP response per api.spec.md, plus new `429` | Auth profile now `AUTH_MEMBER_MANAGE` (was `AUTH_SESSION_ONLY`) | adds `FORBIDDEN`, `RATE_LIMIT_EXCEEDED` | Explicit side effect: HTTP I/O | O(1) | Ordering matters: `authorize()` before the rate-limit check (an unauthorized caller should not even consume rate-limit budget) | feature.spec.md REQ-10, ADR-030 OQ-8 | Integration test: unauthorized caller gets `403` and does not consume the rate-limit window |
| C-012 `registerPublicMemberSignInRequestRoute` | `routes/members/sign-in.ts` | server | exported route registrar | The new public self-service entry point | `MAGIC_LINK_PER_EMAIL.check` AND `MAGIC_LINK_PER_IP.check`, both must pass, then `requestSignInLink` | HTTP request `{email, redirectPath?}` | `{delivered: true}` or `429` | Email format (existing `MemberValidationError` path) | `MEMBER_VALIDATION_ERROR`, `RATE_LIMIT_EXCEEDED` | Explicit side effect: HTTP I/O + delegates to C-007 | O(1) | **Aggregate-risk:** the primary target of ADR-030 OQ-8 — a bug here (e.g. checking only one of the two limiters) reopens the enumeration/abuse surface | ADR-030 OQ-8, ADR-PIPE-013 Decision §2-3 | Integration test: both limiters independently enforced (6th same-email request denied even from different IPs; 21st same-IP request denied even for different emails) |
| C-013 `registerPublicMemberCompleteSignInRoute` | `routes/members/complete-sign-in.ts` | server | exported route registrar | The new public sign-in completion endpoint | `MAGIC_LINK_COMPLETE_ATTEMPT.check`, then `completeSignIn`, then set `tovu_member_session` | HTTP request `{token, userAgent?, ip?}` | `{member: PublicMemberResponse}` + `Set-Cookie: tovu_member_session` | Token existence/consumed/expired (existing `MemberAuthError` paths) | `MEMBER_AUTH_ERROR`, `MEMBER_NOT_FOUND`, `RATE_LIMIT_EXCEEDED` | Explicit side effect: HTTP I/O + cookie + delegates to `completeSignIn` | O(1) | **Aggregate-risk:** must never set `tovu_session` (admin cookie name) and must never read it; a naming or copy-paste error here would reintroduce the exact member/admin session confusion ADR-030 §3 exists to prevent | ADR-030 §3, feature.spec.md REQ-15/REQ-03 | Integration test: response cookie name is `tovu_member_session`, never `tovu_session`; a request carrying only a `tovu_session` cookie (no token) still requires a valid token — the admin cookie has zero effect on this route |
| C-014 `PublicMemberResponse` | `server/http/members.ts` (new, mirrors `server/http/admin/members.ts`) | server | exported type + serializer | The public-facing member shape returned by complete-sign-in | Serialize `MemberRecord` to a minimal public shape | `MemberRecord` | `{id, email, name, status}` | Excludes `note`, `fields`, and anything admin-only (mirrors `toAdminMemberResponse`'s exclusion discipline, even stricter — no `emailVerifiedAt`/`workspaceId`/`version` either, since this response goes to the member's own browser, not an operator) | N/A | N/A (pure mapping) | O(1) | A public serializer leaking an internal field is a direct PII exposure on an unauthenticated response — higher stakes than the admin serializer | ADR-PIPE-013 Enforcement | Unit test: response object has exactly the 4 named keys, nothing else |
| C-015 rate-limit wiring | `routes/admin/members/deps.ts`, `routes/members/deps.ts` | server | (composition, not a contract per se) | Assemble limiter instances once per app boot, shared correctly between the admin and public request-magic-link paths | N/A | N/A | N/A | N/A | N/A | N/A | N/A | ADR-PIPE-013 Wiring Map W-003 | Test: the same `MAGIC_LINK_PER_EMAIL` limiter instance (not two separate counters) is consulted by both the admin and public routes for the same email |
| C-016 Members.tsx row actions | `apps/admin/src/sections/Members.tsx` | apps/admin | (UI-facing, see ui.spec.md) | Disable / resend-link / detail-expand per row | Call the 3 existing `api.ts` methods on click | User interaction | Updated row state | Existing client-side error handling pattern (`notice error` div) | Surfaces the existing client error shape | Explicit side effect: HTTP fetch via `api.ts` | O(1) per action | None beyond normal UI-state risk | ADR-PIPE-013 Decision §7 | Component test (new, since none exists today): disable action calls `api.disableMember` and updates the row's status without a full reload |

## Wiring Map

| Flow ID | Source | Transport/Call Type | Target | Payload/Contract | Ordering/Retry/Idempotency | Failure Handling | Trace |
|---|---|---|---|---|---|---|---|
| W-001 | `routes/admin/members/{list,get-by-id,request-magic-link}.ts` | direct call | `identity.authorize()` | `{principalId, permission:'member.manage', workspaceId, entityType:'member', entityId?}` | Read-only, no retry; must run before any data is read or a token is minted | `allowed:false` → `403 FORBIDDEN`, no further action taken | ADR-PIPE-013 Decision §1, C-011 |
| W-002 | `routes/admin/members/request-magic-link.ts` AND `routes/members/sign-in.ts` | direct call | `MAGIC_LINK_PER_EMAIL` limiter instance (shared) | `.check(normalizedEmail)` | Not idempotent by design (every checked request counts) | `allowed:false` → `429` with `Retry-After` + `RATE_LIMIT_EXCEEDED` | ADR-030 OQ-8, C-008, C-011, C-012 |
| W-003 | `routes/members/sign-in.ts` only | direct call | `MAGIC_LINK_PER_IP` limiter instance | `.check(resolveClientIp(req))` | Same as W-002 | Same shape | C-009, C-012 |
| W-004 | `routes/members/complete-sign-in.ts` | direct call | `MAGIC_LINK_COMPLETE_ATTEMPT` limiter instance | `.check(resolveClientIp(req))` | Same as W-002 | Same shape | C-010, C-013 |
| W-005 | `write-service.ts` (`requestSignInLink`) | direct call | `origin.OriginRegistryPort.canonicalOrigin` | `{workspaceId}` | Read-only, no retry | `OriginNotVerifiedError` caught internally → fall back to the existing relative-path link; logged, not surfaced | ADR-PIPE-013 Decision §3, C-007 |
| W-006 | `routes/members/complete-sign-in.ts` | direct call | `write-service.ts` (`completeSignIn`) | existing signature, unchanged | Not idempotent (each valid token is single-use by `completeSignIn`'s own logic) | Existing `MemberAuthError`/`MemberNotFoundError` mapping, now surfaced on a public route for the first time | REQ-03, REQ-15 |
| W-007 | future Newsletter confirmation-mechanics route (NOT built this pass) | direct call | `consent-service.ts` (`requestConsent`/`confirmConsent`) | `{workspaceId, memberId, purpose, evidence, originModule:'newsletter'}` | N/A — out of this pass's scope; recorded here only so the contract shape is visible to the Newsletter team's future work | N/A | ADR-PIPE-013 Decision §4 (explicitly deferred) |
| W-008 | `apps/admin/src/sections/Members.tsx` | HTTP fetch | existing `api.ts` (`disableMember`/`requestMemberMagicLink`/`getMember`) | unchanged existing contracts | UI disables the clicked control during in-flight requests (new UI-state rule) | Inline error notice per row, matching the existing `notice error` convention | ADR-PIPE-013 Decision §7, C-016 |

## Data And Side-Effect Boundaries

| Boundary | Owner | Reads | Writes | Side Effects | Consistency / Transaction Rule | Migration / Dual-Write Path |
|---|---|---|---|---|---|---|
| `member_consents` | members | `consent-service.ts` (`checkConsent`) | `consent-service.ts` only (`requestConsent`/`confirmConsent`/`revokeConsent`) | `member_revisions` row appended same-tx on every change (`entity_kind='consent'`) | Value write + revision are one transaction, same discipline as ADR-030 §6's other tables | N/A — new table, no legacy data |
| `member_revisions` | members | Not read by any shipping caller this pass (reserved for a future audit view, same posture as `setting_revisions`) | `write-service.ts`, `consent-service.ts`, always same-tx with the row it documents | None beyond the row itself | Append-only; never cascade-deleted, FKs `ON DELETE RESTRICT` (crosscutting sweep §A Round-2 fold) | N/A |
| `members`, `member_tiers`, `member_subscriptions`, `member_sessions`, `member_magic_tokens` | members | `write-service.ts`, `access-resolver.ts`, admin + public route layers | `write-service.ts` only | Same as today, now also backed by a contract-tested SQLite adapter (not boot-wired) | Unchanged from ADR-030 §6 | N/A — the in-memory store has never persisted data; nothing to migrate |
| `identity.principals` | identity (unchanged) | Not read by this remediation (member principals are their own `members` table row, not an `identity.principals` row — ADR-030's OQ-1 `kind='member'` extension remains a separate, still-open item this ADR does not resolve) | Not written by `members` | None | N/A | N/A |
| `origin` settings (workspace canonical origin) | origin (unchanged) | `write-service.ts` W-005 (read-only, via `OriginRegistryPort`) | Not written by `members` | None | N/A — read-only cross-module dependency | N/A |

## Observability And Operational Expectations

| Surface / Flow | Required Signals | Correlation / Trace Context | Metrics | Logs | Alert / Runbook Need | Privacy / Secret Constraints | Trace |
|---|---|---|---|---|---|---|---|
| All 3 modified admin routes + 2 new public routes | Structured `429`/`403` responses (code + details, matching `LOGIN_STRICT`'s existing shape) | None new beyond what `LOGIN_STRICT` already lacks (no `correlationId` — carried Article VIII gap) | None new required this pass | Standard request logging (existing server middleware) | N/A — no new production alerting surface beyond the existing admin-route conventions | `tovu_member_session` and any raw magic-link/session token must never appear in a log line — same discipline `write-service.ts` already follows for the admin session token |
| `write-service.ts` origin-fallback path (W-005) | A warning-level log line when `OriginNotVerifiedError` triggers the relative-path fallback | `workspaceId` on the log line | N/A | One log line per fallback occurrence | N/A — informational, lets an operator notice their workspace's origin isn't verified without breaking the user-facing flow | Never logs the recipient email or the token | ADR-PIPE-013 Consequences (risk 2) |
| `consent-service.ts` writes | Every write's `member_revisions` row IS the audit signal (`entity_kind='consent'`, `op`, `actor`/`originModule`) | `originModule` field on every consent revision row | N/A | N/A (the ledger is the durable record) | N/A | Evidence payload (`consentTextHash`, not raw text) — never store the full consent-text body in the ledger, only a reference/hash |

## Critical Invariants

| Invariant ID | Scope | Rule | Reason | Enforcement Surface | Test Expectation | Trace |
|---|---|---|---|---|---|---|
| INV-06 (existing, extended) | `write-service.ts` `requestSignInLink`, `routes/members/sign-in.ts` | The response to a syntactically valid email must never reveal whether that email is registered — a rate-limit `429` is a volume signal, not an existence signal, and must trigger identically regardless of registration state | Preserves the anti-enumeration guarantee across the new rate-limit and origin-fallback code paths | `write-service.ts` (unchanged logic) + the new route's limiter check (keyed by email/IP volume, never by lookup result) | Integration test: an unregistered email and a registered email both get `{delivered:true}` below the rate limit, and both get `429` identically once the limit is exceeded | feature.spec.md INV-06 |
| INV-NEW-01 `[internal-invariant]` cookie isolation | `routes/members/complete-sign-in.ts`, `routes/admin/*` | The public member-route family must never set or read the `tovu_session` cookie name; the admin route family must never set or read `tovu_member_session` | Closes the exact member/admin session-confusion class ADR-030 §3 exists to prevent | Route-layer code (cookie-name string constants, one per family, never shared) | Integration test: a request bearing only a `tovu_session` cookie is treated as anonymous by the public member routes, and vice versa | ADR-030 §3, ADR-PIPE-013 Enforcement |
| INV-NEW-02 `[internal-invariant]` consent grant path | `consent-service.ts` | `member_consents.status` may only become `'granted'` via `confirmConsent`, and only when an existing `'pending'` row for that `(memberId, purpose)` exists | Closes the crosscutting sweep's explicit "Newsletter cannot assert `granted` directly" rule | `consent-service.ts` (module boundary — no other write path to `member_consents` exists) | Unit test: calling `confirmConsent` with no prior `requestConsent` throws `MemberNotFoundError`, never silently creates a granted row | Crosscutting sweep §B, ADR-PIPE-013 Decision §4 |
| INV-NEW-03 `[internal-invariant]` authz-before-rate-limit ordering | `routes/admin/members/request-magic-link.ts` | `authorize()` must run before the rate-limit check | An unauthorized caller must not be able to consume (or exhaust) the shared rate-limit budget for a target email as a side channel | Route handler ordering | Integration test: an unauthorized caller's request is rejected with `403` and does not decrement the rate-limit window for that email | ADR-PIPE-013 Contract Map C-011 |

## Brownfield / Migration Mapping

| Source Behavior / Contract | Target Module / Contract | Preserve / Change | Characterization Evidence | Migration Safety Note |
|---|---|---|---|---|
| `list`/`get-by-id`/`request-magic-link` admin routes, session-auth-only | Same routes, now also `authorize()`-gated | Change: a caller with a valid admin session but no `member.manage` now gets `403` instead of `200` | `traceability.spec.md` AC-21 ("no permission check blocks it") — this is the exact row this ADR intentionally changes; AC-21 must be updated/superseded, not silently left contradicting the new behavior | Recorded explicitly in ADR-PIPE-013 Migration Safety as the one user-facing breaking change this remediation makes, and why it's correct (closing a live authz gap) |
| `requestSignInLink`'s relative-path link | Absolute link via `OriginRegistryPort`, with fallback | Change (additive-safe): the response contract (`{delivered:true}`) is unchanged; only the mailed link's shape changes, and only for workspaces with a verified origin | `behavior.spec.md` §3 (`redirectPath link construction`) — its "no core/origin port exists yet" note is stale; this ADR corrects it | No compatibility risk — the fallback preserves today's exact behavior for any workspace that hasn't verified an origin yet |
| `apps/admin/src/lib/api.ts`'s `getMember`/`disableMember`/`requestMemberMagicLink` | Same, now actually called from `Members.tsx` | Preserve: no contract change, these methods are unchanged | `spec-manifest.md` Brownfield References (client methods confirmed unused) | Purely additive UI wiring; no risk to the existing client contract |

## Test Expectations

- Contract tests: C-002 `MemberConsentRepoPort` — one shared contract-test suite run against both `repo.memory.ts` and `repo.sqlite.ts`; extend the existing 5-port contract-test suite the same way rather than starting a new one.
- Integration tests: W-001 (authz gating on all 3 modified admin routes), W-002/W-003 (both rate-limit checks independently enforced on the public sign-in route), W-004 (complete-sign-in rate limit), W-005 (origin-fallback behavior), W-006 (end-to-end: public request → mint token → public complete → cookie set, distinct from the admin session cookie).
- Property/invariant tests: INV-06 (constant response under both registered/unregistered and under/over rate-limit conditions), INV-NEW-01 (cookie-family isolation), INV-NEW-02 (consent grant path), INV-NEW-03 (authz-before-rate-limit ordering).
- Characterization tests: none needed beyond the AC-21 update noted in Brownfield/Migration Mapping — there is no complex legacy behavior being preserved-under-refactor here, only additive gating.
- Explicitly N/A suites with reason: No orchestrator/async test suite (unchanged from SPEC-013's own `orchestrator.spec.md` omission — everything here stays synchronous core code). No dual-write reconciliation suite (Migration Safety: no live persisted data exists to reconcile).

## Downstream Handoff Notes

- Coordinator task-generation constraints: The authz fix (item 1) has no dependency on anything else in this outline and can be its own `[P]` slice. The rate-limiter profiles (C-008/009/010) must land before either route that consumes them (C-011/012/013) — sequence rate-limit-profile addition ahead of the two route slices, which can then proceed in parallel with each other (public sign-in vs public complete-sign-in touch different files). The consent module (C-001..C-006) and the SQLite adapter (repo.sqlite.ts) are both independent of the auth/rate-limit/public-route slice and of each other — safe to mark `[P]` against all of the above. The UI wiring (C-016) depends on nothing new on the backend (the 3 methods it calls already exist) and can proceed fully in parallel with every backend slice.
- TDD focus: Certify the two rate-limit profiles' boundary behavior (Nth request denied, window reset) and the origin-fallback branch of `requestSignInLink` (C-007) as isolated unit tests first — both are small, self-contained, and everything else composes them. Then certify the consent chokepoint's grant-path invariant (INV-NEW-02) before any route wiring. Route-level integration tests (authz gating, cookie isolation, end-to-end sign-in) last, once the underlying units are certified.
- Programmer architecture audit focus: Verify no import of `member_consents`' repo methods from outside `consent-service.ts`; verify the public route family (`routes/members/`) never imports `requireAdminSession` or reads/writes the `tovu_session` cookie name; verify `authorize()` runs before the rate-limit check in `request-magic-link.ts` (not after); verify `PublicMemberResponse`'s serializer excludes every field `toAdminMemberResponse` excludes plus `emailVerifiedAt`/`workspaceId`/`version`.
- Open risks or ambiguities: The rate-limit threshold values (5/hour per email, 20/hour per IP, 20/60s for complete-attempts) are judgment calls without production traffic data — captured as a Re-evaluation Trigger in ADR-PIPE-013, not a blocking ambiguity for implementation (the mechanism, not the specific numbers, is what TDD must certify precisely). AC-21 in `traceability.spec.md` will read as contradicted by this remediation until it's updated in a SPEC-013 version bump — Coordinator should schedule that spec update alongside or immediately after this outline's tasks, not leave the spec silently stale.
