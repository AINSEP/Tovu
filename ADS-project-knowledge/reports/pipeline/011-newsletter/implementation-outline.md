# Implementation Outline: newsletter

- Spec: SPEC-011 v1.0.0 (hash: sha256:dc29d67a0df612ed7ba71a36b40279eb3dfb934c3d91e8538fa429cb4eb5a583)
- ADR: ADR-PIPE-011 (Newsletter — Implementation Architecture)
- Status: PRODUCED
- Trigger result: Boundary Cross, Contract Change, System Wiring, Data And Persistence, Brownfield Dependency, Critical Cross-Boundary Invariant, Parallelization Ambiguity
- Date: 2026-07-13
- Author: Software Architect Agent

> Use this artifact only for post-ADR, pre-tasks structure. It defines module boundaries, public/exported contracts, wiring, data boundaries, and critical invariants. It must not contain pseudo-code, private helper inventories, or task sequencing.

## Trigger Decision Matrix

| Trigger | Applies? | Evidence | Source Trace |
|---|---:|---|---|
| Boundary Cross | yes | Feature crosses `src/newsletter/` (domain) + `src/server/routes/admin/newsletter` + `src/server/routes/site` (API) + `apps/admin/src/sections/Newsletter.tsx` (UI) + `src/identity` (permission catalog) + `src/mail`/`src/origin`/`src/integrations`/`src/members` (cross-module read/call seams) | REQ-10, REQ-11, REQ-14, REQ-16, REQ-25, REQ-30 |
| Contract Change | yes | 19 new admin HTTP endpoints + 2 public endpoints; a new `NEWSLETTER_LAUNCH_GATE_BLOCKED` + 10 other new error codes; 6 new exported repo-port interfaces; a new `MembersConsentCapability` cross-module seam; a new `NewsletterSendPipeline` orchestrator contract | api.spec.md §1/§1a, errors.spec.md §2, orchestrator.spec.md §4 |
| System Wiring | **yes** (unlike SPEC-007) | The send pipeline is genuine async orchestration riding the existing outbox/event-bus: a new `newsletter.send.batch.claimed` bus subscription, `SendBatchJob` fan-out, and a boot-time `declareDataModule()` invocation — this is precisely the case `orchestrator.spec.md`'s own Purpose section distinguishes from SPEC-007/009's synchronous-chokepoint-only shape | orchestrator.spec.md §1-§5, REQ-16, REQ-17 |
| Data And Persistence | yes | 2 new bespoke Drizzle tables (`newsletter_campaigns`, `newsletter_campaign_revisions`) + 5 new `p_newsletter__*` tables via `declareDataModule()` | state.spec.md §1, ADR-034 §2, ADR-023 |
| Brownfield Dependency | yes | `NEWSLETTER_PERMISSIONS` constant rename (stale `newsletter.*` -> `admin.newsletter.*`), confirmed zero live call sites (see ADR-PIPE-011 Migration Safety) | spec-manifest.md Brownfield References, feature.spec.md Agent Directives |
| Reverse-Spec Or Migration | no | Not a reverse-spec extraction; the one brownfield surface (permission rename) is scoped and handled under Brownfield Dependency above | — |
| Critical Cross-Boundary Invariant | yes | INV-01 (campaign chokepoint+revision), INV-02/REQ-24 (atomic counters, ADR-026-dependent), INV-03 (never write Members' consent record directly), INV-04 (unsubscribe consent-revision fail-closed), INV-05 (Launch Gate), INV-06 (frozen snapshot immutability), INV-07 (suppression-ledger primacy), INV-08 (never hold a live `MailerPort`), INV-09 (origin-only links), INV-10 (send idempotency) all span module boundaries | feature.spec.md Invariants |
| Parallelization Ambiguity | yes | Schema (both mechanisms) + the Launch Gate + the orchestrator's chokepoint actions must land before the 19 admin routes, the 2 public routes, and the UI can be built against real contracts; without this outline the Coordinator cannot safely mark `/tasks` slices `[P]` | ADR-PIPE-011 Rationale |

## Module Map

| Module/Domain | Owns | Responsibility | Public Contracts | Dependencies | Notes |
|---|---|---|---|---|---|
| `newsletter` (`src/newsletter/`) | `newsletter_campaigns`/`newsletter_campaign_revisions` (Drizzle) + 5 `p_newsletter__*` tables (`declareDataModule()`) | Campaign status machine + chokepoint; list/subscription/confirmation/unsubscribe chokepoints; the Launch Readiness Gate; the send-pipeline orchestrator; hook dispatch; feedback/erasure handlers | C-001..C-018 | `core` (ports/types, outbox/event-bus), `mail` (`MailerPort`, via core injection only — INV-08), `origin` (`OriginRegistryPort`), `integrations` (`KeyringPort.derive`), `members` (`SubscriberDirectoryPort` read-only; `MembersConsentCapability` typed seam, unbound until Members ships), `identity` (`authorize()`, permission catalog) | No `SettingsPort`-style single-evaluator umbrella — six distinct repo ports, each single-evaluator per table family, matching the `members` multi-port convention, not a merged mega-port |
| `identity` (existing, extended) | `admin.newsletter.*` permission strings | Permission catalog registration only | Existing `registerPermission()` (unchanged signature) | — | `newsletter` calls this at module-load time; `identity` gains no new dependency on `newsletter` |
| `server` routes (existing, extended) | HTTP wiring for `newsletter` (admin + public) | Route registration + `authorize()` gating (admin) + token-only auth (public) + error mapping | C-014..C-018 | `newsletter`, `identity` | Admin routes mirror `routes/admin/members/` shape; public routes mirror `routes/site/media-rendition.ts`'s unauthenticated shape |
| `apps/admin` UI (existing, extended) | `Newsletter.tsx` screen | Renders/edits campaigns/lists/subscriptions/send-log via the admin API | (UI-facing, not typed here — see ui.spec.md) | `server` routes (via fetch) | Single flat file per repo convention |

## File Map

| File Path | Module | Creates / Changes | Public Contracts Housed | Responsibility | Why This Separation Exists | Notes |
|---|---|---|---|---|---|---|
| `src/newsletter/ports.ts` | newsletter | changes (extends existing) | C-001 `NewsletterCampaignRepoPort`, C-002 `NewsletterListRepoPort`, C-003 `NewsletterSubscriptionRepoPort`, C-004 `NewsletterAudienceSnapshotRepoPort`, C-005 `NewsletterSendRepoPort`, C-006 `NewsletterConfirmationTokenRepoPort`, C-007 `MembersConsentCapability` | Persistence + cross-module seams | Matches `members`' multi-port-per-table-family convention; keeps the existing `SubscriberDirectoryPort`/`SendBatchJob` untouched | `MembersConsentCapability` is declared here (Newsletter's side of the seam), not in `src/members/*` — avoids any file collision with the parallel Members dispatch |
| `src/newsletter/types.ts` | newsletter | changes (extends existing) | `ConfirmationTokenRecord` (NEW), `NEWSLETTER_PERMISSIONS` renamed to `admin.newsletter.*` | Domain types + renamed permission catalog constant | OQ-03 resolved: dedicated table, mirrors `MagicLinkTokenRecord` | Existing `CampaignFields`/`NewsletterListRow`/etc. types are unchanged in shape |
| `src/newsletter/campaign.ts` | newsletter | creates | C-008 `transitionCampaignStatus` [internal-invariant] | Pure campaign status-machine guard (behavior.spec §1.1 transition-authority rule) | Isolating the status-machine rule as a pure function lets TDD certify every permission-tier transition ceiling independent of the DB/HTTP layers | No I/O — a decision function only, called by `campaign-write-service.ts` |
| `src/newsletter/campaign-write-service.ts` | newsletter | creates | C-009 `saveCampaign`, C-010 `cancelCampaign`, C-011 `scheduleCampaign` | THE campaign write chokepoint (REQ-06) — the only writer of `newsletter_campaigns`/`newsletter_campaign_revisions` | Isolating the chokepoint in its own file lets Code Review enforce "repo write methods imported only here" as a file-boundary check | Calls `campaign.ts`'s `transitionCampaignStatus` before every write; repo write methods are imported here only |
| `src/newsletter/lists.ts` | newsletter | creates | C-012 `saveList`, `archiveList` | List CRUD + default-list protection (REQ-08/09) | Small, independent chokepoint — lists have no status machine, don't belong in `campaign-write-service.ts` | `archiveList` rejects `isDefault: true` with `NEWSLETTER_DEFAULT_LIST_PROTECTED` |
| `src/newsletter/subscriptions.ts` | newsletter | creates | C-013 `saveSubscription`, `importSubscriptions` | Subscription add/import/remove chokepoint (REQ-10/31), calls `SubscriberDirectoryPort` for existence check | REQ-31 requires import to reuse the exact same chokepoint as manual add — keeping both entry points in one file makes that structurally obvious, not just conventionally true | Never creates a new Members identity (REQ-10) |
| `src/newsletter/confirmation.ts` | newsletter | creates | C-014 `issueConfirmationToken`, `consumeConfirmationToken` | ISSUE_CONFIRMATION_TOKEN / CONSUME_CONFIRMATION_TOKEN (REQ-11/12/13), calls `MembersConsentCapability.request`/`.confirm` | Confirmation-token lifecycle is a distinct concern from unsubscribe — different token type, different consent direction (grant vs revoke) | Invalidates the prior unconsumed token before/in the same transaction as issuing a new one (behavior.spec §2.1) |
| `src/newsletter/unsubscribe.ts` | newsletter | creates | C-015 `processUnsubscribe` [internal-invariant] | PROCESS_UNSUBSCRIBE (REQ-14/15), verifies the `KeyringPort`-derived token against `consentRevisionIdAtSubscribe` (INV-04), calls `MembersConsentCapability.revoke` | The fail-closed consent-revision check (INV-04) is this feature's second-highest-risk contract after the Launch Gate — isolating it lets TDD certify the fail-closed branch directly | Idempotent: a repeat of an already-processed unsubscribe is success, not `NEWSLETTER_UNSUBSCRIBE_TOKEN_INVALID` (EC-03) |
| `src/newsletter/launch-gate.ts` | newsletter | creates | C-016 `evaluateLaunchGate` [internal-invariant] | THE Launch Readiness Gate (REQ-21) — the task's central ask | A single source of truth for all four preconditions, in fixed order, always evaluated — never re-implemented at the route layer | See Critical Invariants below; this is this feature's highest-risk contract |
| `src/newsletter/send-pipeline.ts` | newsletter | creates | C-017 `authorizeSend`, `freezeAudience`, `claimBatch`, `dispatchRow`, `recordResult`, `completeIfDrained`, `pauseCampaign`, `resumeCampaign` | `NewsletterSendPipeline` — the orchestrator (orchestrator.spec.md §4) | Six independently-testable orchestrator actions in one cohesive file, matching `orchestrator.spec.md`'s own action table | `authorizeSend` calls `launch-gate.ts`'s `evaluateLaunchGate` exactly once, inside its transaction; never holds a live `MailerPort` (INV-08) |
| `src/newsletter/hooks.ts` | newsletter | creates | C-018 `registerBeforeSendHook`, `registerRecipientFilterHook`, `runHookChain` | Hook registration + fixed-order, fail-closed dispatch (REQ-23, behavior.spec §1.3) | Isolating hook ordering as its own file makes "recipient.filter always runs before beforeSend, always before `MailerPort.send()`" a single-file invariant TDD can certify directly | A throwing filter/hook is treated as fail-closed suppression (behavior.spec §7), never "keep by default" |
| `src/newsletter/feedback.ts` | newsletter | creates | (internal) `applyFeedbackProjection` | `mail.feedback.received` consumer -> subscription status projection (REQ-22) | Separates the read-side projection from the mail-lib's suppression ledger, which stays the source of truth (INV-07) | Filters on `sourceContext.module === 'newsletter'` (EC-09); never blocks the mail-lib ledger write it projects from |
| `src/newsletter/erasure.ts` | newsletter | creates | (internal) `anonymizeSendLog` | `principal.erasure.requested` handler (REQ-27) | GDPR erasure is a distinct lifecycle event handler, kept separate from the send-log's normal read/write path | Preserves `counters`/revision history; anonymizes only `recipientEmail` |
| `src/newsletter/errors.ts` | newsletter | creates | Typed error classes for every `errors.spec.md` `NEWSLETTER_*` code | Typed domain errors -> route-layer error-code mapping | Mirrors `src/members/types.ts`'s error-class pattern (no error classes exist in `newsletter/types.ts` today — errors.spec.md directs this file to add them) | One class per code in errors.spec.md §2 that originates in `newsletter` |
| `src/newsletter/data-module-manifest.ts` | newsletter | creates | (internal) `NEWSLETTER_DATA_MODULE`, `installNewsletterDataModule` | Newsletter's `DataModuleDecl` (5 tables) + the boot-time `declareDataModule()` invocation, scoped first-party-only | Isolating the manifest + invocation lets Code Review review the ADR-PIPE-011-flagged risk surface as one small file, and lets TDD write the required failure-path test against it directly | Not a general third-party registration API — invoked once, by name, from `server/seed.ts` |
| `src/newsletter/repo.memory.ts` | newsletter | creates | (implements C-001..C-006) | In-memory adapter for all six repo ports | Rule-of-two adapter #1; matches the one-memory-file-per-feature convention | — |
| `src/newsletter/repo.sqlite.ts` | newsletter | creates | (implements C-001, C-002, C-004, C-005, C-006) + `declareDataModule()`-backed accessors | Drizzle/SQLite adapter for the campaign pair; `declareDataModule()`-backed accessors for the 5 `p_newsletter__*` tables | Rule-of-two adapter #2 | Reads/writes the 2 tables added to `src/infra/db/schema.ts`; reads/writes the 5 tables via raw, authorizer-sandboxed accessors per ADR-023 §7/§8 |
| `src/infra/db/schema.ts` | infra | changes | (Drizzle table defs, not a "contract" per se) | Adds 2 Drizzle table definitions: `newsletterCampaigns`, `newsletterCampaignRevisions` | Schema is a shared file across all features by existing convention | No behavior — pure schema; the 5 `p_newsletter__*` tables are NOT declared here (they're `declareDataModule()`-owned, per ADR-PIPE-011 Decision) |
| `src/identity/permissions.ts` | identity | changes | (extends the registered catalog, not a new contract) | Registers `admin.newsletter.*` permission strings (REQ-25) via the existing `registerPermission()` pattern | Existing single registration point for feature-registered permissions | Confirmed additive — zero existing call sites reference the old unprefixed strings (ADR-PIPE-011 Migration Safety) |
| `src/server/routes/admin/newsletter/deps.ts` | server | creates | `NewsletterRouteDeps` | `RouteDeps` extended with `newsletter`'s six repo ports + `MembersConsentCapability` binding | Mirrors `MembersRouteDeps`'s exact shape/rationale (RouteDeps not yet amended upstream; each route casts) | Same pattern as `members/deps.ts` — composition-boundary glue only |
| `src/server/routes/admin/newsletter/*.ts` (19 files) | server | creates | C-019a..s route registrars (one per api.spec.md §1 endpoint) | HTTP wiring, each `authorize()`-gated per api.spec.md §2 | One file per endpoint, matches every other `routes/admin/<feature>/<action>.ts` | Full endpoint list: list/get/create/update/cancel/schedule/send/send-test/pause/resume-campaign; list/create/archive-list; list/create/remove/import-subscriptions; resend-confirmation; list-send-log |
| `src/server/routes/site/newsletter-confirm.ts` | server | creates | C-020 `registerNewsletterConfirmRoute` | HTTP wiring for `CONFIRM_SUBSCRIPTION` (public, token-only) | Mirrors `media-rendition.ts`'s unauthenticated-by-design pattern; lives outside `/api/admin` | Renders an HTML page, not JSON (api.spec.md §5) |
| `src/server/routes/site/newsletter-unsubscribe.ts` | server | creates | C-021 `registerNewsletterUnsubscribeRoute` | HTTP wiring for `UNSUBSCRIBE` (public, token-only, GET+POST) | Same as above | Supports RFC 8058 `List-Unsubscribe-Post` on `POST` |
| `src/server/app.ts` | server | changes | (wiring only) | Imports + registers the 19 admin route registrars (inside the `/api/admin` gate) + 2 site route registrars (outside it) + subscribes to `newsletter.send.batch.claimed` on the bus | Existing single wiring point | Mirrors the existing `bus.subscribe("workspace.created", ...)` demonstration exactly |
| `src/server/seed.ts` | server | changes | (wiring only) | Invokes `installNewsletterDataModule()` at boot + seeds each workspace's default `NewsletterListRow` (REQ-08) | Existing seed boot path | Idempotent — safe if boot runs more than once (matches `declareDataModule()`'s own idempotent skip-if-exists behavior) |
| `apps/admin/src/sections/Newsletter.tsx` | apps/admin | creates | (UI-facing contracts per ui.spec.md, not duplicated here) | The Newsletter admin screen | Matches `Members.tsx`/`Menus.tsx`/`Settings.tsx` flat-file convention | Internally composed of the 9 ui.spec.md components |
| `apps/admin/src/App.tsx` | apps/admin | changes | (wiring only) | Mounts `<Newsletter />` | Existing per-section route wiring | — |

## Contract Map

| Contract ID / Name | File | Owner Module | Kind | Why Needed | Job | Inputs | Outputs | Validation | Errors | Effect Boundary | Complexity / Resource View | Aggregate-Risk Note | Spec/ADR Trace | Test Seam / Expectation |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| C-008 `transitionCampaignStatus` `[internal-invariant]` | `campaign.ts` | newsletter | internal pure function | The permission-tier transition-authority rule (behavior.spec §1.1) must be a single source of truth | Given current status, requested status, and the caller's permission tier (pipeline / `.send` / `.schedule` / `.compose`), return allow/deny | `{ from, to, actorTier }` | `{ allowed: true } \| { allowed: false, reason }` | Pipeline-only for `sending`/`sent`; `.send` for `sending<->paused`; `.schedule` for `draft->scheduled`; `.compose` for `draft<->scheduled`/`->canceled` | None (total function) | Pure decision | O(1) | **Why internal-invariant:** a divergence between this function and any route-layer re-implementation is a fail-open authorization bug, same class as SPEC-007's `deriveRequiredPermission` | REQ-04/05/07/18, AC-05/06/07/24, behavior.spec §1.1 | Direct unit test for every (from,to,actorTier) combination named in behavior.spec §1.1's Example |
| C-009 `saveCampaign` | `campaign-write-service.ts` | newsletter | exported function (the chokepoint) | THE single campaign-mutation entry point (REQ-06) | Authorize -> validate -> `transitionCampaignStatus` check (on status-affecting fields) -> write campaign row + revision, same tx | `{ workspaceId, id?, fields, actorId, expectedVersion? }` | `{ campaign, revisionSeq }` | `listId` resolves to an existing non-archived list when scheduling/sending (REQ-09); `expectedVersion` optimistic-concurrency check (EC-06) | `NEWSLETTER_VALIDATION_ERROR`, `NEWSLETTER_CAMPAIGN_NOT_EDITABLE`, `NEWSLETTER_CONFLICT`, `NEWSLETTER_LIST_NOT_FOUND` | Explicit side effect: DB write (campaign row + revision row, same tx) | O(1) | **Aggregate-risk:** the sole path INV-01 depends on — a bug here breaks "no campaign row without a same-tx revision" for every caller | REQ-01/04/06/09, AC-01/05/06/08/11, INV-01 | Integration test at the DB-transaction boundary (AC-08 forces a mid-tx failure and asserts neither row persists) |
| C-013 `saveSubscription` / `importSubscriptions` | `subscriptions.ts` | newsletter | exported functions | THE subscription-mutation entry point (REQ-10/31) | Resolve `subscriberId` via `SubscriberDirectoryPort` -> write `SubscriptionRow` (`status: 'pending'`) -> trigger `issueConfirmationToken` | `{ workspaceId, listId, subscriberId, source }` / `{ workspaceId, listId, subscribers[] }` | `{ subscription }` / `{ created[], failed[] }` | `subscriberId` must resolve to a real Members principal | `NEWSLETTER_SUBSCRIBER_NOT_FOUND` | Explicit side effect: DB write + triggers a `MailerPort.send()` (via `confirmation.ts`) | O(1) per row; O(n) for import (bounded 1-500) | **Aggregate-risk:** `importSubscriptions` MUST call the exact same per-row path as `saveSubscription` — any divergence breaks REQ-31's "no separate unvalidated import path" guarantee | REQ-10/31, AC-12/13/40, EC-08 | Integration test: import a batch with one invalid id, assert the 2 valid rows took the identical code path a manual add would (same error, same confirmation-email trigger) |
| C-014 `issueConfirmationToken` / `consumeConfirmationToken` | `confirmation.ts` | newsletter | exported functions | Confirmation-token lifecycle chokepoint (REQ-11/12/13) | `issue`: mint token, invalidate prior unconsumed token, send email. `consume`: validate token, call `MembersConsentCapability.request`/`.confirm`, flip subscription on `granted` | `{ subscriptionId }` / `{ tokenHash }` | `{ tokenId, expiresAt }` / `{ subscription }` | Token TTL 72h; at most one unconsumed token per subscription (behavior.spec §2.1) | `NEWSLETTER_CONFIRM_TOKEN_INVALID` (expired/consumed/not-found) | Explicit side effect: DB write + `MailerPort.send()` (issue) / DB write + cross-module call (consume) | O(1) | **Aggregate-risk:** `consumeConfirmationToken` must never flip `SubscriptionRow.status` to `subscribed` before `MembersConsentCapability.confirm` reports `granted` — reversing this order breaks REQ-11/REQ-32's "Members is the source of truth" guarantee | REQ-11/12/13/32, AC-14/15/16/17, INV-03 | Unit test: assert local status flip happens strictly after (not before or regardless of) a mocked `granted` response |
| C-015 `processUnsubscribe` `[internal-invariant]` | `unsubscribe.ts` | newsletter | exported function | The fail-closed unsubscribe-token check (REQ-14/15/INV-04) — this feature's second-highest-risk contract | Verify `KeyringPort`-re-derived token against `consentRevisionIdAtSubscribe`; on match, flip subscription + call `MembersConsentCapability.revoke`; on repeat of an already-processed token, return success idempotently | `{ derivedTokenClaims }` | `{ outcome: 'unsubscribed' \| 'already-unsubscribed' }` | `derivedTokenClaims.consentRevisionId === subscription.consentRevisionIdAtSubscribe` (INV-04) | `NEWSLETTER_UNSUBSCRIBE_TOKEN_INVALID` (signature/revision-mismatch/expired/not-found) | Explicit side effect: DB write + cross-module call | O(1) | **Aggregate-risk:** the EC-03 idempotency carve-out (repeat-of-same-already-processed-token = success) must not be implemented as "any revision mismatch is OK" — it is narrowly "this exact token, already at its own resulting terminal state," never a general bypass of INV-04 | REQ-14/15, AC-18/19/20, INV-04, EC-03 | Unit test: (1) stale-revision token from a re-subscribe is rejected (AC-18); (2) current-revision token succeeds (AC-19); (3) repeat of the same now-processed token is idempotent success, not rejected (AC-20/EC-03) — three tests, not one, to keep the narrow carve-out from swallowing INV-04 |
| C-016 `evaluateLaunchGate` `[internal-invariant]` | `launch-gate.ts` | newsletter | exported function | **THE Launch Readiness Gate (REQ-21) — this feature's single highest-risk contract, same treatment SPEC-007 gave its write chokepoint** | Evaluate all four preconditions in fixed (a)->(d) order, every time, never short-circuiting; return every unmet one together | `{ workspaceId, isTestSend: boolean }` | `{ met: boolean; unmetPreconditions: ('sending_enabled_false'\|'consent_capability_unbound'\|'origin_not_verified'\|'mailer_adapter_not_production')[] }` | (a) `newsletter.launch_gate.sending_enabled === true` (skipped for `isTestSend`); (b) `MembersConsentCapability` binding present and non-stub (skipped for `isTestSend`); (c) `OriginRegistryPort.canonicalOrigin` resolves without throwing (never skipped, even for test sends — EC-10); (d) bound `MailerPort.capabilities().driver` not in `{console, memory}` (never skipped) | Returns unmet list; never throws | Reads only (workspace setting, capability-binding presence, origin registry, mailer capabilities) — no writes | O(1) — 4 independent checks | **Aggregate-risk:** this is the sole gate `authorizeSend` may trust — if any caller re-implements even one precondition independently, the whole gate's fail-closed guarantee is void. Must be called exactly once per authorization attempt, never cached, never client-overridable | REQ-21, AC-27/28/29/30/31, INV-05, EC-05/EC-10, behavior.spec §1.2 | Unit test: exactly two of four preconditions unmet -> both and only both named, in fixed (a)->(d) order (behavior.spec §1.2's own Test requirement); separate test: `isTestSend: true` with (a)/(b) unmet but (c)/(d) met -> gate reports met (AC-31); separate test: `isTestSend: true` with no verified origin -> still blocked (EC-10, precondition (c) never waived) |
| C-017 `NewsletterSendPipeline` actions | `send-pipeline.ts` | newsletter | exported functions (orchestrator) | The async orchestration layer (orchestrator.spec.md §4) — audience freeze -> outbox fan-out -> completion, as first-class contracts | `authorizeSend`: calls C-016 inside the `scheduled->sending` tx. `freezeAudience`: materializes `subscribed` subscriptions -> 1 snapshot + N send rows, idempotent-by-presence. `claimBatch`: delegates to `processOutbox`. `dispatchRow`: recipient.filter -> beforeSend -> `MailerPort.send()`. `recordResult`: atomic send-row + counters write. `completeIfDrained`: flips to `sent` when 0 pending. `pauseCampaign`/`resumeCampaign`: flip status, `claimBatch` no-ops while paused | Per orchestrator.spec.md §4 table | Per orchestrator.spec.md §4 table (`Result<...>` shapes) | Per orchestrator.spec.md §4/§6 | `NEWSLETTER_LAUNCH_GATE_BLOCKED`, `NEWSLETTER_CAMPAIGN_NOT_EDITABLE`, `FORBIDDEN`, `INTERNAL_ERROR`, `UPSTREAM_ERROR` (per action, see orchestrator.spec.md §4) | `authorizeSend`/`freezeAudience`/`recordResult` are explicit DB writes; `claimBatch` delegates to `OutboxPort`; `dispatchRow` is an explicit external effect (mail send, core-injected `MailerPort`, INV-08) | `authorizeSend`/`freezeAudience`/`recordResult`: O(1)/O(n-recipients)/O(1) respectively; `claimBatch`: O(batchSize), bounded 1-200 | **Aggregate-risk:** `freezeAudience`'s idempotent-by-presence-check (never a second snapshot for one `sendStartedAt`) and `recordResult`'s atomic write (REQ-24/INV-02, rides the unbuilt ADR-026 envelope) are this contract's two highest-risk internal guarantees | REQ-16/17/18/19/20/24/28/29, AC-21/22/23/24/26/35/37/38, INV-02/06/08/10, orchestrator.spec.md §6 | Integration test: full compose->authorize->freeze->claim->dispatch->drain cycle against `repo.memory.ts` + an in-memory `MailerPort`/outbox double; separate crash-recovery test simulating EC-04 (crash after `send()`, before result-recording) |
| C-018 `runHookChain` | `hooks.ts` | newsletter | exported function | Fixed-order, fail-closed hook dispatch (REQ-23, behavior.spec §1.3) | Run `recipient.filter` -> (if kept) `beforeSend` -> return the transformed message or a suppression verdict | `SendRow` + the rendered message | `{ keep: true, message } \| { keep: false, reason }` | `recipient.filter` always runs before `beforeSend`; a throwing hook is treated as suppression, never "keep by default" (behavior.spec §7) | None thrown to the caller — a throw inside a hook is caught and converted to `{ keep: false }` | Pure decision wrapping hook calls (hooks themselves may have effects, but ordering/fail-closed conversion here is pure) | O(k), k = registered hook count (small, fixed) | **Aggregate-risk:** if `beforeSend` is ever invoked for a row `recipient.filter` suppressed, EC-01/AC-34's ordering guarantee is broken | REQ-23, AC-34, EC-01, behavior.spec §1.3 | Unit test proving `beforeSend` never runs for a suppressed row, and a separate test proving `MailerPort.send()` never runs without a prior `beforeSend` invocation for a kept row |
| C-019a..s route registrars | `routes/admin/newsletter/*.ts` | server | exported functions | HTTP entrypoints, each `authorize()`-gated per api.spec.md §2 | Parse request -> call the matching newsletter function -> map result/errors to api.spec.md §5/§6 | HTTP request per api.spec.md §4 | HTTP response per api.spec.md §5 | Auth profile per endpoint (api.spec.md §2) | Full mapping per api.spec.md §6 | Explicit side effect: HTTP I/O + delegates to C-008..C-018 | O(1) request handling | N/A | REQ-25, AC-42, api.spec.md | Integration test per endpoint hitting the real route + SQLite adapter |
| C-020/C-021 public route registrars | `routes/site/newsletter-{confirm,unsubscribe}.ts` | server | exported functions | Token-only, unauthenticated HTTP entrypoints | Parse `token` query param -> call C-014's `consumeConfirmationToken` or C-015's `processUnsubscribe` -> render HTML result | `{ token }` (+ optional `List-Unsubscribe` body on `POST`) | HTML page (not JSON) | Token verified server-side only — never trusted as a client-supplied claim | `NEWSLETTER_CONFIRM_TOKEN_INVALID`, `NEWSLETTER_UNSUBSCRIBE_TOKEN_INVALID` (rendered as HTML error pages) | Explicit side effect: HTTP I/O, no session read/write | O(1) | **Aggregate-risk:** these two routes must never be registered under `/api/admin` (would inherit the session-gate middleware, breaking the "no login" requirement) nor ever set/read a cookie | REQ-14/15, AC-39, api.spec.md §1a | Integration test asserting no `Set-Cookie`/session-read occurs on either route, even on success |

## Wiring Map

| Flow ID | Source | Transport/Call Type | Target | Payload/Contract | Ordering/Retry/Idempotency | Failure Handling | Trace |
|---|---|---|---|---|---|---|---|
| W-001 | `routes/admin/newsletter/send-campaign.ts` | direct call | `send-pipeline.ts` C-017 `authorizeSend` | none in (path params only, no body per api.spec.md `SEND_CAMPAIGN`) | Not idempotent by design — a second call while already `sending` is rejected (`NEWSLETTER_CAMPAIGN_NOT_EDITABLE`), not silently accepted (behavior.spec §7's double-click case) | Any typed error maps 1:1 to an HTTP error code (api.spec.md §6); `202 Accepted` on success (async) | REQ-19/21, AC-27-30 |
| W-002 | `send-pipeline.ts` `authorizeSend` (on success) | direct call, same transaction commit boundary | `send-pipeline.ts` C-017 `freezeAudience` | `{ workspaceId, campaignId, listId }` | Idempotent by presence-check — a retry finds `campaign.audienceSnapshotId` already set and no-ops (orchestrator.spec.md §6) | `INTERNAL_ERROR` on transaction failure — never a partial write | REQ-16, AC-21, orchestrator.spec.md §5 |
| W-003 | `send-pipeline.ts` `freezeAudience` (on success) | outbox enqueue (one event per bounded batch of `sendIds`, chunked at `batchSize`) | core `OutboxPort` -> `processOutbox` -> `EventBusPort.publish('newsletter.send.batch.claimed', SendBatchJob)` | `SendBatchJob { workspaceId, campaignId, audienceSnapshotId, sendIds }` | Reuses the generic `processOutbox` claim/retry loop unmodified (batchSize default 20, bounded 1-200) — Newsletter does not fork the claim loop | A publish failure marks the outbox row failed and lets `processOutbox`'s existing retry/backoff handle it | REQ-17, orchestrator.spec.md §4 `claimBatch` |
| W-004 | `server/app.ts` bus subscriber for `newsletter.send.batch.claimed` | event-bus subscription (mirrors the existing `bus.subscribe("workspace.created", ...)` demonstration in `app.ts`) | `send-pipeline.ts` C-017 `dispatchRow` (looped per `sendIds` entry) + `hooks.ts` C-018 `runHookChain` + core-injected `MailerPort.send()` + `recordResult` | one `SendRow` id per iteration | Per-row: `recipient.filter` -> `beforeSend` -> `send()` -> `recordResult`, in that fixed order (behavior.spec §1.3); a single row's failure never aborts sibling rows in the same batch | `recordResult`'s atomic write (REQ-24/INV-02) always commits row-status + counters together or neither | REQ-17/22/23/24, orchestrator.spec.md §5 `onRecordResult` |
| W-005 | `send-pipeline.ts` `recordResult` (after every settle) | direct call | `send-pipeline.ts` C-017 `completeIfDrained` | `{ workspaceId, campaignId }` | Checked once per settled row (orchestrator.spec.md §5 `onDrainCheck`); naturally idempotent — safe to call redundantly | No-op (`null`) when not yet drained; never throws | REQ-18, AC-24, orchestrator.spec.md §5 |
| W-006 | `confirmation.ts` C-014 `consumeConfirmationToken` | direct call (typed, cross-module) | `MembersConsentCapability.request` / `.confirm` (unbound until Members ships) | `{ subscriberId, evidence: { consentTextRef, source, confirmTokenId } }` | Read-then-act: only flips `SubscriptionRow.status` after a `granted` response; never asserts `granted` itself (INV-03/REQ-32) | If the capability is unbound (`null`), this flow cannot execute at all — REQ-11/REQ-32 depend on Members shipping it, named in ADR-PIPE-011 Risks | REQ-11/32, AC-15, INV-03 |
| W-007 | `unsubscribe.ts` C-015 `processUnsubscribe` | direct call (typed, cross-module) | `MembersConsentCapability.revoke` | `{ subscriberId }` | Called only after the local `consentRevisionIdAtSubscribe` check passes (INV-04) | Same unbound-capability caveat as W-006 | REQ-14/15/32, AC-19, INV-03 |
| W-008 | `subscriptions.ts` C-013 `saveSubscription`/`importSubscriptions` | direct call (typed, cross-module, read-only) | `SubscriberDirectoryPort.getContact`/`getContacts` (real: `MembersSubscriberDirectory`) | `{ workspaceId, subscriberId(s) }` -> `SubscriberContact \| null` / `readonly SubscriberContact[]` | Read-only; unknown ids silently omitted (`getContacts`) or produce `NEWSLETTER_SUBSCRIBER_NOT_FOUND` (`getContact`-backed single add) | `null`/omitted results are expected, not errors (EC-07) | REQ-10/16, AC-13/22, EC-07 |
| W-009 | `launch-gate.ts` C-016 `evaluateLaunchGate` | direct call (read-only, four independent checks) | workspace setting store (precondition a), `MembersConsentCapability` binding presence (b), `OriginRegistryPort.canonicalOrigin` (c), bound `MailerPort.capabilities()` (d) | none (workspace-scoped reads only) | All four always evaluated, fixed order, never short-circuited (behavior.spec §1.2) | Each precondition's failure is collected, never thrown individually — `evaluateLaunchGate` itself never throws | REQ-21, AC-27-31, behavior.spec §1.2 |
| W-010 | `server/seed.ts` boot path | direct call, once at startup | `data-module-manifest.ts` `installNewsletterDataModule` -> `src/features/plugins/data-module.ts` `declareDataModule()` | `NEWSLETTER_DATA_MODULE: DataModuleDecl` (5 tables) | Idempotent — `declareDataModule()`'s own skip-if-exists logic (safe on repeated boot) | On DDL failure, `declareDataModule()`'s snapshot-before-DDL restores the pre-existing state and Newsletter's tables are left absent — boot continues, Newsletter is effectively "uninstalled" until retried | ADR-PIPE-011 Decision/Risks |
| W-011 | `apps/admin/src/sections/Newsletter.tsx` | HTTP fetch | `routes/admin/newsletter/*.ts` / `routes/site/newsletter-*.ts` | api.spec.md §4/§5 request/response contracts | UI disables `onSend`/`onSendTest`/etc. during in-flight requests (ui.spec.md §4) | `ErrorBanner`/`LaunchGateBanner` render recoverable errors inline (ui.spec.md §2.8/§2.9) | REQ-26, ui.spec.md §3/§4 |

## Data And Side-Effect Boundaries

| Boundary | Owner | Reads | Writes | Side Effects | Consistency / Transaction Rule | Migration / Dual-Write Path |
|---|---|---|---|---|---|---|
| `newsletter_campaigns` / `newsletter_campaign_revisions` | newsletter | `campaign.ts`, route layer (list/get) | `campaign-write-service.ts` only | none beyond the row itself | Campaign write + its revision row are one transaction (INV-01) | N/A — new tables, no legacy data (permission-string rename is the only brownfield surface, see ADR-PIPE-011 Migration Safety) |
| `p_newsletter__lists` | newsletter | `lists.ts`, route layer | `lists.ts` only | none | Single-row write, no revision requirement named for lists in feature.spec.md | Created via `declareDataModule()` at boot (W-010); default row seeded per workspace at workspace-creation time (REQ-08) |
| `p_newsletter__subscriptions` | newsletter | `subscriptions.ts`, `confirmation.ts`, `unsubscribe.ts`, `feedback.ts`, `send-pipeline.ts` (freezeAudience read) | `subscriptions.ts`, `confirmation.ts` (status flip on grant), `unsubscribe.ts` (status flip on revoke), `feedback.ts` (bounced/complained projection) | Confirmation email send (`subscriptions.ts` -> `confirmation.ts` trigger) | Each writer flips exactly the fields it owns; `consentRevisionIdAtSubscribe` is only ever set by `consumeConfirmationToken` (state.spec.md §5) | Created via `declareDataModule()` at boot |
| `p_newsletter__audience_snapshots` | newsletter | `send-pipeline.ts` | `send-pipeline.ts` `freezeAudience` only | none | Immutable after creation (INV-06) — no update action exists | Created via `declareDataModule()` at boot |
| `p_newsletter__sends` | newsletter | `send-pipeline.ts`, route layer (send-log read) | `send-pipeline.ts` `freezeAudience` (insert `pending`) + `recordResult` (terminal status) + `erasure.ts` (anonymize) | none beyond the row itself | `recordResult`'s status write + campaign `counters` update are one atomic multi-write (REQ-24/INV-02, rides the ADR-026 envelope — unbuilt, inherited precondition) | Created via `declareDataModule()` at boot |
| `p_newsletter__confirmation_tokens` | newsletter | `confirmation.ts` | `confirmation.ts` only | none | `consumedAt` once set is never cleared (state.spec.md §5) | Created via `declareDataModule()` at boot |
| `identity.principals` (Members-owned, read via `SubscriberDirectoryPort`) | members (unchanged) | `subscriptions.ts`, `send-pipeline.ts` `freezeAudience` (read-only, via `SubscriberDirectoryPort`) | Not written by `newsletter` | none | N/A — read-only cross-module dependency | N/A |
| `member_consents` (Members-owned, not yet built, called via `MembersConsentCapability`) | members (unchanged, unbuilt) | N/A (Newsletter never reads this table directly) | Not written by `newsletter` — Newsletter only supplies evidence payloads to typed calls (INV-03) | none from Newsletter's side | N/A — this table doesn't exist yet; the seam is declared, not implemented, here | N/A |

## Observability And Operational Expectations

| Surface / Flow | Required Signals | Correlation / Trace Context | Metrics | Logs | Alert / Runbook Need | Privacy / Secret Constraints | Trace |
|---|---|---|---|---|---|---|---|
| All 19 admin routes + 2 public routes | Structured error responses (errors.spec.md §1 envelope) | `correlationId` on every error payload | None new required (no NFR discovery flagged latency/throughput targets) | Standard request logging (existing server middleware) | N/A — no new production alerting surface beyond existing admin-route conventions | Public routes never log the raw token value in a way that would let it be replayed from logs | REQ-10, errors.spec.md §1 |
| `send-pipeline.ts` C-017 actions | Every campaign/send-row state transition emits a `newsletter.*` event carrying `campaignId`/`sendId` | Event payload correlation ids | None new required this pass | Standard | N/A | N/A | Art. VIII, REQ-16-18 |
| `launch-gate.ts` C-016 | `NEWSLETTER_LAUNCH_GATE_BLOCKED`'s `unmetPreconditions` array IS the operability signal — an operator always sees the full picture in one response | N/A | N/A | N/A | N/A — this is a request-time signal, not a background alert | N/A | REQ-21, behavior.spec §1.2 |
| `data-module-manifest.ts` boot-time `installNewsletterDataModule` | The migration's own `declareDataModule()` call already logs a summary via its existing implementation | N/A | N/A | Log a summary line at boot for operator visibility (matches `settings`' `migratedCount` precedent) | N/A — one-time, low-risk on an empty install; higher-risk on an existing install with unrelated retained `p_newsletter__*` data (ADR-023 §6 namespace-binding guard applies) | N/A | ADR-PIPE-011 Risks |

## Critical Invariants

| Invariant ID | Scope | Rule | Reason | Enforcement Surface | Test Expectation | Trace |
|---|---|---|---|---|---|---|
| INV-01 | `campaign-write-service.ts` C-009 | A campaign row must never exist without a same-transaction revision row | Chokepoint integrity — the audit/never-brick guarantee | `campaign-write-service.ts` (module boundary — repo write methods package-private) | Integration test: assert exactly 1 campaign row + 1 revision row per write, same tx, on the real SQLite adapter (AC-08) | feature.spec.md INV-01 |
| INV-05 `[internal-invariant]` — the highest-priority invariant in this feature | `launch-gate.ts` C-016, `send-pipeline.ts` C-017 `authorizeSend` | A full-audience send must never proceed while any Launch Readiness Gate precondition is unmet | Closes the spec's central, user-directed sequencing constraint ("must not ship to real recipients before Members is live") — same treatment SPEC-007 gave its write chokepoint as the highest-risk contract | `evaluateLaunchGate` called exactly once, inside `authorizeSend`'s transaction; no other call site may duplicate or bypass it | Integration test: all four combinations of 2-of-4 preconditions unmet -> send rejected, zero rows written, both preconditions named (AC-27/28/29); all four met -> send proceeds (AC-30) | REQ-21, INV-05, behavior.spec §1.2 |
| INV-04 | `unsubscribe.ts` C-015 | An unsubscribe token must never verify once `consentRevisionId` has advanced past its mint-time value | Prevents a stale token from unsubscribing a subscriber who has since re-subscribed under a new consent grant | `processUnsubscribe`'s revision comparison, narrowly scoped (does not also swallow the EC-03 idempotency carve-out) | Integration test: stale-revision token rejected (AC-18); repeat-of-same-already-processed token is idempotent success, not a INV-04 violation (AC-20/EC-03) | REQ-14, INV-04 |
| INV-06 | `send-pipeline.ts` C-017 `freezeAudience` | `AudienceSnapshotRow.recipientCount` never changes after creation | Mid-send subscription churn must never retroactively alter an in-flight send's already-frozen snapshot | No update action exists for this row; `freezeAudience` idempotent-by-presence-check prevents a second snapshot per `sendStartedAt` | Integration test: a subscriber unsubscribing after freeze does not change `recipientCount`, and their row is separately marked `failed`/`SUPPRESSED_POST_FREEZE` (EC-01) at dispatch time instead | REQ-16, INV-06, EC-01 |
| INV-08 | `send-pipeline.ts` (all C-017 actions) | The Newsletter plugin must never hold a live `MailerPort` instance in its own code | Core injects and calls the port; Newsletter submits send intent as data through the outbox/command spine | Code Review checks `dispatchRow`'s signature/implementation never constructs, stores, or imports a concrete `MailerPort` adapter — only ever receives it as a call-time dependency from core's composition root | Code Review architecture check (no automated test can fully prove a negative here — mirrors how INV-08 is enforced in `orchestrator.spec.md` §6 today, by review discipline) | INV-08, orchestrator.spec.md §6 |
| INV-09 | `unsubscribe.ts`, `confirmation.ts` (link construction) | No code path in this feature may read the raw inbound request `Host`/`:authority` header to construct a confirm/unsubscribe link | Prevents Host-header injection from redirecting subscriber-facing links to an attacker-controlled origin | Every link-building call site uses `OriginRegistryPort.canonicalOrigin` exclusively | Integration test: attacker-controlled `Host: evil.example.com` header does not affect the generated link's origin (AC-39) | REQ-30, INV-09 |

## Brownfield / Migration Mapping (if applicable)

| Source Behavior / Contract | Target Module / Contract | Preserve / Change | Characterization Evidence | Migration Safety Note |
|---|---|---|---|---|
| `src/newsletter/types.ts`'s `NEWSLETTER_PERMISSIONS` (unprefixed `newsletter.*` strings) | `src/newsletter/types.ts`'s renamed `NEWSLETTER_PERMISSIONS` (`admin.newsletter.*` strings) + `src/identity/permissions.ts` registration | Change: string values only; no behavior exists yet to preserve | Repo-wide grep confirmed zero call sites reference the old strings (`ADR-PIPE-011` Migration Safety) | Pure rename, lands in the same task as the permission-catalog registration — see `ADR-PIPE-011` Migration Safety table for the full expand/contract analysis (result: N/A across every row except the rename itself) |

## Test Expectations

- Contract tests: C-001..C-006 (the six repo ports) — one shared contract-test suite per port, run against both `repo.memory.ts` and `repo.sqlite.ts` (matches `members`/`settings` precedent).
- Integration tests: W-001 (SEND through the real HTTP route + SQLite adapter + Launch Gate, AC-27-30), W-002/W-003/W-004 (freeze -> outbox claim -> dispatch -> record, full cycle, AC-21/23), W-005 (drain -> `sent` transition, AC-24), W-010 (`declareDataModule()` boot-time invocation against Newsletter's real manifest, including the required forced-failure/rollback test per ADR-PIPE-011 Mitigations Required), all 19+2 routes (AC-42 permission-gating; the two public routes' no-cookie assertion).
- Property/invariant tests: INV-01 (no orphan campaign row), INV-04 (fail-closed consent-revision check, narrowly scoped around the EC-03 carve-out), INV-05 (Launch Gate never bypassed), INV-06 (frozen snapshot immutability), INV-08 (no live `MailerPort` held — architecture/review check, not a runtime test), INV-10 (send idempotency under outbox redelivery, EC-04).
- Characterization tests: None required — this is a greenfield feature; the one brownfield surface (permission rename) has no prior behavior to characterize (zero live call sites).
- Explicitly N/A suites with reason: None — unlike SPEC-007, this feature's orchestrator IS in scope, so no orchestrator test suite is skipped.

## Downstream Handoff Notes

- Coordinator task-generation constraints: Sequence (1) `src/infra/db/schema.ts` + `data-module-manifest.ts` + the six repo ports/adapters, then (2) `campaign.ts`/`campaign-write-service.ts`/`launch-gate.ts` (certified first — highest aggregate risk), then (3) `lists.ts`/`subscriptions.ts`/`confirmation.ts`/`unsubscribe.ts`, then (4) `send-pipeline.ts`/`hooks.ts` (depends on 2+3), then (5) the 19+2 routes and the UI (depend on 1-4 being real, not mocked, per Article V). `evaluateLaunchGate` must be certified by TDD before `authorizeSend` is wired to call it (mirrors SPEC-007's `deriveRequiredPermission` sequencing). The `declareDataModule()` failure-path test (ADR-PIPE-011 Mitigations Required) must land in the same task as the schema-creation work, not deferred to a later pass.
- TDD focus: Certify C-016 (`evaluateLaunchGate`) and C-009 (`saveCampaign` chokepoint) first — they're the two highest aggregate-risk contracts. Then C-015 (`processUnsubscribe`'s fail-closed check) and C-017 (the orchestrator's `freezeAudience`/`recordResult` atomicity). Route-layer tests last, once the domain layer is certified.
- Programmer architecture audit focus: Verify no import of `repo.memory.ts`/`repo.sqlite.ts` from outside the five named chokepoint files; verify `evaluateLaunchGate` is called exactly once per `authorizeSend` invocation with no route-layer duplication; verify `dispatchRow` never constructs/imports a concrete `MailerPort` adapter (INV-08); verify the two public routes are registered outside `/api/admin` and never touch session middleware; verify `MembersConsentCapability`'s unbound state is treated as "precondition (b) unmet," not silently defaulted to "met."
- Open risks or ambiguities: The `declareDataModule()` reuse (spike-quality code now load-bearing) and `MembersConsentCapability`'s stub-vs-real binding distinction are both already captured with explicit mitigations in ADR-PIPE-011's Risks/Mitigations Required sections — no additional open item beyond what's recorded there.
